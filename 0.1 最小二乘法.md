
## 8.1 问题引出

### 基本设定

给定 n 组数据 $(y_1, x_1), \ldots, (y_n, x_n) \in \mathbb{R}^{p+1}$，假设这些数据满足线性关系 $y_i = x_i^T \beta$，其中 $\beta \in \mathbb{R}^p$ 是待估计的参数向量。

需要注意的是，$x_i \in \mathbb{R}^p$ 是一个向量而非标量。以房价预测为例，$y_i$ 代表房价，$x_i$ 可以是（地段，面积，楼层）等特征向量，$\beta$ 则代表各特征的**回归系数或影响因子**。

### 矩阵形式

将上述关系改写为线性方程组形式 $X\beta = y$，其中设计矩阵为 $X = (x_1^T, x_2^T, \ldots, x_n^T)^T$。

### 引入随机误差

现实中的数据不会完美满足线性关系，因此需要引入随机误差项 $\epsilon_i$，将模型改写为 $y_i = x_i^T \beta + \epsilon_i$。**由于误差的存在，线性方程组 $X\beta = y$ 通常无解**。

### 最小二乘的目标

我们希望找到使 $X\beta$ 与 $y$ 之间距离最小的参数 $\beta$，即最小化范数 $\|X\beta - y\|$。找到这样的 $\beta$ 后，就可以用 $x_i^T \beta$ 来预测 $y_i$。

### 最小二乘问题的正式定义

给定线性方程组 $Ax = b$，其中 $A \in \mathbb{R}^{m \times n}$，$b \in \mathbb{R}^m$，最小二乘问题是寻找 $\hat{x} \in \mathbb{R}^n$ 使得 $\|A\hat{x} - b\|$ 最小。

最小化 $\|Ax - b\|$ 等价于最小化 $\|Ax - b\|^2 = \epsilon_1^2 + \cdots + \epsilon_n^2$，这也是"最小二乘"名称的由来。

---

## 8.2 问题求解

### 几何直观

观察 $Ax$ 的所有可能取值，它们构成矩阵 A 的列空间 $\text{Col}(A)$。我们需要在列空间中找到距离 b 最近的点 $\hat{b} = A\hat{x}$。**根据投影的性质，这个最近点就是 b 在列空间上的正交投影**，即 $\hat{b} = \text{proj}_{\text{Col}(A)} b$。

### 几何方法求解步骤

首先计算 b 在 $\text{Col}(A)$ 上的投影得到 $\hat{b}$，然后求解线性方程组 $A\hat{x} = \hat{b}$。由于 $\hat{b} \in \text{Col}(A)$，该方程组必定有解。

### 正规方程（避免投影计算）

**定理**：最小二乘问题的解集等于正规方程 $A^T A \hat{x} = A^T b$ 的解集。

因此，最小二乘解可以表示为：

$$\hat{x} = (A^T A)^{-1} A^T b$$

### 解的唯一性条件

以下三个命题等价：
- A 的列线性无关
- $A^T A$ 可逆
- 最小二乘问题有唯一解 $\hat{x} = (A^T A)^{-1} A^T b$