## 🎯 预备知识

> [!info] 矩阵范数
> - 谱范数 $\|\mathbf{A}\|_2 = \sqrt{\lambda_{\max}(\mathbf{A}^\top\mathbf{A})}$
> - 正交矩阵 $\mathbf{Q}$，满足 $\mathbf{Q}^\top\mathbf{Q} = \mathbf{I}$

> [!info] 线性代数
> - 特征值和特征向量：$\mathbf{A}\mathbf{v} = \lambda\mathbf{v}$
> - 矩阵的秩
> - 基本子空间：$\text{col}(\mathbf{A})$，$\ker(\mathbf{A})$，$\text{col}(\mathbf{A}^T)$
> - 子空间上的投影

---

## 📑 目录

```

1. 动机：对角化的局限性
2. 奇异值和奇异向量
3. SVD 主定理
4. 简化 SVD 和伪逆
5. 应用
6. 总结

```

---

# 一、动机：对角化的局限性

## 1.1 问题引入

> [!warning] 特征值分解的局限
> - 特征值分解只适用于**方阵**
> - 正交对角化目前只适用于**对称矩阵**
> - **问题**：大多数矩阵既不是方阵也不是对称的

---

## 1.2 我们想要的分解

> [!important] 目标
> 一个分解 $\mathbf{A} = \mathbf{P}\mathbf{D}\mathbf{Q}^{-1}$：
> - 适用于**任意** $m \times n$ 矩阵
> - $\mathbf{D}$ 是"对角"的
> - $\mathbf{P}$ 和 $\mathbf{Q}$ 是正交矩阵

---

## 1.3 几何直观

> [!abstract] 单位球作为测试案例
> - 考虑所有满足 $\|\mathbf{x}\| = 1$ 的向量 $\mathbf{x}$
> - 变换后：$\mathbf{A}\mathbf{x}$ 具有不同的长度
> - **问题**：哪个方向 $\mathbf{A}\mathbf{x}$ 被拉伸得最多？

> [!example] 示例：$\mathbf{A} \in \mathbb{R}^{2 \times 3}$
> - $\mathbb{R}^3$ 中的单位球 → $\mathbb{R}^2$ 中的椭圆
> - 最长向量：椭圆的主轴
> - 最短向量：椭圆的次轴

---

## 1.4 基本思想

> [!important] 最大化问题
> $$
> \max_{\|\mathbf{x}\|=1} \|\mathbf{A}\mathbf{x}\|
> $$
> 
> **技巧**：最大化 $\|\mathbf{A}\mathbf{x}\|^2$ 而不是 $\|\mathbf{A}\mathbf{x}\|$
> $$
> \|\mathbf{A}\mathbf{x}\|^2 = (\mathbf{A}\mathbf{x})^\top(\mathbf{A}\mathbf{x}) = \mathbf{x}^\top(\mathbf{A}^\top\mathbf{A})\mathbf{x}
> $$
> 
> **认识**：这是关于 $\mathbf{A}^\top\mathbf{A}$ 的二次型！

---

# 二、奇异值和奇异向量

## 2.1 矩阵 $\mathbf{A}^\top\mathbf{A}$ 的性质

> [!important] 定理：$\mathbf{A}^\top\mathbf{A}$ 的性质
> 
> 对于 $\mathbf{A} \in \mathbb{R}^{m \times n}$ 有：
> 
> 1. $\mathbf{A}^\top\mathbf{A}$ 是**对称的**：$(\mathbf{A}^\top\mathbf{A})^\top = \mathbf{A}^\top\mathbf{A}$
> 2. $\mathbf{A}^\top\mathbf{A}$ 是**半正定的**：所有特征值 $\lambda_i \geq 0$

> [!note] 性质(2)的证明
> 对于每个特征向量 $\mathbf{v}_i$：
> $$
> 0 \leq \|\mathbf{A}\mathbf{v}_i\|_2^2 = \mathbf{v}_i^\top(\mathbf{A}^\top\mathbf{A})\mathbf{v}_i = \mathbf{v}_i^\top(\lambda_i\mathbf{v}_i) = \lambda_i
> $$

> [!success] 推论
> $\mathbf{A}^\top\mathbf{A}$ 可以**正交对角化**！

---

## 2.2 计算示例

> [!example] 示例：$\mathbf{A}^\top\mathbf{A}$ 的性质
> 
> 设 $\mathbf{A} = \begin{pmatrix} 1 & 2 \\ 2 & 1 \\ 1 & 0 \end{pmatrix} \in \mathbb{R}^{3 \times 2}$
> 
> $$
> \mathbf{A}^\top\mathbf{A} = \begin{pmatrix} 1 & 2 & 1 \\ 2 & 1 & 0 \end{pmatrix}\begin{pmatrix} 1 & 2 \\ 2 & 1 \\ 1 & 0 \end{pmatrix} = \begin{pmatrix} 6 & 4 \\ 4 & 5 \end{pmatrix}
> $$
> 
> **对称性**：✓
> 
> **特征值**：
> $$
> \det(\mathbf{A}^\top\mathbf{A} - \lambda\mathbf{I}) = (6-\lambda)(5-\lambda) - 16 = \lambda^2 - 11\lambda + 14 = 0
> $$
> $$
> \lambda_{1,2} = \frac{11 \pm \sqrt{65}}{2} \Rightarrow \lambda_1 \approx 9.53, \lambda_2 \approx 1.47
> $$
> 
> $\lambda_1, \lambda_2 > 0 \Rightarrow \mathbf{A}^\top\mathbf{A}$ **正定**

---

## 2.3 奇异值的定义

> [!important] 定义：奇异值
> 
> 设 $\lambda_1 \geq \lambda_2 \geq \cdots \geq \lambda_n$ 是 $\mathbf{A}^\top\mathbf{A}$ 的特征值。
> 
> $\mathbf{A}$ 的**奇异值**是：
> $$
> \sigma_i = \sqrt{\lambda_i}, \quad i = 1, 2, \ldots, n
> $$

> [!note] 解释
> - $\sigma_1 = \max_{\|\mathbf{x}\|=1} \|\mathbf{A}\mathbf{x}\|$（最大拉伸）
> - $\sigma_n$ 对应最小拉伸/最大压缩
> - $\sigma_i = 0 \Leftrightarrow \lambda_i = 0$

---

## 2.4 奇异值示例

> [!example] 计算奇异值
> 
> 矩阵 $\mathbf{A} = \begin{pmatrix} 1 & 2 \\ 2 & 1 \\ 1 & 0 \end{pmatrix}$：$\mathbf{A}^\top\mathbf{A}$ 有 $\boldsymbol{\lambda} \approx (9.53, 1.47)$
> 
> **奇异值**：
> $$
> \sigma_1 = \sqrt{\lambda_1} = \sqrt{9.53} \approx 3.09
> $$
> $$
> \sigma_2 = \sqrt{\lambda_2} = \sqrt{1.47} \approx 1.21
> $$
> 
> **解释**：
> - $\sigma_1 \approx 3.09$：最大拉伸（$\mathbf{v}_1$ 被拉伸 3.1 倍）
> - $\sigma_2 \approx 1.21$：最小拉伸（$\mathbf{v}_2$ 被拉伸 1.2 倍）
> - 比值 $\frac{\sigma_{\max}}{\sigma_{\min}} \approx 2.55$ 表明各向异性

---

## 2.5 奇异向量的定义

> [!important] 定义：右奇异向量和左奇异向量
> 
> **右奇异向量 $\mathbf{v}_i$**：
> - $\mathbf{A}^\top\mathbf{A}$ 的归一化特征向量
> - $\mathbf{A}^\top\mathbf{A}\mathbf{v}_i = \lambda_i\mathbf{v}_i = \sigma_i^2\mathbf{v}_i$
> 
> **左奇异向量 $\mathbf{u}_i$**：
> - 定义为：$\mathbf{u}_i = \frac{\mathbf{A}\mathbf{v}_i}{\sigma_i}$（对于 $\sigma_i > 0$）

> [!success] 重要性质
> - $\{\mathbf{v}_1, \ldots, \mathbf{v}_n\}$ 是正交归一的
> - $\{\mathbf{u}_1, \ldots, \mathbf{u}_r\}$ 是正交归一的（$r = \text{rank}(\mathbf{A})$）

---

## 2.6 奇异向量的正交性证明

> [!note] 为什么 $\sigma_i\mathbf{u}_i = \mathbf{A}\mathbf{v}_i$ 是正交的？
> 
> 对于 $i \neq j$：
> $$
> (\mathbf{A}\mathbf{v}_i)^\top(\mathbf{A}\mathbf{v}_j) = \mathbf{v}_i^\top(\mathbf{A}^\top\mathbf{A})\mathbf{v}_j = \mathbf{v}_i^\top(\lambda_j\mathbf{v}_j) = \lambda_j(\mathbf{v}_i^\top\mathbf{v}_j) = \lambda_j \cdot 0 = 0
> $$
> 
> 对于 $i = j$：
> $$
> \|\mathbf{A}\mathbf{v}_i\|^2 = \mathbf{v}_i^\top(\mathbf{A}^\top\mathbf{A})\mathbf{v}_i = \lambda_i\|\mathbf{v}_i\|^2 = \lambda_i = \sigma_i^2
> $$
> 
> $\Rightarrow$ 左奇异向量 $\mathbf{u}_i$ 是正交归一的！

---

## 2.7 基本子空间

> [!important] 定理：子空间的刻画
> 
> 假设 $\mathbf{A}$ 有 $r$ 个非零奇异值。则：
> 
> 1. $\{\mathbf{v}_1, \ldots, \mathbf{v}_r\}$ 是 $\text{col}(\mathbf{A}^\top)$ 的正交基（**行空间**）
> 2. $\{\mathbf{A}\mathbf{v}_1, \ldots, \mathbf{A}\mathbf{v}_r\}$ 是 $\text{col}(\mathbf{A})$ 的正交基（**列空间**）
> 3. $\{\mathbf{v}_{r+1}, \ldots, \mathbf{v}_n\}$ 是 $\ker(\mathbf{A})$ 的正交基（**零空间**）
> 4. $\text{rank}(\mathbf{A}) = r$

> [!tip] 解释
> SVD 给出了所有基本空间的**显式正交基**！

---

# 三、SVD 主定理

## 3.1 定理陈述

> [!important] 定理：奇异值分解
> 
> 设 $\mathbf{A} \in \mathbb{R}^{m \times n}$，$\text{rank}(\mathbf{A}) = r$。则存在分解：
> $$
> \mathbf{A} = \mathbf{U}\boldsymbol{\Sigma}\mathbf{V}^\top
> $$
> 
> 其中：
> - $\mathbf{V} \in \mathbb{R}^{n \times n}$：正交矩阵（右奇异向量在列中）
> - $\mathbf{U} \in \mathbb{R}^{m \times m}$：正交矩阵（左奇异向量扩展为基）
> - $\boldsymbol{\Sigma} \in \mathbb{R}^{m \times n}$：包含奇异值的"对角矩阵"

---

## 3.2 $\boldsymbol{\Sigma}$ 的结构

> [!abstract] 矩阵 $\boldsymbol{\Sigma}$ 的形式
> $$
> \boldsymbol{\Sigma} = \begin{pmatrix} \text{diag}(\sigma_1, \ldots, \sigma_r) & \mathbf{0}_{r \times (n-r)} \\ \mathbf{0}_{(m-r) \times r} & \mathbf{0}_{(m-r) \times (n-r)} \end{pmatrix}
> $$

> [!example] 示例
> 
> **$\mathbf{A}$ 方阵**（$m = n = 3, r = 2$）：
> $$
> \boldsymbol{\Sigma} = \begin{pmatrix} \sigma_1 & 0 & 0 \\ 0 & \sigma_2 & 0 \\ 0 & 0 & 0 \end{pmatrix}
> $$
> 
> **$\mathbf{A}$ 矩形**（$m = 2, n = 3, r = 2$）：
> $$
> \boldsymbol{\Sigma} = \begin{pmatrix} \sigma_1 & 0 & 0 \\ 0 & \sigma_2 & 0 \end{pmatrix}
> $$

---

## 3.3 几何解释

> [!success] 变换：$\mathbf{x} \mapsto \mathbf{A}\mathbf{x} = \mathbf{U}\boldsymbol{\Sigma}\mathbf{V}^\top\mathbf{x}$
> 
> **三个步骤**：
> 
> 1. **$\mathbf{V}^\top$**：坐标变换（旋转/反射）到"最优"方向
> 2. **$\boldsymbol{\Sigma}$**：沿这些轴按因子 $\sigma_i$ "拉伸"，对于 $i > r$ 置零
> 3. **$\mathbf{U}$**：坐标变换（旋转/反射）回到目标空间

> [!tip] 总结
> **SVD 将每个从 $\mathbb{R}^n$ 到 $\mathbb{R}^m$ 的线性变换分解为"旋转-拉伸-旋转"**

---

# 四、简化 SVD 和伪逆

## 4.1 简化 SVD 的动机

> [!note] 问题
> $\boldsymbol{\Sigma}$ 中有很多零元素
> 
> 当 $\text{rank}(\mathbf{A}) = r < \min(m, n)$ 时：
> - $\boldsymbol{\Sigma}$ 包含 $(m-r)$ 个零行或 $(n-r)$ 个零列
> - $\mathbf{U}$ 或 $\mathbf{V}$ 中相应的列与 0 相乘

> [!tip] 想法
> **简化 SVD** 只使用前 $r$ 个奇异值和向量

---

## 4.2 简化 SVD 定义

> [!important] 定义：简化 SVD
> $$
> \mathbf{A} = \mathbf{U}_r\boldsymbol{\Sigma}_r\mathbf{V}_r^\top
> $$
> 
> 其中：
> - $\mathbf{U}_r \in \mathbb{R}^{m \times r}$：前 $r$ 个左奇异向量
> - $\mathbf{V}_r \in \mathbb{R}^{n \times r}$：前 $r$ 个右奇异向量
> - $\boldsymbol{\Sigma}_r \in \mathbb{R}^{r \times r}$：$\text{diag}(\sigma_1, \ldots, \sigma_r)$

> [!success] 优点
> - **更紧凑**：没有无用的零块
> - **更高效**：更少的存储和计算时间
> - $\boldsymbol{\Sigma}_r$ **可逆**（所有 $\sigma_i > 0$）

---

## 4.3 Moore-Penrose 伪逆

> [!important] 定义：Moore-Penrose 伪逆
> 
> 对于任意矩阵 $\mathbf{A} \in \mathbb{R}^{m \times n}$，秩为 $r$，简化 SVD 为 $\mathbf{A} = \mathbf{U}_r\boldsymbol{\Sigma}_r\mathbf{V}_r^\top$，**伪逆**为：
> $$
> \mathbf{A}^+ := \mathbf{V}_r\boldsymbol{\Sigma}_r^{-1}\mathbf{U}_r^\top \in \mathbb{R}^{n \times m}
> $$
> 
> **注**：$\boldsymbol{\Sigma}_r^{-1} = \text{diag}(1/\sigma_1, \ldots, 1/\sigma_r)$

---

## 4.4 伪逆的性质

> [!success] 定理：$\mathbf{A}^+$ 的性质
> 
> 1. $\mathbf{A}\mathbf{A}^+\mathbf{x} = \text{proj}_{\text{col}(\mathbf{A})}\mathbf{x}$，对所有 $\mathbf{x} \in \mathbb{R}^m$
> 2. $\mathbf{A}^+\mathbf{A}\mathbf{x} = \text{proj}_{\text{col}(\mathbf{A}^\top)}\mathbf{x}$，对所有 $\mathbf{x} \in \mathbb{R}^n$
> 3. $\mathbf{A}\mathbf{A}^+\mathbf{A} = \mathbf{A}$（一致性）
> 4. $\mathbf{A}^+\mathbf{A}\mathbf{A}^+ = \mathbf{A}^+$（一致性）

> [!tip] 特殊情况
> - 当 $\text{rank}(\mathbf{A}) = m$ 时：$\mathbf{A}\mathbf{A}^+ = \mathbf{I}_m$
> - 当 $\text{rank}(\mathbf{A}) = n$ 时：$\mathbf{A}^+\mathbf{A} = \mathbf{I}_n$
> - 当 $\mathbf{A}$ 方阵且可逆时：$\mathbf{A}^+ = \mathbf{A}^{-1}$

---

# 五、应用

## 5.1 谱范数与 SVD

> [!important] 谱范数
> 
> 已知：$\|\mathbf{A}\|_2 = \sqrt{\lambda_{\max}(\mathbf{A}^\top\mathbf{A})}$
> 
> 由 SVD 定义知：$\lambda_j(\mathbf{A}^\top\mathbf{A}) = \sigma_j(\mathbf{A})^2$
> 
> 因此：
> $$
> \|\mathbf{A}\|_2 := \sigma_{\max}(\mathbf{A})
> $$
> 
> **解释**：最大奇异值 = 最大拉伸

---

## 5.2 SVD 作为秩-1 矩阵之和

> [!important] SVD 的求和形式
> $$
> \mathbf{A} = \mathbf{U}_r\boldsymbol{\Sigma}_r\mathbf{V}_r^\top = \sum_{i=1}^{r} \sigma_i\mathbf{u}_i\mathbf{v}_i^\top
> $$

> [!note] 解释
> - 每一项 $\mathbf{u}_i\mathbf{v}_i^\top$ 是**秩-1 矩阵**（且 $\|\mathbf{u}_i\mathbf{v}_i^\top\|_F = 1$）
> - $\sigma_i$ 加权第 $i$ 项的重要性
> - 大的 $\sigma_i$ → 重要的方向
> - 小的 $\sigma_i$ → 不太重要的方向

> [!tip] 近似的想法
> **只使用最重要的项！**

---

## 5.3 最优低秩近似

> [!important] 低秩近似
> $$
> \mathbf{A}_k = \sum_{i=1}^{k} \sigma_i\mathbf{u}_i\mathbf{v}_i^\top \quad (k < r)
> $$

> [!success] 定理：最优近似
> 
> $\mathbf{A}_k$ 是 $\mathbf{A}$ 关于 Frobenius 范数和谱范数的**最佳秩-$k$ 近似**：
> $$
> \mathbf{A}_k = \arg\min_{\substack{\mathbf{B} \in \mathbb{R}^{m \times n} \\ \text{rank}(\mathbf{B}) \leq k}} \|\mathbf{A} - \mathbf{B}\|_F = \arg\min_{\substack{\mathbf{B} \in \mathbb{R}^{m \times n} \\ \text{rank}(\mathbf{B}) \leq k}} \|\mathbf{A} - \mathbf{B}\|_2
> $$

> [!abstract] 近似误差
> - $\|\mathbf{A} - \mathbf{A}_k\|_F = \sqrt{\sum_{i=k+1}^{r} \sigma_i^2}$
> - $\|\mathbf{A} - \mathbf{A}_k\|_2 = \sigma_{k+1}$
> 
> **解释**：误差由被舍弃的奇异值决定

---

## 5.4 选择 $k$ 的标准

> [!tip] 选择 $k$ 的方法
> 
> **1. 相对误差**（谱范数）：
> $$
> \frac{\|\mathbf{A} - \mathbf{A}_k\|_2}{\|\mathbf{A}\|_2} = \frac{\sigma_{k+1}}{\sigma_1}
> $$
> 
> **2. 相对误差**（Frobenius 范数）：
> $$
> \frac{\|\mathbf{A} - \mathbf{A}_k\|_F}{\|\mathbf{A}\|_F} = \frac{\sqrt{\sum_{i=k+1}^{r} \sigma_i^2}}{\sqrt{\sum_{i=1}^{r} \sigma_i^2}}
> $$
> 
> **3. "能量保留"**：
> $$
> \frac{\sum_{i=1}^{k} \sigma_i^2}{\sum_{i=1}^{r} \sigma_i^2} \geq p \quad \text{（$p \times 100\%$ 的"能量"）}
> $$

---

## 5.5 实用奇异值分析

> [!abstract] 典型的奇异值变化趋势
> 
> | 类型 | 模式 | 特点 |
> |:---|:---|:---|
> | **快速下降** | $\sigma_i \approx c \cdot e^{-\alpha i}$ | 非常适合低秩近似，少数主导奇异值 |
> | **线性下降** | $\sigma_i \approx c - \alpha i$ | 可以进行适度近似，中等压缩率 |
> | **缓慢下降** | $\sigma_i \approx c \cdot i^{-\alpha}$ | 难以近似，很多重要分量 |

> [!tip] 启发式方法
> 在 $\log(\sigma_i)$ 图中寻找"肘部"！

---

## 5.6 应用：数据压缩

> [!example] 示例：图像压缩
> 
> 原始图像：659 × 512 像素矩阵
> - 完整存储：659 × 512 = 337,408 个数字（每通道）
> - 秩-50 SVD：(659 × 50) + 50 + (50 × 512) = 58,600 个数字（每通道）
> - ⇒ **压缩率**：≈ **减少 83%** 的存储！

> [!note] 步骤
> 1. 计算 SVD：$\mathbf{A} = \mathbf{U}\boldsymbol{\Sigma}\mathbf{V}^\top$
> 2. 根据奇异值选择 $k$
> 3. 近似：$\mathbf{A}_k = \mathbf{U}_k\boldsymbol{\Sigma}_k\mathbf{V}_k^\top$
> 4. 只存储 $\mathbf{U}_k$，$\boldsymbol{\Sigma}_k$，$\mathbf{V}_k$

---

## 5.7 应用：推荐系统

> [!important] 协同过滤 / 推荐系统
> 
> **问题**：$m$ 个用户，$n$ 部电影，但数据非常稀疏
> $$
> \mathbf{R} = \begin{pmatrix} 5 & ? & 1 & ? & 4 \\ ? & 3 & ? & 4 & ? \\ 2 & 1 & ? & ? & 5 \\ ? & 5 & 4 & 3 & ? \end{pmatrix} \in \mathbb{R}^{m \times n}
> $$
> 
> **想法**：用户-电影偏好存在于一个低维空间中

> [!success] SVD 方法
> 1. 填补缺失项（例如用列均值）
> 2. 计算秩-$k$ 近似：$\mathbf{R}_k = \mathbf{U}_k\boldsymbol{\Sigma}_k\mathbf{V}_k^\top$
> 3. 使用 $\mathbf{R}_k$ 预测缺失评分

---

## 5.8 因子的解释

> [!abstract] 推荐系统中的 SVD 因子
> 
> | 因子 | 解释 |
> |:---|:---|
> | **右奇异向量 $\mathbf{v}_j$** | "学习到的"电影类型（如动作 vs 剧情） |
> | **左奇异向量 $\mathbf{u}_i$** | 用户在这些类型维度上的偏好 |
> | **奇异值 $\sigma_i$** | 该类型维度的重要性 |

> [!success] 优点
> - 减少存储：$k + (m+n) \cdot k \ll m \cdot n$
> - "平滑"数据（低秩 = 正则化）
> - 即使在非常稀疏的数据上也能工作

---

## 5.9 SVD 何时特别有用？

> [!tip] SVD 的理想情况
> 
> 1. **低秩结构**：$\text{rank}(\mathbf{A}) \ll \min(m, n)$
> 2. **快速下降的奇异值**：少数主导
> 3. **超定系统**：$m > n$，可能不一致
> 4. **数据分析**：主成分分析（PCA），无需计算叉积
> 5. **数值稳健性**：正规方程的替代方案

> [!warning] 计算开销对比
> 
> | 方法 | 复杂度（$m \gg n$） |
> |:---|:---|
> | SVD | $O(\min(m^2n, mn^2))$，约 $4mn^2$ |
> | 正规方程 | $O(mn^2 + n^3)$，约 $mn^2$ |
> 
> 正规方程更快，但 SVD 更稳健 → **速度与稳定性之间的权衡**

---

# 六、总结

## 📌 SVD vs. 特征值分解

> [!tip] 对比
> 
> | 性质 | 谱分解 | SVD |
> |:---|:---|:---|
> | 矩阵类型 | 方阵 | 任意 $m \times n$ |
> | 正交分解 | 仅对称 | 总是 |
> | 数值稳定性 | 可能不稳定 | 稳定 |
> | 奇异性 | $\det(\mathbf{A}) = 0$ 时有问题 | 无问题 |
> | 几何解释 | 主轴（对称） | "最优"坐标 |

---

## 📝 核心公式速查

> [!summary] 关键公式
> 
> **SVD 分解**
> $$
> \mathbf{A} = \mathbf{U}\boldsymbol{\Sigma}\mathbf{V}^\top
> $$
> 
> **奇异值与特征值的关系**
> $$
> \sigma_i(\mathbf{A}) = \sqrt{\lambda_i(\mathbf{A}^\top\mathbf{A})}
> $$
> 
> **伪逆**
> $$
> \mathbf{A}^+ = \mathbf{V}_r\boldsymbol{\Sigma}_r^{-1}\mathbf{U}_r^\top
> $$
> 
> **谱范数**
> $$
> \|\mathbf{A}\|_2 = \sigma_{\max}(\mathbf{A})
> $$
> 
> **低秩近似误差**
> $$
> \|\mathbf{A} - \mathbf{A}_k\|_F = \sqrt{\sum_{i=k+1}^{r} \sigma_i^2}, \quad \|\mathbf{A} - \mathbf{A}_k\|_2 = \sigma_{k+1}
> $$

---

## 🔗 交叉联系

> [!abstract] SVD 连接了许多概念
> 
> - **特征值理论**：通过 $\mathbf{A}^\top\mathbf{A}$
> - **基本子空间**：显式正交基
> - **投影**：$\mathbf{A}\mathbf{A}^+$ 和 $\mathbf{A}^+\mathbf{A}$
> - **范数**：谱范数 $\|\mathbf{A}\|_2 = \sigma_1(\mathbf{A})$
> - **优化**：最小二乘，矩阵近似

> [!success] 重要性
> **SVD 是线性代数中非常核心的概念！**

---

## ✅ 自测清单

- [ ] 能解释 $\mathbf{A}^\top\mathbf{A}$ 的对称性和半正定性
- [ ] 能写出奇异值与特征值的关系
- [ ] 能写出完整 SVD 分解的形式
- [ ] 理解 SVD 的几何意义（旋转-拉伸-旋转）
- [ ] 能写出伪逆的定义
- [ ] 理解低秩近似的最优性
- [ ] 能计算低秩近似的误差
- [ ] 了解 SVD 在数据压缩和推荐系统中的应用

---

## 💻 实用提示

> [!tip] SVD 的实现
> 
> - **不要**通过 $\mathbf{A}^\top\mathbf{A}$ 计算（数值不稳定）
> - 使用专门的算法（例如 Golub-Reinsch）
> - 在 R 中：`svd(A)` 函数
> - 对于非常大的矩阵使用随机化（截断）SVD：`irlba::irlba(A, nv = k)`
