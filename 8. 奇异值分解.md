

## 第3页 - 所需预备知识

- **矩阵范数及其性质**，特别是：
    - 谱范数 ∥A∥2=λmax⁡(A⊤A)\|\mathbf{A}\|_2 = \sqrt{\lambda_{\max}(\mathbf{A}^\top\mathbf{A})}∥A∥2​=λmax​(A⊤A)​
    - 正交矩阵 Q\mathbf{Q}Q，满足 Q⊤Q=I\mathbf{Q}^\top\mathbf{Q} = \mathbf{I}Q⊤Q=I
- **矩阵分解概念**
- **线性代数**
    - 特征值和特征向量：Av=λv\mathbf{A}\mathbf{v} = \lambda\mathbf{v}Av=λv
    - 矩阵的秩
    - 基本子空间：col(A)\text{col}(\mathbf{A})col(A)，ker⁡(A)\ker(\mathbf{A})ker(A)，col(AT)\text{col}(\mathbf{A}^T)col(AT)
    - 子空间上的投影

---

## 第4页 - 目录

**动机**  
SVD：奇异值和奇异向量  
简化SVD和伪逆  
应用  
总结

---

## 第5页 - 对角化的局限性

- 特征值分解只适用于**方阵**
- 正交对角化目前只适用于**对称矩阵**
- **问题**：  
    大多数矩阵既不是方阵也不是对称的。

---

## 第6页 - 我们想要什么

一个分解 A=PDQ−1\mathbf{A} = \mathbf{P}\mathbf{D}\mathbf{Q}^{-1}A=PDQ−1：

- 适用于**任意** m×nm \times nm×n 矩阵，使得
- D\mathbf{D}D 是"对角"的，且
- P\mathbf{P}P 和 Q\mathbf{Q}Q 是正交矩阵

---

## 第7页 - 几何直观

**单位球作为测试案例：**

- 考虑所有满足 ∥x∥=1\|\mathbf{x}\| = 1∥x∥=1 的向量 x\mathbf{x}x
- 变换后：Ax\mathbf{A}\mathbf{x}Ax 具有不同的长度
- **问题**：哪个方向 Ax\mathbf{A}\mathbf{x}Ax 被拉伸得最多（第二多、第三多……）？

**示例**：A∈R2×3\mathbf{A} \in \mathbb{R}^{2 \times 3}A∈R2×3

- R3\mathbb{R}^3R3 中的单位球 → R2\mathbb{R}^2R2 中的椭圆
- 最长向量：椭圆的主轴
- 最短向量：椭圆的次轴

---

## 第8页 - 基本思想

**最大化问题：**

max⁡∥x∥=1∥Ax∥\max_{\|\mathbf{x}\|=1} \|\mathbf{A}\mathbf{x}\|∥x∥=1max​∥Ax∥

**技巧**：最大化 ∥Ax∥2\|\mathbf{A}\mathbf{x}\|^2∥Ax∥2 而不是 ∥Ax∥\|\mathbf{A}\mathbf{x}\|∥Ax∥

∥Ax∥2=(Ax)⊤(Ax)=x⊤(A⊤A)x\|\mathbf{A}\mathbf{x}\|^2 = (\mathbf{A}\mathbf{x})^\top(\mathbf{A}\mathbf{x}) = \mathbf{x}^\top(\mathbf{A}^\top\mathbf{A})\mathbf{x}∥Ax∥2=(Ax)⊤(Ax)=x⊤(A⊤A)x

**认识**：这是关于 A⊤A\mathbf{A}^\top\mathbf{A}A⊤A 的二次型！

以下始终使用：∥⋅∥≡∥⋅∥2\|\cdot\| \equiv \|\cdot\|_2∥⋅∥≡∥⋅∥2​！

---

## 第9页 - 目录

动机  
**SVD：奇异值和奇异向量**  
简化SVD和伪逆  
应用  
总结

---

## 第10页 - 矩阵 A⊤A\mathbf{A}^\top\mathbf{A}A⊤A

**定理：A⊤A\mathbf{A}^\top\mathbf{A}A⊤A 的性质**

对于 A∈Rm×n\mathbf{A} \in \mathbb{R}^{m \times n}A∈Rm×n 有：

1. A⊤A\mathbf{A}^\top\mathbf{A}A⊤A 是**对称的**：(A⊤A)⊤=A⊤A(\mathbf{A}^\top\mathbf{A})^\top = \mathbf{A}^\top\mathbf{A}(A⊤A)⊤=A⊤A
2. A⊤A\mathbf{A}^\top\mathbf{A}A⊤A 是**半正定的**：所有特征值 λi≥0\lambda_i \geq 0λi​≥0

**性质(2)的证明**：对于每个特征向量 vi\mathbf{v}_ivi​ 有：

0≤∥Avi∥22=vi⊤(A⊤A)vi=vi⊤(λivi)=λi0 \leq \|\mathbf{A}\mathbf{v}_i\|_2^2 = \mathbf{v}_i^\top(\mathbf{A}^\top\mathbf{A})\mathbf{v}_i = \mathbf{v}_i^\top(\lambda_i\mathbf{v}_i) = \lambda_i0≤∥Avi​∥22​=vi⊤​(A⊤A)vi​=vi⊤​(λi​vi​)=λi​

**推论**：A⊤A\mathbf{A}^\top\mathbf{A}A⊤A 可以正交对角化！

---

## 第11页 - 示例：A⊤A\mathbf{A}^\top\mathbf{A}A⊤A 的性质

设 A=(122110)∈R3×2\mathbf{A} = \begin{pmatrix} 1 & 2 \\ 2 & 1 \\ 1 & 0 \end{pmatrix} \in \mathbb{R}^{3 \times 2}A=​121​210​​∈R3×2

A⊤A=(121210)(122110)=(6445)\mathbf{A}^\top\mathbf{A} = \begin{pmatrix} 1 & 2 & 1 \\ 2 & 1 & 0 \end{pmatrix}\begin{pmatrix} 1 & 2 \\ 2 & 1 \\ 1 & 0 \end{pmatrix} = \begin{pmatrix} 6 & 4 \\ 4 & 5 \end{pmatrix}A⊤A=(12​21​10​)​121​210​​=(64​45​)

**对称性**：✓

**半正定**：

det⁡(A⊤A−λI)=(6−λ)(5−λ)−16=λ2−11λ+14=0\det(\mathbf{A}^\top\mathbf{A} - \lambda\mathbf{I}) = (6-\lambda)(5-\lambda) - 16 = \lambda^2 - 11\lambda + 14 = 0det(A⊤A−λI)=(6−λ)(5−λ)−16=λ2−11λ+14=0

λ1,2=11±652⇒λ1≈9.53,λ2≈1.47\lambda_{1,2} = \frac{11 \pm \sqrt{65}}{2} \Rightarrow \lambda_1 \approx 9.53, \lambda_2 \approx 1.47λ1,2​=211±65​​⇒λ1​≈9.53,λ2​≈1.47

λ1,λ2>0⇒A⊤A\lambda_1, \lambda_2 > 0 \Rightarrow \mathbf{A}^\top\mathbf{A}λ1​,λ2​>0⇒A⊤A 正定

---

## 第12页 - 定义：奇异值

设 λ1≥λ2≥⋯≥λn\lambda_1 \geq \lambda_2 \geq \cdots \geq \lambda_nλ1​≥λ2​≥⋯≥λn​ 是 A⊤A\mathbf{A}^\top\mathbf{A}A⊤A 的特征值。

**定义：奇异值**

A\mathbf{A}A 的**奇异值**是：

σi=λi,i=1,2,…,n\sigma_i = \sqrt{\lambda_i}, \quad i = 1, 2, \ldots, nσi​=λi​​,i=1,2,…,n

**解释：**

- σ1=max⁡∥x∥=1∥Ax∥\sigma_1 = \max_{\|\mathbf{x}\|=1} \|\mathbf{A}\mathbf{x}\|σ1​=max∥x∥=1​∥Ax∥（最大拉伸）
- σn\sigma_nσn​ 对应最小拉伸/最大压缩
- σi=0⇔λi=0\sigma_i = 0 \Leftrightarrow \lambda_i = 0σi​=0⇔λi​=0

---

## 第13页 - 示例：奇异值

矩阵 A=(122110)\mathbf{A} = \begin{pmatrix} 1 & 2 \\ 2 & 1 \\ 1 & 0 \end{pmatrix}A=​121​210​​：A⊤A\mathbf{A}^\top\mathbf{A}A⊤A 有 λ≈(9.53,1.47)\boldsymbol{\lambda} \approx (9.53, 1.47)λ≈(9.53,1.47)：

**奇异值：**

σ1=λ1=9.53≈3.09\sigma_1 = \sqrt{\lambda_1} = \sqrt{9.53} \approx 3.09σ1​=λ1​​=9.53​≈3.09

σ2=λ2=1.47≈1.21\sigma_2 = \sqrt{\lambda_2} = \sqrt{1.47} \approx 1.21σ2​=λ2​​=1.47​≈1.21

**解释：**

- σ1≈3.09\sigma_1 \approx 3.09σ1​≈3.09：矩阵 A\mathbf{A}A 的最大拉伸（v1\mathbf{v}_1v1​ 被拉伸3.1倍）
- σ2≈1.21\sigma_2 \approx 1.21σ2​≈1.21：最小拉伸（v2\mathbf{v}_2v2​ 被拉伸1.2倍）
- 比值 σmax⁡σmin⁡≈2.55\frac{\sigma_{\max}}{\sigma_{\min}} \approx 2.55σmin​σmax​​≈2.55 表明不同方向的拉伸差异（各向异性）

---

## 第14页 - 定义：奇异向量

**定义：右奇异向量和左奇异向量**

**右奇异向量 vi\mathbf{v}_ivi​**：

- A⊤A\mathbf{A}^\top\mathbf{A}A⊤A 的归一化特征向量
- A⊤Avi=λivi=σi2vi\mathbf{A}^\top\mathbf{A}\mathbf{v}_i = \lambda_i\mathbf{v}_i = \sigma_i^2\mathbf{v}_iA⊤Avi​=λi​vi​=σi2​vi​

**左奇异向量 ui\mathbf{u}_iui​**：

- 定义为：ui=Aviσi\mathbf{u}_i = \frac{\mathbf{A}\mathbf{v}_i}{\sigma_i}ui​=σi​Avi​​（对于 σi>0\sigma_i > 0σi​>0）

**重要性质：**

- {v1,…,vn}\{\mathbf{v}_1, \ldots, \mathbf{v}_n\}{v1​,…,vn​} 是正交归一的
- {u1,…,ur}\{\mathbf{u}_1, \ldots, \mathbf{u}_r\}{u1​,…,ur​} 是正交归一的（r=rank(A)r = \text{rank}(\mathbf{A})r=rank(A)）

---

## 第15页 - 奇异向量的正交性

**为什么 σiui=Avi\sigma_i\mathbf{u}_i = \mathbf{A}\mathbf{v}_iσi​ui​=Avi​ 是正交的？**

对于 i≠ji \neq ji=j：

(Avi)⊤(Avj)=vi⊤(A⊤A)vj=vi⊤(λjvj)=λj(vi⊤vj)=λj⋅0=0(\mathbf{A}\mathbf{v}_i)^\top(\mathbf{A}\mathbf{v}_j) = \mathbf{v}_i^\top(\mathbf{A}^\top\mathbf{A})\mathbf{v}_j = \mathbf{v}_i^\top(\lambda_j\mathbf{v}_j) = \lambda_j(\mathbf{v}_i^\top\mathbf{v}_j) = \lambda_j \cdot 0 = 0(Avi​)⊤(Avj​)=vi⊤​(A⊤A)vj​=vi⊤​(λj​vj​)=λj​(vi⊤​vj​)=λj​⋅0=0

对于 i=ji = ji=j：

∥Avi∥2=vi⊤(A⊤A)vi=λi∥vi∥2=λi=σi2\|\mathbf{A}\mathbf{v}_i\|^2 = \mathbf{v}_i^\top(\mathbf{A}^\top\mathbf{A})\mathbf{v}_i = \lambda_i\|\mathbf{v}_i\|^2 = \lambda_i = \sigma_i^2∥Avi​∥2=vi⊤​(A⊤A)vi​=λi​∥vi​∥2=λi​=σi2​

⇒\Rightarrow⇒ 左奇异向量 ui\mathbf{u}_iui​ 是正交归一的！

---

## 第16页 - 示例：奇异向量 (1)

矩阵 A=(122110)\mathbf{A} = \begin{pmatrix} 1 & 2 \\ 2 & 1 \\ 1 & 0 \end{pmatrix}A=​121​210​​：A⊤A\mathbf{A}^\top\mathbf{A}A⊤A 有 λ≈(9.53,1.47)\boldsymbol{\lambda} \approx (9.53, 1.47)λ≈(9.53,1.47)：

**右奇异向量（A⊤A\mathbf{A}^\top\mathbf{A}A⊤A 的特征向量）：**

对于 λ1≈9.53\lambda_1 \approx 9.53λ1​≈9.53：

(6445)(v1(1)v2(1))=9.53(v1(1)v2(1))\begin{pmatrix} 6 & 4 \\ 4 & 5 \end{pmatrix}\begin{pmatrix} v_1^{(1)} \\ v_2^{(1)} \end{pmatrix} = 9.53\begin{pmatrix} v_1^{(1)} \\ v_2^{(1)} \end{pmatrix}(64​45​)(v1(1)​v2(1)​​)=9.53(v1(1)​v2(1)​​)

解：v1≈(−0.75−0.662)\mathbf{v}_1 \approx \begin{pmatrix} -0.75 \\ -0.662 \end{pmatrix}v1​≈(−0.75−0.662​)

对于 λ2≈1.47\lambda_2 \approx 1.47λ2​≈1.47：

v2≈(0.662−0.75)\mathbf{v}_2 \approx \begin{pmatrix} 0.662 \\ -0.75 \end{pmatrix}v2​≈(0.662−0.75​)

（归一化，与 v1\mathbf{v}_1v1​ 正交）

---

## 第17页 - 示例：奇异向量 (2)

**左奇异向量：**

u1=Av1σ1≈(122110)(−0.75−0.662)/3.09=(−0.672−0.7−0.243)\mathbf{u}_1 = \frac{\mathbf{A}\mathbf{v}_1}{\sigma_1} \approx \begin{pmatrix} 1 & 2 \\ 2 & 1 \\ 1 & 0 \end{pmatrix}\begin{pmatrix} -0.75 \\ -0.662 \end{pmatrix} / 3.09 = \begin{pmatrix} -0.672 \\ -0.7 \\ -0.243 \end{pmatrix}u1​=σ1​Av1​​≈​121​210​​(−0.75−0.662​)/3.09=​−0.672−0.7−0.243​​

类似地：u2=Av2σ2≈(0.691−0.474−0.546)\mathbf{u}_2 = \frac{\mathbf{A}\mathbf{v}_2}{\sigma_2} \approx \begin{pmatrix} 0.691 \\ -0.474 \\ -0.546 \end{pmatrix}u2​=σ2​Av2​​≈​0.691−0.474−0.546​​

**验证正交归一性：**

- u1⊤u2≈(−0.672)(0.691)+(−0.7)(−0.474)+(−0.243)(−0.546)≈0\mathbf{u}_1^\top\mathbf{u}_2 \approx (-0.672)(0.691) + (-0.7)(-0.474) + (-0.243)(-0.546) \approx 0u1⊤​u2​≈(−0.672)(0.691)+(−0.7)(−0.474)+(−0.243)(−0.546)≈0 ✓
- ∥u1∥≈0.6722+0.72+0.2432≈1\|\mathbf{u}_1\| \approx \sqrt{0.672^2 + 0.7^2 + 0.243^2} \approx 1∥u1​∥≈0.6722+0.72+0.2432​≈1 ✓

---

## 第18页 - 基本子空间

**定理：子空间的刻画**

假设 A\mathbf{A}A 有 rrr 个非零奇异值。则：

1. {v1,…,vr}\{\mathbf{v}_1, \ldots, \mathbf{v}_r\}{v1​,…,vr​} 是 col(A⊤)\text{col}(\mathbf{A}^\top)col(A⊤) 的正交基（**行空间**）
2. {Av1,…,Avr}\{\mathbf{A}\mathbf{v}_1, \ldots, \mathbf{A}\mathbf{v}_r\}{Av1​,…,Avr​} 是 col(A)\text{col}(\mathbf{A})col(A) 的正交基（**列空间**）
3. {vr+1,…,vn}\{\mathbf{v}_{r+1}, \ldots, \mathbf{v}_n\}{vr+1​,…,vn​} 是 ker⁡(A)\ker(\mathbf{A})ker(A) 的正交基（**零空间**）
4. rank(A)=r\text{rank}(\mathbf{A}) = rrank(A)=r

**解释**：SVD给出了所有基本空间的显式正交基！

---

## 第19页 - 计算示例：基本子空间

对于 A=(111100)\mathbf{A} = \begin{pmatrix} 1 & 1 \\ 1 & 1 \\ 0 & 0 \end{pmatrix}A=​110​110​​，有 A⊤A=(2222)\mathbf{A}^\top\mathbf{A} = \begin{pmatrix} 2 & 2 \\ 2 & 2 \end{pmatrix}A⊤A=(22​22​)，rank(A)=1\text{rank}(\mathbf{A}) = 1rank(A)=1：

- **奇异值**：λ1(A⊤A)=4\lambda_1(\mathbf{A}^\top\mathbf{A}) = 4λ1​(A⊤A)=4，λ2(A⊤A)=0⇒σ1(A)=2\lambda_2(\mathbf{A}^\top\mathbf{A}) = 0 \Rightarrow \sigma_1(\mathbf{A}) = 2λ2​(A⊤A)=0⇒σ1​(A)=2
    
    矩阵 A\mathbf{A}A 在 v1\mathbf{v}_1v1​ 方向拉伸2倍；在 v2\mathbf{v}_2v2​ 方向拉伸0倍
    
- **行空间**：col(A⊤)=span{v1}=span{(11)}\text{col}(\mathbf{A}^\top) = \text{span}\{\mathbf{v}_1\} = \text{span}\left\{\begin{pmatrix} 1 \\ 1 \end{pmatrix}\right\}col(A⊤)=span{v1​}=span{(11​)}
    
- **列空间**：col(A)=span{u1}=span{(110)}\text{col}(\mathbf{A}) = \text{span}\{\mathbf{u}_1\} = \text{span}\left\{\begin{pmatrix} 1 \\ 1 \\ 0 \end{pmatrix}\right\}col(A)=span{u1​}=span⎩⎨⎧​​110​​⎭⎬⎫​
    
- **（右）零空间**：ker⁡(A)=span{v2}=span{(1−1)}\ker(\mathbf{A}) = \text{span}\{\mathbf{v}_2\} = \text{span}\left\{\begin{pmatrix} 1 \\ -1 \end{pmatrix}\right\}ker(A)=span{v2​}=span{(1−1​)}
    
- **左零空间**：ker⁡(A⊤)=span{u2,u3}\ker(\mathbf{A}^\top) = \text{span}\{\mathbf{u}_2, \mathbf{u}_3\}ker(A⊤)=span{u2​,u3​}
    

---

## 第20页 - SVD主定理

**定理：奇异值分解**

设 A∈Rm×n\mathbf{A} \in \mathbb{R}^{m \times n}A∈Rm×n，rank(A)=r\text{rank}(\mathbf{A}) = rrank(A)=r。则存在分解：

A=UΣV⊤\mathbf{A} = \mathbf{U}\boldsymbol{\Sigma}\mathbf{V}^\topA=UΣV⊤

其中：

- V∈Rn×n\mathbf{V} \in \mathbb{R}^{n \times n}V∈Rn×n：正交矩阵（右奇异向量在列中）
- U∈Rm×m\mathbf{U} \in \mathbb{R}^{m \times m}U∈Rm×m：正交矩阵（左奇异向量扩展为基）
- Σ∈Rm×n\boldsymbol{\Sigma} \in \mathbb{R}^{m \times n}Σ∈Rm×n：包含奇异值的"对角矩阵"

---

## 第21页 - Σ\boldsymbol{\Sigma}Σ 的结构

**矩阵 Σ\boldsymbol{\Sigma}Σ 的形式：**

Σ=(diag(σ1,…,σr)0r×(n−r)0(m−r)×r0(m−r)×(n−r))\boldsymbol{\Sigma} = \begin{pmatrix} \text{diag}(\sigma_1, \ldots, \sigma_r) & \mathbf{0}_{r \times (n-r)} \\ \mathbf{0}_{(m-r) \times r} & \mathbf{0}_{(m-r) \times (n-r)} \end{pmatrix}Σ=(diag(σ1​,…,σr​)0(m−r)×r​​0r×(n−r)​0(m−r)×(n−r)​​)

**示例：**

A\mathbf{A}A 方阵（m=n=3,r=2m = n = 3, r = 2m=n=3,r=2）：

Σ=(σ1000σ20000)\boldsymbol{\Sigma} = \begin{pmatrix} \sigma_1 & 0 & 0 \\ 0 & \sigma_2 & 0 \\ 0 & 0 & 0 \end{pmatrix}Σ=​σ1​00​0σ2​0​000​​

A\mathbf{A}A 矩形（m=2,n=3,r=2m = 2, n = 3, r = 2m=2,n=3,r=2）：

Σ=(σ1000σ20)\boldsymbol{\Sigma} = \begin{pmatrix} \sigma_1 & 0 & 0 \\ 0 & \sigma_2 & 0 \end{pmatrix}Σ=(σ1​0​0σ2​​00​)

---

## 第22页 - SVD定理的证明（概要）

**分解的构造：**

1. **右奇异向量**：A⊤A\mathbf{A}^\top\mathbf{A}A⊤A 的特征向量  
    V=(v1∣v2∣⋯∣vn)\mathbf{V} = (\mathbf{v}_1 \mid \mathbf{v}_2 \mid \cdots \mid \mathbf{v}_n)V=(v1​∣v2​∣⋯∣vn​)
    
2. **左奇异向量**：对于 i≤ri \leq ri≤r，ui=Avi/σi\mathbf{u}_i = \mathbf{A}\mathbf{v}_i / \sigma_iui​=Avi​/σi​  
    Ur=(u1∣u2∣⋯∣ur)\mathbf{U}_r = (\mathbf{u}_1 \mid \mathbf{u}_2 \mid \cdots \mid \mathbf{u}_r)Ur​=(u1​∣u2​∣⋯∣ur​)
    
3. **完备化**：将 Ur\mathbf{U}_rUr​ 扩展为 Rm\mathbb{R}^mRm 的正交基
    
4. **验证**：对于 i≤ri \leq ri≤r，Avi=σiui\mathbf{A}\mathbf{v}_i = \sigma_i\mathbf{u}_iAvi​=σi​ui​；对于 i>ri > ri>r，Avi=0\mathbf{A}\mathbf{v}_i = \mathbf{0}Avi​=0
    

---

## 第23页 - 几何解释

**变换**：x↦Ax=UΣV⊤x\mathbf{x} \mapsto \mathbf{A}\mathbf{x} = \mathbf{U}\boldsymbol{\Sigma}\mathbf{V}^\top\mathbf{x}x↦Ax=UΣV⊤x

**三个步骤：**

1. **V⊤\mathbf{V}^\topV⊤**：坐标变换（旋转/反射）到"最优"方向
    
2. **Σ\boldsymbol{\Sigma}Σ**：沿这些轴按因子 σi\sigma_iσi​ "拉伸"，对于 i>ri > ri>r 置零
    
3. **U\mathbf{U}U**：坐标变换（旋转/反射）回到目标空间
    

⇒\Rightarrow⇒ **SVD将每个从 Rn\mathbb{R}^nRn 到 Rm\mathbb{R}^mRm 的线性变换分解为"旋转-拉伸-旋转"。**

---

## 第24页 - 目录

动机  
SVD：奇异值和奇异向量  
**简化SVD和伪逆**  
应用  
总结

---

## 第25页 - 简化SVD：动机

**问题**：Σ\boldsymbol{\Sigma}Σ 中有很多零元素

当 rank(A)=r<min⁡(m,n)\text{rank}(\mathbf{A}) = r < \min(m, n)rank(A)=r<min(m,n) 时：

- Σ\boldsymbol{\Sigma}Σ 包含 (m−r)(m-r)(m−r) 个零行或 (n−r)(n-r)(n−r) 个零列
- U\mathbf{U}U 或 V\mathbf{V}V 中相应的列与0相乘

**想法**：**简化SVD**只使用前 rrr 个奇异值和向量

对 U\mathbf{U}U 和 V\mathbf{V}V 进行分块：

U=(Ur∣Um−r),V=(Vr∣Vn−r)\mathbf{U} = (\mathbf{U}_r \mid \mathbf{U}_{m-r}), \quad \mathbf{V} = (\mathbf{V}_r \mid \mathbf{V}_{n-r})U=(Ur​∣Um−r​),V=(Vr​∣Vn−r​)

其中 Ur∈Rm×r\mathbf{U}_r \in \mathbb{R}^{m \times r}Ur​∈Rm×r，Vr∈Rn×r\mathbf{V}_r \in \mathbb{R}^{n \times r}Vr​∈Rn×r

---

## 第26页 - 简化SVD：定义

**定义：简化SVD**

A=UrΣrVr⊤\mathbf{A} = \mathbf{U}_r\boldsymbol{\Sigma}_r\mathbf{V}_r^\topA=Ur​Σr​Vr⊤​

其中：

- Ur∈Rm×r\mathbf{U}_r \in \mathbb{R}^{m \times r}Ur​∈Rm×r：前 rrr 个左奇异向量
- Vr∈Rn×r\mathbf{V}_r \in \mathbb{R}^{n \times r}Vr​∈Rn×r：前 rrr 个右奇异向量
- Σr∈Rr×r\boldsymbol{\Sigma}_r \in \mathbb{R}^{r \times r}Σr​∈Rr×r：diag(σ1,…,σr)\text{diag}(\sigma_1, \ldots, \sigma_r)diag(σ1​,…,σr​)

**优点：**

- **更紧凑**：没有无用的零块
- **更高效**：更少的存储和计算时间
- Σr\boldsymbol{\Sigma}_rΣr​ **可逆**（所有 σi>0\sigma_i > 0σi​>0）

---

## 第27页 - Moore-Penrose 伪逆

**动机**：当 A\mathbf{A}A 不是方阵/不可逆时怎么办？

**定义：Moore-Penrose 伪逆**

对于任意矩阵 A∈Rm×n\mathbf{A} \in \mathbb{R}^{m \times n}A∈Rm×n，秩为 rrr，简化SVD为 A=UrΣrVr⊤\mathbf{A} = \mathbf{U}_r\boldsymbol{\Sigma}_r\mathbf{V}_r^\topA=Ur​Σr​Vr⊤​，**伪逆**为：

A+:=VrΣr−1Ur⊤∈Rn×m\mathbf{A}^+ := \mathbf{V}_r\boldsymbol{\Sigma}_r^{-1}\mathbf{U}_r^\top \in \mathbb{R}^{n \times m}A+:=Vr​Σr−1​Ur⊤​∈Rn×m

**注**：Σr−1=diag(1/σ1,…,1/σr)\boldsymbol{\Sigma}_r^{-1} = \text{diag}(1/\sigma_1, \ldots, 1/\sigma_r)Σr−1​=diag(1/σ1​,…,1/σr​)

---

## 第28页 - 伪逆的性质

**定理：A+\mathbf{A}^+A+ 的性质**

1. AA+x=projcol(A)x\mathbf{A}\mathbf{A}^+\mathbf{x} = \text{proj}_{\text{col}(\mathbf{A})}\mathbf{x}AA+x=projcol(A)​x，对所有 x∈Rm\mathbf{x} \in \mathbb{R}^mx∈Rm
2. A+Ax=projcol(A⊤)x\mathbf{A}^+\mathbf{A}\mathbf{x} = \text{proj}_{\text{col}(\mathbf{A}^\top)}\mathbf{x}A+Ax=projcol(A⊤)​x，对所有 x∈Rn\mathbf{x} \in \mathbb{R}^nx∈Rn
3. AA+A=A\mathbf{A}\mathbf{A}^+\mathbf{A} = \mathbf{A}AA+A=A（一致性）
4. A+AA+=A+\mathbf{A}^+\mathbf{A}\mathbf{A}^+ = \mathbf{A}^+A+AA+=A+（一致性）

**特殊情况：**

- 当 rank(A)=m\text{rank}(\mathbf{A}) = mrank(A)=m 时：AA+=Im\mathbf{A}\mathbf{A}^+ = \mathbf{I}_mAA+=Im​
- 当 rank(A)=n\text{rank}(\mathbf{A}) = nrank(A)=n 时：A+A=In\mathbf{A}^+\mathbf{A} = \mathbf{I}_nA+A=In​
- 当 A\mathbf{A}A 方阵且可逆时：A+=A−1\mathbf{A}^+ = \mathbf{A}^{-1}A+=A−1

---

## 第29页 - 目录

动机  
SVD：奇异值和奇异向量  
简化SVD和伪逆  
**应用**  
总结

---

## 第30页 - 谱范数与SVD

已知：∥A∥2=λmax⁡(A⊤A)\|\mathbf{A}\|_2 = \sqrt{\lambda_{\max}(\mathbf{A}^\top\mathbf{A})}∥A∥2​=λmax​(A⊤A)​

由SVD定义知：λj(A⊤A)=σj(A)2\lambda_j(\mathbf{A}^\top\mathbf{A}) = \sigma_j(\mathbf{A})^2λj​(A⊤A)=σj​(A)2

因此：

∥A∥2:=σmax⁡(A)\|\mathbf{A}\|_2 := \sigma_{\max}(\mathbf{A})∥A∥2​:=σmax​(A)

**解释**：最大奇异值 = 最大拉伸

---

## 第31页 - SVD作为秩-1矩阵之和

**SVD的求和形式：**

A=UrΣrVr⊤=∑i=1rσiuivi⊤\mathbf{A} = \mathbf{U}_r\boldsymbol{\Sigma}_r\mathbf{V}_r^\top = \sum_{i=1}^{r} \sigma_i\mathbf{u}_i\mathbf{v}_i^\topA=Ur​Σr​Vr⊤​=i=1∑r​σi​ui​vi⊤​

**解释：**

- 每一项 uivi⊤\mathbf{u}_i\mathbf{v}_i^\topui​vi⊤​ 是**秩-1矩阵**（且 ∥uivi⊤∥F=1\|\mathbf{u}_i\mathbf{v}_i^\top\|_F = 1∥ui​vi⊤​∥F​=1）
- σi\sigma_iσi​ 加权第 iii 项的重要性
- 大的 σi\sigma_iσi​ → 重要的方向
- 小的 σi\sigma_iσi​ → 不太重要的方向

**近似的想法**：只使用最重要的项！

---

## 第32页 - 秩-k近似

**低秩近似：**

Ak=∑i=1kσiuivi⊤(k<r)\mathbf{A}_k = \sum_{i=1}^{k} \sigma_i\mathbf{u}_i\mathbf{v}_i^\top \quad (k < r)Ak​=i=1∑k​σi​ui​vi⊤​(k<r)

**定理：最优近似**

Ak\mathbf{A}_kAk​ 是 A\mathbf{A}A 关于Frobenius范数和谱范数的**最佳秩-k近似**：

Ak=arg⁡min⁡B∈Rm×nrank(B)≤k∥A−B∥F=arg⁡min⁡B∈Rm×nrank(B)≤k∥A−B∥2\mathbf{A}_k = \arg\min_{\substack{\mathbf{B} \in \mathbb{R}^{m \times n} \\ \text{rank}(\mathbf{B}) \leq k}} \|\mathbf{A} - \mathbf{B}\|_F = \arg\min_{\substack{\mathbf{B} \in \mathbb{R}^{m \times n} \\ \text{rank}(\mathbf{B}) \leq k}} \|\mathbf{A} - \mathbf{B}\|_2Ak​=argB∈Rm×nrank(B)≤k​min​∥A−B∥F​=argB∈Rm×nrank(B)≤k​min​∥A−B∥2​

**近似误差：**

- ∥A−Ak∥F=∑i=k+1rσi2\|\mathbf{A} - \mathbf{A}_k\|_F = \sqrt{\sum_{i=k+1}^{r} \sigma_i^2}∥A−Ak​∥F​=∑i=k+1r​σi2​​
- ∥A−Ak∥2=σk+1\|\mathbf{A} - \mathbf{A}_k\|_2 = \sigma_{k+1}∥A−Ak​∥2​=σk+1​

**解释**：误差由被舍弃的奇异值决定

---

## 第33页 - 最优秩-k近似的选择

**选择 kkk 的标准：**

**1. 相对误差**（谱范数）：

∥A−Ak∥2∥A∥2=σk+1σ1\frac{\|\mathbf{A} - \mathbf{A}_k\|_2}{\|\mathbf{A}\|_2} = \frac{\sigma_{k+1}}{\sigma_1}∥A∥2​∥A−Ak​∥2​​=σ1​σk+1​​

**2. 相对误差**（Frobenius范数）：

∥A−Ak∥F∥A∥F=∑i=k+1rσi2∑i=1rσi2\frac{\|\mathbf{A} - \mathbf{A}_k\|_F}{\|\mathbf{A}\|_F} = \frac{\sqrt{\sum_{i=k+1}^{r} \sigma_i^2}}{\sqrt{\sum_{i=1}^{r} \sigma_i^2}}∥A∥F​∥A−Ak​∥F​​=∑i=1r​σi2​​∑i=k+1r​σi2​​​

**3. "能量保留"**：

∑i=1kσi2∑i=1rσi2≥p（p×100%的"能量"）\frac{\sum_{i=1}^{k} \sigma_i^2}{\sum_{i=1}^{r} \sigma_i^2} \geq p \quad \text{（p×100\%的"能量"）}∑i=1r​σi2​∑i=1k​σi2​​≥p（p×100%的"能量"）

---

## 第34页 - 实用奇异值分析

**典型的奇异值变化趋势：**

**1. 快速下降**：σi≈c⋅e−αi\sigma_i \approx c \cdot e^{-\alpha i}σi​≈c⋅e−αi

- 非常适合低秩近似
- 少数主导的奇异值

**2. 线性下降**：σi≈c−αi\sigma_i \approx c - \alpha iσi​≈c−αi

- 可以进行适度近似
- 中等压缩率

**3. 缓慢下降**：σi≈c⋅i−α\sigma_i \approx c \cdot i^{-\alpha}σi​≈c⋅i−α

- 难以近似
- 很多重要分量

**启发式方法**：在 log⁡(σi)\log(\sigma_i)log(σi​) 图中寻找"肘部"！

---

## 第35页 - 应用：数据压缩

**示例：图像压缩**

原始图像：659 × 512 像素矩阵

- 完整存储：659 × 512 = 337,408 个数字（每通道）
- 秩-50 SVD：(659 × 50) + 50 + (50 × 512) = 58,600 个数字（每通道）
- ⇒\Rightarrow⇒ **压缩率**：≈ **减少83%**的存储！

**步骤：**

1. 计算SVD：A=UΣV⊤\mathbf{A} = \mathbf{U}\boldsymbol{\Sigma}\mathbf{V}^\topA=UΣV⊤
2. 根据奇异值选择 kkk
3. 近似：Ak=UkΣkVk⊤\mathbf{A}_k = \mathbf{U}_k\boldsymbol{\Sigma}_k\mathbf{V}_k^\topAk​=Uk​Σk​Vk⊤​
4. 只存储 Uk\mathbf{U}_kUk​，Σk\boldsymbol{\Sigma}_kΣk​，Vk\mathbf{V}_kVk​

---

## 第36页 - 示例：数据压缩

（图示：高斯肖像的不同秩近似）

- (a) 原图
- (b) 每个通道的奇异值（对数图）
- (c) k = 5
- (d) k = 20
- (e) k = 50
- (f) k = 200

---

## 第37页 - 应用：推荐系统

**协同过滤 / 推荐系统**

**问题：**

Netflix场景：mmm 个用户，nnn 部电影，但数据非常稀疏：大多数用户没有评价大多数电影

R=(5?1?4?3?4?21??5?543?)∈Rm×n\mathbf{R} = \begin{pmatrix} 5 & ? & 1 & ? & 4 \\ ? & 3 & ? & 4 & ? \\ 2 & 1 & ? & ? & 5 \\ ? & 5 & 4 & 3 & ? \end{pmatrix} \in \mathbb{R}^{m \times n}R=​5?2?​?315​1??4​?4?3​4?5?​​∈Rm×n

**想法**：用户-电影偏好存在于一个低维空间中（类型和用户类别）。

**SVD方法：**

1. 填补缺失项（例如用列均值）
2. 计算秩-k近似：Rk=UkΣkVk⊤\mathbf{R}_k = \mathbf{U}_k\boldsymbol{\Sigma}_k\mathbf{V}_k^\topRk​=Uk​Σk​Vk⊤​
3. 使用 Rk\mathbf{R}_kRk​ 预测缺失评分

---

## 第38页 - 应用：推荐系统（续）

**因子的解释：**

- **右奇异向量 vj\mathbf{v}_jvj​**："学习到的"电影类型
    - v1\mathbf{v}_1v1​ 可能编码"动作vs剧情"，v2\mathbf{v}_2v2​ "喜剧vs惊悚"等
- **左奇异向量 ui\mathbf{u}_iui​**：用户在这些类型维度上的偏好
- **奇异值 σi\sigma_iσi​**：该类型维度的重要性

**优点：**

- 减少存储：k+(m+n)⋅k≪m⋅nk + (m+n) \cdot k \ll m \cdot nk+(m+n)⋅k≪m⋅n（当 k≪min⁡(m,n)k \ll \min(m,n)k≪min(m,n) 时）
- "平滑"数据（低秩 = 正则化）
- 即使在非常稀疏的数据上也能工作

**更现代的变体：**

- 矩阵补全：例如（非负）矩阵分解

---

## 第39页 - SVD何时特别有用？

**SVD的理想情况：**

1. **低秩结构**：rank(A)≪min⁡(m,n)\text{rank}(\mathbf{A}) \ll \min(m, n)rank(A)≪min(m,n)
2. **快速下降的奇异值**：少数主导
3. **超定系统**：m>nm > nm>n，可能不一致
4. **数据分析**：主成分分析（PCA），无需计算叉积
5. **数值稳健性**：正规方程的替代方案

**计算开销：**

- SVD：O(min⁡(m2n,mn2))O(\min(m^2n, mn^2))O(min(m2n,mn2))，对于 m≫nm \gg nm≫n 约为 4mn24mn^24mn2
- 正规方程：O(mn2+n3)O(mn^2 + n^3)O(mn2+n3)，对于 m≫nm \gg nm≫n 约为 mn2mn^2mn2
- 对于 m≫nm \gg nm≫n，正规方程更快，但SVD更稳健  
    → **速度与稳定性之间的权衡**

---

## 第40页 - SVD vs. 特征值分解

|性质|谱分解|SVD|
|---|---|---|
|矩阵类型|方阵|任意 m×nm \times nm×n|
|正交分解|仅对称|总是|
|数值稳定性|可能不稳定|稳定|
|奇异性|det⁡(A)=0\det(\mathbf{A}) = 0det(A)=0 时有问题|无问题|
|几何解释|主轴（对称）|"最优"坐标|

---

## 第41页 - 目录

动机  
SVD：奇异值和奇异向量  
简化SVD和伪逆  
应用  
**总结**

---

## 第42页 - 我们学到了什么？

**核心概念：**

1. SVD将特征值分解扩展到任意 m×nm \times nm×n 矩阵
2. 奇异值 vs. 特征值：σi(A)=λi(A⊤A)\sigma_i(\mathbf{A}) = \sqrt{\lambda_i(\mathbf{A}^\top\mathbf{A})}σi​(A)=λi​(A⊤A)​
3. **主定理**：  
    A=UΣV⊤\mathbf{A} = \mathbf{U}\boldsymbol{\Sigma}\mathbf{V}^\topA=UΣV⊤，其中 U\mathbf{U}U（m×mm \times mm×m）、V\mathbf{V}V（n×nn \times nn×n）正交，Σ\boldsymbol{\Sigma}Σ（m×nm \times nm×n）对角
4. **简化SVD**：  
    A=UrΣrVr⊤\mathbf{A} = \mathbf{U}_r\boldsymbol{\Sigma}_r\mathbf{V}_r^\topA=Ur​Σr​Vr⊤​ 提高效率（r=rank(A)r = \text{rank}(\mathbf{A})r=rank(A)）
5. **伪逆**：  
    A+=VrΣr−1Ur⊤\mathbf{A}^+ = \mathbf{V}_r\boldsymbol{\Sigma}_r^{-1}\mathbf{U}_r^\topA+=Vr​Σr−1​Ur⊤​

**应用：**

- 求解最小二乘问题
- 最优低秩近似
- 数据压缩和分析

---

## 第43页 - 交叉联系

**SVD连接了许多概念：**

- **特征值理论**：通过 A⊤A\mathbf{A}^\top\mathbf{A}A⊤A
- **基本子空间**：显式正交基
- **投影**：AA+\mathbf{A}\mathbf{A}^+AA+ 和 A+A\mathbf{A}^+\mathbf{A}A+A
- **范数**：谱范数 ∥A∥2=σ1(A)\|\mathbf{A}\|_2 = \sigma_1(\mathbf{A})∥A∥2​=σ1​(A)
- **优化**：最小二乘，矩阵近似

**SVD是线性代数中非常核心的概念！**

---

## 第44页 - 测验

**哪些陈述是正确的？**

1. 如果 A∈Rm×n\mathbf{A} \in \mathbb{R}^{m \times n}A∈Rm×n，则 A⊤A∈Rm×m\mathbf{A}^\top\mathbf{A} \in \mathbb{R}^{m \times m}A⊤A∈Rm×m
2. 如果 A∈Rm×n\mathbf{A} \in \mathbb{R}^{m \times n}A∈Rm×n，则 A⊤A\mathbf{A}^\top\mathbf{A}A⊤A 是对称的
3. A⊤A\mathbf{A}^\top\mathbf{A}A⊤A 的特征值是 A\mathbf{A}A 的奇异值
4. 如果 A∈Rn×n\mathbf{A} \in \mathbb{R}^{n \times n}A∈Rn×n，则 A\mathbf{A}A 总是可对角化的
5. A⊤A\mathbf{A}^\top\mathbf{A}A⊤A 总是可以正交对角化的

**答案**：正确的是 2 和 5。

（1错误：应为 n×nn \times nn×n；3错误：特征值的平方根才是奇异值；4错误：不是所有方阵都可对角化）

---

## 第45页 - 实用提示

**典型参数：**

- **完整SVD**：A=UΣV⊤\mathbf{A} = \mathbf{U}\boldsymbol{\Sigma}\mathbf{V}^\topA=UΣV⊤（所有矩阵完整）
- **简化SVD**：只到矩阵的秩
- **截断SVD**：只取 kkk 个最大奇异值
    - 根据奇异值选择 kkk（例如肘部方法）

**SVD的实现：**

- **不要**通过 A⊤A\mathbf{A}^\top\mathbf{A}A⊤A 计算（数值不稳定）
- 使用专门的算法（例如 Golub-Reinsch）
- 在R中：`svd(A)` 函数
- 对于非常大的矩阵使用随机化（截断）SVD：  
    `irlba::irlba(A, nv = k)`（随机近似）