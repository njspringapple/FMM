## 1. 从一个简单的二元方程组出发

### 1.1 问题引入与向量记号

考虑以下二元二次方程组：

$$
\begin{cases} x^2 + y^2 - 5 = 0 \\ x^2 - y^2 - 3 = 0 \end{cases}
$$

手动求解可以得到 $x^2 = 4,\; y^2 = 1$，即 $(x,y) = (\pm 2,\, \pm 1)$。但我们的目标不是手动解这道题，而是借助它来理解一套**通用的数值求解框架**——这套框架在变量成千上万时依然适用。

**为什么要改写记号？** 当变量只有两个时，用 $x, y$ 很自然。但如果有 100 个未知数，总不能用 $x, y, z, w, \ldots$ 逐一命名。所以我们统一使用下标记号：令 $x_1 = x$，$x_2 = y$，方程组变成：

$$
\begin{cases} f_1(x_1, x_2) = x_1^2 + x_2^2 - 5 = 0 \\ f_2(x_1, x_2) = x_1^2 - x_2^2 - 3 = 0 \end{cases}
$$

进一步，我们把所有未知数打包成一个**向量** $X = \begin{pmatrix} x_1 \\ x_2 \end{pmatrix}$，把所有方程打包成一个**向量函数**：

$$
F(X) = \begin{pmatrix} f_1(x_1, x_2) \\ f_2(x_1, x_2) \end{pmatrix} = \begin{pmatrix} x_1^2 + x_2^2 - 5 \\ x_1^2 - x_2^2 - 3 \end{pmatrix}
$$

这样，原来的方程组就可以简洁地写成：

$$
F(X) = \mathbf{0}
$$

这个记号看起来只是缩写，但它是后续所有方法的基础——无论方程有 2 个还是 20000 个，形式都是一样的。

### 1.2 偏导数与梯度：理解函数对变量的"敏感度"

现在我们要用数值方法来求解 $F(X) = \mathbf{0}$。核心思路是：从一个**初始猜测**出发，逐步"调整"未知数的值，让 $F(X)$ 越来越接近零向量。

但问题是：**该怎么调整？**

假设我们随便猜一个初始点 $X_0 = (1,\, 1)^T$，代入得到：

$$
F(X_0) = \begin{pmatrix} 1 + 1 - 5 \\ 1 - 1 - 3 \end{pmatrix} = \begin{pmatrix} -3 \\ -3 \end{pmatrix}
$$

结果不是零，所以我们需要移动这个点。但往哪个方向移动？移动多少？

要回答这个问题，我们首先需要知道：**当某个变量发生微小变化时，函数值会如何响应。** 这就是**偏导数**的概念——固定其他变量不动，只看一个变量变化对函数的影响。

对 $f_1(x_1, x_2) = x_1^2 + x_2^2 - 5$ 来说：

$$
\frac{\partial f_1}{\partial x_1} = 2x_1 \quad \text{（只动 } x_1 \text{，} f_1 \text{ 变化多快）}
$$

$$
\frac{\partial f_1}{\partial x_2} = 2x_2 \quad \text{（只动 } x_2 \text{，} f_1 \text{ 变化多快）}
$$

把一个函数对所有变量的偏导数**打包成一个向量**，就得到了**梯度**：

$$
\nabla f_1 = \begin{pmatrix} \dfrac{\partial f_1}{\partial x_1} \\[6pt] \dfrac{\partial f_1}{\partial x_2} \end{pmatrix} = \begin{pmatrix} 2x_1 \\ 2x_2 \end{pmatrix}, \qquad \nabla f_2 = \begin{pmatrix} \dfrac{\partial f_2}{\partial x_1} \\[6pt] \dfrac{\partial f_2}{\partial x_2} \end{pmatrix} = \begin{pmatrix} 2x_1 \\ -2x_2 \end{pmatrix}
$$

在 $X_0 = (1,\,1)^T$ 处代入：

$$
\nabla f_1 = \begin{pmatrix} 2 \\ 2 \end{pmatrix}, \qquad \nabla f_2 = \begin{pmatrix} 2 \\ -2 \end{pmatrix}
$$

梯度的含义是**变化率**（或者说**敏感度**）：它告诉你"稍微动一下某个变量，函数会跟着动多少"。而且有一个重要性质——**梯度指向的方向，是函数值增长最快的方向**。

现在回到我们的问题。当前 $f_1 = -3$，需要增大到 0，而 $\nabla f_1 = (2,\,2)^T$ 告诉我们沿这个方向走可以让 $f_1$ 增大。同理 $f_2 = -3$，也需要增大到 0，而 $\nabla f_2 = (2,\,-2)^T$ 告诉我们沿这个方向走可以让 $f_2$ 增大。

**矛盾出现了**：想让 $f_1$ 增大，需要增大 $x_2$（因为 $\partial f_1 / \partial x_2 = 2 > 0$）；但想让 $f_2$ 增大，需要**减小** $x_2$（因为 $\partial f_2 / \partial x_2 = -2 < 0$）。两个目标对 $x_2$ 的要求是**冲突**的！

所以我们不能简单地"沿着某一个梯度方向走"。我们需要一个工具把所有函数的梯度信息**综合**起来，找到一个**折中**的移动方向——这就引出了**雅可比矩阵**。

### 1.3 雅可比矩阵：线性近似与综合决策

把 $f_1$ 和 $f_2$ 的梯度**按行排列**，就得到了**雅可比矩阵**（Jacobian matrix）：

$$
J(X) = \begin{pmatrix} \nabla f_1^T \\ \nabla f_2^T \end{pmatrix} = \begin{pmatrix} \dfrac{\partial f_1}{\partial x_1} & \dfrac{\partial f_1}{\partial x_2} \\[6pt] \dfrac{\partial f_2}{\partial x_1} & \dfrac{\partial f_2}{\partial x_2} \end{pmatrix} = \begin{pmatrix} 2x_1 & 2x_2 \\ 2x_1 & -2x_2 \end{pmatrix}
$$

在点 $X_0 = (1,\,1)^T$ 处：

$$
J(X_0) = \begin{pmatrix} 2 & 2 \\ 2 & -2 \end{pmatrix}
$$

雅可比矩阵的核心作用是提供了一个**线性近似**（本质是泰勒展开的一阶项）：当 $X$ 发生微小变化 $\Delta X$ 时，$F(X)$ 的变化量近似为：

$$
\Delta F \approx J \cdot \Delta X
$$

这个公式非常有用！我们知道当前 $F(X_0) = (-3,\,-3)^T$，希望 $F$ 变成 $(0,\,0)^T$，即希望变化量为 $\Delta F = (0,0)^T - (-3,-3)^T = (3,\,3)^T$。那么需要怎样的 $\Delta X$ 呢？

$$
J \cdot \Delta X = \Delta F \quad \Rightarrow \quad \begin{pmatrix} 2 & 2 \\ 2 & -2 \end{pmatrix} \begin{pmatrix} \Delta x_1 \\ \Delta x_2 \end{pmatrix} = \begin{pmatrix} 3 \\ 3 \end{pmatrix}
$$

解这个线性方程组（第一行加第二行得 $4\Delta x_1 = 6$，第一行减第二行得 $4\Delta x_2 = 0$），得到 $\Delta X = (1.5,\; 0)^T$。

注意这个结果：$x_1$ 增加 1.5，而 $x_2$ 不动——雅可比矩阵自动帮我们找到了解决梯度冲突的**折中方案**！$x_2$ 不动，恰好兼顾了 $f_1$ 和 $f_2$ 对 $x_2$ 的相反需求。

更新后 $X_1 = X_0 + \Delta X = (2.5,\; 1)^T$，代入验证：

$$
F(X_1) = \begin{pmatrix} 6.25 + 1 - 5 \\ 6.25 - 1 - 3 \end{pmatrix} = \begin{pmatrix} 2.25 \\ 2.25 \end{pmatrix}
$$

$\|F\|$ 从 $\sqrt{(-3)^2+(-3)^2} = \sqrt{18} \approx 4.24$ 降到了 $\sqrt{2.25^2+2.25^2} \approx 3.18$，确实在向零靠近。虽然还没有到达——这是因为雅可比矩阵只是**线性近似**，而我们的方程是非线性的——但好消息是：只要不断重复这个过程（重新算 $J$、重新解方程、重新更新 $X$），就能越来越逼近真正的解。这就是**牛顿迭代法**的核心思想。

### 1.4 补充视角：方向导数

在继续讨论牛顿法的完整算法之前，我们从另一个角度来理解 $J \cdot \Delta X$ 这个表达式，这有助于更深入地看清梯度、雅可比矩阵之间的关系。

对于**单个标量函数** $f(\mathbf{x})$，如果我们沿着方向 $\mathbf{h}$ 移动，函数的变化率可以写成梯度与移动方向的**内积**：

$$
D_\mathbf{x} f(\mathbf{h}) = \nabla f(\mathbf{x})^T \, \mathbf{h}
$$

这就是**方向导数**。以 $f_1$ 为例，在点 $X_0 = (1,1)^T$ 处，沿 $\mathbf{h} = (1, 0)^T$（只动 $x_1$）移动：

$$
D_\mathbf{x} f_1(\mathbf{h}) = \begin{pmatrix} 2 & 2 \end{pmatrix} \begin{pmatrix} 1 \\ 0 \end{pmatrix} = 2
$$

意思是沿 $(1,0)^T$ 方向移动单位长度，$f_1$ 大约增加 2。如果沿着梯度方向 $\mathbf{h} = (1,1)^T$ 移动，变化率是 4——这是增长最快的方向。

对于**向量函数** $F = (f_1, f_2)^T$，把每个分量的方向导数堆起来，就自然得到了雅可比矩阵乘以方向向量：

$$
D_\mathbf{x} F(\mathbf{h}) = \begin{pmatrix} \nabla f_1^T \, \mathbf{h} \\ \nabla f_2^T \, \mathbf{h} \end{pmatrix} = \begin{pmatrix} \nabla f_1^T \\ \nabla f_2^T \end{pmatrix} \mathbf{h} = J(\mathbf{x})\, \mathbf{h}
$$

所以**梯度、方向导数、雅可比矩阵**三者不过是同一件事的不同组织方式。**梯度** $\nabla f$ 是单个函数对所有变量的偏导数向量；**方向导数** $\nabla f^T \mathbf{h}$ 是单个函数沿某方向的变化率；**雅可比矩阵** $J$ 是多个函数的梯度按行排列，$J\mathbf{h}$ 给出整个向量函数沿方向 $\mathbf{h}$ 的变化。理解了这一点，就可以在不同教材的记号之间自由切换。

### 1.5 牛顿迭代法

现在我们正式写出完整的迭代算法。每一步的逻辑很简单：当前 $F(X_k)$ 不是零，我们希望它变成零，所以期望的变化量是 $\Delta F = \mathbf{0} - F(X_k) = -F(X_k)$。代入线性近似公式 $\Delta F \approx J \cdot \Delta X$，就得到了牛顿法的迭代公式：

$$
J(X_k) \cdot \Delta X = -F(X_k)
$$

$$
X_{k+1} = X_k + \Delta X
$$

从数学上看，$\Delta X$ 可以写成 $\Delta X = -J(X_k)^{-1} F(X_k)$。但在实际计算中，当未知数很多时（比如 $n = 10000$），雅可比矩阵 $J$ 是 $n \times n$ 的矩阵，求逆的计算量是 $O(n^3)$，而且每迭代一步都要重新计算 $J$ 并求逆，代价极大。这引出了下一节的讨论。

### 1.6 避免求逆：直接解线性方程组

仔细想一下，我们其实不需要 $J^{-1}$ 本身，我们只需要 $\Delta X = J^{-1}(-F)$ 这个**结果向量**。所以换个思路：不求逆，直接解线性方程组

$$
J \cdot \Delta X = -F
$$

解线性方程组比求逆高效得多。常用方法包括 **LU 分解**（虽然复杂度仍是 $O(n^3)$，但常数更小，且分解后可以快速处理多个右端项）和**迭代法**（如 GMRES、共轭梯度法，对于稀疏矩阵可以做到接近 $O(n)$）。在实际应用中，雅可比矩阵往往是**稀疏的**（大部分元素为 0），这时迭代法的优势尤为明显。

### 1.7 换个视角：把解方程变成优化问题

到目前为止，我们一直在"正面进攻"——直接求解 $F(X) = \mathbf{0}$。现在我们换一个思路：能不能把"解方程"转化为"求最小值"？

构造一个**目标函数**来衡量"离目标还有多远"：

$$
g(X) = \|F(X)\|^2 = f_1(X)^2 + f_2(X)^2 = (x_1^2 + x_2^2 - 5)^2 + (x_1^2 - x_2^2 - 3)^2
$$

由于每一项都是平方，所以 $g(X) \geq 0$。而且 $g(X) = 0$ 当且仅当 $f_1 = 0$ 且 $f_2 = 0$，即 $F(X) = \mathbf{0}$。所以**解方程 $F(X) = \mathbf{0}$ 等价于求 $g(X)$ 的最小值（最小值为 0）**。

用之前的数值验证一下：在 $X_0 = (1,1)^T$ 处 $g = (-3)^2 + (-3)^2 = 18$；牛顿法走一步后到 $X_1 = (2.5,\,1)^T$，$g = 2.25^2 + 2.25^2 = 10.125$。$g$ 确实在下降，说明在"靠近"解。

#### 梯度下降法

既然目标是最小化 $g(X)$，最直接的办法就是沿着 $g$ 的**负梯度方向**走——因为梯度方向是函数增长最快的方向，负梯度方向就是下降最快的方向：

$$
X_{k+1} = X_k - \alpha \, \nabla g(X_k)
$$

其中 $\alpha > 0$ 是步长（也叫**学习率**）。

下面计算 $\nabla g$。根据链式法则：

$$
\nabla g = 2 f_1 \nabla f_1 + 2 f_2 \nabla f_2
$$

在 $X_0 = (1,1)^T$ 处，$f_1 = -3$，$\nabla f_1 = (2,2)^T$，$f_2 = -3$，$\nabla f_2 = (2,-2)^T$，代入得：

$$
\nabla g = 2(-3)\begin{pmatrix}2\\2\end{pmatrix} + 2(-3)\begin{pmatrix}2\\-2\end{pmatrix} = \begin{pmatrix}-12\\-12\end{pmatrix} + \begin{pmatrix}-12\\12\end{pmatrix} = \begin{pmatrix}-24\\0\end{pmatrix}
$$

负梯度方向是 $(24,\,0)^T$，说明应该**增大 $x_1$**，而 $x_2$ 暂时不动。取步长 $\alpha = 0.05$：

$$
X_1 = \begin{pmatrix}1\\1\end{pmatrix} + 0.05 \times \begin{pmatrix}24\\0\end{pmatrix} = \begin{pmatrix}2.2\\1\end{pmatrix}
$$

验证：$g(2.2,\,1) = (4.84+1-5)^2 + (4.84-1-3)^2 = 0.84^2 + 0.84^2 \approx 1.41$。$g$ 从 18 降到了 1.41，效果很好！

#### 牛顿法 vs 梯度下降法

两种方法各有优劣。**牛顿法**通过解线性方程组 $J \cdot \Delta X = -F$ 来确定移动方向，需要计算雅可比矩阵，收敛速度快（**二次收敛**——大致意味着每迭代一步，误差平方级地缩小），但每步计算量大，适合变量较少、精度要求高的场景。**梯度下降法**只需计算一阶梯度 $\nabla g$，每步计算量小，但收敛速度慢（**线性收敛**——误差每步只按固定比例缩小），适合变量多、对精度要求不那么高的场景。

### 1.8 动量法：给梯度下降加"惯性"

梯度下降有一个常见问题：当目标函数的等高线呈**狭长椭圆**形状时（即不同方向上的曲率差异很大），梯度方向会来回震荡，形成"之字形"路径，收敛效率很低。比如某一步梯度让你往右上走，下一步又让你往右下走，来回摆动，虽然整体在往右靠，但浪费了大量步数。

**动量法的直觉**：想象一个球从山坡上滚下来。球不会每一瞬间都严格沿着当前最陡方向走——它会积累**惯性**（动量），保持一定的运动趋势。即使坡面方向突然变了，球也不会立刻急转弯。这样，沿着一致方向的运动会不断加速，而来回震荡的分量会相互抵消。

更新公式如下：

$$
\mathbf{v}^{(k+1)} = \beta \, \mathbf{v}^{(k)} - \gamma \, \nabla g(\mathbf{x}^{(k)})
$$

$$
\mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} + \mathbf{v}^{(k+1)}
$$

其中 $\mathbf{v}$ 是**速度**（记录历史梯度的累积效果），$\beta \in [0,1)$ 是**阻尼系数**（通常取 0.9，控制历史动量的衰减），$\gamma$ 是**学习率**。

用之前的例子演示。初始 $X_0 = (1,1)^T$，$\mathbf{v}^{(0)} = (0,0)^T$。第一步 $\nabla g = (-24,\,0)^T$：

$$
\mathbf{v}^{(1)} = 0.9 \times (0,0)^T - 0.05 \times (-24, 0)^T = (1.2,\; 0)^T
$$

$$
X_1 = (1,1)^T + (1.2,\, 0)^T = (2.2,\, 1)^T
$$

第一步和普通梯度下降完全一样（因为没有历史动量）。假设第二步 $\nabla g(X_1) = (-10,\, 4)^T$：

$$
\mathbf{v}^{(2)} = 0.9 \times (1.2,\, 0)^T - 0.05 \times (-10,\, 4)^T = (1.08,\, 0)^T + (0.5,\, -0.2)^T = (1.58,\; -0.2)^T
$$

$$
X_2 = (2.2,\, 1)^T + (1.58,\, -0.2)^T = (3.78,\; 0.8)^T
$$

注意看发生了什么：即使当前梯度的 $x_2$ 分量想让 $x_2$ 减小，但历史动量 $(1.08,\, 0)^T$ 仍然保持着 $x_1$ 方向的惯性。$x_1$ 方向的运动在持续加速（从 1.2 增大到 1.58），$x_2$ 方向的震荡则被部分抑制——这正是动量法的优势所在。

与普通梯度下降相比，动量法多了一个阻尼系数 $\beta$ 需要调节，但换来了更快的收敛和更平稳的轨迹。

### 1.9 随机梯度下降（SGD）：用"抽样"代替"全算"

在机器学习中，我们通常要最小化**经验风险**：

$$
R(\boldsymbol{\theta}) = \frac{1}{N}\sum_{i=1}^N L\bigl(y_i,\; p_{\boldsymbol{\theta}}(\mathbf{x}_i)\bigr)
$$

其中 $N$ 是训练样本数量（可能是百万级），$L$ 是损失函数（衡量预测值 $p_{\boldsymbol{\theta}}(\mathbf{x}_i)$ 与真实标签 $y_i$ 的差距），$\boldsymbol{\theta}$ 是要优化的模型参数。

如果用标准梯度下降，每走一步都要计算所有 $N$ 个样本的梯度之和：

$$
\nabla R(\boldsymbol{\theta}) = \frac{1}{N}\sum_{i=1}^N \nabla L\bigl(y_i,\; p_{\boldsymbol{\theta}}(\mathbf{x}_i)\bigr)
$$

当 $N = 1{,}000{,}000$ 时，每一步都要算一百万次梯度，这太慢了。

**关键观察**：如果随机抽一个样本 $i$，它的梯度 $\nabla L(y_i,\, p_{\boldsymbol{\theta}}(\mathbf{x}_i))$ 的期望值恰好等于全梯度：

$$
\mathbb{E}\bigl[\nabla L(y_i,\, p_{\boldsymbol{\theta}}(\mathbf{x}_i))\bigr] = \nabla R(\boldsymbol{\theta})
$$

也就是说，单个样本的梯度是全梯度的**无偏估计**——虽然每次有噪声，但方向**平均来看是对的**。

于是 SGD 的更新公式变成：每步只随机抽一个样本 $i_k$，用它的梯度来更新：

$$
\boldsymbol{\theta}^{(k+1)} = \boldsymbol{\theta}^{(k)} - \gamma \, \nabla L\bigl(y_{i_k},\; p_{\boldsymbol{\theta}^{(k)}}(\mathbf{x}_{i_k})\bigr)
$$

回到我们的方程组做类比。如果 $F(X)$ 有 $N$ 个分量，则 $g(X) = \frac{1}{N}\sum_{i=1}^N f_i(X)^2$，全梯度需要求和 $N$ 项。SGD 的思路是每次随机挑一个 $f_j$，只用 $2f_j \nabla f_j$ 来近似全梯度。我们的例子只有 $N=2$，感受不明显；但想象 $N=10{,}000$ 时，SGD 每步只算 1 个，快了一万倍。

当然，代价是收敛轨迹变得震荡（因为每步的梯度有噪声）。全梯度下降每步计算量 $O(N)$ 但轨迹平滑；SGD 每步 $O(1)$ 但轨迹震荡。总体效率上 SGD 往往更优——步数虽多但每步极便宜。

实践中常用的改进包括：**Mini-batch SGD**（每次抽 $B$ 个样本，如 $B=32$，在噪声和计算量之间折中）和 **SGD + 动量**（结合 1.8 节的动量法来抑制震荡）。

### 1.10 牛顿法的两种场景

"牛顿法"这个名字在两种不同场景下都会出现，初学者很容易混淆，这里专门澄清。

**场景一：解方程组 $F(X) = \mathbf{0}$**。这是 1.5 节讨论的情况。我们对 $F$ 做线性近似，只用到 $F$ 的**一阶导数**（雅可比矩阵 $J$），迭代公式为 $J(X_k) \cdot \Delta X = -F(X_k)$。

**场景二：优化问题 $\min f(X)$**。目标是找到 $X^*$ 使得标量函数 $f(X^*)$ 最小。最小值点满足必要条件 $\nabla f(X^*) = \mathbf{0}$——这本身就是一个"解方程"问题！把 $\nabla f$ 看成一个向量函数 $G(X) = \nabla f(X)$，对它套用场景一的牛顿法：需要 $G$ 的雅可比矩阵，即 $\nabla f$ 的导数，也就是 $f$ 的**二阶导数**——**海森矩阵**（Hessian matrix）：

$$
H = \begin{pmatrix} \dfrac{\partial^2 f}{\partial x_1^2} & \dfrac{\partial^2 f}{\partial x_1 \partial x_2} \\[6pt] \dfrac{\partial^2 f}{\partial x_2 \partial x_1} & \dfrac{\partial^2 f}{\partial x_2^2} \end{pmatrix}
$$

迭代公式变为 $H(X_k) \cdot \Delta X = -\nabla f(X_k)$。

一句话总结：**解方程用雅可比（一阶导），优化用海森（二阶导），但背后是同一个牛顿法框架。**

在实际应用中，计算海森矩阵的代价很高（$n$ 个变量就有 $n^2$ 个二阶偏导），所以诞生了**拟牛顿法**（如 BFGS）：只用一阶导数信息，但在迭代过程中逐步**近似**出海森矩阵，在计算量和收敛速度之间取得折中。下表做一个总结：

| 方法 | 所需导数阶数 | 每步计算量 | 收敛速度 |
|---|---|---|---|
| 梯度下降 | 一阶 | 小 | 慢（线性收敛） |
| 牛顿法（优化） | 二阶 | 大 | 快（二次收敛） |
| 拟牛顿法（BFGS） | 一阶 | 中等 | 较快（超线性收敛） |

### 1.11 Lagrange 乘数法：带约束的优化

之前讨论的优化问题都是**无约束**的：在整个空间中自由地寻找最小值。但很多实际问题有**约束条件**。考虑一个简单的例子：

$$
\min \; f(x,y) = x^2 + y^2 \qquad \text{subject to} \qquad g(x,y) = x + y - 1 = 0
$$

也就是说，我们要在直线 $x + y = 1$ 上找到离原点最近的点。

#### 几何直觉：为什么最优点处 $\nabla f$ 要平行于 $\nabla g$？

目标函数 $f = x^2 + y^2$ 的等高线是一圈圈同心圆，约束 $g = 0$ 是一条直线。想象你站在这条直线上走动：如果 $\nabla f$ 与直线**不垂直**，那么 $\nabla f$ 沿直线方向有一个分量，说明沿着直线走 $f$ 还能继续变小（或变大），所以当前点不是极值。只有当 $\nabla f$ 与直线**垂直**时，沿直线往哪边走 $f$ 都不会再减小了——这才是约束下的极值点。

而 $\nabla g$ 天然垂直于约束曲面（等值面）。对于 $g = x + y - 1$，梯度是 $\nabla g = (1,1)^T$，确实垂直于直线 $x+y=1$。所以极值点的条件是 **$\nabla f$ 与 $\nabla g$ 平行**，即存在某个常数 $\lambda$ 使得：

$$
\nabla f + \lambda \, \nabla g = \mathbf{0}
$$

#### Lagrange 函数：把条件"打包"

与其分开处理 $\nabla f + \lambda \nabla g = \mathbf{0}$ 和 $g = 0$ 两组条件，不如构造一个函数把它们统一起来。定义 **Lagrange 函数**：

$$
\mathcal{L}(x, y, \lambda) = f(x,y) + \lambda \cdot g(x,y) = x^2 + y^2 + \lambda(x + y - 1)
$$

对所有变量（包括 $\lambda$）求偏导并令其为零：

$$
\frac{\partial \mathcal{L}}{\partial x} = 2x + \lambda = 0
$$

$$
\frac{\partial \mathcal{L}}{\partial y} = 2y + \lambda = 0
$$

$$
\frac{\partial \mathcal{L}}{\partial \lambda} = x + y - 1 = 0
$$

前两个方程合起来正是 $\nabla f + \lambda \nabla g = \mathbf{0}$，第三个方程正是约束条件 $g = 0$。所以**对 $\mathcal{L}$ 求驻点，就自动得到了带约束优化的必要条件**——这就是 Lagrange 函数的巧妙之处。

#### 求解

从前两个方程得 $x = y = -\lambda/2$，代入第三个方程 $-\lambda/2 - \lambda/2 - 1 = 0$，解出 $\lambda = -1$，于是：

$$
x^* = y^* = \frac{1}{2}, \qquad \lambda^* = -1
$$

验证：$f(0.5,\, 0.5) = 0.25 + 0.25 = 0.5$，且确实在直线 $x+y=1$ 上。而在最优点处 $\nabla f = (1,1)^T$ 与 $\nabla g = (1,1)^T$ 确实平行，符合我们的几何直觉。

#### $\lambda$ 的含义

$\lambda$ 叫做 **Lagrange 乘子**，它有明确的物理意义：**约束条件变化时，最优值会如何响应**。在这个例子中 $\lambda = -1$，如果把约束从 $x + y = 1$ 改为 $x + y = 1 + \epsilon$（即把直线稍微远离原点），最优的 $f$ 值大约会增加 $|\epsilon|$——约束越紧，付出的代价越大。

从更大的视角看，无约束优化只需 $\nabla f = \mathbf{0}$，而带约束优化需要构造 $\mathcal{L} = f + \lambda g$ 再求 $\nabla \mathcal{L} = \mathbf{0}$。额外引入的变量 $\lambda$ 既编码了约束信息，也给出了约束的"影子价格"。