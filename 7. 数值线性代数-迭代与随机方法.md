## 🎯 预备知识

> [!info] 复杂度与稳定性
> - 复杂度分析 / $O()$ 记号
> - 稳定性和条件数
> - 谱范数 $\|\cdot\|_2$

> [!info] 线性代数
> - 特征值和特征向量：$\mathbf{A}\mathbf{v} = \lambda\mathbf{v}$
> - 相似变换：$\mathbf{B} = \mathbf{Q}\mathbf{A}\mathbf{Q}^{-1}$
> - 正交矩阵

> [!info] 分析与概率
> - 数列收敛性
> - 几何级数 $\sum_k p^k$，当 $|p| < 1$
> - 随机向量

---

## 📑 目录

```

1. 概述：迭代与概率方法
2. 特征值问题
3. 线性方程组的迭代求解
4. 概率方法与矩阵草图
5. 总结

```

---

# 一、概述

## 1.1 核心思想

> [!important] 两个额外的核心思想
> 
> **迭代方法**
> - 以牺牲精度换取更短的运行时间
> 
> **概率方法**
> - 用较小的问题替代大问题
> - 以*高概率*获得非常相似的解

> [!note] 背景
> - 数值线性代数是一个非常广阔的领域
> - 核心思想是**矩阵分解**（LU、QR、Cholesky 等）
> - 这两个方向都是活跃的研究领域

---

# 二、特征值问题

## 2.1 直接方法的局限

> [!warning] 直接方法的问题
> 
> - 特征多项式：$\det(\mathbf{A} - \lambda\mathbf{I}) = 0$ → 求根
> - 问题：对于 $n \times n$ 矩阵，多项式为 $n$ 次
> - 当 $n \geq 5$ 时无直接解
> - 数值不稳定且代价高：$O(n^4)$ 或更差

> [!tip] 何时需要迭代方法？
> 
> | 场景 | 原因 |
> |:---|:---|
> | 大矩阵（$n > 1000$） | $O(n^3)$ 或更差太慢 |
> | 稀疏矩阵 | 直接方法会破坏稀疏性结构 |
> | 只需少数特征值（$k \ll n$） | 计算全部太浪费 |

---

## 2.2 幂方法

> [!important] 幂方法（von Mises 方法）
> 
> 通过迭代找到矩阵 $\mathbf{A}$ 的**最大**（分离的！）特征值 $\lambda_1$ 及对应的特征向量 $\mathbf{v}_1$
> 
> **迭代公式**：
> $$
> \mathbf{x}^{(k)} = \frac{\mathbf{A}\mathbf{x}^{(k-1)}}{\|\mathbf{A}\mathbf{x}^{(k-1)}\|}
> $$
> 
> **收敛性**：
> $$
> \lambda^{(k)} = \frac{\|\mathbf{A}\mathbf{x}^{(k)}\|}{\|\mathbf{A}\mathbf{x}^{(k-1)}\|} \xrightarrow{k \to \infty} \lambda_1 \quad \text{且} \quad \mathbf{x}^{(k)} \xrightarrow{k \to \infty} \pm\mathbf{v}_1
> $$

---

## 2.3 幂方法的原理

> [!abstract] 直觉
> - 每个初始向量都是 $\mathbf{A}$ 的特征向量 $\mathbf{v}_i$ 的线性组合：$\mathbf{x}^{(0)} = \sum_i^n c_i \mathbf{v}_i$
> - 每次迭代会放大 $\mathbf{x}^{(k)}$ 在 $\mathbf{v}_1$ 方向上的分量

> [!note] 代数推导
> 由于 $\mathbf{A}\mathbf{v} = \lambda\mathbf{v}$，可得 $\mathbf{A}^k\mathbf{v} = \lambda^k\mathbf{v}$，因此：
> $$
> \mathbf{A}^k\mathbf{x}^{(0)} = \lambda_1^k \left( c_1\mathbf{v}_1 + \sum_{i=2}^n c_i \left(\frac{\lambda_i}{\lambda_1}\right)^k \mathbf{v}_i \right) \approx \lambda_1^k c_1 \mathbf{v}_1 \quad \text{当 } k \to \infty
> $$
> 
> 因为 $\left(\frac{\lambda_i}{\lambda_1}\right)^k \xrightarrow{k \to \infty} 0$，当 $i \geq 2$

---

## 2.4 幂方法示例

> [!example] 计算示例
> 
> $$
> \mathbf{A} = \begin{pmatrix} 5 & -2 \\ -2 & 8 \end{pmatrix} \implies \lambda_1 = 9, \lambda_2 = 4
> $$
> 
> $$
> \mathbf{v}_1 = \frac{1}{\sqrt{5}}\begin{pmatrix} -1 \\ 2 \end{pmatrix}, \quad \mathbf{v}_2 = \frac{1}{\sqrt{5}}\begin{pmatrix} 2 \\ 1 \end{pmatrix}
> $$
> 
> 初始向量 $\mathbf{x}^{(0)} = \begin{pmatrix} 2 \\ 1.3 \end{pmatrix}$
> 
> | $k$ | 到 $\mathbf{v}_1$ 的角度 |
> |:---:|:---:|
> | 0 | 83.54° |
> | 5 | 8.71° |
> | 10 | 0.15° |
> | 15 | 0.00° |

> [!warning] 失效情况
> 如果初始向量恰好是非主导特征向量的倍数（即 $c_1 = 0$），则幂方法将收敛到错误的特征向量

---

## 2.5 相似变换

> [!important] 定理：相似矩阵的特征值
> 
> 设 $\mathbf{B}$ 是与 $\mathbf{A}$ 相似的矩阵（即 $\mathbf{B} = \mathbf{Q}\mathbf{A}\mathbf{Q}^{-1}$），则：
> - $\mathbf{A}$ 和 $\mathbf{B}$ 有**相同的特征值**
> - 若 $\mathbf{y}$ 是 $\mathbf{B}$ 的特征向量，则 $\mathbf{x} = \mathbf{Q}^{-1}\mathbf{y}$ 是 $\mathbf{A}$ 对应同一特征值的特征向量

> [!note] 证明
> $$
> \mathbf{B}\mathbf{y} = \lambda\mathbf{y} \implies \mathbf{Q}\mathbf{A}\mathbf{Q}^{-1}\mathbf{y} = \lambda\mathbf{y} \implies \mathbf{A}\mathbf{Q}^{-1}\mathbf{y} = \lambda\mathbf{Q}^{-1}\mathbf{y} \implies \mathbf{A}\mathbf{x} = \lambda\mathbf{x}
> $$

---

## 2.6 QR 迭代

> [!important] 定义：QR 迭代
> 
> QR 迭代利用 QR 分解进行迭代：
> 
> 1. 从 $\mathbf{A}^{(0)} = \mathbf{A}$ 开始
> 2. 对于 $k = 1, 2, \ldots$：
>    - 计算 QR 分解：$\mathbf{A}^{(k-1)} = \mathbf{Q}^{(k)}\mathbf{R}^{(k)}$
>    - 设置 $\mathbf{A}^{(k)} = (\mathbf{Q}^{(k)})^\top \mathbf{A}^{(k-1)}\mathbf{Q}^{(k)} = \mathbf{R}^{(k)}\mathbf{Q}^{(k)}$

> [!success] 关键性质
> - $\mathbf{A}^{(k)}$ 与 $\mathbf{A}$ **相似**
> - 当 $k \to \infty$ 时，$\mathbf{A}^{(k)}$ 收敛到**上三角矩阵**
> - 特征值在对角线上

---

## 2.7 QR 迭代的解释

> [!abstract] 核心思想
> 
> - $\mathbf{Q}_k$ 的列是 $\mathbf{A}^k\mathbf{e}_1, \mathbf{A}^k\mathbf{e}_2, \ldots, \mathbf{A}^k\mathbf{e}_n$ 的正交化版本
> - 相当于对**所有**单位向量同时应用幂方法！
> - 因此 $\mathbf{Q}_k \xrightarrow{k \to \infty} \mathbf{V}$（特征向量矩阵）

> [!note] 收敛性
> $$
> \mathbf{Q}_k^\top \mathbf{A}\mathbf{Q}_k \xrightarrow{k \to \infty} \mathbf{V}^\top \mathbf{A}\mathbf{V} = \mathbf{V}^\top \mathbf{V}\mathbf{\Lambda}\mathbf{V}^\top \mathbf{V} = \mathbf{\Lambda}
> $$

---

## 2.8 应用：Google PageRank

> [!example] Google PageRank
> 
> **问题**：对互联网上约 $n \approx 10^{10}$ 个网页进行排序
> 
> **模型**：每个页面 $i$ 有一个"重要性分数" $x_i$：
> $$
> x_i = \sum_{j: j \to i} \frac{x_j}{d_j}
> $$
> 其中 $j \to i$ 表示"页面 $j$ 链接到 $i$"，$d_j$ 是 $j$ 的出链数量
> 
> **矩阵形式**：$\mathbf{x} = \mathbf{A}\mathbf{x}$ → 特征值为 $\lambda = 1$ 的特征值问题！

> [!success] 为什么用迭代方法？
> - 矩阵 $\mathbf{A} \in \mathbb{R}^{10^{10} \times 10^{10}}$ — 直接方法不可能！
> - 但 $\mathbf{A}$ 是*极度稀疏*的（每个页面只有约 10 个链接）
> - 存储需求：$O(n)$ 而非 $O(n^2)$
> - **总计**：$O(n)$ 而非 $O(n^3)$ — 这使得 Google 成为可能！

---

## 2.9 应用：主成分分析 (PCA)

> [!important] PCA 与特征值
> 
> **问题**：给定数据矩阵 $\mathbf{X} \in \mathbb{R}^{n \times p}$（$n$ 个观测，$p$ 个变量）
> 
> **目标**：找到最大方差的方向用于降维
> 
> **解法**：计算协方差矩阵 $\mathbf{\Sigma} = \frac{1}{n-1}\mathbf{X}^\top\mathbf{X} \in \mathbb{R}^{p \times p}$
> - $\mathbf{\Sigma}$ 的特征向量称为*主成分*
> - 特征值给出每个方向解释的方差

> [!tip] 为什么用迭代方法？
> - 现代应用：$p = 10^4$ 到 $10^6$（如基因表达、图像分析等）
> - 通常只需要最大的 $k$ 个特征值（$k \ll p$）
> - Lanczos 迭代：以 $O(kp^2)$ 计算最大的 $k$ 个特征值，而非 $O(p^3)$ 计算全部
> - 当 $p = 100,000$ 且 $k = 10$ 时，Lanczos 快约 **10,000 倍**！

---

## 2.10 迭代特征值方法比较

> [!tip] 方法比较
> 
> | 方法 | 每次迭代复杂度 | 应用场景 | 输出 |
> |:---|:---|:---|:---|
> | 幂方法 | $O(\text{nnz}(\mathbf{A}))$ | 最大特征值/向量 | 1 个特征对 |
> | (shifted) QR | $O(\text{nnz}(\mathbf{A}))$-$O(n^3)$ | 所有特征值 | $n$ 个特征值 |
> | Lanczos | $O(m \cdot \text{nnz}(\mathbf{A}) + m^2)$ | 少数极端特征值，*稀疏* $\mathbf{A}$ | $m \ll n$ 个特征对 |
> 
> $\text{nnz}(\cdot)$ = 非零元素数量

---

# 三、线性方程组的迭代求解

## 3.1 基本思想

> [!important] 迭代求解的直觉
> 
> - **残差**：$\mathbf{r}^{(k)} = \mathbf{b} - \mathbf{A}\mathbf{x}^{(k)}$ 衡量 $\mathbf{x}^{(k)}$ 距真实解有多远
> - 如果 $\mathbf{A}\mathbf{x}^{(k)} = \mathbf{b}$，则 $\mathbf{r}^{(k)} = \mathbf{0}$，我们找到了解
> - **校正**：只要 $\mathbf{r}^{(k)} \neq \mathbf{0}$，就校正估计：
> $$
> \mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} + \underbrace{\mathbf{C}\mathbf{r}^{(k)}}_{\text{校正步}}
> $$

> [!note] 类比
> 梯度下降 — 我们逐步向解移动

---

## 3.2 收敛定理

> [!important] 定理：迭代收敛性
> 
> 设 $\mathbf{A} \in \mathbb{R}^{n \times n}$，$\mathbf{b} \in \mathbb{R}^n$，$\mathbf{x}$ 是 $\mathbf{A}\mathbf{x} = \mathbf{b}$ 的唯一解
> 
> 定义迭代：
> $$
> \mathbf{x}^{(k)} = \mathbf{x}^{(k-1)} + \mathbf{C}(\mathbf{b} - \mathbf{A}\mathbf{x}^{(k-1)}), \quad k = 1, 2, \ldots
> $$
> 
> 若 $\rho := \|\mathbf{I}_n - \mathbf{C}\mathbf{A}\|_2 < 1$（*谱范数*），则对所有 $k \geq 1$：
> $$
> \|\mathbf{x}^{(k)} - \mathbf{x}\|_2 \leq \rho^k \|\mathbf{x}^{(0)} - \mathbf{x}\|_2
> $$
> 
> 特别地：$\lim_{k \to \infty} \mathbf{x}^{(k)} = \mathbf{x}$（*收敛*）

---

## 3.3 收敛定理的证明

> [!note] 证明
> 
> 定义 $\mathbf{B} = \mathbf{I} - \mathbf{C}\mathbf{A}$，使得 $\|\mathbf{B}\|_2 = \rho$
> 
> **步骤 1**：$\mathbf{x}$ 是此迭代的不动点：
> $$
> \mathbf{B}\mathbf{x} + \mathbf{C}\mathbf{b} = (\mathbf{I} - \mathbf{C}\mathbf{A})\mathbf{x} + \mathbf{C}\mathbf{b} = \mathbf{x} - \mathbf{C}\underbrace{(\mathbf{A}\mathbf{x} - \mathbf{b})}_{=0} = \mathbf{x}
> $$
> 
> **步骤 2**：误差递推
> $$
> \|\mathbf{x}^{(k)} - \mathbf{x}\|_2 = \|\mathbf{B}(\mathbf{x}^{(k-1)} - \mathbf{x})\|_2 \leq \rho\|\mathbf{x}^{(k-1)} - \mathbf{x}\|_2
> $$
> 
> **步骤 3**：迭代此不等式得 $\|\mathbf{x}^{(k)} - \mathbf{x}\|_2 \leq \rho^k\|\mathbf{x}^{(0)} - \mathbf{x}\|_2$

---

## 3.4 常用迭代方法

> [!tip] 常用 $\mathbf{C}$ 选择
> 
> | 方法 | $\mathbf{C}$ 的选择 |
> |:---|:---|
> | **Richardson 迭代** | $\mathbf{C} = \gamma\mathbf{I}_n$，$\gamma > 0$ 足够小 |
> | **Jacobi 迭代** | $\mathbf{C} = \text{diag}(\mathbf{A}_{11}, \ldots, \mathbf{A}_{nn})^{-1}$ |
> | **Gauss-Seidel 迭代** | $\mathbf{C}^{-1}$ 等于 $\mathbf{A}$ 的下三角部分 |

> [!success] 复杂度分析
> - 单次迭代：$O(n^2)$
> - 达到 $\|\mathbf{x}^{(k)} - \mathbf{x}\| \leq \epsilon$ 需要 $O(n^2 \log(1/\epsilon))$ 次操作
> - 比较：通过 LU 或 QR 求**精确解**需要 $O(n^3)$

---

## 3.5 收敛所需迭代次数

> [!abstract] 迭代次数分析
> 
> 给定 $\|\mathbf{x}^{(k)} - \mathbf{x}\| \leq \rho^k \|\mathbf{x}^{(0)} - \mathbf{x}\|$，要使 $\|\mathbf{x}^{(k)} - \mathbf{x}\| \leq \epsilon$：
> 
> 1. 设 $e_0 = \|\mathbf{x}^{(0)} - \mathbf{x}\|$
> 2. 求解：$\rho^k e_0 \leq \epsilon \implies \rho^k \leq \epsilon/e_0$
> 3. 两边取对数：
> $$
> k \geq \frac{\log(\epsilon) - \log(e_0)}{\log(\rho)} = \frac{\log(e_0/\epsilon)}{-\log(\rho)}
> $$
> 
> **结论**：$k = O(\log(1/\epsilon))$（对数收敛）

---

## 3.6 Richardson 迭代示例

> [!example] 计算示例
> 
> 给定：$\mathbf{A} = \begin{pmatrix} 4 & 1 \\ 1 & 3 \end{pmatrix}$，$\mathbf{b} = \begin{pmatrix} 1 \\ 2 \end{pmatrix}$
> 
> 真实解：$\mathbf{x} = \mathbf{A}^{-1}\mathbf{b} = \frac{1}{11}\begin{pmatrix} 1 \\ 7 \end{pmatrix} \approx \begin{pmatrix} 0.091 \\ 0.636 \end{pmatrix}$
> 
> **Richardson 迭代**，$\gamma = 0.25$，初始值 $\mathbf{x}^{(0)} = \begin{pmatrix} 0 \\ 0 \end{pmatrix}$：
> 
> | 迭代 | $\mathbf{x}^{(k)}$ | 误差 $\|\mathbf{x}^{(k)} - \mathbf{x}\|_2$ |
> |:---:|:---|:---:|
> | 0 | $(0, 0)$ | 0.642 |
> | 1 | $(0.25, 0.5)$ | 0.209 |
> | 2 | $(0.125, 0.563)$ | 0.081 |
> | 3 | $(0.109, 0.610)$ | 0.032 |
> | 4 | $(0.095, 0.628)$ | 0.009 |
> 
> **收敛率**：$\rho = \|\mathbf{I} - \gamma\mathbf{A}\|_2 \approx 0.41$ → 误差每次迭代下降约 2.5 倍

---

# 四、概率方法与矩阵草图

## 4.1 核心思想

> [!important] 有目的的随机性
> 
> **核心思想**：
> - 用一个*随机*生成的、*更小*的问题*替代*大问题
> - 该小问题*高概率*具有*相似的解*

> [!abstract] 高维空间的几何事实
> 对于大的 $n$，两个随机向量 $\mathbf{s}_1, \mathbf{s}_2 \in \mathbb{R}^n$ **高概率几乎正交**

---

## 4.2 矩阵草图

> [!important] 矩阵草图（Matrix-Sketching）
> 
> 适当的*随机*抽取的矩阵 $\mathbf{S} \in \mathbb{R}^{m \times n}$（$m \ll n$）高概率保持：
> 
> - **距离**：$\|\mathbf{S}\mathbf{x} - \mathbf{S}\mathbf{y}\|_2 \approx \|\mathbf{x} - \mathbf{y}\|_2$
> - **角度**：$\angle(\mathbf{S}\mathbf{x}, \mathbf{S}\mathbf{y}) \approx \angle(\mathbf{x}, \mathbf{y})$

> [!success] 意义
> **随机**"投影" $\mathbf{S}$ 到低维空间可以（通常近似地）保持几何结构

---

## 4.3 随机嵌入定理

> [!important] 定理：子空间的随机嵌入
> 
> 设 $\mathbf{S} \in \mathbb{R}^{m \times n}$ 是具有独立同分布行向量 $\mathbf{s}_i$ 的矩阵，满足：
> - $\mathbb{E}[\mathbf{s}_i\mathbf{s}_i^\top] = \mathbf{I}_n/m$
> - $\mathbb{E}[\|\mathbf{s}_i\|^4] \leq K/m^2$
> 
> 则以至少 $1 - \delta$ 的概率有
> $$
> (1 - \epsilon)\|\mathbf{x}\|^2 \leq \|\mathbf{S}\mathbf{x}\|^2 \leq (1 + \epsilon)\|\mathbf{x}\|^2 \quad \text{对所有 } \mathbf{x} \in \mathbb{R}^n
> $$
> 
> 其中 $\epsilon = \sqrt{K/\delta m}$

> [!example] 常用草图矩阵
> - **高斯**：$\mathbf{s}_i \stackrel{iid}{\sim} \mathcal{N}(\mathbf{0}, \mathbf{I}_n/m)$
> - **Rademacher**：$s_{ij}$ 独立，$\mathbb{P}(s_{ij} = \pm 1/\sqrt{m}) = 1/2$
> - **子采样**：$\mathbf{s}_i = \sqrt{n/m} \, \mathbf{e}_{K_i}$，$K_i \sim \text{Unif}\{1, \ldots, n\}$

---

## 4.4 降维示例

> [!example] 使用矩阵草图进行降维
> 
> **给定**：$n = 10000$ 维，$\delta = 0.05$，$\epsilon = 0.1$
> 
> **问题**：$m$ 可以多小？
> 
> **由定理**：当 $\mathbf{s}_i \sim \mathcal{N}(\mathbf{0}, \mathbf{I}_n/m)$ 时，$K = 3$
> $$
> \epsilon = \sqrt{\frac{K}{\delta m}} \implies m = \frac{K}{\delta\epsilon^2} = \frac{3}{0.05 \cdot 0.01} = 600
> $$
> 
> **结果**：
> 仅用 $m = 600$ 维，我们就能表示 $n = 10000$ 维，使得所有距离以 95% 的概率最多被扭曲 10%！

> [!success] 实际意义
> - **压缩**：$10000 \to 600$ ≈ 17 倍
> - **加速**：距离计算 $O(n^2) \to O(m^2)$ ≈ 280 倍
> - **应用**：高维空间中的快速最近邻搜索、聚类分析等

---

## 4.5 草图矩阵比较

> [!tip] 草图矩阵比较
> 
> | 类型 | 存储 | $\mathbf{S}\mathbf{x}$ 时间 | 质量 |
> |:---|:---:|:---:|:---|
> | 高斯 | $O(mn)$ | $O(mn)$ | 最优 |
> | Rademacher ($\pm 1/\sqrt{m}$) | $O(mn)$ | $O(mn)$ | 最优 |
> | 子采样 | $O(m)$ | $O(m)$ | 良好（对结构化 $\mathbf{A}$） |
> | SRHT (Hadamard) | $O(m)$ | $O(n \log m)$ | 非常好 |

> [!abstract] 权衡
> - **高斯**：理论最优，但存储和应用代价高
> - **Rademacher**：与高斯同样好，更容易生成
> - **子采样**：选择 $m$ 个随机行 — 极快，但只对特定矩阵效果好
> - **SRHT**：最先进 — 结合了速度和质量

---

## 4.6 应用

> [!success] 矩阵草图的应用
> 
> - **矩阵乘法**：$\mathbf{A}\mathbf{B} \approx (\mathbf{A}\mathbf{S}^\top)(\mathbf{S}\mathbf{B})$
> - **最小二乘问题**：$\min \|\mathbf{A}\mathbf{x} - \mathbf{b}\|_2 \approx \min \|\mathbf{S}\mathbf{A}\mathbf{x} - \mathbf{S}\mathbf{b}\|_2$
> - **近似 SVD**：通过随机化截断 SVD（如 `irlba::svdr()`）

---

# 五、总结

## 📌 方法比较

> [!tip] 迭代 vs. 直接方法
> 
> | 方面 | 直接方法 | 迭代方法 |
> |:---|:---|:---|
> | 精度 | 精确（机器精度内） | 可控（可选择精度） |
> | 复杂度 | $O(n^3)$ | $O(n^2 \log(1/\epsilon))$ |
> | 稀疏矩阵 | 通常不利用 | 充分利用 |
> | 适用场景 | 小到中等规模 | 大规模、稀疏 |

---

## 📝 核心公式速查

> [!summary] 关键公式
> 
> **幂方法**
> $$
> \mathbf{x}^{(k)} = \frac{\mathbf{A}\mathbf{x}^{(k-1)}}{\|\mathbf{A}\mathbf{x}^{(k-1)}\|}
> $$
> 
> **迭代求解**
> $$
> \mathbf{x}^{(k)} = \mathbf{x}^{(k-1)} + \mathbf{C}(\mathbf{b} - \mathbf{A}\mathbf{x}^{(k-1)})
> $$
> 
> **收敛界**
> $$
> \|\mathbf{x}^{(k)} - \mathbf{x}\|_2 \leq \rho^k \|\mathbf{x}^{(0)} - \mathbf{x}\|_2, \quad \rho = \|\mathbf{I} - \mathbf{C}\mathbf{A}\|_2
> $$
> 
> **随机嵌入**
> $$
> (1 - \epsilon)\|\mathbf{x}\|^2 \leq \|\mathbf{S}\mathbf{x}\|^2 \leq (1 + \epsilon)\|\mathbf{x}\|^2
> $$

---

## ✅ 自测清单

- [ ] 能解释幂方法的原理和收敛条件
- [ ] 能写出 QR 迭代的步骤
- [ ] 理解相似变换保持特征值
- [ ] 能写出线性方程组迭代求解的收敛定理
- [ ] 能计算达到给定精度所需的迭代次数
- [ ] 理解矩阵草图保持距离的原理
- [ ] 能比较不同草图矩阵的优缺点
- [ ] 了解迭代和概率方法的典型应用场景
