

## 所需预备知识

- **复杂度分析** / O()O()O()-记号
- **稳定性**和**条件数**
- **正交矩阵**
- **谱范数** ∥⋅∥2\|\cdot\|_2∥⋅∥2​
- **线性方程组(LGS)**：问题与直接算法
- **线性代数**：
    - 特征值和特征向量
    - 相似变换：B=QAQ−1\mathbf{B} = \mathbf{Q}\mathbf{A}\mathbf{Q}^{-1}B=QAQ−1
- **概率论**：随机向量
- **分析I**：
    - 数列收敛性
    - 几何级数 ∑kpk\sum_k p^k∑k​pk，当 ∣p∣<1|p| < 1∣p∣<1

---

## 概述

- 数值线性代数是一个非常广阔的领域。
- 我们此前已经学习了最重要的求解方法。
- 核心思想是**矩阵分解**（LU、QR、Cholesky 等）。
- 今天我们将简要介绍两个额外的核心思想：
    - **迭代方法**：
        - 以牺牲精度换取更短的运行时间
    - **概率方法**：
        - 用较小的问题替代大问题，以_高概率_获得非常相似的解
- 这两个方向在数值线性代数中都是活跃的研究领域。

---

## 特征值问题

### 特征值问题的求解方法

- **直接方法存在**：
    
    - 特征多项式：det⁡(A−λI)=0\det(\mathbf{A} - \lambda\mathbf{I}) = 0det(A−λI)=0 → 求根
    - 问题：对于 n×nn \times nn×n 矩阵，多项式为 nnn 次 → 当 n≥5n \geq 5n≥5 时无直接解
    - 数值不稳定且代价高：O(n4)O(n^4)O(n4) 或更差
- **何时直接方法不够用？**
    
    - 大矩阵（n>1000n > 1000n>1000）：O(n3)O(n^3)O(n3) 或更差
    - 稀疏矩阵：直接方法会破坏_稀疏性_结构
    - 通常只需要少数特征值（例如最大的 k≪nk \ll nk≪n 个）
- **为什么用迭代方法？**
    
    - 只需少数特征值时更快
    - 利用_稀疏性_结构（仅需矩阵-向量乘法！）
    - 可控制精度与运行时间的权衡

---

## 幂方法

**幂方法**（von Mises 方法）通过迭代找到矩阵 A\mathbf{A}A 的最大（分离的！）特征值 λ1\lambda_1λ1​（及对应的特征向量 v1\mathbf{v}_1v1​）。

**思想**：令 x(k)=Ax(k−1)∥Ax(k−1)∥\mathbf{x}^{(k)} = \frac{\mathbf{A}\mathbf{x}^{(k-1)}}{\|\mathbf{A}\mathbf{x}^{(k-1)}\|}x(k)=∥Ax(k−1)∥Ax(k−1)​，则有

λ(k)=∥Ax(k)∥∥Ax(k−1)∥→k→∞λ1且x(k)→k→∞±v1\lambda^{(k)} = \frac{\|\mathbf{A}\mathbf{x}^{(k)}\|}{\|\mathbf{A}\mathbf{x}^{(k-1)}\|} \xrightarrow{k \to \infty} \lambda_1 \quad \text{且} \quad \mathbf{x}^{(k)} \xrightarrow{k \to \infty} \pm\mathbf{v}_1λ(k)=∥Ax(k−1)∥∥Ax(k)∥​k→∞​λ1​且x(k)k→∞​±v1​

**直觉**：

- 每个初始向量都是 A\mathbf{A}A 的特征向量 vi\mathbf{v}_ivi​ 的线性组合：x(0)=∑incivi\mathbf{x}^{(0)} = \sum_i^n c_i \mathbf{v}_ix(0)=∑in​ci​vi​
- 每次迭代会放大 x(k)\mathbf{x}^{(k)}x(k) 在 v1\mathbf{v}_1v1​ 方向上的分量，使其超过其他分量

**代数推导**：由于 Av=λv\mathbf{A}\mathbf{v} = \lambda\mathbf{v}Av=λv，可得 Akv=λkv\mathbf{A}^k\mathbf{v} = \lambda^k\mathbf{v}Akv=λkv，因此：

Akx(0)=λ1k(c1v1+∑i=2nci(λiλ1)kvi)≈λ1kc1v1当 k→∞\mathbf{A}^k\mathbf{x}^{(0)} = \lambda_1^k \left( c_1\mathbf{v}_1 + \sum_{i=2}^n c_i \left(\frac{\lambda_i}{\lambda_1}\right)^k \mathbf{v}_i \right) \approx \lambda_1^k c_1 \mathbf{v}_1 \quad \text{当 } k \to \inftyAkx(0)=λ1k​(c1​v1​+∑i=2n​ci​(λ1​λi​​)kvi​)≈λ1k​c1​v1​当 k→∞

因为 (λiλ1)k→k→∞0\left(\frac{\lambda_i}{\lambda_1}\right)^k \xrightarrow{k \to \infty} 0(λ1​λi​​)kk→∞​0，当 i≥2i \geq 2i≥2

Akx(0)∥Akx(0)∥≈λ1kc1v1∣λ1kc1∣∥v1∥=±v1\frac{\mathbf{A}^k\mathbf{x}^{(0)}}{\|\mathbf{A}^k\mathbf{x}^{(0)}\|} \approx \frac{\lambda_1^k c_1 \mathbf{v}_1}{|\lambda_1^k c_1| \|\mathbf{v}_1\|} = \pm\mathbf{v}_1∥Akx(0)∥Akx(0)​≈∣λ1k​c1​∣∥v1​∥λ1k​c1​v1​​=±v1​

---

## 幂方法：示例

A=(5−2−28)  ⟹  λ1=9,λ2=4;v1=15(−12),v2=15(21)\mathbf{A} = \begin{pmatrix} 5 & -2 \\ -2 & 8 \end{pmatrix} \implies \lambda_1 = 9, \lambda_2 = 4; \quad \mathbf{v}_1 = \frac{1}{\sqrt{5}}\begin{pmatrix} -1 \\ 2 \end{pmatrix}, \mathbf{v}_2 = \frac{1}{\sqrt{5}}\begin{pmatrix} 2 \\ 1 \end{pmatrix}A=(5−2​−28​)⟹λ1​=9,λ2​=4;v1​=5​1​(−12​),v2​=5​1​(21​)

初始向量 x(0)=(21.3)\mathbf{x}^{(0)} = \begin{pmatrix} 2 \\ 1.3 \end{pmatrix}x(0)=(21.3​)

|kkk|到 v1\mathbf{v}_1v1​ 的角度|
|---|---|
|0|83.54°|
|1|75.71°|
|2|60.18°|
|3|37.79°|
|5|8.71°|
|10|0.15°|
|15|0.00°|

---

## 测验

**对于哪些 x(0)\mathbf{x}^{(0)}x(0) 幂方法会失效？**

1. x(0)=cv1\mathbf{x}^{(0)} = c\mathbf{v}_1x(0)=cv1​
2. x(0)=cv2\mathbf{x}^{(0)} = c\mathbf{v}_2x(0)=cv2​
3. x(0)=c(v1+v2)\mathbf{x}^{(0)} = c(\mathbf{v}_1 + \mathbf{v}_2)x(0)=c(v1​+v2​)

**答案**：选项 2。如果初始向量恰好是非主导特征向量的倍数，则 c1=0c_1 = 0c1​=0，幂方法将收敛到 v2\mathbf{v}_2v2​ 而非 v1\mathbf{v}_1v1​。

---

## 迭代计算方法

- 我们希望找到 A∈Rn×n\mathbf{A} \in \mathbb{R}^{n \times n}A∈Rn×n 的所有特征值/特征向量
- 思想：将 A\mathbf{A}A 转化为特殊矩阵 B\mathbf{B}B，使问题更容易求解

**定理**

设 B\mathbf{B}B 是与 A\mathbf{A}A 相似的矩阵（即 B=QAQ−1\mathbf{B} = \mathbf{Q}\mathbf{A}\mathbf{Q}^{-1}B=QAQ−1），则：

- A\mathbf{A}A 和 B\mathbf{B}B 有相同的特征值（行列式、迹）
- 若 y\mathbf{y}y 是 B\mathbf{B}B 的特征向量，则 x=Q−1y\mathbf{x} = \mathbf{Q}^{-1}\mathbf{y}x=Q−1y 是 A\mathbf{A}A 对应同一特征值的特征向量

**证明**：

By=λy  ⟹  QAQ−1y=λy  ⟹  AQ−1y=λQ−1y  ⟹  Ax=λx\mathbf{B}\mathbf{y} = \lambda\mathbf{y} \implies \mathbf{Q}\mathbf{A}\mathbf{Q}^{-1}\mathbf{y} = \lambda\mathbf{y} \implies \mathbf{A}\mathbf{Q}^{-1}\mathbf{y} = \lambda\mathbf{Q}^{-1}\mathbf{y} \implies \mathbf{A}\mathbf{x} = \lambda\mathbf{x}By=λy⟹QAQ−1y=λy⟹AQ−1y=λQ−1y⟹Ax=λx

---

## 示例

A=(5−2−28),Q=15(211−2),Q⊤Q=I\mathbf{A} = \begin{pmatrix} 5 & -2 \\ -2 & 8 \end{pmatrix}, \quad \mathbf{Q} = \frac{1}{\sqrt{5}}\begin{pmatrix} 2 & 1 \\ 1 & -2 \end{pmatrix}, \quad \mathbf{Q}^\top\mathbf{Q} = \mathbf{I}A=(5−2​−28​),Q=5​1​(21​1−2​),Q⊤Q=I

**步骤1**：计算 AQ⊤\mathbf{A}\mathbf{Q}^\topAQ⊤：

AQ⊤=15(5−2−28)(211−2)=15(894−18)\mathbf{A}\mathbf{Q}^\top = \frac{1}{\sqrt{5}}\begin{pmatrix} 5 & -2 \\ -2 & 8 \end{pmatrix}\begin{pmatrix} 2 & 1 \\ 1 & -2 \end{pmatrix} = \frac{1}{\sqrt{5}}\begin{pmatrix} 8 & 9 \\ 4 & -18 \end{pmatrix}AQ⊤=5​1​(5−2​−28​)(21​1−2​)=5​1​(84​9−18​)

**步骤2**：计算 B=Q(AQ⊤)\mathbf{B} = \mathbf{Q}(\mathbf{A}\mathbf{Q}^\top)B=Q(AQ⊤)：

B=15(211−2)(894−18)=15(200045)=(4009)\mathbf{B} = \frac{1}{5}\begin{pmatrix} 2 & 1 \\ 1 & -2 \end{pmatrix}\begin{pmatrix} 8 & 9 \\ 4 & -18 \end{pmatrix} = \frac{1}{5}\begin{pmatrix} 20 & 0 \\ 0 & 45 \end{pmatrix} = \begin{pmatrix} 4 & 0 \\ 0 & 9 \end{pmatrix}B=51​(21​1−2​)(84​9−18​)=51​(200​045​)=(40​09​)

B\mathbf{B}B 是对角矩阵   ⟹  \implies⟹ 特征值可直接读出：λ1=4,λ2=9\lambda_1 = 4, \lambda_2 = 9λ1​=4,λ2​=9

**验证**：A\mathbf{A}A 有相同的特征值：

det⁡(A−λI)=(5−λ)(8−λ)−4=λ2−13λ+36=(λ−4)(λ−9)=0\det(\mathbf{A} - \lambda\mathbf{I}) = (5-\lambda)(8-\lambda) - 4 = \lambda^2 - 13\lambda + 36 = (\lambda - 4)(\lambda - 9) = 0det(A−λI)=(5−λ)(8−λ)−4=λ2−13λ+36=(λ−4)(λ−9)=0

---

## 方法概述

为了（迭代地）计算与 A\mathbf{A}A 相似且易于确定特征值的矩阵 B(k)\mathbf{B}^{(k)}B(k)，有多种方法：

- **QR 迭代**：导向（近似）上三角矩阵 B\mathbf{B}B（Schur 分解）
    - 类似于 LU 和 Cholesky 迭代
- **Lanczos 迭代**：导向三对角矩阵 B\mathbf{B}B（bij=0b_{ij} = 0bij​=0 当 ∣i−j∣>1|i-j| > 1∣i−j∣>1），可限制于"最重要的"特征值。对大型稀疏矩阵更高效。
- 实际中需要额外的数值技巧使这些方法稳定且足够快：_Shifted QR_、_Implicitly Restarted Lanczos_

---

## QR 迭代

**定义：QR 迭代**

QR 迭代利用 QR 分解进行迭代：

1. 从 A(0)=A\mathbf{A}^{(0)} = \mathbf{A}A(0)=A 开始
2. 对于 k=1,2,…k = 1, 2, \ldotsk=1,2,…：
    - 计算 QR 分解：A(k−1)=Q(k)R(k)\mathbf{A}^{(k-1)} = \mathbf{Q}^{(k)}\mathbf{R}^{(k)}A(k−1)=Q(k)R(k)
    - 设置 A(k)=(Q(k))⊤A(k−1)Q(k)=R(k)Q(k)\mathbf{A}^{(k)} = (\mathbf{Q}^{(k)})^\top \mathbf{A}^{(k-1)}\mathbf{Q}^{(k)} = \mathbf{R}^{(k)}\mathbf{Q}^{(k)}A(k)=(Q(k))⊤A(k−1)Q(k)=R(k)Q(k)

- A(k)\mathbf{A}^{(k)}A(k) 与 A\mathbf{A}A 相似：  
    A(k)=R(k)Q(k)=(Q(k))−1A(k−1)Q(k)=(Q(k)⋯Q(1))−1A(Q(1)⋯Q(k))\mathbf{A}^{(k)} = \mathbf{R}^{(k)}\mathbf{Q}^{(k)} = (\mathbf{Q}^{(k)})^{-1}\mathbf{A}^{(k-1)}\mathbf{Q}^{(k)} = (\mathbf{Q}^{(k)} \cdots \mathbf{Q}^{(1)})^{-1}\mathbf{A}(\mathbf{Q}^{(1)} \cdots \mathbf{Q}^{(k)})A(k)=R(k)Q(k)=(Q(k))−1A(k−1)Q(k)=(Q(k)⋯Q(1))−1A(Q(1)⋯Q(k))
    
- 当 k→∞k \to \inftyk→∞ 时，A(k)\mathbf{A}^{(k)}A(k) 收敛到上三角矩阵（！）  
    → 特征值在对角线上
    
- 特征向量 V\mathbf{V}V 通过乘积 Qk:=∏j=1kQ(j)\mathbf{Q}_k := \prod_{j=1}^k \mathbf{Q}^{(j)}Qk​:=∏j=1k​Q(j) 计算
    

---

## QR 迭代：证明

定义 Qk:=Q(1)Q(2)⋯Q(k)\mathbf{Q}_k := \mathbf{Q}^{(1)}\mathbf{Q}^{(2)} \cdots \mathbf{Q}^{(k)}Qk​:=Q(1)Q(2)⋯Q(k)，Rk=R(k)R(k−1)⋯R(1)\mathbf{R}_k = \mathbf{R}^{(k)}\mathbf{R}^{(k-1)} \cdots \mathbf{R}^{(1)}Rk​=R(k)R(k−1)⋯R(1)

**证明**：Ak=QkRk\mathbf{A}^k = \mathbf{Q}_k\mathbf{R}_kAk=Qk​Rk​

**归纳法**：

- **归纳基础**（k=1k = 1k=1）：  
    A=Q(1)R(1)=Q1R1\mathbf{A} = \mathbf{Q}^{(1)}\mathbf{R}^{(1)} = \mathbf{Q}_1\mathbf{R}_1A=Q(1)R(1)=Q1​R1​ ✓
    
- **归纳步骤**（k→k+1k \to k+1k→k+1）：  
    A(i)=R(i)Q(i)\mathbf{A}^{(i)} = \mathbf{R}^{(i)}\mathbf{Q}^{(i)}A(i)=R(i)Q(i)，A(i)=Q(i+1)R(i+1)  ⟹  R(i)Q(i)=Q(i+1)R(i+1)\mathbf{A}^{(i)} = \mathbf{Q}^{(i+1)}\mathbf{R}^{(i+1)} \implies \mathbf{R}^{(i)}\mathbf{Q}^{(i)} = \mathbf{Q}^{(i+1)}\mathbf{R}^{(i+1)}A(i)=Q(i+1)R(i+1)⟹R(i)Q(i)=Q(i+1)R(i+1) (⋆\star⋆)
    
    Ak+1=AAk=(Q(1)R(1))(QkRk)\mathbf{A}^{k+1} = \mathbf{A}\mathbf{A}^k = (\mathbf{Q}^{(1)}\mathbf{R}^{(1)})(\mathbf{Q}_k\mathbf{R}_k)Ak+1=AAk=(Q(1)R(1))(Qk​Rk​)  
    =Q(1)(R(1)Q(1))Q(2)⋯Q(k)R(k)⋯R(1)= \mathbf{Q}^{(1)}(\mathbf{R}^{(1)}\mathbf{Q}^{(1)})\mathbf{Q}^{(2)} \cdots \mathbf{Q}^{(k)}\mathbf{R}^{(k)} \cdots \mathbf{R}^{(1)}=Q(1)(R(1)Q(1))Q(2)⋯Q(k)R(k)⋯R(1)  
    [利用 kkk 次 (⋆\star⋆)：R(1)Q(1)=Q(2)R(2)\mathbf{R}^{(1)}\mathbf{Q}^{(1)} = \mathbf{Q}^{(2)}\mathbf{R}^{(2)}R(1)Q(1)=Q(2)R(2)，R(2)Q(2)=Q(3)R(3)\mathbf{R}^{(2)}\mathbf{Q}^{(2)} = \mathbf{Q}^{(3)}\mathbf{R}^{(3)}R(2)Q(2)=Q(3)R(3)，...]  
    =Q(1)Q(2)⋯Q(k+1)R(k+1)⋯R(1)= \mathbf{Q}^{(1)}\mathbf{Q}^{(2)} \cdots \mathbf{Q}^{(k+1)}\mathbf{R}^{(k+1)} \cdots \mathbf{R}^{(1)}=Q(1)Q(2)⋯Q(k+1)R(k+1)⋯R(1)  
    =Qk+1Rk+1= \mathbf{Q}_{k+1}\mathbf{R}_{k+1}=Qk+1​Rk+1​ ✓
    

---

## QR 迭代：解释

- QR 迭代实际上是迭代计算 Ak\mathbf{A}^kAk 的 QR 分解
    
- Qk\mathbf{Q}_kQk​ 的列是 Ake1,Ake2,…,Aken\mathbf{A}^k\mathbf{e}_1, \mathbf{A}^k\mathbf{e}_2, \ldots, \mathbf{A}^k\mathbf{e}_nAke1​,Ake2​,…,Aken​ 的正交化版本  
    → 相当于对**所有**单位向量同时应用幂方法！  
      ⟹  Qk→k→∞V\implies \mathbf{Q}_k \xrightarrow{k \to \infty} \mathbf{V}⟹Qk​k→∞​V
    
- A(k)=Qk⊤AQk\mathbf{A}^{(k)} = \mathbf{Q}_k^\top \mathbf{A}\mathbf{Q}_kA(k)=Qk⊤​AQk​ 收敛到上三角或对角矩阵，特征值在对角线上（Schur 分解），因为 Qk→k→∞V\mathbf{Q}_k \xrightarrow{k \to \infty} \mathbf{V}Qk​k→∞​V：
    

Qk⊤AQk→k→∞V⊤AV=V⊤VΛV⊤V=Λ\mathbf{Q}_k^\top \mathbf{A}\mathbf{Q}_k \xrightarrow{k \to \infty} \mathbf{V}^\top \mathbf{A}\mathbf{V} = \mathbf{V}^\top \mathbf{V}\mathbf{\Lambda}\mathbf{V}^\top \mathbf{V} = \mathbf{\Lambda}Qk⊤​AQk​k→∞​V⊤AV=V⊤VΛV⊤V=Λ

---

## QR 迭代：示例

A=(5−2−28)  ⟹  λ1=9,λ2=4;v1=15(−12),v2=15(21)\mathbf{A} = \begin{pmatrix} 5 & -2 \\ -2 & 8 \end{pmatrix} \implies \lambda_1 = 9, \lambda_2 = 4; \quad \mathbf{v}_1 = \frac{1}{\sqrt{5}}\begin{pmatrix} -1 \\ 2 \end{pmatrix}, \mathbf{v}_2 = \frac{1}{\sqrt{5}}\begin{pmatrix} 2 \\ 1 \end{pmatrix}A=(5−2​−28​)⟹λ1​=9,λ2​=4;v1​=5​1​(−12​),v2​=5​1​(21​)

|kkk|到 v1\mathbf{v}_1v1​ 的角度|到 v2\mathbf{v}_2v2​ 的角度|
|---|---|---|
|0|116.57°|116.57°|
|1|41.63°|138.37°|
|2|158.44°|158.44°|
|3|9.96°|170.04°|
|5|1.99°|178.01°|
|10|179.97°|179.97°|
|15|0.00°|180.00°|

## 注释

**特征值问题的条件数：**

- 当矩阵接近_对称_时，特征值问题是_良好条件_的
- 当_特征值彼此接近_时，特征向量问题是_病态条件_的

**运行时间：**

- 每次 QR 迭代是 O(n3)O(n^3)O(n3)
- 总体通常只需很少的迭代次数（取决于特征值分布）
- 迭代式 _SVD_ 方法（如 _Lanczos 双对角化_；`{irlba}`）对大型稀疏矩阵更快

---

## 应用：Google PageRank

**问题：** 对互联网上约 n≈1010n \approx 10^{10}n≈1010 个网页进行排序

**模型：** 每个页面 iii 有一个"重要性分数" xix_ixi​。分数满足：

xi=∑j:j→ixjdjx_i = \sum_{j: j \to i} \frac{x_j}{d_j}xi​=∑j:j→i​dj​xj​​

其中 j→ij \to ij→i 表示"页面 jjj 链接到 iii"，djd_jdj​ 是 jjj 的出链数量。

**矩阵形式：** 令 A=[I(j→i)dj]i,j\mathbf{A} = \left[\frac{I(j \to i)}{d_j}\right]_{i,j}A=[dj​I(j→i)​]i,j​，则 x=Ax\mathbf{x} = \mathbf{A}\mathbf{x}x=Ax   ⟹  \implies⟹ 特征值为 λ=1\lambda = 1λ=1 的特征值问题！

**为什么用迭代方法？**

- 矩阵 A∈R1010×1010\mathbf{A} \in \mathbb{R}^{10^{10} \times 10^{10}}A∈R1010×1010 — 直接方法不可能！
- 但 A\mathbf{A}A 是_极度稀疏_的（每个页面只有约 10 个链接）
- 存储需求：O(n)O(n)O(n) 而非 O(n2)O(n^2)O(n2)
- **幂迭代：** x(k+1)=Ax(k)/∥Ax(k)∥\mathbf{x}^{(k+1)} = \mathbf{A}\mathbf{x}^{(k)} / \|\mathbf{A}\mathbf{x}^{(k)}\|x(k+1)=Ax(k)/∥Ax(k)∥
- 约 50 次迭代收敛（网络图有特殊结构）
- 单次迭代：O(n)O(n)O(n) 次操作
- **总计：** O(n)O(n)O(n) 而非 O(n3)O(n^3)O(n3) — 这使得 Google 成为可能！

---

## 应用：主成分分析 (PCA)

计算特征值/特征向量对数据分析至关重要：

- **问题：** 给定（均值中心化的）数据矩阵 X∈Rn×p\mathbf{X} \in \mathbb{R}^{n \times p}X∈Rn×p（nnn 个观测，ppp 个变量）
- **目标：** 找到最大方差的方向用于降维
- **解法：** 计算协方差矩阵 Σ=1n−1X⊤X∈Rp×p\mathbf{\Sigma} = \frac{1}{n-1}\mathbf{X}^\top\mathbf{X} \in \mathbb{R}^{p \times p}Σ=n−11​X⊤X∈Rp×p
    - Σ\mathbf{\Sigma}Σ 的特征向量称为_主成分_（Rp\mathbb{R}^pRp 中最大方差的"方向"）
    - 特征值给出每个方向解释的方差
- 实践中通过 X\mathbf{X}X 的 _SVD_ 更快/更高效  
    （如 `{irlba}`：`irlba()`（_迭代_），`svdr()`（_迭代 & 概率_））

**为什么用迭代方法？**

- 现代应用：p=104p = 10^4p=104 到 10610^6106（如基因表达、图像分析等）
- 通常只需要最大的 kkk 个特征值（k≪pk \ll pk≪p）— 参见通过_截断 SVD_ 进行图像压缩
- Lanczos 迭代：以 O(kp2)O(kp^2)O(kp2) 计算最大的 kkk 个特征值，而非 O(p3)O(p^3)O(p3) 计算全部
    - 当 p=100,000p = 100,000p=100,000 且 k=10k = 10k=10 时，Lanczos 快约 10,000 倍！

---

## 应用：近似 SVD

通过 SVD 进行图像压缩：

```r
library(irlba); suppressMessages(library(magick))
# "img" 是 2500 × 3300 像素（灰度）
img <- paste0(
  "https://upload.wikimedia.org/wikipedia/commons/7/7e/",
  "Bolzano_City_Image_-_Photo_by_Giovanni_Ussi_-_In_Black_and_White_19.jpg")
X <- (image_read(img) |>
        image_data(channels = "gray") |>
        as.numeric())[,,1]
r <- 50
microbenchmark::microbenchmark(
  svd_full   = svd(X),
  svd_irlba  = irlba::irlba(X, nv = r, nu = r),
  svd_random = irlba::svdr(X, k = r),
  times = 5)

## 单位：秒
##          expr    min     lq   mean median     uq    max neval cld
##      svd_full 97.38 98.13  99.12  98.32  98.50 103.26     5   a
##     svd_irlba  2.62  2.66   2.75   2.75   2.84   2.88     5   b
##    svd_random 27.56 28.68  29.24  28.75  29.72  31.51     5   c
```

---

## 应用：近似 SVD（续）

计算低秩近似的相对 Frobenius 误差（相对于 X\mathbf{X}X 以及彼此之间）：

```r
r <- 50
X_r <- with(svd(X, nu = r, nv = r), u %*% diag(d[1:r]) %*% t(v))
X_irlba <- with(irlba::irlba(X, nu = r, nv = r), u %*% diag(d) %*% t(v))
X_svdr <- with(irlba::svdr(X, k = r), u %*% diag(d) %*% t(v))
norm(X - X_r, "F")/norm(X, "F")

## [1] 0.129

c(norm(X_r - X_irlba, "F"), norm(X_r - X_svdr, "F"))/norm(X_r, "F")

## [1] 0.000106 0.000252
```

_图像显示使用秩 50 近似时，原图、完整 SVD、IRLBA 和 SVDR 方法的比较，结果视觉上几乎相同。_

---

## 迭代特征值方法：比较

|方法|每次迭代复杂度|应用场景|输出|
|---|---|---|---|
|幂方法|O(nnz(A))O(\text{nnz}(\mathbf{A}))O(nnz(A))|最大特征值/向量|1 个特征对|
|(shifted) QR|O(nnz(A))-O(n3)O(\text{nnz}(\mathbf{A}))\text{-}O(n^3)O(nnz(A))-O(n3)|所有特征值|nnn 个特征值|
|Lanczos|O(m⋅nnz(A)+m2)O(m \cdot \text{nnz}(\mathbf{A}) + m^2)O(m⋅nnz(A)+m2)（求 mmm 个特征值）|少数极端特征值，_稀疏_ A\mathbf{A}A|m≪nm \ll nm≪n 个特征对|

- 幂方法收敛率：∣λ2/λ1∣|\lambda_2/\lambda_1|∣λ2​/λ1​∣
- _QR 迭代_通常具有二次收敛，最稳定
- 应用：
    - _稀疏_矩阵：Lanczos 明显更优
    - 所有特征值，内存次要：(shifted) QR 迭代
    - 少数特征值：幂方法（1 个），Lanczos（m≪nm \ll nm≪n）

nnz(⋅)\text{nnz}(\cdot)nnz(⋅) = 非零元素数量

---

## 迭代求解方法

- 目前为止我们学习了求解_线性方程组_和_最小二乘问题_的直接方法
- 另一种流行的方法是_迭代方法_
- _迭代方法_通常遵循_不动点迭代_
- 如果愿意牺牲一些精度，它们通常快得多

---

## 迭代求解方法：直觉

**如何迭代逼近解？**

- _残差_：r(k)=b−Ax(k)\mathbf{r}^{(k)} = \mathbf{b} - \mathbf{A}\mathbf{x}^{(k)}r(k)=b−Ax(k) 衡量 x(k)\mathbf{x}^{(k)}x(k) 距真实解有多远
- 如果 Ax(k)=b\mathbf{A}\mathbf{x}^{(k)} = \mathbf{b}Ax(k)=b，则 r(k)=0\mathbf{r}^{(k)} = \mathbf{0}r(k)=0，我们找到了解
- _校正_：只要 r(k)≠0\mathbf{r}^{(k)} \neq \mathbf{0}r(k)=0，就校正我们的估计：

x(k+1)=x(k)+Cr(k)⏟校正步\mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} + \underbrace{\mathbf{C}\mathbf{r}^{(k)}}_{\text{校正步}}x(k+1)=x(k)+校正步Cr(k)​​

- C\mathbf{C}C 决定我们沿残差方向校正的强度
- **类比：** 梯度下降 — 我们逐步向解移动

**为什么不直接求解？**  
→ 对于大型和/或稀疏矩阵，迭代方法通常比直接方法快几个数量级！

---

## 线性方程组的迭代求解

- 设 A∈Rn×n\mathbf{A} \in \mathbb{R}^{n \times n}A∈Rn×n，b∈Rn\mathbf{b} \in \mathbb{R}^nb∈Rn，x\mathbf{x}x 是 Ax=b\mathbf{A}\mathbf{x} = \mathbf{b}Ax=b 的解
- 对于任意初始值 x(0)∈Rn\mathbf{x}^{(0)} \in \mathbb{R}^nx(0)∈Rn 和 C∈Rn×n\mathbf{C} \in \mathbb{R}^{n \times n}C∈Rn×n，定义迭代

x(k)=x(k−1)+C(b−Ax(k−1)),k=1,2,…\mathbf{x}^{(k)} = \mathbf{x}^{(k-1)} + \mathbf{C}(\mathbf{b} - \mathbf{A}\mathbf{x}^{(k-1)}), \quad k = 1, 2, \ldotsx(k)=x(k−1)+C(b−Ax(k−1)),k=1,2,…

**定理**

设 x\mathbf{x}x 是 Ax=b\mathbf{A}\mathbf{x} = \mathbf{b}Ax=b 的唯一解。  
若 ρ:=∥In−CA∥2<1\rho := \|\mathbf{I}_n - \mathbf{C}\mathbf{A}\|_2 < 1ρ:=∥In​−CA∥2​<1（_谱范数_），则对所有 k≥1k \geq 1k≥1：

∥x(k)−x∥2≤ρk∥x(0)−x∥2\|\mathbf{x}^{(k)} - \mathbf{x}\|_2 \leq \rho^k \|\mathbf{x}^{(0)} - \mathbf{x}\|_2∥x(k)−x∥2​≤ρk∥x(0)−x∥2​

- 特别地：lim⁡k→∞x(k)=x\lim_{k \to \infty} \mathbf{x}^{(k)} = \mathbf{x}limk→∞​x(k)=x（_收敛_）

---

## 线性方程组的迭代求解：证明

定义 B=I−CA\mathbf{B} = \mathbf{I} - \mathbf{C}\mathbf{A}B=I−CA，使得 ∥B∥2=ρ\|\mathbf{B}\|_2 = \rho∥B∥2​=ρ 且

x(k)=Bx(k−1)+Cb,k=1,2,…\mathbf{x}^{(k)} = \mathbf{B}\mathbf{x}^{(k-1)} + \mathbf{C}\mathbf{b}, \quad k = 1, 2, \ldotsx(k)=Bx(k−1)+Cb,k=1,2,…

首先注意 x\mathbf{x}x 是此迭代的不动点：

Bx+Cb=(I−CA)x+Cb=x−C(Ax−b)⏟=0=x\mathbf{B}\mathbf{x} + \mathbf{C}\mathbf{b} = (\mathbf{I} - \mathbf{C}\mathbf{A})\mathbf{x} + \mathbf{C}\mathbf{b} = \mathbf{x} - \mathbf{C}\underbrace{(\mathbf{A}\mathbf{x} - \mathbf{b})}_{=0} = \mathbf{x}Bx+Cb=(I−CA)x+Cb=x−C=0(Ax−b)​​=x

因此：  
∥x(k)−x∥2=∥Bx(k−1)+Cb−x∥2\|\mathbf{x}^{(k)} - \mathbf{x}\|_2 = \|\mathbf{B}\mathbf{x}^{(k-1)} + \mathbf{C}\mathbf{b} - \mathbf{x}\|_2∥x(k)−x∥2​=∥Bx(k−1)+Cb−x∥2​  
=∥Bx(k−1)+Cb−(Bx+Cb)∥2= \|\mathbf{B}\mathbf{x}^{(k-1)} + \mathbf{C}\mathbf{b} - (\mathbf{B}\mathbf{x} + \mathbf{C}\mathbf{b})\|_2=∥Bx(k−1)+Cb−(Bx+Cb)∥2​  
=∥B(x(k−1)−x)∥2= \|\mathbf{B}(\mathbf{x}^{(k-1)} - \mathbf{x})\|_2=∥B(x(k−1)−x)∥2​  
≤ρ∥x(k−1)−x∥2\leq \rho\|\mathbf{x}^{(k-1)} - \mathbf{x}\|_2≤ρ∥x(k−1)−x∥2​

迭代此不等式，得到：

∥x(k)−x∥2≤ρ∥x(k−1)−x∥2≤ρ2∥x(k−2)−x∥2≤⋯≤ρk∥x(0)−x∥2\|\mathbf{x}^{(k)} - \mathbf{x}\|_2 \leq \rho\|\mathbf{x}^{(k-1)} - \mathbf{x}\|_2 \leq \rho^2\|\mathbf{x}^{(k-2)} - \mathbf{x}\|_2 \leq \cdots \leq \rho^k\|\mathbf{x}^{(0)} - \mathbf{x}\|_2∥x(k)−x∥2​≤ρ∥x(k−1)−x∥2​≤ρ2∥x(k−2)−x∥2​≤⋯≤ρk∥x(0)−x∥2​

---

## 测验

设

x(k)=(I−CA)x(k−1)+Cb\mathbf{x}^{(k)} = (\mathbf{I} - \mathbf{C}\mathbf{A})\mathbf{x}^{(k-1)} + \mathbf{C}\mathbf{b}x(k)=(I−CA)x(k−1)+Cb

其中 C=γIn\mathbf{C} = \gamma\mathbf{I}_nC=γIn​，γ>0\gamma > 0γ>0。单次迭代的时间复杂度是多少？

1. O(1)O(1)O(1)
2. O(n)O(n)O(n)
3. O(n2)O(n^2)O(n2)
4. O(n3)O(n^3)O(n3)

**答案：3。** O(n2)O(n^2)O(n2)。主要计算是矩阵-向量乘法 Ax(k−1)\mathbf{A}\mathbf{x}^{(k-1)}Ax(k−1)，需要 O(n2)O(n^2)O(n2) 次操作。

---

## 测验

设 ρ∈(0,1)\rho \in (0, 1)ρ∈(0,1)，ϵ>0\epsilon > 0ϵ>0，且

∥x(k)−x∥≤ρk∥x(0)−x∥\|\mathbf{x}^{(k)} - \mathbf{x}\| \leq \rho^k \|\mathbf{x}^{(0)} - \mathbf{x}\|∥x(k)−x∥≤ρk∥x(0)−x∥

kkk 至少要多大才能使 ∥x(k)−x∥≤ϵ\|\mathbf{x}^{(k)} - \mathbf{x}\| \leq \epsilon∥x(k)−x∥≤ϵ？

1. k=O(1)k = O(1)k=O(1)
2. k=O(log⁡(1/ϵ))k = O(\log(1/\epsilon))k=O(log(1/ϵ))
3. k=O(1/ϵ)k = O(1/\epsilon)k=O(1/ϵ)
4. k=O(1/ϵk)k = O(1/\epsilon^k)k=O(1/ϵk)

---

## 测验：解答

k=O(log⁡(1/ϵ))k = O(\log(1/\epsilon))k=O(log(1/ϵ))

1. 设 e0=∥x(0)−x∥e_0 = \|\mathbf{x}^{(0)} - \mathbf{x}\|e0​=∥x(0)−x∥
2. 求解：ρke0≤ϵ  ⟹  ρk≤ϵ/e0\rho^k e_0 \leq \epsilon \implies \rho^k \leq \epsilon/e_0ρke0​≤ϵ⟹ρk≤ϵ/e0​
3. 两边取对数（0<ρ<1  ⟹  log⁡(ρ)<00 < \rho < 1 \implies \log(\rho) < 00<ρ<1⟹log(ρ)<0）：  
    k≥log⁡(ϵ)−log⁡(e0)log⁡(ρ)=log⁡(e0)+log⁡(1/ϵ)−log⁡(ρ)k \geq \frac{\log(\epsilon) - \log(e_0)}{\log(\rho)} = \frac{\log(e_0) + \log(1/\epsilon)}{-\log(\rho)}k≥log(ρ)log(ϵ)−log(e0​)​=−log(ρ)log(e0​)+log(1/ϵ)​
4. 最小的 k=⌈log⁡(e0/ϵ)−log⁡(ρ)⌉k = \left\lceil \frac{\log(e_0/\epsilon)}{-\log(\rho)} \right\rceilk=⌈−log(ρ)log(e0​/ϵ)​⌉，  
    因此 kkk 随 log⁡(1/ϵ)\log(1/\epsilon)log(1/ϵ) 增长 ✓

---

## 线性方程组的迭代求解

问题在于如何选择 C\mathbf{C}C 使得：

- ρ=∥I−CA∥2<1\rho = \|\mathbf{I} - \mathbf{C}\mathbf{A}\|_2 < 1ρ=∥I−CA∥2​<1 尽可能小（最优：C≈A−1\mathbf{C} \approx \mathbf{A}^{-1}C≈A−1）
- x(k)=(I−CA)x(k−1)+Cb\mathbf{x}^{(k)} = (\mathbf{I} - \mathbf{C}\mathbf{A})\mathbf{x}^{(k-1)} + \mathbf{C}\mathbf{b}x(k)=(I−CA)x(k−1)+Cb 易于计算

---

## 线性方程组的迭代求解

迭代代价为 O(n2)O(n^2)O(n2) 的常用 C\mathbf{C}C 选择：

- _Richardson_ 迭代：C=γIn\mathbf{C} = \gamma\mathbf{I}_nC=γIn​，γ>0\gamma > 0γ>0 足够小
    
- _Jacobi_ 迭代：C=diag(A11,…,Ann)−1\mathbf{C} = \text{diag}(\mathbf{A}_{11}, \ldots, \mathbf{A}_{nn})^{-1}C=diag(A11​,…,Ann​)−1
    
- _Gauss-Seidel_ 迭代：C−1\mathbf{C}^{-1}C−1 等于 A\mathbf{A}A 的下三角部分
    
- 因此达到 ∥x(k)−x∥≤ϵ\|\mathbf{x}^{(k)} - \mathbf{x}\| \leq \epsilon∥x(k)−x∥≤ϵ 需要 O(n2log⁡(1/ϵ))O(n^2 \log(1/\epsilon))O(n2log(1/ϵ)) 次操作
    
- 比较：通过 _LU_ 或 _QR_ 求精确解需要 O(n3)O(n^3)O(n3)
    

---

## 例子：Richardson 迭代

给定：A=(4113)\mathbf{A} = \begin{pmatrix} 4 & 1 \\ 1 & 3 \end{pmatrix}A=(41​13​)，b=(12)\mathbf{b} = \begin{pmatrix} 1 \\ 2 \end{pmatrix}b=(12​)。真实解：x=A−1b=111(17)≈(0.0910.636)\mathbf{x} = \mathbf{A}^{-1}\mathbf{b} = \frac{1}{11}\begin{pmatrix} 1 \\ 7 \end{pmatrix} \approx \begin{pmatrix} 0.091 \\ 0.636 \end{pmatrix}x=A−1b=111​(17​)≈(0.0910.636​)

**Richardson 迭代**，γ=0.25\gamma = 0.25γ=0.25，初始值 x(0)=(00)\mathbf{x}^{(0)} = \begin{pmatrix} 0 \\ 0 \end{pmatrix}x(0)=(00​)：

**迭代 0：** r(0)=b−Ax(0)=(12)\mathbf{r}^{(0)} = \mathbf{b} - \mathbf{A}\mathbf{x}^{(0)} = \begin{pmatrix} 1 \\ 2 \end{pmatrix}r(0)=b−Ax(0)=(12​)，x(1)=x(0)+0.25r(0)=(0.250.5)\mathbf{x}^{(1)} = \mathbf{x}^{(0)} + 0.25\mathbf{r}^{(0)} = \begin{pmatrix} 0.25 \\ 0.5 \end{pmatrix}x(1)=x(0)+0.25r(0)=(0.250.5​)

**迭代 1：** Ax(1)=(1.51.75)\mathbf{A}\mathbf{x}^{(1)} = \begin{pmatrix} 1.5 \\ 1.75 \end{pmatrix}Ax(1)=(1.51.75​)，r(1)=b−Ax(1)=(−0.50.25)\mathbf{r}^{(1)} = \mathbf{b} - \mathbf{A}\mathbf{x}^{(1)} = \begin{pmatrix} -0.5 \\ 0.25 \end{pmatrix}r(1)=b−Ax(1)=(−0.50.25​)，x(2)=x(1)+0.25r(1)=(0.1250.563)\mathbf{x}^{(2)} = \mathbf{x}^{(1)} + 0.25\mathbf{r}^{(1)} = \begin{pmatrix} 0.125 \\ 0.563 \end{pmatrix}x(2)=x(1)+0.25r(1)=(0.1250.563​)

**迭代 2：** Ax(2)=(1.0631.813)\mathbf{A}\mathbf{x}^{(2)} = \begin{pmatrix} 1.063 \\ 1.813 \end{pmatrix}Ax(2)=(1.0631.813​)，r(2)=(−0.0630.188)\mathbf{r}^{(2)} = \begin{pmatrix} -0.063 \\ 0.188 \end{pmatrix}r(2)=(−0.0630.188​)，x(3)=(0.1090.610)\mathbf{x}^{(3)} = \begin{pmatrix} 0.109 \\ 0.610 \end{pmatrix}x(3)=(0.1090.610​)

**迭代 3：** x(4)=(0.0950.628)\mathbf{x}^{(4)} = \begin{pmatrix} 0.095 \\ 0.628 \end{pmatrix}x(4)=(0.0950.628​)

**误差** ∥x(k)−x∥2\|\mathbf{x}^{(k)} - \mathbf{x}\|_2∥x(k)−x∥2​：0.642→0.209→0.081→0.032→0.009→⋯0.642 \to 0.209 \to 0.081 \to 0.032 \to 0.009 \to \cdots0.642→0.209→0.081→0.032→0.009→⋯

**收敛率：** ρ=∥I−γA∥2≈0.41\rho = \|\mathbf{I} - \gamma\mathbf{A}\|_2 \approx 0.41ρ=∥I−γA∥2​≈0.41  
  ⟹  \implies⟹ 误差应每次迭代下降约 2.5 倍。

---

## 迭代求解方法

**注释**

- _迭代方法_不仅适用于特征值和线性方程组问题，也适用于_最小二乘_问题
- 少数情况下它们是_精确_的
- 大多数情况下存在_运行时间_与精度之间的权衡
- 当_运行时间_更重要时，迭代是首选方法。精度可通过迭代次数控制


## 概率方法

---

## 有目的的随机性

- 问题常常太大，以至于我们愿意用精度换取更好的_复杂度_
- 如今越来越多地通过_随机化_来实现这一点
- **核心思想**：
    - 用一个_随机_生成的、_更小_的问题_替代_大问题
    - 该小问题_高概率_具有_相似的解_

---

## 思想：矩阵草图（Matrix-Sketching）

高维空间 Rn\mathbb{R}^nRn（n≫1n \gg 1n≫1）中的几何事实：

> _对于大的 nnn，两个随机向量 s1,s2∈Rn\mathbf{s}_1, \mathbf{s}_2 \in \mathbb{R}^ns1​,s2​∈Rn 高概率几乎正交。_

  ⟹  \implies⟹ 适当的_随机_抽取的矩阵 S∈Rm×n\mathbf{S} \in \mathbb{R}^{m \times n}S∈Rm×n（m≪nm \ll nm≪n）高概率保持：

- 距离：∥Sx−Sy∥2≈∥x−y∥2\|\mathbf{S}\mathbf{x} - \mathbf{S}\mathbf{y}\|_2 \approx \|\mathbf{x} - \mathbf{y}\|_2∥Sx−Sy∥2​≈∥x−y∥2​
- 角度：∠(Sx,Sy)≈∠(x,y)\angle(\mathbf{S}\mathbf{x}, \mathbf{S}\mathbf{y}) \approx \angle(\mathbf{x}, \mathbf{y})∠(Sx,Sy)≈∠(x,y)

  ⟹  \implies⟹ **随机**"投影" S\mathbf{S}S 到低维空间可以（通常近似地）保持几何结构。

---

## 示例

```r
set.seed(1312)
## 两个任意的 n 维向量
n <- 10000
x <- runif(n)
y <- runif(n)
# (m x n) 矩阵，m << n，元素随机
m <- 50 # m << n !!
S <- rnorm(m * n, sd = sqrt(1 / m)) |> matrix(nrow = m, ncol = n)
Sx <- S %*% x
Sy <- S %*% y
## 距离 ||x - y|| 和 ||Sx - Sy||
c(norm(x - y, "2"), norm(Sx - Sy, "2"))

## [1] 41.0 42.2

## 角度: cos(angle) = (x'y) / (||x|| ||y||)
angle <- function(x, y) (sum(x * y) / (norm(x, "2") * norm(y, "2"))) |> acos()
c(angle(x, y), angle(Sx, Sy))

## [1] 0.728 0.735
```

→ 距离偏差小于 1%，角度偏差小于 3%！

---

## 子空间的随机嵌入

**定理**

设

- S∈Rm×n\mathbf{S} \in \mathbb{R}^{m \times n}S∈Rm×n 是具有独立同分布行向量 si\mathbf{s}_isi​ 的矩阵
- E[sisi⊤]=In/m\mathbb{E}[\mathbf{s}_i\mathbf{s}_i^\top] = \mathbf{I}_n/mE[si​si⊤​]=In​/m 且 E[∥si∥4]≤K/m2\mathbb{E}[\|\mathbf{s}_i\|^4] \leq K/m^2E[∥si​∥4]≤K/m2

则以至少 1−δ1 - \delta1−δ 的概率有

(1−ϵ)∥x∥2≤∥Sx∥2≤(1+ϵ)∥x∥2对所有 x∈Rn(1 - \epsilon)\|\mathbf{x}\|^2 \leq \|\mathbf{S}\mathbf{x}\|^2 \leq (1 + \epsilon)\|\mathbf{x}\|^2 \quad \text{对所有 } \mathbf{x} \in \mathbb{R}^n(1−ϵ)∥x∥2≤∥Sx∥2≤(1+ϵ)∥x∥2对所有 x∈Rn

其中 δ>0\delta > 0δ>0 且 ϵ=K/δm\epsilon = \sqrt{K/\delta m}ϵ=K/δm​。

**示例：**

- _高斯_：siN(0,In/m)\mathbf{s}_i \stackrel{iid}{\sim} \mathcal{N}(\mathbf{0}, \mathbf{I}_n/m)si​∼iidN(0,In​/m)
- _Rademacher_：sijs_{ij}sij​ 独立同分布，P(sij=1/m)=P(sij=−1/m)=1/2\mathbb{P}(s_{ij} = 1/\sqrt{m}) = \mathbb{P}(s_{ij} = -1/\sqrt{m}) = 1/2P(sij​=1/m​)=P(sij​=−1/m​)=1/2
- _子采样_：si=n/m eKi\mathbf{s}_i = \sqrt{n/m} \, \mathbf{e}_{K_i}si​=n/m​eKi​​，缩放的单位向量，Ki∼Unif{1,…,n}K_i \sim \text{Unif}\{1, \ldots, n\}Ki​∼Unif{1,…,n}

---

## 子空间的随机嵌入：证明

**1.** 不失一般性设 ∥x∥=1\|\mathbf{x}\| = 1∥x∥=1。否则将不等式除以 ∥x∥2\|\mathbf{x}\|^2∥x∥2。需证明

P(1−K/δm≤∥Sx∥2≤1+K/δm)=P(∣∥Sx∥2−1∣≤K/δm)≥1−δ\mathbb{P}\left(1 - \sqrt{K/\delta m} \leq \|\mathbf{S}\mathbf{x}\|^2 \leq 1 + \sqrt{K/\delta m}\right) = \mathbb{P}\left(\left|\|\mathbf{S}\mathbf{x}\|^2 - 1\right| \leq \sqrt{K/\delta m}\right) \geq 1 - \deltaP(1−K/δm​≤∥Sx∥2≤1+K/δm​)=P(​∥Sx∥2−1​≤K/δm​)≥1−δ

**2.** 由于 ∥Sx∥2=∑i=1m(si⊤x)2\|\mathbf{S}\mathbf{x}\|^2 = \sum_{i=1}^m \left(\mathbf{s}_i^\top \mathbf{x}\right)^2∥Sx∥2=∑i=1m​(si⊤​x)2，有

E[∥Sx∥2]=∑i=1mE[(si⊤x)2]=mx⊤E[s1s1⊤]x=∥x∥2\mathbb{E}\left[\|\mathbf{S}\mathbf{x}\|^2\right] = \sum_{i=1}^m \mathbb{E}\left[(\mathbf{s}_i^\top \mathbf{x})^2\right] = m\mathbf{x}^\top \mathbb{E}\left[\mathbf{s}_1\mathbf{s}_1^\top\right]\mathbf{x} = \|\mathbf{x}\|^2E[∥Sx∥2]=∑i=1m​E[(si⊤​x)2]=mx⊤E[s1​s1⊤​]x=∥x∥2

**3.** 由于各项独立，还有

Var[∥Sx∥2]=∑i=1mVar[(si⊤x)2]≤mE[(s1⊤x)4]≤mE[∥s1∥4]\text{Var}\left[\|\mathbf{S}\mathbf{x}\|^2\right] = \sum_{i=1}^m \text{Var}\left[(\mathbf{s}_i^\top \mathbf{x})^2\right] \leq m\mathbb{E}\left[(\mathbf{s}_1^\top \mathbf{x})^4\right] \leq m\mathbb{E}\left[\|\mathbf{s}_1\|^4\right]Var[∥Sx∥2]=∑i=1m​Var[(si⊤​x)2]≤mE[(s1⊤​x)4]≤mE[∥s1​∥4]

因为 ∣s1⊤x∣≤∥s1∥∥x∥|\mathbf{s}_1^\top \mathbf{x}| \leq \|\mathbf{s}_1\|\|\mathbf{x}\|∣s1⊤​x∣≤∥s1​∥∥x∥（Cauchy-Schwarz 不等式）。

---

## 子空间的随机嵌入（续）

**4.** 现在由 Markov 不等式得

P(∣∥Sx∥2−E[∥Sx∥2]∣>ϵ)≤Var[∥Sx∥2]ϵ2≤mE[∥si∥4]ϵ2≤Kϵ2m\mathbb{P}\left(\left|\|\mathbf{S}\mathbf{x}\|^2 - \mathbb{E}[\|\mathbf{S}\mathbf{x}\|^2]\right| > \epsilon\right) \leq \frac{\text{Var}[\|\mathbf{S}\mathbf{x}\|^2]}{\epsilon^2} \leq \frac{m\mathbb{E}[\|\mathbf{s}_i\|^4]}{\epsilon^2} \leq \frac{K}{\epsilon^2 m}P(​∥Sx∥2−E[∥Sx∥2]​>ϵ)≤ϵ2Var[∥Sx∥2]​≤ϵ2mE[∥si​∥4]​≤ϵ2mK​

选择 ϵ=K/δm\epsilon = \sqrt{K/\delta m}ϵ=K/δm​ 即得所需结论。

---

## 应用

- 嵌入到随机子空间（_矩阵草图_）有众多应用：
    - 矩阵乘法：AB≈(AS⊤)(SB)\mathbf{A}\mathbf{B} \approx (\mathbf{A}\mathbf{S}^\top)(\mathbf{S}\mathbf{B})AB≈(AS⊤)(SB)
    - _最小二乘问题_：min⁡∥Ax−b∥2≈min⁡∥SAx−Sb∥2\min \|\mathbf{A}\mathbf{x} - \mathbf{b}\|_2 \approx \min \|\mathbf{S}\mathbf{A}\mathbf{x} - \mathbf{S}\mathbf{b}\|_2min∥Ax−b∥2​≈min∥SAx−Sb∥2​
    - ……
- 算法只有在能快速计算乘积 Sx\mathbf{S}\mathbf{x}Sx 时才真正变快
- _示例_：
    - 如果 S\mathbf{S}S 从 x\mathbf{x}x 中抽取 mmm 个随机行，只需 O(mn)O(mn)O(mn) 次操作
    - 最先进方法：_Subsampled Randomized Hadamard Transform_ 只需 O(nlog⁡m)O(n \log m)O(nlogm) 次计算

---

## 示例：使用矩阵草图进行降维

**给定：**  
n=10000n = 10000n=10000 维，δ=0.05\delta = 0.05δ=0.05，ϵ=0.1\epsilon = 0.1ϵ=0.1

**问题：** mmm 可以多小？

**由定理：** 当 si∼N(0,In/m)\mathbf{s}_i \sim \mathcal{N}(\mathbf{0}, \mathbf{I}_n/m)si​∼N(0,In​/m) 时，K=3K = 3K=3，且

ϵ=Kδm  ⟹  m=Kδϵ2=30.05⋅0.01=600\epsilon = \sqrt{\frac{K}{\delta m}} \implies m = \frac{K}{\delta\epsilon^2} = \frac{3}{0.05 \cdot 0.01} = 600ϵ=δmK​​⟹m=δϵ2K​=0.05⋅0.013​=600

**结果：**

仅用 m=600m = 600m=600 维，我们就能表示 n=10000n = 10000n=10000 维，使得所有距离以 95% 的概率最多被扭曲 10%！

**实际意义：**

- 存储：10000→60010000 \to 60010000→600   ⟹  \implies⟹ **压缩：** 约 17 倍
- 距离计算时间：O(n2)→O(m2)O(n^2) \to O(m^2)O(n2)→O(m2)   ⟹  \implies⟹ **加速** 约 280 倍
- 应用：高维空间中的快速最近邻搜索、聚类分析等

---

## 草图矩阵比较

|类型|存储|Sx\mathbf{S}\mathbf{x}Sx 时间|质量|
|---|---|---|---|
|高斯|O(mn)O(mn)O(mn)|O(mn)O(mn)O(mn)|最优|
|Rademacher (±1/m\pm 1/\sqrt{m}±1/m​)|O(mn)O(mn)O(mn)|O(mn)O(mn)O(mn)|最优|
|子采样|O(m)O(m)O(m)|O(m)O(m)O(m)|良好（对结构化 A\mathbf{A}A）|
|SRHT (Hadamard)|O(m)O(m)O(m)|O(nlog⁡m)O(n \log m)O(nlogm)|非常好|

**权衡：**

- **高斯：** 理论最优，但存储和应用代价高
- **Rademacher：** 与高斯同样好，更容易生成（只有 ±1\pm 1±1）
- **子采样：** 选择 mmm 个随机行 — 极快，但只对特定矩阵效果好
- **SRHT：** 最先进 — 结合了速度和质量
    - _Subsampled Randomized Hadamard Transform_
    - S\mathbf{S}S 不显式存储，而是通过快速变换应用

---

## 总结

**今天学习的内容**

- 特征值问题的求解方法
- _迭代方法_通过_不动点迭代_逐步逼近解
- _概率方法_用随机的、更简单的（低维）问题替代困难问题，获得近似解
- 两者都提供_运行时间_与精度之间的权衡

**下次课程**

- 张量与张量积