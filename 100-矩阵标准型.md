#### 快速记忆口诀

- **任何矩阵** 都可以通过 **高斯消元法** 化简为 行阶梯矩阵，会改变 **特征值**，用于 **解方程、求秩** $EA = R$，其中，$E$  是一系列 **初等变换矩阵** 的乘积
- **任何方阵** 都可以化简为 **Schur标准型（上三角矩阵）**，不会改变 **特征值**，用于 **特征值分析** $Q^TAQ = U, A = QUQ^T$，对角线是 **特征值**，$Q$ 是正交矩阵
- **常用符号**：
	- x，y，v - 向量
	- A，B - 一般矩阵
	- Q - 正交矩阵
	- D，$\Lambda$ - 对角矩阵
	- P - 可逆矩阵
	- U - 上三角矩阵
	- L - 下三角矩阵
	- $\Sigma$ - 奇异值对角矩阵
	- I - 单位矩阵
	- R - 行阶梯矩阵
	- E - 初等矩阵
	- J - Jordan标准型

1. **相似变换类（保持特征值）**

左右都乘，A 在中间，保持 **特征值**

| 分解      | 公式                 | 适用     | 用途    | 中间符号      | 两侧符号   |
| :------ | :----------------- | :----- | :---- | --------- | ------ |
| 特征分解    | $A = PDP^{-1}$     | 方阵可对角化 | 求幂、指数 | 对角矩阵      | 特征向量矩阵 |
| 谱分解     | $A = Q\Lambda Q^T$ | **对称** | 主成分分析 | 对角矩阵      | 正交矩阵   |
| Schur分解 | $A = QUQ^T$        | 方阵     | 特征值计算 | 上三角矩阵     | 正交矩阵   |
| 若尔当分解   | $A = PJP^{-1}$     | 方阵     | 理论分析  | Jordan标准型 |        |
2. **非相似变换类（不保持特征值）**

| 分解       | 公式                | 适用   | 用途    |
| :------- | :---------------- | :--- | :---- |
| SVD      | $A = U\Sigma V^T$ | 任意矩阵 | 降维、伪逆 |
| LU       | $A = LU$          | 方阵   | 解方程   |
| QR       | $A = QR$          | 任意矩阵 | 最小二乘  |
| Cholesky | $A = LL^T$        | 正定矩阵 | 快速求解  |
#### 1. 对角型

- **适用范围**：可对角化的方阵（特征向量够用）

- **长什么样**：

$D = \begin{pmatrix} \lambda_1 & 0 & 0 \\ 0 & \lambda_2 & 0 \\ 0 & 0 & \lambda_3 \end{pmatrix}$

- **用途**：
	- 计算最方便，$D^{100}$ 就是对角元素各自乘100次
	- 看清每个方向的伸缩倍数

- **定义**：存在可逆矩阵 $P$ 使得 $P^{-1}AP = D$，其中 $P$ 的列是特征向量，$D$ 的对角线是特征值
#### 2. Schur标准型

- **长什么样**：上三角矩阵，对角线是特征值，上三角部分可以是任意值

$D = \begin{pmatrix} \lambda_1 & * & * \\ 0 & \lambda_2 & * \\ 0 & 0 & \lambda_3 \end{pmatrix}$

- **适用范围**：**所有方阵**（一定存在）

- **构造方法**：

	- 通过 QR 迭代算法获得舒尔标准型
	- **步骤**：
		- 对 A 做 **QR分解**
		- 计算 RQ 的结果获得新的 A
		- 重复迭代上述过程，直到收敛
	- **复杂度**：通过 k 次迭代即 k 次 QR分解，还要矩阵乘法，所以复杂度 $O(kn^3)$，**很高**
	- 工业级算法先化成 **Hessenberg** 形式，把每次迭代从 $O(n^3)$ 降到 $O(n^2)$

- **用途**：
	- **数值计算更稳定**
	- 求特征值的实际算法（QR迭代）都是先化成这个

- **定义**：存在 **正交矩阵** $Q$ 使得 $Q^TAQ = U$，比若尔当标准形好算，因为 **正交矩阵数值稳定**


- **代码练习**：

```r
A <- matrix(c(4, 2, 1,
              1, 3, 1,
              0, 1, 2), nrow = 3, byrow = TRUE)

A_iter <- A
Q_total <- diag(3)  # 从单位矩阵开始
tol <- 1e-10
max_iter <- 100

for (i in 1:max_iter) {
  qr_result <- qr(A_iter)
  Q <- qr.Q(qr_result)
  R <- qr.R(qr_result)
  
  A_iter <- R %*% Q
  Q_total <- Q_total %*% Q  # 累积每次的 Q
  
  if (sum(abs(A_iter[lower.tri(A_iter)])) < tol) {
    cat("第", i, "次收敛\n")
    break
  }
}

cat("\n舒尔标准形 U:\n")
print(round(A_iter, 6))

cat("\n正交矩阵 Q:\n")
print(round(Q_total, 6))

cat("\n验证 Q'AQ = U:\n")
print(round(t(Q_total) %*% A %*% Q_total, 6))

cat("\n验证 Q 是正交矩阵 (Q'Q = I):\n")
print(round(t(Q_total) %*% Q_total, 6))
```
#### 3. 行阶梯型

- **长什么样**：阶梯状，每行第一个非零元（主元）右边的行主元更靠右，主元下方全是 0

$R = \begin{pmatrix} 1 & * & * & * \\ 0 & 1 & * & * \\ 0 & 0 & 0 & 1 \\ 0 & 0 & 0 & 0 \end{pmatrix}$

- **适用范围**：**任意矩阵**（不限方阵）

- **构造方法**：

	- **高斯消元法** 的本质
	- 变换后的行阶梯矩阵在 **方阵 + 满秩** 场景下获得 **单位矩阵 I**，非满秩情况下，有零行
	- **复杂度**：
		- $O(mn⋅min(m,n))$
		- 方阵 - $O(n^3)$   瘦高($m > n$) - $O(mn^2)$  矮宽($m < n$) - $O(m^2n)$ 

- **用途**：
	- 解线性方程组
	- 求矩阵的秩
	- 判断解的存在性和唯一性

- **定义**：通过 **初等行变换** 得到，不是相似变换，会改变特征值

- **代码练习**：

```r
A <- matrix(c(1, 2, 3,
              4, 5, 6,
              7, 8, 9), nrow = 3, byrow = TRUE)

# 化成行阶梯形（简化行阶梯形）
library(pracma)
rref(A)
```
#### 4. Jordan标准型

若尔当形是 **理论工具**（证明定理、分析矩阵结构），数值计算用**Schur分解**代替

- **长什么样**：对角线是特征值，上方可能有 1（**若尔当块**），其他全 0

$D = \begin{pmatrix} \lambda_1 & 1 & 0 \\ 0 & \lambda_2 & 1 \\ 0 & 0 & \lambda_3 \end{pmatrix}$

- **适用范围**：**所有方阵**（对角化失败时的退路）

- **构造方法**：

	1. 求 **特征值** 和 **代数重数**
	2. 求 **广义特征空间的结构**
	3. 确定若尔当块的个数和大小
	4. 求广义特征向量，构造变换矩阵 PPP

- **用途**：
	- 分析不可对角化矩阵的结构
	- 研究矩阵的幂次行为

- **定义**：存在可逆矩阵 $P$ 使得 $P^{-1}AP = J$，其中 $P$ 由 **特征向量** 和 **广义特征向量** 构成

- **代码练习**：

```r
A <- matrix(c(2.0, 1, 0.0, -0.5, 3, 0.5, 0.0, 1, 2.0), nrow = 3, byrow = TRUE)
cat("矩阵 A:\n")
print(A)
# 第一步：特征分解，直接拿特征值和特征向量
eig <- eigen(A)
cat("\n特征值:\n")
print(round(eig$values, 4))
cat("\n特征向量:\n")
print(round(eig$vectors, 4))
# λ=3 的特征向量直接取第一列
v1 <- eig$vectors[,1]
# λ=2 的特征向量取第二列（第三列是重复的）
v2_raw <- eig$vectors[,2]
cat("\nv1 (λ=3):", round(v1, 4), "\n")
cat("v2 (λ=2):", round(v2_raw, 4), "\n")
# 第二步：λ=2 特征向量不够，需要广义特征向量
lambda2 <- 2
B2 <- A - lambda2 * diag(3)
B2_sq <- B2 %*% B2
# 从 ker((A-2I)^2) 找广义特征向量
null2_sq <- MASS::Null(t(B2_sq))
v3 <- null2_sq[,1]
# 如果 v3 在 ker(A-2I) 里，换一个
if (sum(abs(B2 %*% v3)) < 1e-10) {
  v3 <- null2_sq[,2]
}
# v2 = (A-2I)v3
v2 <- as.vector(B2 %*% v3)
cat("\n广义特征向量 v3:", round(v3, 4), "\n")
cat("调整后 v2 = (A-2I)v3:", round(v2, 4), "\n")
# 第三步：组成 P，算若尔当形
P <- cbind(v2, v3, v1)
J <- solve(P) %*% A %*% P
J[abs(J) < 1e-10] <- 0
cat("\n若尔当形 J:\n")
print(round(J, 4))
```
#### 5. 它们的关系

任意矩阵
    │
    ├── 相似变换（方阵）──→ 舒尔标准形（上三角，一定存在，数值稳定）
    │                           │
    │                           ↓ 特征向量够用？
    │                      是 → 对角形（最简）
    │                      否 → 若尔当标准形（次简）
    │
    ├── 行变换 ────────────→ 行阶梯形（解方程用）


