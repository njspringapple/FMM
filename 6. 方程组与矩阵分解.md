矩阵分解：LU与Cholesky的奥秘 https://www.coze.cn/s/XzvL9k3Afj0/
## 🎯 预备知识

> [!info] 算法基础
> - 复杂度分析，$O()$ 记号
> - 算法类型（直接方法）
> - 误差分析：稳定性，问题的条件数

> [!info] 线性代数
> - 线性方程组的求解（基本方法）
> - 三角矩阵，对称矩阵，正定矩阵
> - 矩阵范数，正交矩阵

---

## 📑 目录

```

1. 矩阵运算
2. 线性方程组
3. LU 分解
4. Cholesky 分解
5. 总结与比较

```

---

# 一、矩阵运算

## 1.1 基本运算复杂度

> [!important] 矩阵加法
> 
> 对于 $\mathbf{A}, \mathbf{B} \in \mathbb{R}^{n \times m}$：
> $$
> \mathbf{A} + \mathbf{B} = \begin{pmatrix} a_{11} + b_{11} & \cdots & a_{1m} + b_{1m} \\ \vdots & & \vdots \\ a_{n1} + b_{n1} & \cdots & a_{nm} + b_{nm} \end{pmatrix}
> $$
> 
> **复杂度**：$O(nm)$

> [!important] 矩阵乘法
> 
> 对于 $\mathbf{A} \in \mathbb{R}^{n \times m}$，$\mathbf{B} \in \mathbb{R}^{m \times k}$：
> $$
> (\mathbf{AB})_{ij} = \sum_{l=1}^{m} a_{il} b_{lj}
> $$
> 
> **复杂度**：$O(nmk)$

---

## 1.2 更快的矩阵乘法

> [!note] 渐近更优的算法
> 
> 对于两个 $(n \times n)$ 矩阵的乘法：
> 
> | 算法 | 复杂度 |
> |:---|:---|
> | 标准算法 | $O(n^3)$ |
> | Strassen (1969) | $\approx O(n^{2.8})$ |
> | Alman & Williams (2020) | $\approx O(n^{2.37})$（目前最优） |
> 
> 但是，$O(\cdot)$ 中隐藏的常数通常非常大，以至于这些算法很少被使用。

---

# 二、线性方程组

## 2.1 问题设定

> [!important] 线性方程组
> 
> 求解：
> $$
> \mathbf{x} : \mathbf{Ax} = \mathbf{b}
> $$
> 
> 假设 $\mathbf{A} \in \mathbb{R}^{n \times n}$ 可逆，则存在唯一解。

---

## 2.2 为什么不求逆？

> [!danger] 核心原则
> # 永远不要求逆矩阵！

> [!warning] 原因
> 
> 解可以写成 $\mathbf{x} = \mathbf{A}^{-1}\mathbf{b}$，但数值求逆矩阵：
> - **相对不稳定**
> - **通常比直接求解 LGS 更慢**

---

## 2.3 高斯消元法回顾

> [!abstract] 高斯消元法步骤
> 
> 1. 将 $(\mathbf{A} | \mathbf{b})$ 化为行阶梯形 $(\mathbf{U} | \mathbf{c})$，其中 $\mathbf{U}$ 为上三角矩阵
> 2. 通过**回代**求解系统：
> $$
> x_n = c_n / u_{nn}, \quad x_i = \left( c_i - \sum_{j=i+1}^{n} u_{ij} x_j \right) / u_{ii} \quad (i \leq n-1)
> $$

> [!tip] 回代复杂度
> **$O(n^2)$**（三角矩阵求解是 $O(n^2)$ 而非 $O(n^3)$）

---

# 三、LU 分解

## 3.1 矩阵分解的动机

> [!important] 核心思想：分而治之
> 
> **类比：数的质因数分解**
> - $84 = 2^2 \cdot 3 \cdot 7$
> - 简化计算：$84 \cdot 126 = (2^2 \cdot 3 \cdot 7) \cdot (2 \cdot 3^2 \cdot 7) = 2^3 \cdot 3^3 \cdot 7^2$
> 
> **对于矩阵**：$\mathbf{A} = \mathbf{BC}$，其中 $\mathbf{B}$、$\mathbf{C}$ 是"简单"矩阵

> [!success] 简单矩阵的优势
> 
> | 矩阵类型 | 特点 |
> |:---|:---|
> | **三角矩阵** | 求解复杂度为 $O(n^2)$ 而非 $O(n^3)$ |
> | **正交矩阵** | 数值稳定，易于求逆 |
> | **对角矩阵** | 运算平凡 |

---

## 3.2 分解的优点

> [!tip] LU 分解的优势
> 
> 1. **高效求解多个方程组**：具有相同系数矩阵的线性方程组
> 2. **改善数值稳定性**
> 3. **揭示矩阵结构**：矩阵 $\mathbf{A}$ 的性质
> 4. **为其他算法奠定基础**：谱分解、SVD 等

---

## 3.3 LU 分解的直觉

> [!important] 基本思想
> 
> 高斯消元法将 $\mathbf{A}$ 分解为：
> - $\mathbf{L}$（Lower，下三角）：存储所有消元步骤
> - $\mathbf{U}$（Upper，上三角）：得到的上三角矩阵

> [!success] 求解过程
> 
> 一旦计算出 $\mathbf{A} = \mathbf{LU}$，我们可以高效地对*任意* $\mathbf{b}_i$ 求解 $\mathbf{Ax} = \mathbf{b}_i$：
> $$
> \mathbf{Ax} = \mathbf{b} \implies \mathbf{LUx} = \mathbf{b} \implies \begin{cases} 1. \text{ 求解 } \mathbf{Ly} = \mathbf{b} \\ 2. \text{ 求解 } \mathbf{Ux} = \mathbf{y} \end{cases}
> $$

---

## 3.4 LU 分解的构造

> [!abstract] 消元过程
> 
> 在消元过程的第 $k$ 步，我们左乘矩阵 $\mathbf{L}_k$：
> $$
> \mathbf{L}_k = \begin{pmatrix} 1 & & & & \\ & \ddots & & & \\ & & 1 & & \\ & & -l_{(k+1)k} & 1 & \\ & & \vdots & & \ddots \\ & & -l_{nk} & & & 1 \end{pmatrix}
> $$
> 
> 其中 $l_{ik} = m_{ik} / m_{kk}$

> [!note] 结果
> - $\mathbf{U} = \mathbf{L}_n \cdots \mathbf{L}_1 \mathbf{A}$
> - $\mathbf{L} = \mathbf{L}_1^{-1} \cdots \mathbf{L}_n^{-1}$

---

## 3.5 $\mathbf{L}$ 的结构

> [!important] $\mathbf{L}$ 的显式形式
> 
> $$
> \mathbf{L} = \begin{pmatrix} 1 & & & \\ l_{21} & \ddots & & \\ l_{31} & l_{32} & 1 & \\ \vdots & & \ddots & \ddots \\ l_{n1} & \cdots & \cdots & l_{n(n-1)} & 1 \end{pmatrix}
> $$
> 
> - 每个 $\mathbf{L}_k^{-1}$ 都是下三角矩阵
> - 因此 $\mathbf{L} = \mathbf{L}_1^{-1} \cdots \mathbf{L}_n^{-1}$ 也是下三角矩阵
> - 对角线元素全为 1

---

## 3.6 示例：$3 \times 3$ 矩阵的 LU 分解

> [!example] 计算示例
> 
> 设 $\mathbf{A} = \begin{pmatrix} 2 & 1 & -1 \\ 4 & -6 & 0 \\ -2 & 7 & 2 \end{pmatrix}$
> 
> **步骤 1**：消去第 1 列
> - $l_{21} = 4/2 = 2$，$l_{31} = -2/2 = -1$
> $$
> \mathbf{L}_1 = \begin{pmatrix} 1 & 0 & 0 \\ -2 & 1 & 0 \\ 1 & 0 & 1 \end{pmatrix}, \quad \mathbf{L}_1 \mathbf{A} = \begin{pmatrix} 2 & 1 & -1 \\ 0 & -8 & 2 \\ 0 & 8 & 1 \end{pmatrix}
> $$
> 
> **步骤 2**：消去第 2 列
> - $l_{32} = 8/(-8) = -1$
> $$
> \mathbf{L}_2 = \begin{pmatrix} 1 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 1 & 1 \end{pmatrix}, \quad \mathbf{U} = \mathbf{L}_2 \mathbf{L}_1 \mathbf{A} = \begin{pmatrix} 2 & 1 & -1 \\ 0 & -8 & 2 \\ 0 & 0 & 3 \end{pmatrix}
> $$
> 
> **结果**：
> $$
> \mathbf{L} = \begin{pmatrix} 1 & 0 & 0 \\ 2 & 1 & 0 \\ -1 & -1 & 1 \end{pmatrix}, \quad \mathbf{U} = \begin{pmatrix} 2 & 1 & -1 \\ 0 & -8 & 2 \\ 0 & 0 & 3 \end{pmatrix}
> $$

---

## 3.7 用 LU 分解求解方程组

> [!important] 求解步骤
> 
> 假设已有分解 $\mathbf{A} = \mathbf{LU}$，求解 $\mathbf{Ax} = \mathbf{b}$：
> 
> 1. 通过**前向替换**求解 $\mathbf{Ly} = \mathbf{b}$
> 2. 通过**回代**求解 $\mathbf{Ux} = \mathbf{y}$

> [!tip] 关键优势
> 分解 $\mathbf{A} = \mathbf{LU}$ 对所有 $\mathbf{b}$ 都相同，因此**只需计算一次**！

---

## 3.8 求解示例

> [!example] 用 LU 分解求解
> 
> 使用之前的 $\mathbf{L}$、$\mathbf{U}$，设 $\mathbf{b} = \begin{pmatrix} 5 \\ -2 \\ 9 \end{pmatrix}$
> 
> **步骤 1：前向替换** 求解 $\mathbf{Ly} = \mathbf{b}$：
> $$
> \begin{cases} y_1 = 5 \\ y_2 = -2 - 2 \cdot 5 = -12 \\ y_3 = 9 - (-1) \cdot 5 - (-1) \cdot (-12) = 2 \end{cases}
> $$
> 
> **步骤 2：回代** 求解 $\mathbf{Ux} = \mathbf{y}$：
> $$
> \begin{cases} x_3 = 2/3 \\ x_2 = (-12 - 2 \cdot (2/3))/(-8) = 5/3 \\ x_1 = (5 - 1 \cdot (5/3) - (-1) \cdot (2/3))/2 = 2 \end{cases}
> $$

---

## 3.9 选主元

> [!warning] 问题
> 当第 $k$ 步中 $m_{kk} = 0$ 时，方法会失败

> [!success] 解决方案：选主元
> - 通过交换 $\mathbf{A}$ 的行来避免这一问题
> - 求解 $\mathbf{PA} = \mathbf{LU}$，其中 $\mathbf{P}$ 是置换矩阵
> - *选主元*即使对于 $m_{kk} \approx 0$ 的情况也能提高稳定性

---

## 3.10 复杂度分析

> [!important] LU 分解复杂度
> 
> $$
> \sum_{k=1}^{n} O((n-k)^2) = O(n^3)
> $$

> [!success] 总运行时间
> 
> | 步骤 | 复杂度 |
> |:---|:---|
> | LU 分解（一次） | $O(n^3)$ |
> | 前向替换（每次） | $O(n^2)$ |
> | 回代（每次） | $O(n^2)$ |
> | **求解 $J$ 个方程组** | $O(n^3 + Jn^2)$ |

---

# 四、Cholesky 分解

## 4.1 SPD 矩阵

> [!important] 定义：SPD 矩阵
> 
> 矩阵 $\mathbf{A} \in \mathbb{R}^{n \times n}$ 称为 **SPD**（对称正定），如果
> 
> - $\mathbf{A} = \mathbf{A}^\top$（**s**ymmetric，对称）
> - $\mathbf{x}^\top \mathbf{Ax} > 0 \; \forall \, \mathbf{x} \in \mathbb{R}^n \setminus \{\mathbf{0}\}$（**p**ositive **d**efinite，正定）

> [!note] 正半定
> $\mathbf{A}$ 正*半*定 $\iff \mathbf{x}^\top \mathbf{Ax} \geq 0 \; \forall \, \mathbf{x} \in \mathbb{R}^n \setminus \{\mathbf{0}\}$

---

## 4.2 统计学和机器学习中的 SPD 矩阵

> [!success] 常见应用
> 
> **1. 协方差矩阵**
> $$
> \Sigma = \mathbb{E}[(\mathbf{x} - \mu)(\mathbf{x} - \mu)^\top]
> $$
> - 多元正态分布 $\sim \mathcal{N}_p(\mu, \Sigma)$
> - 主成分分析（PCA）
> 
> **2. Gram 矩阵**
> $$
> \mathbf{K} = \mathbf{X}^\top \mathbf{X}
> $$
> - 最小二乘问题的正规方程 $\mathbf{X}^\top \mathbf{X}\beta = \mathbf{X}^\top \mathbf{y}$
> - 核矩阵：支持向量机、高斯过程等
> 
> **3. Hesse 矩阵**
> $$
> \mathbf{H} = \left[\frac{\partial^2}{\partial x_i \partial x_j} f(\mathbf{x})\right]_{i,j}
> $$
> - Fisher 信息
> - 优化中的凸性
> - 牛顿法

---

## 4.3 SPD 矩阵的重要性

> [!tip] 为什么 SPD 矩阵重要？
> 
> SPD 矩阵保证：
> - Cholesky 分解的**存在性和唯一性**
> - **数值稳定性**
> - 所有特征值为正 $\implies$ **满秩**

---

## 4.4 Cholesky 分解定理

> [!important] 定理：Cholesky 分解
> 
> 每个 SPD 矩阵 $\mathbf{A}$ 都可以分解为
> $$
> \mathbf{A} = \mathbf{LL}^\top
> $$
> 其中 $\mathbf{L}$ 是下三角矩阵。

> [!success] 优势
> 计算和存储开销约为完整 LU 分解的**一半**

---

## 4.5 Cholesky 分解的证明（归纳法）

> [!note] 证明概要
> 
> **基础情况** $n = 1$：
> - 因为 $A \in \mathbb{R}$ 是 SPD，所以 $A > 0$
> - 于是 $A = LL^\top$，其中 $L = \sqrt{A}$
> 
> **归纳步骤** $n > 1$：
> - 设 $\mathbf{A} = \begin{pmatrix} a & \mathbf{c}^\top \\ \mathbf{c} & \mathbf{B} \end{pmatrix}$
> - 令 $\mathbf{l} = \frac{\mathbf{c}}{\sqrt{a}}$（$a > 0$ 因为 $a = \mathbf{e}_1^\top \mathbf{A} \mathbf{e}_1$）
> - 可证 $(\mathbf{B} - \mathbf{ll}^\top)$ 是 SPD
> - 由归纳假设 $(\mathbf{B} - \mathbf{ll}^\top) = \tilde{\mathbf{L}}\tilde{\mathbf{L}}^\top$
> - 选择 $\mathbf{L} = \begin{pmatrix} \sqrt{a} & 0 \\ \mathbf{l} & \tilde{\mathbf{L}} \end{pmatrix}$

---

## 4.6 Cholesky 分解示例

> [!example] 计算示例
> 
> 计算 $\mathbf{A} = \begin{pmatrix} 4 & 2 \\ 2 & 3 \end{pmatrix}$ 的 Cholesky 分解
> 
> **假设**：$\mathbf{A} = \mathbf{LL}^\top$，其中 $\mathbf{L} = \begin{pmatrix} l_{11} & 0 \\ l_{21} & l_{22} \end{pmatrix}$
> 
> **展开乘法**：
> $$
> \mathbf{LL}^\top = \begin{pmatrix} l_{11}^2 & l_{11}l_{21} \\ l_{11}l_{21} & l_{21}^2 + l_{22}^2 \end{pmatrix} = \begin{pmatrix} 4 & 2 \\ 2 & 3 \end{pmatrix}
> $$
> 
> **比较系数**：
> - $l_{11}^2 = 4 \implies l_{11} = 2$
> - $l_{11}l_{21} = 2 \implies l_{21} = 1$
> - $l_{21}^2 + l_{22}^2 = 3 \implies l_{22} = \sqrt{2}$
> 
> **结果**：$\mathbf{L} = \begin{pmatrix} 2 & 0 \\ 1 & \sqrt{2} \end{pmatrix}$

---

## 4.7 Cholesky 分解的应用

> [!example] 从多元正态分布采样
> 
> - 设 $\mathbf{x} \in \mathbb{R}^d$ 满足 $\mathbb{E}[\mathbf{x}] = \mathbf{0}$ 且 $\mathbb{V}\text{ar}(\mathbf{x}) = \mathbf{I}_d$
> - 设 $\Sigma$ 是 SPD 矩阵，且 $\Sigma = \mathbf{LL}^\top$
> - 则 $\mathbf{y} = \mathbf{Lx}$ 的协方差为：
> $$
> \mathbb{V}\text{ar}(\mathbf{y}) = \mathbb{E}[\mathbf{yy}^\top] = \mathbf{L}\mathbb{E}[\mathbf{xx}^\top]\mathbf{L}^\top = \mathbf{LL}^\top = \Sigma
> $$
> 
> **应用**：从 $\mathcal{N}(\mu, \Sigma)$ 进行采样

---

## 4.8 带选主元的 Cholesky 分解

> [!tip] 何时需要选主元？
> 
> 当 $\mathbf{A}$ 奇异或数值病态时：
> - 对于正*半*定矩阵（$\mathbf{x}^\top \mathbf{Ax} \geq 0$ 而非 $> 0$）

> [!success] 完全选主元
> $$
> \mathbf{P}^\top \mathbf{AP} = \mathbf{LL}^\top
> $$
> 
> - 置换矩阵 $\mathbf{P}$（行列同时交换）
> - **秩揭示**：$\mathbf{L}$ 中的零列显示 $\mathbf{A}$ 的秩亏损
> - **稳定性**：即使对于半定矩阵也稳定

> [!note] ML/统计中的应用
> - 核矩阵（高斯过程，SVM 等）
> - 低秩近似矩阵分解：$O(k^2n)$ 而非 $O(n^3)$
> - 当 $p > n$ 时的 Gram 矩阵 $\mathbf{X}^\top \mathbf{X}$

---

# 五、总结与比较

## 📌 LU 与 Cholesky 比较

> [!tip] 方法比较
> 
> | 方面 | LU 分解 | Cholesky 分解 |
> |:---|:---|:---|
> | **适用矩阵** | 一般方阵 | SPD 矩阵 |
> | **稳定性** | 不带选主元时不稳定 | 天然稳定，无需选主元 |
> | **效率** | $O(n^3)$ | 约为 LU 的 **2 倍快** |
> | **数值精度** | 误差 $\approx \kappa(\mathbf{A}) \cdot \epsilon_{\text{机器}}$ | 更优 |

> [!success] 结论
> - **LU**：通用，需要选主元保证稳定性
> - **Cholesky**：对于 SPD 矩阵**最优**（统计学！），数值上更优越

---

## 📝 核心要点

> [!summary] 关键总结
> 
> **永远不要直接求逆矩阵！**
> 
> **LU 分解**
> $$
> \mathbf{A} = \mathbf{LU}
> $$
> - $\mathbf{L}$：下三角矩阵（消元步骤）
> - $\mathbf{U}$：上三角矩阵（行阶梯形）
> 
> **Cholesky 分解**（SPD 矩阵）
> $$
> \mathbf{A} = \mathbf{LL}^\top
> $$
> - 计算量约为 LU 的一半
> - 数值稳定

---

## ✅ 自测清单

- [ ] 能解释为什么不应该直接求逆矩阵
- [ ] 理解矩阵分解的优势（分而治之）
- [ ] 能写出 LU 分解的基本形式
- [ ] 能用 LU 分解求解线性方程组
- [ ] 理解选主元的必要性
- [ ] 能定义 SPD 矩阵
- [ ] 能写出 Cholesky 分解的形式
- [ ] 理解 LU 和 Cholesky 分解的复杂度
- [ ] 能比较 LU 和 Cholesky 分解的优缺点
