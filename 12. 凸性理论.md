# 凸性理论：从几何直观到跨领域应用

## 🎯 预备知识

### 从高中数学说起

在高中，你可能已经遇到过"凸"这个概念，但可能没有意识到。

> [!example] 你熟悉的凸的例子
> - **二次函数** $f(x) = x^2$ 的图像是"开口向上"的抛物线
> - **三角形内部**：任意两点连线都在三角形内部
> - **圆盘**：任意两点连线都在圆盘内部

这些都是"凸"的例子！本节课会把这个直观概念精确化，并展示它在优化、统计、机器学习中的强大威力。

---

### 预备知识回顾

> [!info] 线性代数
> - **向量空间**：你可以把它理解为"箭头"的集合，可以做加法和数乘
> - **线性组合**：$\sum w_i \mathbf{v}_i$，就是把几个向量"按权重加起来"
> - **正定矩阵**：$\mathbf{x}^\top \mathbf{A} \mathbf{x} > 0$ 对所有 $\mathbf{x} \neq \mathbf{0}$
>   
>   直观理解：正定矩阵对应的二次型 $\mathbf{x}^\top \mathbf{A} \mathbf{x}$ 是一个"碗状"的曲面

> [!info] 分析学
> - **极限**：描述"无限接近"的概念
> - **闭集**：包含所有边界点的集合（比如 $[0,1]$ 是闭的，$(0,1)$ 不是）
> - **梯度** $\nabla f(\mathbf{x})$：函数增长最快的方向

> [!info] 概率论
> - **期望值** $\mathbb{E}[X]$：随机变量的"加权平均"
> - **Jensen 不等式**：这是本节课的重点之一！

---

## 📑 目录

1. 引言
2. 凸组合与凸包
3. 凸集
4. 凸函数
5. 凸函数在优化中的应用
6. 总结

---

# 一、引言

## 1.1 什么是凸性

> [!abstract] 核心概念
> 凸性是数学中的重要概念，涉及两个相关概念：
> - **凸集**：没有"凹陷"或"孔洞"的集合
> - **凸函数**：图像"向上开口"的函数
> 
> 两者都是**线性性概念的推广**

> [!example] 直观感受
> 想象你在一个房间里（代表一个集合）：
> - **凸的房间**：你站在房间里任意两点之间，中间不会撞墙
> - **非凸的房间**：比如 L 形走廊，两个端点之间的直线会穿过墙壁

---

## 1.2 为什么凸性重要

> [!success] 在优化/机器学习/统计学中
> 
> | 优势 | 说明 |
> |:---|:---|
> | **唯一全局最小值** | 凸优化问题不存在局部最小值陷阱 |
> | **高效算法** | 梯度下降等方法保证收敛到最优解 |

**通俗地说**：如果你知道一个问题是凸的，那你就可以放心地用梯度下降，不用担心陷入局部最优！

> [!tip] 典型应用
> - **最大似然估计**：多数对数似然是凸的（GLM 等）
> - **支持向量机**（SVM）
> - **岭回归**（Ridge Regression）
> - **LASSO**

> [!success] 在概率论中
> - **Jensen 不等式**：处理期望值的核心工具
> - **KL 散度是凸的**：变分推断的基础
> - **方差非负性**：$\mathbb{E}[X^2] \geq (\mathbb{E}[X])^2$（这个用 Jensen 不等式可以秒证！）

---

# 二、凸组合与凸包

## 2.1 凸组合的定义

### 从加权平均说起

你熟悉的加权平均：期末成绩 = 0.3 × 平时成绩 + 0.7 × 考试成绩

这里 0.3 和 0.7 都是非负的，而且加起来等于 1。这就是**凸组合**的核心特征！

> [!important] 定义：凸组合
> 设 $V$ 是向量空间，$\mathcal{X} = \{\mathbf{x}_1, \ldots, \mathbf{x}_k\} \subset V$。若存在系数 $w_1, \ldots, w_k \geq 0$ 使得
> 
> $$
> \mathbf{x} = \sum_{i=1}^{k} w_i \mathbf{x}_i \quad \text{且} \quad \sum_{i=1}^{k} w_i = 1
> $$
> 
> 则称 $\mathbf{x}$ 为 $\mathcal{X}$ 中向量的**凸组合**。

> [!example] 两点的凸组合
> 设 $\mathbf{x} = (0, 0)$，$\mathbf{y} = (2, 2)$。它们的凸组合是：
> $$
> \lambda \mathbf{x} + (1-\lambda)\mathbf{y} = (1-\lambda)(2, 2) = (2-2\lambda, 2-2\lambda), \quad \lambda \in [0, 1]
> $$
> 
> 当 $\lambda$ 从 0 变到 1，这个点从 $\mathbf{y}$ 移动到 $\mathbf{x}$——这正是连接两点的**线段**！

> [!note] 关键性质
> - 凸组合是**特殊的线性组合**（系数非负且和为1）
> - 可以解释为**加权平均**
> - 两个向量的所有凸组合构成连接它们的**线段**

---

## 2.2 凸组合与期望值

> [!example] 期望值是凸组合
> 离散随机变量 $X$ 的期望值是支撑集的凸组合：
> 
> $$
> \mathbb{E}(X) = \sum_{x_j \in T_X} \mathbb{P}(X = x_j) \cdot x_j
> $$
> 
> 其中概率 $\mathbb{P}(X = x_j) \geq 0$ 且 $\sum_j \mathbb{P}(X = x_j) = 1$

**这个观察非常重要！** 它告诉我们：期望值永远落在随机变量可能取值的"凸包"内。

> [!example] 具体例子
> 掷一个骰子，$X \in \{1, 2, 3, 4, 5, 6\}$，每个概率都是 $1/6$。
> 
> $\mathbb{E}[X] = \frac{1}{6}(1 + 2 + 3 + 4 + 5 + 6) = 3.5$
> 
> 注意 3.5 在 1 到 6 之间——它是这些值的凸组合！

---

## 2.3 凸包的定义

> [!important] 定义：凸包
> 设 $\mathcal{X} \subset V$ 是任意一组向量。**凸包**定义为：
> 
> $$
> \text{conv}(\mathcal{X}) = \left\{\mathbf{x} = \sum_{i=1}^{N} w_i \mathbf{x}_i : N \in \mathbb{N}, \mathbf{x}_i \in \mathcal{X}, w_i \geq 0, \sum_{i=1}^{N} w_i = 1\right\}
> $$

**直观理解**：想象你在这些点上套一根橡皮筋，橡皮筋围成的区域就是凸包。

> [!note] 关键性质
> - 凸包是 $\mathcal{X}$ 中有限多个向量的**所有凸组合的集合**
> - **极值点**：只能通过某个 $w_i = 1$（其他权重都是 0）到达的点
> - 凸包是**包含 $\mathcal{X}$ 的最小凸集**

---

## 2.4 凸包示例

> [!example] $\mathbb{R}^2$ 中的三角形
> 
> 设 $\mathcal{X} = \{(0,0), (2,0), (1,2)\}$
> 
> **极值点**：三个顶点都是极值点（只能通过把全部权重放在一个点上到达）(**凸包里不能被其他点凸组合表示的点**)
> 
> **凸包**：所有形如以下的点
> $$
> \mathbf{x} = w_1 \binom{0}{0} + w_2 \binom{2}{0} + w_3 \binom{1}{2}, \quad w_1 + w_2 + w_3 = 1, \quad w_i \geq 0
> $$
> 
> **具体计算**：取 $w_1 = w_2 = w_3 = \frac{1}{3}$（等权重）：
> $$
> \mathbf{x} = \frac{1}{3}\binom{0}{0} + \frac{1}{3}\binom{2}{0} + \frac{1}{3}\binom{1}{2} = \binom{1}{\frac{2}{3}}
> $$
> 这是三角形的**重心**！

---

# 三、凸集

## 3.1 凸集的定义

> [!important] 定义：凸集
> 设 $\mathcal{X} \subset V$ 是向量空间 $V$ 中的集合。若对所有 $\mathbf{x}, \mathbf{y} \in \mathcal{X}$ 及所有 $\lambda \in [0,1]$ 均有
> 
> $$
> \lambda \mathbf{x} + (1-\lambda)\mathbf{y} \in \mathcal{X}
> $$
> 
> 则称 $\mathcal{X}$ 为**凸集**。

> [!abstract] 几何直观
> 凸集对任意两点都**包含连接这两点的线段**
> 
> → 凸集没有"凹陷"或"孔洞"

> [!example] 判断凸性的技巧
> 想象你在集合内部拉一根绳子连接任意两点。如果绳子始终在集合内部，则是凸集；如果绳子可能"穿出去"，则不是凸集。

---

## 3.2 判断凸性

> [!question] 小测验
> 以下哪些集合是凸的？
> 
> 1. 空集 $\emptyset$
> 2. 向量空间 $V$
> 3. 单位球面 $\{\mathbf{x} \in V : \|\mathbf{x}\| = 1\}$
> 4. 闭球 $\{\mathbf{x} \in V : \|\mathbf{x}\| \leq r\}$
> 5. 集合 $\{(x,y) \in \mathbb{R}^2 : y \leq x^2\}$

> [!success] 答案
> 
> | 集合 | 凸性 | 原因 |
> |:---|:---:|:---|
> | 空集 $\emptyset$ | ✓ | 没有两点可选，条件空真 |
> | 向量空间 $V$ | ✓ | 线性组合封闭，凸组合当然也封闭 |
> | 单位球面 | ✗ | 比如 $(1,0)$ 和 $(-1,0)$ 的中点是 $(0,0)$，不在球面上 |
> | 闭球 | ✓ | 任意两点连线在球内 |
> | $y \leq x^2$ 区域 | ✗ |  |

> [!note] 更正：$y \leq x^2$ 的判断
> 
> 设 $(x_1, y_1)$ 和 $(x_2, y_2)$ 满足 $y_1 \leq x_1^2$，$y_2 \leq x_2^2$。
> 
> 它们的凸组合是 $(\lambda x_1 + (1-\lambda)x_2, \lambda y_1 + (1-\lambda)y_2)$。
> 
> 需要验证：$\lambda y_1 + (1-\lambda)y_2 \leq (\lambda x_1 + (1-\lambda)x_2)^2$？
> 
> 这不一定成立！比如取 $(x_1, y_1) = (-1, 1)$，$(x_2, y_2) = (1, 1)$，$\lambda = 1/2$：
> - 凸组合：$(0, 1)$
> - 需要验证：$1 \leq 0^2 = 0$？显然不成立！
> 
> 所以 $y \leq x^2$ **不是**凸集（它是抛物线下方，有"凹陷"）。

---

## 3.3 凸集的重要例子

> [!tip] 常见凸集
> 
> **几何对象**
> - 开球 $\{\mathbf{x} \in V : \|\mathbf{x}\| < r\}$
> - 闭球 $\{\mathbf{x} \in V : \|\mathbf{x}\| \leq r\}$
> - 半空间 $\{\mathbf{x} : \mathbf{a}^\top \mathbf{x} \leq b\}$（线性不等式定义的区域）
> 
> **代数对象**
> - 单纯形 $\Delta^N = \{\mathbf{x} \in \mathbb{R}^N : x_i \geq 0, \sum_{i=1}^{N} x_i = 1\}$
> - $\mathbb{R}^{n \times n}$ 中所有**半正定矩阵**的集合

---

## 3.4 单纯形

### 什么是单纯形？

单纯形就是"概率分布"所在的空间！

> [!important] 单纯形 $\Delta^N$
> $$
> \Delta^N = \left\{\mathbf{x} \in \mathbb{R}^N : x_i \geq 0, \sum_{i=1}^{N} x_i = 1\right\}
> $$

> [!example] 各维度的单纯形
> 
> **$\Delta^2$（二维）**：满足 $x_1 + x_2 = 1$，$x_1, x_2 \geq 0$ 的所有点。
> 这是一条从 $(1, 0)$ 到 $(0, 1)$ 的线段。
> 
> **$\Delta^3$（三维）**：满足 $x_1 + x_2 + x_3 = 1$，$x_i \geq 0$ 的所有点。
> 这是一个以 $(1,0,0)$、$(0,1,0)$、$(0,0,1)$ 为顶点的三角形。

> [!success] 统计学应用
> $\Delta^N$ 定义了 $N$ 个事件上的**概率分布**
> 
> 例如：$\mathbf{p} = (0.2, 0.5, 0.3)$ 描述三类别分类分布
> 
> 应用于：Softmax 回归、多项式模型、Dirichlet 分布等

---

## 3.5 半正定矩阵集合是凸集

这个例子在统计学中非常重要，因为协方差矩阵都是半正定的。

> [!note] 证明
> 
> **定义**：$\mathbf{A} \in \mathbb{R}^{n \times n}$ 是半正定的（PSD）当且仅当 $\mathbf{x}^\top \mathbf{A} \mathbf{x} \geq 0$ 对所有 $\mathbf{x} \in \mathbb{R}^n$ 成立，记作 $\mathbf{A} \succeq 0$
> 
> **待证**：若 $\mathbf{A}_1, \mathbf{A}_2 \succeq 0$ 且 $\lambda \in [0,1]$，则 $\mathbf{A}_\lambda = \lambda \mathbf{A}_1 + (1-\lambda)\mathbf{A}_2 \succeq 0$
> 
> 对任意 $\mathbf{x} \in \mathbb{R}^n$：
> $$
> \mathbf{x}^\top \mathbf{A}_\lambda \mathbf{x} = \lambda \mathbf{x}^\top \mathbf{A}_1 \mathbf{x} + (1-\lambda) \mathbf{x}^\top \mathbf{A}_2 \mathbf{x} \geq \lambda \cdot 0 + (1-\lambda) \cdot 0 = 0 \quad \square
> $$

> [!success] 应用
> 协方差矩阵估计时，我们经常需要保证估计量是半正定的。由于半正定矩阵构成凸集，某些正则化方法（如收缩估计）天然保持半正定性。

---

## 3.6 凸性保持定理

> [!important] 定理：凸性保持
> 设 $\mathcal{X}, \mathcal{X}_1, \mathcal{X}_2, \ldots$ 为凸集，则以下集合也是凸的：
> 
> | 运算 | 结果 |
> |:---|:---|
> | **交集** | $\mathcal{S} = \bigcap_{i=1}^{\infty} \mathcal{X}_i$ |
> | **笛卡尔积** | $\mathcal{X}_1 \times \cdots \times \mathcal{X}_k$ |
> | **线性映射** | $f(\mathcal{X}) = \{f(\mathbf{x}) : \mathbf{x} \in \mathcal{X}\}$，其中 $f(\mathbf{x}) = \mathbf{A}\mathbf{x} + \mathbf{b}$ |
> | **Minkowski 和** | $\mathcal{X}_1 \oplus \mathcal{X}_2 = \{\mathbf{x}_1 + \mathbf{x}_2 : \mathbf{x}_1 \in \mathcal{X}_1, \mathbf{x}_2 \in \mathcal{X}_2\}$ |

> [!warning] 注意
> **并集通常不保持凸性！** 两个凸集的并可能是非凸的。

---

## 3.7 凸性保持的证明

> [!note] 交集的证明
> 
> 设 $\mathbf{x}, \mathbf{y} \in \mathcal{S}$，$\lambda \in [0,1]$，令 $\mathbf{z} = \lambda \mathbf{x} + (1-\lambda)\mathbf{y}$
> 
> 由 $\mathbf{x}, \mathbf{y} \in \mathcal{S}$，得 $\mathbf{x}, \mathbf{y} \in \mathcal{X}_i$ 对所有 $i$ 成立
> 
> 每个 $\mathcal{X}_i$ 凸 $\Rightarrow$ $\mathbf{z} \in \mathcal{X}_i$ 对所有 $i$ $\Rightarrow$ $\mathbf{z} \in \mathcal{S}$ $\square$

> [!example] 应用：多面体
> 多面体是半空间的交集：
> $$
> \mathcal{P} = \{\mathbf{x} : \mathbf{A}\mathbf{x} \leq \mathbf{b}\} = \bigcap_{i=1}^{m} \{\mathbf{x} : \mathbf{a}_i^\top \mathbf{x} \leq b_i\}
> $$
> 
> 每个半空间是凸的 $\Rightarrow$ 多面体是凸的
> 
> **这是线性规划的基础！**

---

## 3.8 投影定理

> [!important] 定理：投影定理
> 设 $\mathcal{X} \neq \emptyset$ 是内积空间 $V$ 的非空闭凸子集。则对每个点 $\mathbf{x} \in V$，存在**唯一**一个点 $\hat{\mathbf{x}} \in \mathcal{X}$ 使得与 $\mathbf{x}$ 的距离最小：
> 
> $$
> \|\mathbf{x} - \hat{\mathbf{x}}\| = \min_{\mathbf{y} \in \mathcal{X}} \|\mathbf{x} - \mathbf{y}\|
> $$
> 
> 该点称为 $\mathbf{x}$ 在 $\mathcal{X}$ 上的**投影**。

> [!example] 最小二乘就是投影
> 
> 回归问题 $\min_{\mathbf{w}} \|\mathbf{y} - \mathbf{X}\mathbf{w}\|^2$
> 
> 几何理解：找 $\mathbf{X}$ 列空间（凸集！）中离 $\mathbf{y}$ 最近的点。
> 
> 解 $\hat{\mathbf{y}} = \mathbf{X}\hat{\mathbf{w}}$ 就是 $\mathbf{y}$ 在列空间上的投影！

> [!warning] 注意
> 若 $\mathcal{X}$ **不是凸的**，则 $\hat{\mathbf{x}}$ 不一定唯一（可能存在多个点同时达到最小距离）

---

# 四、凸函数

## 4.1 上方图

> [!important] 定义：上方图
> 函数 $f: \mathcal{X} \to \mathbb{R}$ 的**上方图**是集合：
> 
> $$
> \text{epi}(f) = \{(\mathbf{x}, t) \in \mathcal{X} \times \mathbb{R} : t \geq f(\mathbf{x})\}
> $$

**直观理解**：上方图就是函数图像"上方"的所有点构成的区域。

> [!note] 凸函数的等价定义
> $f: \mathcal{X} \to \mathbb{R}$ 是**凸函数** $\Leftrightarrow$ 上方图 $\text{epi}(f)$ 是凸集

---

## 4.2 凸函数的定义

> [!important] 定义：凸函数
> 设 $\mathcal{X}$ 是凸集，$f: \mathcal{X} \to \mathbb{R}$。若对所有 $\mathbf{x}, \mathbf{y} \in \mathcal{X}$ 及所有 $\lambda \in [0,1]$ 均有
> 
> $$
> f(\lambda \mathbf{x} + (1-\lambda)\mathbf{y}) \leq \lambda f(\mathbf{x}) + (1-\lambda)f(\mathbf{y})
> $$
> 
> 则称 $f$ 是**凸的**。

> [!abstract] 几何直观
> **连接函数值 $f(\mathbf{x})$ 和 $f(\mathbf{y})$ 的线段从不低于函数图像**
> 
> 换句话说：函数的"弦"在"弧"的上方。

> [!example] 最熟悉的例子
> $f(x) = x^2$ 是凸的：
> - 取 $x = 0$，$y = 2$，$\lambda = 1/2$
> - 左边：$f(1) = 1$
> - 右边：$\frac{1}{2} f(0) + \frac{1}{2} f(2) = \frac{1}{2}(0 + 4) = 2$
> - 确实 $1 \leq 2$ ✓

> [!tip] 凹函数
> 若 $-f$ 是凸的，则称 $f$ 是**凹的**
> 
> 例如：$\ln x$ 是凹函数，$-\ln x$ 是凸函数

---

## 4.3 凸函数示例

> [!example] 验证 $f(x) = |x|$ 是凸的
> 
> 设 $x, y \in \mathbb{R}$ 任意，$\lambda \in [0,1]$
> 
> 需验证：$f(\lambda x + (1-\lambda)y) \leq \lambda f(x) + (1-\lambda)f(y)$
> 
> **用三角不等式**：
> $$
> f(\lambda x + (1-\lambda)y) = |\lambda x + (1-\lambda)y| \leq |\lambda x| + |(1-\lambda)y|
> $$
> $$
> = \lambda|x| + (1-\lambda)|y| = \lambda f(x) + (1-\lambda)f(y) \quad \square
> $$

**注意**：$|x|$ 在 $x=0$ 处不可微，但它仍然是凸的！凸性不要求可微。

---
## 4.4 判断凸性

> [!question] 小测验
> 以下哪些函数是凸的？
> 
> 1. $f(x) = ax + b$，$a, b \in \mathbb{R}$
> 2. $f(x) = x^4$
> 3. $f(x) = \sqrt{x}$，$x \geq 0$
> 4. $f(x) = e^x$
> 5. $f(x) = \ln x$，$x > 0$

> [!success] 答案
> 
> | 函数 | 凸性 | 原因（用二阶导数判断） |
> |:---|:---:|:---|
> | $ax + b$ | ✓ | 线性函数既凸又凹（$f'' = 0$） |
> | $x^4$ | ✓ | $f''(x) = 12x^2 \geq 0$ |
> | $\sqrt{x}$ | ✗ | $f''(x) = -\frac{1}{4}x^{-3/2} < 0$（凹） |
> | $e^x$ | ✓ | $f''(x) = e^x > 0$ |
> | $\ln x$ | ✗ | $f''(x) = -1/x^2 < 0$（凹） |

> [!tip] 快速判断技巧
> 对于二次可微函数，看二阶导数：
> - $f'' \geq 0$ 处处成立 $\Rightarrow$ 凸
> - $f'' \leq 0$ 处处成立 $\Rightarrow$ 凹
> - $f''$ 变号 $\Rightarrow$ 既非凸也非凹

---

## 4.5 重要凸函数

> [!success] 多元凸函数
> 
> **仿射函数**
> $$
> f(\mathbf{x}) = \mathbf{b}^\top \mathbf{x} + a
> $$
> 仿射函数**既凸又凹**！（这是唯一同时满足两者的函数类）
> 
> **二次函数**（$\mathbf{Q}$ 半正定时）
> $$
> f(\mathbf{x}) = \mathbf{x}^\top \mathbf{Q} \mathbf{x} + \mathbf{b}^\top \mathbf{x} + a
> $$
> 
> **任意范数**
> $$
> f(\mathbf{x}) = \|\mathbf{x}\|
> $$

> [!example] 为什么范数是凸的？
> 利用三角不等式和齐次性：
> $$
> \|\lambda \mathbf{x} + (1-\lambda)\mathbf{y}\| \leq \|\lambda \mathbf{x}\| + \|(1-\lambda)\mathbf{y}\| = \lambda\|\mathbf{x}\| + (1-\lambda)\|\mathbf{y}\|
> $$

---

## 4.6 机器学习中的凸函数

> [!tip] 最小二乘损失
> $$
> f(\mathbf{w}) = \|\mathbf{A}\mathbf{w} - \mathbf{y}\|^2 = \mathbf{w}^\top(\mathbf{A}^\top \mathbf{A})\mathbf{w} - 2\mathbf{y}^\top \mathbf{A}\mathbf{w} + \|\mathbf{y}\|^2
> $$
> 
> 其中 $\mathbf{Q} = \mathbf{A}^\top \mathbf{A} \succeq 0$（始终半正定！因为 $\mathbf{x}^\top \mathbf{A}^\top \mathbf{A} \mathbf{x} = \|\mathbf{A}\mathbf{x}\|^2 \geq 0$）
> 
> $\Rightarrow$ **凸优化问题**，梯度下降一定能找到全局最优解！(**损失函数转换后方便求导**)

> [!tip] 岭回归
> $$
> f(\mathbf{w}) = \|\mathbf{A}\mathbf{w} - \mathbf{y}\|^2 + \lambda\|\mathbf{w}\|^2 \quad (\lambda > 0)
> $$
> 
> **严格凸**（加上 $\lambda \mathbf{I}$ 后矩阵变成正定的）$\Rightarrow$ 唯一解

---

## 4.7 凸函数的性质

> [!important] 命题：凸性保持运算
> 设 $f_1, f_2, \ldots: \mathcal{X} \to \mathbb{R}$ 是凸函数，则以下函数也是凸的：
> 
> | 运算 | 结果 |
> |:---|:---|
> | **加法** | $f(\mathbf{x}) = f_1(\mathbf{x}) + f_2(\mathbf{x})$ |
> | **非负数乘** | $f(\mathbf{x}) = c f_1(\mathbf{x})$，$c \geq 0$ |
> | **逐点最大** | $f(\mathbf{x}) = \max_i f_i(\mathbf{x})$ |
> | **极限** | $f(\mathbf{x}) = \lim_{n \to \infty} f_n(\mathbf{x})$（若极限存在） |

> [!example] 逐点最大值的应用
> $f(\mathbf{x}) = \max\{x_1, x_2, \ldots, x_n\}$ 是凸的
> 
> 因为每个 $x_i$（作为 $\mathbf{x}$ 的线性函数）是凸的，它们的最大值也是凸的。

---

## 4.8 Jensen 不等式

这是凸性最重要的应用之一！

> [!important] 定理：Jensen 不等式
> 设 $\mathcal{X} \subseteq \mathbb{R}^n$ 是凸集，$f: \mathcal{X} \to \mathbb{R}$ 是凸函数。则对所有 $N \in \mathbb{N}$，所有 $\mathbf{x}_1, \ldots, \mathbf{x}_N \in \mathcal{X}$ 以及满足 $\sum_{i=1}^N w_i = 1$ 的 $w_1, \ldots, w_N \geq 0$：
> 
> $$
> f\left(\sum_{i=1}^{N} w_i \mathbf{x}_i\right) \leq \sum_{i=1}^{N} w_i f(\mathbf{x}_i)
> $$

> [!abstract] 直观理解
> **凸组合的函数值 ≤ 函数值的凸组合**
> 
> 即：$f(\text{加权平均}) \leq \text{加权平均}(f)$

> [!tip] 概率版本
> 若 $X$ 是随机变量，$f$ 是凸函数，则：
> $$
> f(\mathbb{E}[X]) \leq \mathbb{E}[f(X)]
> $$
> 
> 若 $f$ 是**凹**函数，不等式方向相反：
> $$
> f(\mathbb{E}[X]) \geq \mathbb{E}[f(X)]
> $$

---

## 4.9 应用：方差非负

> [!example] 用 Jensen 不等式证明方差非负
> 
> 由于 $x \mapsto x^2$ 是凸的，由 Jensen 不等式：
> $$
> (\mathbb{E}[X])^2 = f(\mathbb{E}[X]) \leq \mathbb{E}[f(X)] = \mathbb{E}[X^2]
> $$
> 
> 整理得：
> $$
> \text{Var}(X) = \mathbb{E}[X^2] - (\mathbb{E}[X])^2 \geq 0 \quad \square
> $$

> [!example] 另一个应用：调和平均 ≤ 几何平均 ≤ 算术平均
> 
> 设 $x_1, \ldots, x_n > 0$，权重相等（各 $1/n$）。
> 
> **算术平均 ≥ 几何平均**：
> 由于 $\ln$ 是凹的，Jensen 不等式给出：
> $$
> \ln\left(\frac{x_1 + \cdots + x_n}{n}\right) \geq \frac{\ln x_1 + \cdots + \ln x_n}{n} = \ln(x_1 \cdots x_n)^{1/n}
> $$
> 
> 取指数：$\frac{x_1 + \cdots + x_n}{n} \geq (x_1 \cdots x_n)^{1/n}$

---

## 4.10 凸函数的三个等价刻画

> [!important] 定理
> 设 $\mathcal{X} \subseteq \mathbb{R}^n$ 是开凸集，$f: \mathcal{X} \to \mathbb{R}$ 二阶连续可微，则以下命题等价：
> 
> **(i)** $f$ 是凸的
> 
> **(ii)** 切线在函数下方（一阶条件）：
> $$
> f(\mathbf{y}) \geq f(\mathbf{x}) + \nabla f(\mathbf{x})(\mathbf{y}-\mathbf{x}) \quad \forall \mathbf{x}, \mathbf{y} \in \mathcal{X}
> $$
> 
> **(iii)** Hesse 矩阵半正定（二阶条件）：
> $$
> \mathbf{H}_f(\mathbf{x}) \succeq 0 \quad \forall \mathbf{x} \in \mathcal{X}
> $$

这三个条件是等价的，你可以选择最方便的一个来验证凸性！

---

## 4.11 三个刻画的几何意义

> [!abstract] 几何直观
> 
> | 刻画 | 几何意义 | 如何使用 |
> |:---|:---|:---|
> | **(i) 定义** | 两点连线在图像上方 | 直接验证定义 |
> | **(ii) 切线** | 切平面在图像下方 | 用于证明全局最优性 |
> | **(iii) Hesse** | 各方向曲率非负 | 计算最简单！ |

> [!example] 用 Hesse 矩阵判断凸性
> 
> $f(x, y) = x^2 + xy + y^2$
> 
> $$
> \nabla f = (2x + y, x + 2y)
> $$
> 
> $$
> \mathbf{H}_f = \begin{pmatrix} 2 & 1 \\ 1 & 2 \end{pmatrix}
> $$
> 
> 特征值：$\lambda = 3$ 和 $\lambda = 1$，都是正的
> 
> $\Rightarrow$ $\mathbf{H}_f \succ 0$（正定）$\Rightarrow$ $f$ **严格凸**！

---

## 4.12 次梯度

对于不可微的凸函数（比如 $|x|$），我们用**次梯度**来推广导数的概念。

> [!important] 命题：次梯度存在性
> 设 $\mathcal{X} \subseteq \mathbb{R}^n$，$f: \mathcal{X} \to \mathbb{R}$ 是凸函数。则对每个 $\mathbf{x} \in \mathcal{X}$，存在向量 $\mathbf{v} \in \mathbb{R}^n$ 使得
> 
> $$
> f(\mathbf{y}) \geq f(\mathbf{x}) + \mathbf{v}^\top(\mathbf{y}-\mathbf{x}) \quad \forall \mathbf{y} \in \mathcal{X}
> $$

> [!example] $f(x) = |x|$ 在 $x = 0$ 的次梯度
> 
> 在 $x = 0$，任何满足 $|y| \geq v \cdot y$ 对所有 $y$ 成立的 $v$ 都是次梯度。
> 
> 这要求 $v \in [-1, 1]$。所以次梯度不是唯一的！

> [!note] 关键点
> - 满足上述不等式的 $\mathbf{v}$ 称为**次梯度**
> - 次梯度**不唯一**
> - 函数**不需要可微** → 对非光滑优化很重要（LASSO、分位数回归等）

---

# 五、凸函数在优化中的应用

## 5.1 全局最优性定理

这是凸优化最重要的结果！

> [!important] 命题
> 对每个可微凸函数 $f: \mathbb{R}^n \to \mathbb{R}$，以下命题等价：
> 
> **(i)** $\mathbf{x}^\star$ 是 $f$ 的**全局最小值点**
> 
> **(ii)** $\nabla f(\mathbf{x}^\star) = \mathbf{0}^\top$

> [!success] 核心结论
> - 每个**临界点**都是**局部最小值点**
> - 每个**局部最小值点**都是**全局最小值点**
> 
> **若我们求解一个凸优化问题，必然能找到全局最优解！**

这就是为什么凸优化如此重要：你不用担心陷入局部最优，梯度下降找到的解就是全局最优！

---

## 5.2 全局最优性的证明

> [!note] 证明
> 
> **(i) $\Rightarrow$ (ii)**：
> 
> 全局最小值点是局部最小值点，由费马引理必有 $\nabla f(\mathbf{x}^\star) = \mathbf{0}^\top$
> 
> **(ii) $\Rightarrow$ (i)**：
> 
> 若 $\nabla f(\mathbf{x}^\star) = \mathbf{0}^\top$，由凸性的切线刻画（一阶条件），对所有 $\mathbf{y}$：
> $$
> f(\mathbf{y}) \geq f(\mathbf{x}^\star) + \underbrace{\nabla f(\mathbf{x}^\star)}_{= \mathbf{0}^\top}(\mathbf{y}-\mathbf{x}^\star) = f(\mathbf{x}^\star) \quad \square
> $$

---

## 5.3 严格凸函数

> [!important] 定义：严格凸性
> 若对所有 $\mathbf{x} \neq \mathbf{y} \in \mathcal{X}$ 及所有 $\lambda \in (0,1)$ 均有
> 
> $$
> f(\lambda \mathbf{x} + (1-\lambda)\mathbf{y}) < \lambda f(\mathbf{x}) + (1-\lambda)f(\mathbf{y})
> $$
> 
> 则称 $f$ 是**严格凸的**。

**注意不等号是严格的 $<$！**

> [!success] 唯一性定理
> 若 $f: \mathbb{R}^n \to \mathbb{R}$ 是**严格凸的**，则 $f$ **至多有一个最小值点**
> 
> **若我们求解一个严格凸优化问题，必然能找到唯一的全局最优解！**

> [!note] 证明唯一性
> 反证：假设 $\mathbf{x}^\star \neq \mathbf{y}^\star$ 都是最小值点，即 $f(\mathbf{x}^\star) = f(\mathbf{y}^\star) = m$（最小值）。
> 
> 取 $\lambda = 1/2$，由严格凸性：
> $$
> f\left(\frac{\mathbf{x}^\star + \mathbf{y}^\star}{2}\right) < \frac{1}{2}f(\mathbf{x}^\star) + \frac{1}{2}f(\mathbf{y}^\star) = m
> $$
> 
> 这与 $m$ 是最小值矛盾！$\square$

---

## 5.4 优化地形比较

> [!abstract] 三种情况
> 
> | 类型 | 特点 | 最小值 | 例子 |
> |:---|:---|:---|:---|
> | **凸（有平坦区域）** | 可能有最小值区域 | 可能不唯一 | $f(x) = \|x\|$ |
> | **严格凸** | "碗形" | **唯一** | $f(x) = x^2$ |
> | **非凸** | 多个山谷和鞍点 | 可能有多个局部最小值 | 神经网络损失 |

---

## 5.5 机器学习中的凸与非凸

> [!success] 严格凸问题（唯一解）
> - 满秩 $\mathbf{X}$ 的最小二乘
> - 带正则化的 GLM 最大似然估计
> - 支持向量机（SVM）
> - 岭回归与弹性网

> [!warning] 凸但非严格（解集）
> - LASSO（当设计矩阵不满秩时）
> - 分位数回归
> - 非正则 GLM

> [!danger] 非凸问题（局部最小值和鞍点）
> - 神经网络
> - 聚类分析（k-means）
> - 潜变量模型
> - 矩阵分解
> - 图模型结构学习

---

# 六、总结

## 📌 核心概念脑图

```
                ┌─────────────────┐
                │      凸性       │
                │  （几何 + 分析） │
                └────────┬────────┘
                         │
        ┌────────────────┼────────────────┐
        ↓                                 ↓
┌───────────────┐                 ┌───────────────┐
│     凸集      │                 │    凸函数     │
├───────────────┤                 ├───────────────┤
│ • 线段封闭    │                 │ • 弦在图像上方│
│ • 投影唯一    │                 │ • 切线在下方  │
│ • 交集保持凸性│                 │ • Hesse半正定 │
└───────────────┘                 └───────────────┘
        │                                 │
        ↓                                 ↓
┌───────────────┐                 ┌───────────────┐
│ 凸包、单纯形  │                 │ Jensen不等式  │
│ 半正定矩阵集  │                 │ 全局最优性    │
│ 线性规划可行域│                 │ 凸优化算法    │
└───────────────┘                 └───────────────┘
```

## 📌 凸性在各领域的应用

| 领域 | 凸集 | 凸函数 |
|:---|:---|:---|
| **线性代数** | 单纯形、半正定矩阵锥 | 二次型 |
| **优化** | 可行域 | 目标函数 |
| **统计** | 参数空间 | 负对数似然 |
| **概率** | 概率分布空间 | Jensen 不等式 |
| **机器学习** | 模型参数空间 | 损失函数（岭回归、SVM等） |

---

## 📝 必背公式速查

> [!summary] 核心公式
> 
> **凸组合**
> $$
> \mathbf{x} = \sum_{i=1}^{k} w_i \mathbf{x}_i, \quad w_i \geq 0, \quad \sum_{i=1}^{k} w_i = 1
> $$
> 
> **凸函数定义**
> $$
> f(\lambda \mathbf{x} + (1-\lambda)\mathbf{y}) \leq \lambda f(\mathbf{x}) + (1-\lambda)f(\mathbf{y})
> $$
> 
> **Jensen 不等式**
> $$
> f(\mathbb{E}[X]) \leq \mathbb{E}[f(X)] \quad (f \text{ 凸})
> $$
> 
> **凸函数的切线刻画（一阶条件）**
> $$
> f(\mathbf{y}) \geq f(\mathbf{x}) + \nabla f(\mathbf{x})(\mathbf{y}-\mathbf{x})
> $$
> 
> **凸函数的 Hesse 刻画（二阶条件）**
> $$
> f \text{ 凸} \Leftrightarrow \mathbf{H}_f(\mathbf{x}) \succeq 0 \text{ 处处成立}
> $$
> 
> **全局最优性**
> $$
> f \text{ 凸且可微：} \nabla f(\mathbf{x}^\star) = \mathbf{0} \Leftrightarrow \mathbf{x}^\star \text{ 是全局最小值点}
> $$

---

## ✅ 自测清单

- [ ] 能用自己的话解释什么是凸集和凸函数
- [ ] 能判断一个集合是否为凸集（画图或用定义）
- [ ] 能计算点集的凸包
- [ ] 能证明半正定矩阵集合是凸集
- [ ] 能用 Hesse 矩阵判断一个函数是否为凸函数
- [ ] 能应用 Jensen 不等式（证明方差非负）
- [ ] 理解凸函数的三个等价刻画
- [ ] 能解释为什么凸优化问题有全局最优解

---

> [!quote] 下次课预告
> **优化算法** — 梯度下降、牛顿法、约束优化
