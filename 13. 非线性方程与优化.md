优化算法大揭秘：从数学到AI应用 https://www.coze.cn/s/T2jl-o0Gp94/
## 🎯 预备知识

> [!info] 微分学
> - **梯度** $\nabla f(\mathbf{x})$：最陡上升方向
> - **Hesse 矩阵** $\mathbf{H}_f(\mathbf{x})$：二阶导数矩阵，描述曲率
> - 极值的必要条件 $f'(x^\star) = 0$；极小值的充分条件 $f''(x^\star) > 0$

> [!info] 凸性
> - 集合与函数的**凸性**
> - 凸函数的局部极小值 = 全局极小值

> [!info] 线性代数
> - **正定矩阵**：$\mathbf{x}^\top \mathbf{A} \mathbf{x} > 0$ 对所有 $\mathbf{x} \neq \mathbf{0}$

---

## 📑 目录

```

1. 引言：为什么需要优化
2. 非线性方程组求解
3. 无约束优化基础
4. 梯度下降法
5. 牛顿法与拟牛顿法
6. 随机优化方法
7. 有约束优化
8. 总结

```

---

# 一、引言：为什么需要优化

## 1.1 优化问题的基本形式

> [!important] 三类基本问题
> 
> **非线性方程组**（$f: \mathbb{R}^n \to \mathbb{R}^m$）
> $$
> f(\mathbf{x}) = \mathbf{0}
> $$
> 
> **无约束优化**（$f: \mathbb{R}^n \to \mathbb{R}$）
> $$
> \mathbf{x}^\star = \arg\min_{\mathbf{x}} f(\mathbf{x})
> $$
> 
> **有约束优化**
> $$
> \mathbf{x}^\star = \arg\min_{\mathbf{x} \in S} f(\mathbf{x})
> $$

> [!note] 关键联系
> 若 $f$ 可微，则在极小值处必有 $\nabla f(\mathbf{x}^\star) = \mathbf{0}^\top$
> 
> → **优化问题 ≈ 求解方程组**

---

## 1.2 统计学与机器学习中的优化

> [!success] 典型优化问题
> 
> **极大似然估计**
> $$
> \hat{\boldsymbol{\theta}}_{\text{ML}} = \arg\max_{\boldsymbol{\theta}} \sum_{i=1}^n \log f_Y(y_i \mid \boldsymbol{\theta})
> $$
> 
> **监督学习**（损失函数 $L$，预测值 $\hat{y}_i = p_\theta(\mathbf{x}_i)$）
> $$
> \hat{\boldsymbol{\theta}} = \arg\min_{\boldsymbol{\theta}} \sum_{i=1}^n L(y_i, p_\theta(\mathbf{x}_i))
> $$
> 
> **线性回归**
> $$
> \hat{\boldsymbol{\beta}} = \arg\min_{\boldsymbol{\beta}} \|\mathbf{y} - \mathbf{X}\boldsymbol{\beta}\|_2^2
> $$
> 
> **正则化回归**（岭回归 $p=2$，Lasso $p=1$）
> $$
> \hat{\boldsymbol{\beta}} = \arg\min_{\boldsymbol{\beta}} \|\mathbf{y} - \mathbf{X}\boldsymbol{\beta}\|_2^2 + \lambda\|\boldsymbol{\beta}\|_p
> $$

---

# 二、非线性方程组求解

## 2.1 二分法

> [!important] 二分法（Bisection）
> 
> **目标**：求解 $f(x) = 0$，$f: \mathbb{R} \to \mathbb{R}$
> 
> **前提条件**：
> - $f$ 连续
> - 找到 $a, b$ 使得 $f(a) \cdot f(b) < 0$（异号）
> 
> **算法**：
> 1. 计算中点 $\text{mid} = (a + b) / 2$
> 2. 若 $f(a) \cdot f(\text{mid}) < 0$，则 $b \leftarrow \text{mid}$，否则 $a \leftarrow \text{mid}$
> 3. 重复直到 $b - a < \epsilon$

> [!note] 性质
> - 需要 $\lceil\log_2\left(\frac{b-a}{\epsilon}\right)\rceil$ 步
> - 仅适用于**单变量**函数
> - 可用于计算反函数 $f^{-1}(y)$（令 $f(x) - y = 0$）

---

## 2.2 Newton-Raphson 法

> [!important] Newton-Raphson 迭代
> 
> **目标**：求解 $f(x) = 0$，$f: \mathbb{R} \to \mathbb{R}$ 可微
> 
> **思路**：用切线局部近似 $f$，求切线的零点
> 
> **迭代格式**：
> $$
> x^{(k+1)} = x^{(k)} - \frac{f(x^{(k)})}{f'(x^{(k)})}
> $$

> [!abstract] 几何意义
> 取 $f$ 在 $x^{(k)}$ 处切线与 $x$ 轴的交点

> [!success] 收敛性
> 在解附近具有**二次收敛**：每步误差平方
> $$
> |x^{(k+1)} - x^\star| \approx C|x^{(k)} - x^\star|^2
> $$

---

## 2.3 一阶不动点迭代

> [!important] 不动点迭代
> 
> **目标**：求解 $f(\mathbf{x}) = \mathbf{0}$，$f: \mathbb{R}^n \to \mathbb{R}^n$
> 
> **迭代格式**：
> $$
> \mathbf{x}^{(k)} = \mathbf{x}^{(k-1)} - \gamma f(\mathbf{x}^{(k-1)})
> $$
> 
> **收敛条件**：若 $\rho := \|\mathbf{I}_n - \gamma \mathbf{J}_f(\mathbf{x}^\star)\| < 1$，则
> $$
> \|\mathbf{x}^{(k)} - \mathbf{x}^\star\| = O(\rho^k)
> $$

> [!warning] 步长选择
> - 太小 → 收敛缓慢
> - 太大 → 不收敛
> - 需要仔细调节 $\gamma$

---

# 三、无约束优化基础

## 3.1 局部最优与全局最优

> [!important] 凸函数的全局最优性
> 若 $f: \mathbb{R}^n \to \mathbb{R}$ 是凸函数，则**每个局部极小值也是全局极小值**。

> [!abstract] 三种情况
> 
> | 类型 | 特点 |
> |:---|:---|
> | **凸问题** | 只有全局最优 |
> | **非凸问题** | 存在局部最优 |
> | **严格凸问题** | 极小值唯一 |

---

## 3.2 鞍点

> [!important] 定义：鞍点
> 若驻点 $\mathbf{x}^\star$ 处 Hesse 矩阵 $\mathbf{H}_f(\mathbf{x}^\star)$ **不定**（既有正特征值又有负特征值），则称为**鞍点**。

> [!example] 经典例子
> $f(x,y) = x^2 - y^2$，在 $(0,0)$ 处：
> $$
> \mathbf{H}_f = \begin{pmatrix}2 & 0 \\ 0 & -2\end{pmatrix}, \quad \lambda_1 = 2 > 0, \quad \lambda_2 = -2 < 0
> $$
> → Hesse 矩阵不定 → 鞍点

> [!warning] 对优化的影响
> - 牛顿法可能收敛到鞍点
> - 高维非凸问题（如神经网络）中**几乎所有临界点都是鞍点**
> - 需要能"逃离"鞍点的方法

---

## 3.3 最优性条件总结

> [!success] 二阶最优性条件
> 
> 对二阶连续可微 $f: \mathbb{R}^n \to \mathbb{R}$：
> 
> | $\mathbf{H}_f(\mathbf{x}^\star)$ | 点的类型 |
> |:---:|:---:|
> | $\succ 0$（正定） | 局部极小值 |
> | $\prec 0$（负定） | 局部极大值 |
> | 不定 | 鞍点 |
> | 半定 | 待进一步分析 |
> 
> **必要条件**：$\mathbf{x}^\star$ 局部极小值 $\Rightarrow$ $\nabla f(\mathbf{x}^\star) = \mathbf{0}^\top$
> 
> **充分条件**：$\nabla f(\mathbf{x}^\star) = \mathbf{0}$ 且 $\mathbf{H}_f(\mathbf{x}^\star) \succ 0$ $\Leftrightarrow$ $\mathbf{x}^\star$ 是局部极小值

---

## 3.4 方法分类

> [!tip] 按导数阶次分类
> 
> | 阶次 | 使用信息 | 典型算法 |
> |:---:|:---|:---|
> | 零阶 | 仅 $f(\mathbf{x})$ | Nelder-Mead、网格搜索 |
> | 一阶 | $f, \nabla f$ | 梯度下降、动量法、SGD |
> | 二阶 | $f, \nabla f, \mathbf{H}_f$ | 牛顿法 |

> [!abstract] 权衡
> 阶次越高 → 收敛越快，但每步代价越高
> 
> 实践中常用**拟牛顿法**（如 BFGS）作为折中

---

# 四、梯度下降法

## 4.1 基本思想

> [!important] 梯度下降迭代
> 
> **核心思想**：梯度指向上升方向 → 负梯度指向下降方向
> 
> **迭代格式**：
> $$
> \mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} - \gamma \nabla f(\mathbf{x}^{(k)})^\top
> $$
> 
> 其中 $\gamma > 0$ 为**步长**（学习率）

> [!note] 对凸函数
> $-\nabla f(\mathbf{x})$ 始终是指向全局极小值的下降方向

---

## 4.2 步长的影响

> [!warning] 步长选择
> 
> | 步长 | 效果 |
> |:---:|:---|
> | 太小 | 收敛缓慢 |
> | 太大 | 震荡/发散 |
> | 适当 | 高效收敛 |

```

步长太小 步长适当 步长太大  
● ● ●  
↓ ↓ ↓  
↓ ↓ ↑  
↓ ● ● ●  
● (收敛) (震荡)  
(慢)

```

---

## 4.3 收敛性分析

> [!important] Lipschitz 连续性
> 若 $\|f(\mathbf{x}) - f(\mathbf{y})\| \le L\|\mathbf{x} - \mathbf{y}\|$，则称 $f$ 以常数 $L$ **Lipschitz 连续**
> 
> 梯度 Lipschitz 连续等价于 $\|\mathbf{H}_f(\mathbf{x})\| \le L$（曲率有界）

**函数值的变化不能比自变量的变化快太多**， - L 是"最大变化率"的上界

> [!success] 收敛速率
> 
> **一般凸函数**（梯度 Lipschitz，取 $\gamma \le \frac{1}{L}$）
> $$
> f(\mathbf{x}^{(k)}) - f(\mathbf{x}^\star) \le \frac{\|\mathbf{x}^{(0)} - \mathbf{x}^\star\|^2}{2\gamma k}
> $$
> → 误差以 $O(1/k)$ 下降，需要 $k \sim 1/\varepsilon$ 步
> 
> **强凸函数**（参数 $\mu > 0$，即 $\mathbf{H}_f(\mathbf{x}) - \mu\mathbf{I} \succeq 0$）
> $$
> f(\mathbf{x}^{(k)}) - f(\mathbf{x}^\star) \le \rho^k \cdot (f(\mathbf{x}^{(0)}) - f(\mathbf{x}^\star)), \quad \rho = 1 - \frac{\mu}{L} < 1
> $$
> → **指数收敛**，仅需 $k \sim \log(1/\varepsilon)$ 步

---

## 4.4 条件数

> [!important] 条件数
> $$
> \kappa_f = \frac{\lambda_{\max}(\mathbf{H}_f)}{\lambda_{\min}(\mathbf{H}_f)} = \frac{L}{\mu}
> $$

> [!warning] 影响
> 条件数大 → 收敛慢（锯齿形轨迹）
> 
> 直观理解：等高线是扁长的椭圆，梯度方向与最优方向偏差大

---

## 4.5 停止准则

> [!tip] 常用停止准则
> 
> 1. **梯度足够小**：$\|\nabla f(\mathbf{x}^{(k)})\| < \varepsilon_{\text{grad}}$
> 
> 2. **函数值停滞**：$\frac{|f(\mathbf{x}^{(k+1)}) - f(\mathbf{x}^{(k)})|}{|f(\mathbf{x}^{(k)})|} < \varepsilon_f$
> 
> 3. **超过最大迭代次数**：$k > k_{\max}$
> 
> 通常**组合使用**多个准则
> 
> 典型值：$\varepsilon_{\text{grad}} \approx 10^{-6}$ 至 $10^{-8}$，$k_{\max} \approx 1000$ 至 $10000$

---

## 4.6 直线搜索

> [!important] 回溯直线搜索（Armijo 条件）
> 
> 给定方向 $\mathbf{d} = -\nabla f(\mathbf{x}^{(k)})^\top$，参数 $c \in (0,1)$，$\rho \in (0,1)$：
> 
> 1. 初始令 $\gamma = 1$
> 2. 当 $f(\mathbf{x}^{(k)} + \gamma\mathbf{d}) > f(\mathbf{x}^{(k)}) + c\gamma\nabla f(\mathbf{x}^{(k)})\mathbf{d}$ 时，令 $\gamma \leftarrow \rho\gamma$
> 3. 返回 $\gamma$

> [!note] 说明
> Armijo 条件要求"足够的下降量"
> 
> 典型参数：$c = 10^{-4}$，$\rho = 0.5$

---

# 五、牛顿法与拟牛顿法

## 5.1 牛顿法

> [!important] 牛顿法迭代
> 
> **思路**：用二阶 Taylor 近似局部近似 $f$
> $$
> f(\mathbf{x} + \mathbf{h}) \approx f(\mathbf{x}) + \nabla f(\mathbf{x})\mathbf{h} + \frac{1}{2}\mathbf{h}^\top \mathbf{H}_f(\mathbf{x})\mathbf{h}
> $$
> 
> **迭代格式**：
> $$
> \mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} - \mathbf{H}_f(\mathbf{x}^{(k)})^{-1} \nabla f(\mathbf{x}^{(k)})^\top
> $$

> [!warning] 要求
> $\mathbf{H}_f(\mathbf{x}^{(k)})$ 必须可逆（凸函数时 $\mathbf{H}_f \succ 0$）
> 
> 实践中**求解线性方程组**，而不是显式计算逆矩阵
> 
> → **永远不要直接求矩阵的逆！**

---

## 5.2 牛顿法的特点

> [!success] 优点
> - 近解附近**二次收敛**
> - 对条件数不敏感

> [!warning] 缺点
> - 每步需要计算 Hesse 矩阵：$O(n^2)$ 存储，$O(n^3)$ 求解
> - 对非凸函数可能收敛到局部最优或鞍点
> - 初始点远离最优时可能发散

---

## 5.3 拟牛顿法

> [!important] 拟牛顿法思想
> 
> **问题**：Hesse 矩阵计算代价高
> 
> **思路**：从梯度信息**迭代近似** $\mathbf{H}_f^{-1}$
> 
> **迭代格式**：
> $$
> \mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} - \mathbf{B}_k \nabla f(\mathbf{x}^{(k)})^\top, \quad \mathbf{B}_k \approx \mathbf{H}_f^{-1}(\mathbf{x}^{(k)})
> $$

---

## 5.4 BFGS 算法

> [!important] BFGS 更新公式
> 
> 令 $\mathbf{y}_k = (\nabla f(\mathbf{x}^{(k+1)}) - \nabla f(\mathbf{x}^{(k)}))^\top$，$\mathbf{s}_k = \mathbf{x}^{(k+1)} - \mathbf{x}^{(k)}$，$\rho_k = \frac{1}{\mathbf{y}_k^\top \mathbf{s}_k}$
> 
> $$
> \mathbf{B}_{k+1} = (\mathbf{I} - \rho_k \mathbf{s}_k \mathbf{y}_k^\top)\mathbf{B}_k(\mathbf{I} - \rho_k \mathbf{y}_k \mathbf{s}_k^\top) + \rho_k \mathbf{s}_k \mathbf{s}_k^\top
> $$

> [!success] 特点
> - 每步复杂度 $O(n^2)$（牛顿法为 $O(n^3)$）
> - **超线性收敛**，接近牛顿法
> - **L-BFGS**：有限内存版本，仅存储最近 $m$ 个向量，内存 $O(mn)$

---

## 5.5 方法对比

> [!tip] 各方法对比
> 
> | 属性 | Nelder-Mead | 梯度下降 | 拟牛顿 | 牛顿法 |
> |:---|:---:|:---:|:---:|:---:|
> | **阶次** | 零阶 | 一阶 | "一阶半" | 二阶 |
> | **需要** | 仅 $f$ | $\nabla f$ | $\nabla f$ | $\nabla f, \mathbf{H}_f$ |
> | **每步复杂度** | $O(n)$ | $O(n)$ | $O(n^2)$ | $O(n^3)$ |
> | **收敛性** | 无保证 | 线性 | 超线性 | 二次 |
> | **可扩展性** | 差 | 极好 | 好 | 差 |

---

# 六、随机优化方法

## 6.1 动量梯度下降

> [!important] 动量法
> 
> **动机**：高条件数时，梯度下降频繁震荡
> 
> **类比**：滚下山坡的球会积累惯性
> 
> **动量更新**：
> $$
> \mathbf{v}^{(k+1)} = \alpha \mathbf{v}^{(k)} - \gamma \nabla f(\mathbf{x}^{(k)})^\top
> $$
> $$
> \mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} + \mathbf{v}^{(k+1)}
> $$
> 
> 其中 $\alpha \in [0,1)$ 为阻尼系数（通常取 0.9）

$v^{k} = x^{k} - x^{k-1}$ - 速度

> [!success] 作用
> - **抑制震荡**
> - **加速一致下降方向上的进展**
> - 深度学习中几乎是标配

## 1. 问题设定

考虑一个简单的二次型损失函数：
$$f(x, y) = x^2 + 10y^2$$

等高线在 $y$ 方向更陡（曲率大），在 $x$ 方向平缓（曲率小）。条件数 = 10，普通梯度下降在 y 方向会来回震荡。

**初始点：**
$$(x^{(0)}, y^{(0)}) = (1.0, 0.8)$$

**学习率（步长）：**
$$\gamma = 0.1$$

**动量阻尼系数：**
$$\alpha = 0.9$$

**初始速度：**
$$\mathbf{v}^{(0)} = (0, 0)$$

## 2. 梯度计算

$$\nabla f(x, y) = (2x, \; 20y)$$

在初始点 $(1, 0.8)$：
$$\nabla f(1, 0.8) = (2, 16)$$

## 3. 普通 SGD（无动量）更新

**公式：**
$$(x^{(k+1)}, y^{(k+1)}) = (x^{(k)}, y^{(k)}) - \gamma \nabla f(x^{(k)}, y^{(k)})$$

### 第一次迭代（k=0）
$$(x^{(1)}, y^{(1)}) = (1, 0.8) - 0.1 \times (2, 16)$$
$$x^{(1)} = 1 - 0.2 = 0.8$$
$$y^{(1)} = 0.8 - 1.6 = -0.8$$
得到 $(0.8, -0.8)$。

### 第二次迭代（k=1）
**梯度：**
$$\nabla f(0.8, -0.8) = (1.6, -16)$$

**更新：**
$$x^{(2)} = 0.8 - 0.1 \times 1.6 = 0.8 - 0.16 = 0.64$$
$$y^{(2)} = -0.8 - 0.1 \times (-16) = -0.8 + 1.6 = 0.8$$
得到 $(0.64, 0.8)$。

可以看到 y 方向在 -0.8 和 0.8 之间来回跳，x 方向缓慢减小。

## 4. 动量法

**动量更新规则：**
$$\mathbf{v}^{(k+1)} = \alpha \mathbf{v}^{(k)} - \gamma \nabla f(\mathbf{x}^{(k)})$$
$$\mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} + \mathbf{v}^{(k+1)}$$

初始 $\mathbf{v}^{(0)} = (0,0)$，$\alpha = 0.9$，$\gamma = 0.1$。

### 第一次迭代（k=0）
梯度 $\nabla f = (2,16)$。

$$\mathbf{v}^{(1)} = 0.9 \times (0,0) - 0.1 \times (2,16) = (-0.2, -1.6)$$
$$\mathbf{x}^{(1)} = (1,0.8) + (-0.2, -1.6) = (0.8, -0.8)$$

第一步结果与 SGD 相同，因为 $\mathbf{v}^{(0)}$ 为零，动量还没有发挥效果。

### 第二次迭代（k=1）
**梯度：**
$$\nabla f(0.8, -0.8) = (1.6, -16)$$

$$\mathbf{v}^{(2)} = 0.9 \times (-0.2, -1.6) - 0.1 \times (1.6, -16)$$

分开计算：
- x 分量：$0.9 \times (-0.2) - 0.1 \times 1.6 = -0.18 - 0.16 = -0.34$
- y 分量：$0.9 \times (-1.6) - 0.1 \times (-16) = -1.44 + 1.6 = 0.16$

$$\mathbf{v}^{(2)} = (-0.34, 0.16)$$

**位置更新：**
$$x^{(2)} = 0.8 + (-0.34) = 0.46$$
$$y^{(2)} = -0.8 + 0.16 = -0.64$$

得到 $(0.46, -0.64)$。

与 SGD 的第二次迭代 $(0.64, 0.8)$ 相比，动量法的 y 变化较小，不是直接从 -0.8 跳到 0.8，而是减缓了震荡（速度在 y 方向只有 +0.16 的变化），同时 x 方向速度因历史积累而变大（-0.34 > 无动量的 -0.16）。

### 第三次迭代（k=2）
**梯度：**
$$\nabla f(0.46, -0.64) = (0.92, -12.8)$$

$$\mathbf{v}^{(3)} = 0.9 \times (-0.34, 0.16) - 0.1 \times (0.92, -12.8)$$

分开计算：
- x 分量：$0.9 \times (-0.34) - 0.1 \times 0.92 = -0.306 - 0.092 = -0.398$
- y 分量：$0.9 \times 0.16 - 0.1 \times (-12.8) = 0.144 + 1.28 = 1.424$

$$\mathbf{v}^{(3)} = (-0.398, 1.424)$$

**位置更新：**
$$x^{(3)} = 0.46 + (-0.398) = 0.062$$
$$y^{(3)} = -0.64 + 1.424 = 0.784$$

得到 $(0.062, 0.784)$。

## 5. 对比小结

经过 3 步：

- **SGD**：从 (1.0, 0.8) → (0.8, -0.8) → (0.64, 0.8) → 还在震荡，y 跳幅很大，x 减小慢。

- **动量法**：从 (1.0, 0.8) → (0.8, -0.8) → (0.46, -0.64) → (0.062, 0.784)，x 更快接近 0，y 振幅减小且已到 0.78（接近 0.8 但比无动量稳定）。

动量法利用了之前的速度，在梯度方向变化大的地方（y 轴）抑制了振荡，同时增加了在平坦方向（x 轴）的加速度，因此更快收敛。



---

## 6.2 随机梯度下降（SGD）

> [!important] SGD 思想
> 
> **问题**：经验风险最小化
> $$
> R(\boldsymbol{\theta}) := \frac{1}{N}\sum_{i=1}^N L(y_i, p_{\boldsymbol{\theta}}(\mathbf{x}_i))
> $$
> 全梯度每步需要计算 $N$ 个梯度，代价极高
> 
> **关键观察**：单个数据点的梯度是全梯度的**无偏估计**
> $$
> \mathbb{E}[\nabla L(y_i, p_{\boldsymbol{\theta}}(\mathbf{x}_i))] = \nabla R(\boldsymbol{\theta})
> $$

---

## 6.3 SGD 更新

> [!important] SGD 迭代
> 
> 随机选取索引 $i_k$：
> $$
> \boldsymbol{\theta}^{(k+1)} = \boldsymbol{\theta}^{(k)} - \gamma^{(k)} \nabla L(y_{i_k}, p_{\boldsymbol{\theta}}(\mathbf{x}_{i_k}))^\top
> $$

> [!success] 特点
> - 每步代价 $O(1)$ 而非 $O(N)$
> - 方向"平均而言"正确，但有噪声
> - 实践中使用**小批量**（通常 32-256 个样本）

> [!tip] 重要性
> **SGD 是训练所有现代神经网络的基础！**

---

# 七、有约束优化

## 7.1 问题形式

> [!important] 有约束优化问题
> $$
> \min_{\mathbf{x}} f(\mathbf{x}) \quad \text{s.t.} \quad g_i(\mathbf{x}) = 0,\ i=1,\ldots,m; \quad h_j(\mathbf{x}) \le 0,\ j=1,\ldots,p
> $$

---

## 7.2 Lagrange 乘数法

> [!important] Lagrange 函数
> $$
> \mathcal{L}(\mathbf{x}, \boldsymbol{\lambda}) = f(\mathbf{x}) + \boldsymbol{\lambda}^\top \mathbf{g}(\mathbf{x})
> $$

> [!abstract] 几何直觉
> 在约束 $g(\mathbf{x}) = 0$ 上的最优点处，$\nabla f$ **平行于** $\nabla g$
> 
> 即存在 $\lambda$ 使得 $\nabla f(\mathbf{x}^\star) + \lambda \nabla g(\mathbf{x}^\star) = \mathbf{0}$

> [!success] 必要条件
> 在约束最优点 $\mathbf{x}^\star$ 处，存在 $\boldsymbol{\lambda}^\star$ 使得：
> $$
> \nabla_{\mathbf{x}} \mathcal{L}(\mathbf{x}^\star, \boldsymbol{\lambda}^\star) = \mathbf{0}^\top \quad \text{且} \quad \mathbf{g}(\mathbf{x}^\star) = \mathbf{0}
> $$

---

## 7.3 例题 

我们要找的是**驻点**，就是函数不再变化的点，所以求偏导数

> [!example] Lagrange 乘数法示例
> 
> **问题**：最小化 $f(x,y) = x^2 + y^2$ 使得 $x + y = 1$
> 
> **Lagrange 函数**：
> $$
> \mathcal{L}(x, y, \lambda) = x^2 + y^2 + \lambda(x + y - 1)
> $$
> 
> **驻定条件**：
> $$
> \frac{\partial \mathcal{L}}{\partial x} = 2x + \lambda = 0
> $$
> $$
> \frac{\partial \mathcal{L}}{\partial y} = 2y + \lambda = 0
> $$
> $$
> \frac{\partial \mathcal{L}}{\partial \lambda} = x + y - 1 = 0
> $$
> 
> **解**：$x^\star = y^\star = \frac{1}{2}$，$\lambda^\star = -1$，$f(x^\star, y^\star) = \frac{1}{2}$

---

## 7.4 KKT 条件

> [!important] KKT 条件（Karush-Kuhn-Tucker）
> 
> 在最优点 $\mathbf{x}^\star$ 处：
> 
> 1. **驻定性**：
> $$
> \nabla f(\mathbf{x}^\star) + \sum_i \lambda_i \nabla g_i(\mathbf{x}^\star) + \sum_j \mu_j \nabla h_j(\mathbf{x}^\star) = \mathbf{0}^\top
> $$
> 
> 2. **原始可行性**：$g_i(\mathbf{x}^\star) = 0$，$h_j(\mathbf{x}^\star) \le 0$
> 
> 3. **对偶可行性**：$\mu_j \ge 0$
> 
> 4. **互补松弛性**：$\mu_j h_j(\mathbf{x}^\star) = 0$（对每个 $j$）

> [!note] 互补松弛含义
> 对每个不等式约束：
> - 若 $h_j(\mathbf{x}^\star) < 0$（不活跃）→ $\mu_j = 0$
> - 若 $\mu_j > 0$ → $h_j(\mathbf{x}^\star) = 0$（活跃，"起约束作用"）

---

## 7.5 正则化回归的几何解释

> [!tip] 岭回归与 Lasso 的约束形式
> 
> **惩罚形式**：
> $$
> \min_{\boldsymbol{\beta}} \|\mathbf{y} - \mathbf{X}\boldsymbol{\beta}\|_2^2 + \lambda\|\boldsymbol{\beta}\|_p^p
> $$
> 
> **约束形式**：
> $$
> \min_{\boldsymbol{\beta}} \|\mathbf{y} - \mathbf{X}\boldsymbol{\beta}\|_2^2 \quad \text{s.t.} \quad \|\boldsymbol{\beta}\|_p^p \le c
> $$

> [!abstract] 几何对比
> 
> | 方法 | 约束区域 | 解的位置 |
> |:---|:---|:---|
> | **岭回归** ($p=2$) | 圆形 | 圆弧上 |
> | **Lasso** ($p=1$) | 菱形 | 常在角点 → **稀疏性** |

---

## 7.6 KKT 与凸性

> [!important] 定理：凸优化的充分性
> 若 $f$ 和所有 $h_j$ 是**凸函数**，所有 $g_i$ 是**仿射函数**，则：
> 
> **KKT 条件不仅是全局最优的必要条件，也是充分条件**

> [!success] 实际意义
> - 对凸优化问题：KKT 点 = 全局最优
> - 许多统计问题是凸的（最小二乘、指数族的 MLE 等）
> - 非凸问题（如神经网络）：KKT 仅给出局部最优

---

# 八、总结

## 📌 方法选择指南

> [!tip] 方法选择
> 
> | 方法 | 适用场景 |
> |:---|:---|
> | **Nelder-Mead** | 无导数、低维（$n \le 10$） |
> | **梯度下降** | 大规模问题、实现简单 |
> | **拟牛顿（BFGS）** | 中等规模问题，**默认推荐** |
> | **牛顿法** | 小规模问题，需快速收敛 |
> | **动量梯度下降** | 病态条件数问题 |
> | **SGD** | 超大数据集与高维参数 |
> | **Lagrange/KKT** | 有约束优化 |

---

## 📝 必背公式速查

> [!summary] 核心公式
> 
> **梯度下降**
> $$
> \mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} - \gamma \nabla f(\mathbf{x}^{(k)})^\top
> $$
> 
> **牛顿法**
> $$
> \mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} - \mathbf{H}_f(\mathbf{x}^{(k)})^{-1} \nabla f(\mathbf{x}^{(k)})^\top
> $$
> 
> **动量更新**
> $$
> \mathbf{v}^{(k+1)} = \alpha \mathbf{v}^{(k)} - \gamma \nabla f(\mathbf{x}^{(k)})^\top, \quad \mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} + \mathbf{v}^{(k+1)}
> $$
> 
> **Lagrange 函数**
> $$
> \mathcal{L}(\mathbf{x}, \boldsymbol{\lambda}) = f(\mathbf{x}) + \boldsymbol{\lambda}^\top \mathbf{g}(\mathbf{x})
> $$

---

## ✅ 自测清单

- [ ] 能写出梯度下降的迭代公式
- [ ] 理解步长对收敛的影响
- [ ] 能解释牛顿法的二阶近似思想
- [ ] 能区分凸问题和非凸问题的最优性
- [ ] 能解释鞍点的定义和危害
- [ ] 能写出 Lagrange 函数
- [ ] 理解 KKT 条件的四个组成部分
- [ ] 能解释 Lasso 产生稀疏解的几何原因

---

> [!quote] 课程结束
> 本讲义涵盖了数学基础的核心内容：线性代数、多元微积分、张量、凸性与优化
