
## 预备知识

- **梯度** ∇f(x)\nabla f(\mathbf{x})∇f(x)（f:Rn→Rf: \mathbb{R}^n \to \mathbb{R}f:Rn→R）：最陡上升方向
- **黑塞矩阵** Hf(x)\mathbf{H}_f(\mathbf{x})Hf​(x)：二阶导数矩阵，描述曲率
- 集合与函数的**凸性**
- 分析学：极值的必要条件 f′(x⋆)=0f'(x^\star) = 0f′(x⋆)=0；极小值的充分条件 f′′(x⋆)>0f''(x^\star) > 0f′′(x⋆)>0
- 线性代数：**正定矩阵**

---

## 一、引言

**非线性方程组**（f:Rn→Rmf: \mathbb{R}^n \to \mathbb{R}^mf:Rn→Rm）的形式为：  
f(x)=0f(\mathbf{x}) = \mathbf{0}f(x)=0

**无约束优化问题**（f:Rn→Rf: \mathbb{R}^n \to \mathbb{R}f:Rn→R）的形式为：  
x⋆=arg⁡min⁡xf(x)\mathbf{x}^\star = \arg\min_{\mathbf{x}} f(\mathbf{x})x⋆=argminx​f(x)  
若 fff 可微，则在极小值处必有 ∇f(x⋆)=0⊤\nabla f(\mathbf{x}^\star) = \mathbf{0}^\top∇f(x⋆)=0⊤。

**有约束优化问题**附加约束条件 x⋆∈S⊂Rn\mathbf{x}^\star \in S \subset \mathbb{R}^nx⋆∈S⊂Rn，例如：

- 等式约束：S={x:gi(x)=0, i=1,…,m}S = \{\mathbf{x}: g_i(\mathbf{x}) = 0,\ i=1,\ldots,m\}S={x:gi​(x)=0, i=1,…,m}
- 不等式约束：S={x:hj(x)≤0, j=1,…,p}S = \{\mathbf{x}: h_j(\mathbf{x}) \le 0,\ j=1,\ldots,p\}S={x:hj​(x)≤0, j=1,…,p}

所有这些问题通常都需要**迭代求解**。

---

## 二、为什么需要优化？

统计学与机器学习在很大程度上由优化问题构成：

- **极大似然估计**：  
    θ^ML=arg⁡max⁡θ∑i=1nlog⁡fY(yi∣θ)\hat{\boldsymbol{\theta}}_{\text{ML}} = \arg\max_{\boldsymbol{\theta}} \sum_{i=1}^n \log f_Y(y_i \mid \boldsymbol{\theta})θ^ML​=argmaxθ​∑i=1n​logfY​(yi​∣θ)
    
- **监督学习**（损失函数 L(y,y^)L(y, \hat{y})L(y,y^​)，预测值 y^i=pθ(xi)\hat{y}_i = p_\theta(\mathbf{x}_i)y^​i​=pθ​(xi​)）：  
    θ^=arg⁡min⁡θ∑i=1nL(yi,pθ(xi))\hat{\boldsymbol{\theta}} = \arg\min_{\boldsymbol{\theta}} \sum_{i=1}^n L(y_i, p_\theta(\mathbf{x}_i))θ^=argminθ​∑i=1n​L(yi​,pθ​(xi​))
    
- **线性回归**：β^=arg⁡min⁡β∥y−Xβ∥22\hat{\boldsymbol{\beta}} = \arg\min_{\boldsymbol{\beta}} \|\mathbf{y} - \mathbf{X}\boldsymbol{\beta}\|_2^2β^​=argminβ​∥y−Xβ∥22​
    
- **正则化回归**（岭回归 p=2p=2p=2，Lasso p=1p=1p=1）：  
    β^=arg⁡min⁡β∥y−Xβ∥22+λ∥β∥p\hat{\boldsymbol{\beta}} = \arg\min_{\boldsymbol{\beta}} \|\mathbf{y} - \mathbf{X}\boldsymbol{\beta}\|_2^2 + \lambda\|\boldsymbol{\beta}\|_pβ^​=argminβ​∥y−Xβ∥22​+λ∥β∥p​  
    等价于有约束形式：arg⁡min⁡β∥y−Xβ∥22\arg\min_{\boldsymbol{\beta}} \|\mathbf{y} - \mathbf{X}\boldsymbol{\beta}\|_2^2argminβ​∥y−Xβ∥22​ 使得 ∥β∥p≤c\|\boldsymbol{\beta}\|_p \le c∥β∥p​≤c。
    

---

## 三、非线性方程组

### 可解性定理

若 f:[a,b]→Rf: [a,b] \to \mathbb{R}f:[a,b]→R 严格单调、连续且 f(a)⋅f(b)≤0f(a) \cdot f(b) \le 0f(a)⋅f(b)≤0，则方程 f(x)=0f(x) = 0f(x)=0 有唯一解。

**证明思路**：严格单调保证唯一性；由介值定理保证存在性。注意这些条件是充分非必要的。

### 二分法（Bisektionsverfahren）

**目标**：求解 f(x)=0f(x) = 0f(x)=0，f:R→Rf: \mathbb{R} \to \mathbb{R}f:R→R。

**输入**：连续函数 fff；两点 a,ba, ba,b 满足 f(a)⋅f(b)<0f(a) \cdot f(b) < 0f(a)⋅f(b)<0；目标精度 ϵ\epsilonϵ。

```r
bisect <- function(f, a, b, eps) {
  while (b - a > eps) {
    mid <- (a + b) / 2
    if (f(a) * f(mid) < 0) b <- mid else a <- mid
  }
  return(mid)
}
```

**注**：

- 可用于计算反函数 f−1(y)f^{-1}(y)f−1(y)（令 f(x)−y=0f(x) - y = 0f(x)−y=0）
- 算法需要 ⌈log⁡2 ⁣(b−aϵ)⌉\lceil\log_2\!\left(\frac{b-a}{\epsilon}\right)\rceil⌈log2​(ϵb−a​)⌉ 步
- 仅适用于**单变量**函数

### 牛顿-拉弗森法（Newton-Raphson）

**目标**：求解 f(x)=0f(x) = 0f(x)=0，f:R→Rf: \mathbb{R} \to \mathbb{R}f:R→R 可微。

**思路**：用切线局部近似 fff，求切线的零点。

对 x(k)x^{(k)}x(k) 作泰勒展开：  
f(x)≈f(x(k))+f′(x(k))(x−x(k))0f(x) \approx f(x^{(k)}) + f'(x^{(k)})(x - x^{(k)}) \overset{!}{=} 0f(x)≈f(x(k))+f′(x(k))(x−x(k))=!0

**迭代格式**：  
x(k+1)=x(k)−f(x(k))f′(x(k))x^{(k+1)} = x^{(k)} - \frac{f(x^{(k)})}{f'(x^{(k)})}x(k+1)=x(k)−f′(x(k))f(x(k))​

**几何意义**：取 fff 在 x(k)x^{(k)}x(k) 处切线与 xxx 轴的交点。**收敛阶**：在解附近具有二次收敛（每步误差平方）。

**与优化的联系**：min⁡f(x)\min f(\mathbf{x})minf(x) 的牛顿法 = 对 ∇f(x)=0⊤\nabla f(\mathbf{x}) = \mathbf{0}^\top∇f(x)=0⊤ 应用牛顿-拉弗森法。

### 一阶不动点迭代

**目标**：求解 f(x)=0f(\mathbf{x}) = \mathbf{0}f(x)=0，f:Rn→Rnf: \mathbb{R}^n \to \mathbb{R}^nf:Rn→Rn 单调递增。

**迭代格式**：  
x(k)=x(k−1)−γf(x(k−1))\mathbf{x}^{(k)} = \mathbf{x}^{(k-1)} - \gamma f(\mathbf{x}^{(k-1)})x(k)=x(k−1)−γf(x(k−1))

若 ρ:=∥In−γJf(x⋆)∥<1\rho := \|\mathbf{I}_n - \gamma \mathbf{J}_f(\mathbf{x}^\star)\| < 1ρ:=∥In​−γJf​(x⋆)∥<1，则 ∥x(k)−x⋆∥=O(ρk)\|\mathbf{x}^{(k)} - \mathbf{x}^\star\| = O(\rho^k)∥x(k)−x⋆∥=O(ρk)。

**优点**：简单，可用于多变量函数；**缺点**：步长 γ\gammaγ 需要仔细选取（太小→收敛慢；太大→不收敛）。

---

## 四、优化问题

### 基本设定

**目标**：求 f(x)f(\mathbf{x})f(x) 的极小值，f:Rn→Rf: \mathbb{R}^n \to \mathbb{R}f:Rn→R：  
x⋆=arg⁡min⁡x∈Rnf(x)\mathbf{x}^\star = \arg\min_{\mathbf{x} \in \mathbb{R}^n} f(\mathbf{x})x⋆=argminx∈Rn​f(x)

- 极大化等价：max⁡f(x)=−min⁡(−f(x))\max f(\mathbf{x}) = -\min(-f(\mathbf{x}))maxf(x)=−min(−f(x))
- 若 fff 可微，极小值处必有 ∇f(x⋆)=0⊤\nabla f(\mathbf{x}^\star) = \mathbf{0}^\top∇f(x⋆)=0⊤（**优化 ≃ 求解方程组**）
- ∇f(x)=0⊤\nabla f(\mathbf{x}) = \mathbf{0}^\top∇f(x)=0⊤ 通常无法解析求解 → 需要迭代方法

### 局部最优与全局最优

**定理（凸函数的全局最优性）**：若 f:Rn→Rf: \mathbb{R}^n \to \mathbb{R}f:Rn→R 是凸函数，则每个局部极小值也是全局极小值。

- 凸问题：只有全局最优
- 非凸问题：存在局部最优
- 若 fff 严格凸且可行集凸，则极小值唯一

### 鞍点

对于**非凸函数**，∇f(x)=0⊤\nabla f(\mathbf{x}) = \mathbf{0}^\top∇f(x)=0⊤ 不一定意味着极小值。

**定义（鞍点）**：若驻点 x⋆\mathbf{x}^\starx⋆ 处黑塞矩阵 Hf(x⋆)\mathbf{H}_f(\mathbf{x}^\star)Hf​(x⋆) 不定（既有正特征值又有负特征值），则称为**鞍点**。

**例**：f(x,y)=x2−y2f(x,y) = x^2 - y^2f(x,y)=x2−y2，在 (0,0)(0,0)(0,0) 处：  
Hf=(200−2),λ1=2>0,λ2=−2<0⇒不定\mathbf{H}_f = \begin{pmatrix}2 & 0 \\ 0 & -2\end{pmatrix},\quad \lambda_1 = 2 > 0,\quad \lambda_2 = -2 < 0 \quad\Rightarrow\text{不定}Hf​=(20​0−2​),λ1​=2>0,λ2​=−2<0⇒不定

**对优化的影响**：

- 牛顿法可能收敛到鞍点
- 高维非凸问题（如神经网络）中几乎所有临界点都是鞍点
- 需要能"逃离"鞍点的方法：（随机）梯度下降等

### 最优性条件总结

对二阶连续可微 f:Rn→Rf: \mathbb{R}^n \to \mathbb{R}f:Rn→R：

|Hf(x⋆)\mathbf{H}_f(\mathbf{x}^\star)Hf​(x⋆)|点的类型|
|---|---|
|≻0\succ 0≻0（正定）|局部极小值|
|≺0\prec 0≺0（负定）|局部极大值|
|不定|鞍点|
|半定|待进一步分析|

- **必要条件（一阶）**：x⋆\mathbf{x}^\starx⋆ 局部极小值 ⇒\Rightarrow⇒ ∇f(x⋆)=0⊤\nabla f(\mathbf{x}^\star) = \mathbf{0}^\top∇f(x⋆)=0⊤
- **充分条件（二阶）**：∇f(x⋆)=0\nabla f(\mathbf{x}^\star) = \mathbf{0}∇f(x⋆)=0 且 Hf(x⋆)≻0\mathbf{H}_f(\mathbf{x}^\star) \succ 0Hf​(x⋆)≻0 ⇔\Leftrightarrow⇔ x⋆\mathbf{x}^\starx⋆ 是局部极小值

---

## 五、无约束优化：迭代方法

### 方法分类（按导数阶次）

|阶次|使用信息|典型算法|
|---|---|---|
|零阶|仅 f(x)f(\mathbf{x})f(x)|Nelder-Mead、网格搜索|
|一阶|f,∇ff, \nabla ff,∇f|梯度下降、动量法、SGD|
|二阶|f,∇f,Hff, \nabla f, \mathbf{H}_ff,∇f,Hf​|牛顿法|

**权衡**：阶次越高→收敛越快，但每步代价越高；实践中常用拟牛顿法（如 BFGS）作为折中。

### Nelder-Mead 法（零阶）

**单纯形法**：在 Rn\mathbb{R}^nRn 中维护一个 n+1n+1n+1 个点构成的单纯形（二维中为三角形，三维中为四面体），迭代地用更好的点替换最差的点。

**四种操作**：

1. **反射（Reflection）**：将最差点关于其余点的重心进行反射
2. **扩张（Expansion）**：沿有希望的方向走得更远
3. **收缩（Contraction）**：反射无效时向内收缩
4. **压缩（Shrink）**：将整个单纯形向最优点收缩

**适用场景**：无法获取导数、低维（n≤10n \le 10n≤10）、非光滑目标函数；**不适用**：高维（n>50n > 50n>50）、需要精确收敛保证的情形。

### 梯度下降法（Gradient Descent, GD）

**核心思想**：梯度指向上升方向 ⇒\Rightarrow⇒ 负梯度指向下降方向。

**迭代格式**：  
x(k+1)=x(k)−γ∇f ⁣(x(k))⊤\mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} - \gamma \nabla f\!\left(\mathbf{x}^{(k)}\right)^\topx(k+1)=x(k)−γ∇f(x(k))⊤

其中 γ>0\gamma > 0γ>0 为步长（学习率）。对凸函数，−∇f(x)-\nabla f(\mathbf{x})−∇f(x) 始终是指向全局极小值的下降方向。

**步长的影响**：

- 太小 → 收敛缓慢
- 太大 → 震荡/发散
- 适当 → 高效收敛

### 梯度下降的收敛性

**Lipschitz 连续性**：若 ∥f(x)−f(y)∥≤L∥x−y∥\|f(\mathbf{x}) - f(\mathbf{y})\| \le L\|\mathbf{x} - \mathbf{y}\|∥f(x)−f(y)∥≤L∥x−y∥，则称 fff 以常数 LLL Lipschitz 连续。梯度 Lipschitz 连续等价于 ∥Hf(x)∥≤L\|\mathbf{H}_f(\mathbf{x})\| \le L∥Hf​(x)∥≤L（曲率有界）。

**收敛速率（一般凸函数）**：对凸函数 fff，梯度 Lipschitz 连续（常数 LLL），取 γ≤1L\gamma \le \frac{1}{L}γ≤L1​，则：  
f ⁣(x(k))−f(x⋆)≤∥x(0)−x⋆∥22γkf\!\left(\mathbf{x}^{(k)}\right) - f(\mathbf{x}^\star) \le \frac{\|\mathbf{x}^{(0)} - \mathbf{x}^\star\|^2}{2\gamma k}f(x(k))−f(x⋆)≤2γk∥x(0)−x⋆∥2​  
→ 误差以 O(1/k)O(1/k)O(1/k) 的速率下降，需要约 k∼1/εk \sim 1/\varepsilonk∼1/ε 步才能使误差小于 ε\varepsilonε（相对较慢）。

**强凸函数的收敛速率**：若 fff 是参数为 μ>0\mu > 0μ>0 的强凸函数（即 Hf(x)−μI⪰0\mathbf{H}_f(\mathbf{x}) - \mu\mathbf{I} \succeq 0Hf​(x)−μI⪰0），则：  
f ⁣(x(k))−f(x⋆)≤ρk⋅(f(x(0))−f(x⋆)),ρ=1−μL<1f\!\left(\mathbf{x}^{(k)}\right) - f(\mathbf{x}^\star) \le \rho^k \cdot (f(\mathbf{x}^{(0)}) - f(\mathbf{x}^\star)),\quad \rho = 1 - \frac{\mu}{L} < 1f(x(k))−f(x⋆)≤ρk⋅(f(x(0))−f(x⋆)),ρ=1−Lμ​<1  
→ 指数收敛，仅需 k∼log⁡(1/ε)k \sim \log(1/\varepsilon)k∼log(1/ε) 步。

**条件数** κf=λmax⁡(Hf)λmin⁡(Hf)=Lμ\kappa_f = \frac{\lambda_{\max}(\mathbf{H}_f)}{\lambda_{\min}(\mathbf{H}_f)} = \frac{L}{\mu}κf​=λmin​(Hf​)λmax​(Hf​)​=μL​：条件数大 ⇒\Rightarrow⇒ 收敛慢（锯齿形轨迹）。

### 停止准则

1. **梯度足够小**：∥∇f(x(k))∥<εgrad\|\nabla f(\mathbf{x}^{(k)})\| < \varepsilon_{\text{grad}}∥∇f(x(k))∥<εgrad​
2. **函数值停滞**：∣f(x(k+1))−f(x(k))∣∣f(x(k))∣<εf\frac{|f(\mathbf{x}^{(k+1)}) - f(\mathbf{x}^{(k)})|}{|f(\mathbf{x}^{(k)})|} < \varepsilon_f∣f(x(k))∣∣f(x(k+1))−f(x(k))∣​<εf​
3. **超过最大迭代次数**：k>kmax⁡k > k_{\max}k>kmax​

通常组合使用多个准则。典型值：εgrad≈10−6\varepsilon_{\text{grad}} \approx 10^{-6}εgrad​≈10−6 至 10−810^{-8}10−8，kmax⁡≈1000k_{\max} \approx 1000kmax​≈1000 至 100001000010000。

### 直线搜索（自适应步长）

**回溯直线搜索（Armijo 条件）**：

给定方向 d=−∇f(x(k))⊤\mathbf{d} = -\nabla f(\mathbf{x}^{(k)})^\topd=−∇f(x(k))⊤，参数 c∈(0,1)c \in (0,1)c∈(0,1)，ρ∈(0,1)\rho \in (0,1)ρ∈(0,1)：

1. 初始令 γ=1\gamma = 1γ=1
2. 当 f(x(k)+γd)>f(x(k))+cγ∇f(x(k))df(\mathbf{x}^{(k)} + \gamma\mathbf{d}) > f(\mathbf{x}^{(k)}) + c\gamma\nabla f(\mathbf{x}^{(k)})\mathbf{d}f(x(k)+γd)>f(x(k))+cγ∇f(x(k))d 时，令 γ←ργ\gamma \leftarrow \rho\gammaγ←ργ
3. 返回 γ\gammaγ

Armijo 条件要求"足够的下降量"。典型参数：c=10−4c = 10^{-4}c=10−4，ρ=0.5\rho = 0.5ρ=0.5。

### 牛顿法（二阶优化）

**思路**：梯度下降只考虑斜率（一阶泰勒近似）；牛顿法用抛物线（二阶泰勒近似）局部近似 fff：  
f(x+h)≈f(x)+∇f(x)h+12h⊤Hf(x)hf(\mathbf{x} + \mathbf{h}) \approx f(\mathbf{x}) + \nabla f(\mathbf{x})\mathbf{h} + \frac{1}{2}\mathbf{h}^\top \mathbf{H}_f(\mathbf{x})\mathbf{h}f(x+h)≈f(x)+∇f(x)h+21​h⊤Hf​(x)h

对 h\mathbf{h}h 求极小：∇f(x)⊤+Hf(x)h=0⊤\nabla f(\mathbf{x})^\top + \mathbf{H}_f(\mathbf{x})\mathbf{h} = \mathbf{0}^\top∇f(x)⊤+Hf​(x)h=0⊤。

**牛顿迭代格式**：  
x(k+1)=x(k)−Hf(x(k))−1∇f(x(k))⊤\mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} - \mathbf{H}_f(\mathbf{x}^{(k)})^{-1} \nabla f(\mathbf{x}^{(k)})^\topx(k+1)=x(k)−Hf​(x(k))−1∇f(x(k))⊤

**要求**：Hf(x(k))\mathbf{H}_f(\mathbf{x}^{(k)})Hf​(x(k)) 必须可逆（凸函数时 Hf≻0\mathbf{H}_f \succ 0Hf​≻0）。实践中求解线性方程组，而不是显式计算逆矩阵——**永远不要直接求矩阵的逆**！

**与牛顿-拉弗森的联系**：对 min⁡f(x)\min f(\mathbf{x})minf(x) 的牛顿法 = 对 g(x):=∇f(x)⊤=0g(\mathbf{x}) := \nabla f(\mathbf{x})^\top = \mathbf{0}g(x):=∇f(x)⊤=0 应用牛顿-拉弗森法，其中 Jg=Hf\mathbf{J}_g = \mathbf{H}_fJg​=Hf​。

**特点**：近解附近**二次收敛**；但对非凸函数可能收敛到局部而非全局最优。

### 拟牛顿法（Quasi-Newton）

**问题**：黑塞矩阵 Hf(x)\mathbf{H}_f(\mathbf{x})Hf​(x) 计算代价高，存储 O(n2)O(n^2)O(n2)，求逆 O(n3)O(n^3)O(n3)。

**思路**：从梯度信息迭代近似 Hf−1\mathbf{H}_f^{-1}Hf−1​。

**迭代格式**：  
x(k+1)=x(k)−Bk∇f(x(k))⊤,Bk≈Hf−1(x(k))\mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} - \mathbf{B}_k \nabla f(\mathbf{x}^{(k)})^\top,\quad \mathbf{B}_k \approx \mathbf{H}_f^{-1}(\mathbf{x}^{(k)})x(k+1)=x(k)−Bk​∇f(x(k))⊤,Bk​≈Hf−1​(x(k))

### BFGS 算法

通过满足割线条件来更新 Bk\mathbf{B}_kBk​（其中 yk=(∇f(x(k+1))−∇f(x(k)))⊤\mathbf{y}_k = (\nabla f(\mathbf{x}^{(k+1)}) - \nabla f(\mathbf{x}^{(k)}))^\topyk​=(∇f(x(k+1))−∇f(x(k)))⊤，sk=x(k+1)−x(k)\mathbf{s}_k = \mathbf{x}^{(k+1)} - \mathbf{x}^{(k)}sk​=x(k+1)−x(k)，ρk=1yk⊤sk\rho_k = \frac{1}{\mathbf{y}_k^\top \mathbf{s}_k}ρk​=yk⊤​sk​1​）：  
Bk+1=(I−ρkskyk⊤)Bk(I−ρkyksk⊤)+ρksksk⊤\mathbf{B}_{k+1} = (\mathbf{I} - \rho_k \mathbf{s}_k \mathbf{y}_k^\top)\mathbf{B}_k(\mathbf{I} - \rho_k \mathbf{y}_k \mathbf{s}_k^\top) + \rho_k \mathbf{s}_k \mathbf{s}_k^\topBk+1​=(I−ρk​sk​yk⊤​)Bk​(I−ρk​yk​sk⊤​)+ρk​sk​sk⊤​

- 每步复杂度 O(n2)O(n^2)O(n2)（牛顿法为 O(n3)O(n^3)O(n3)）
- **超线性收敛**，接近牛顿法
- **L-BFGS**：有限内存版本，仅存储最近 mmm 个向量，内存复杂度 O(mn)O(mn)O(mn)

### 各方法对比

|属性|Nelder-Mead|梯度下降|拟牛顿|牛顿法|
|---|---|---|---|---|
|阶次|零阶|一阶|"一阶半"|二阶|
|需要|仅 fff|∇f\nabla f∇f|∇f\nabla f∇f|∇f,Hf\nabla f, \mathbf{H}_f∇f,Hf​|
|每步复杂度|O(n)O(n)O(n)|O(n)O(n)O(n)|O(n2)O(n^2)O(n2)|O(n3)O(n^3)O(n3)|
|收敛性|无保证|线性|超线性|二次|
|步长|自动|关键（γ\gammaγ）|直线搜索|自动|
|可扩展性|差（n≤10n \le 10n≤10）|极好|好|差|

- **Nelder-Mead**：R 中 `optim()` 的默认方法（无梯度时）
- **梯度下降**：现代神经网络优化器的基础
- **BFGS**：R 中 `optim()` 提供梯度时的默认方法，性价比最优
- **牛顿法**：极大似然推断（GLM 的 IWLS）、XGBoost 等

### 动量梯度下降（Momentum）

**动机**：高条件数 κ(Hf)\kappa(\mathbf{H}_f)κ(Hf​) 时，梯度下降频繁震荡。

**类比**：滚下山坡的球会积累惯性（"Schwung"）。

**动量更新**：  
v(k+1)=αv(k)−γ∇f(x(k))⊤\mathbf{v}^{(k+1)} = \alpha \mathbf{v}^{(k)} - \gamma \nabla f(\mathbf{x}^{(k)})^\topv(k+1)=αv(k)−γ∇f(x(k))⊤  
x(k+1)=x(k)+v(k+1)\mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} + \mathbf{v}^{(k+1)}x(k+1)=x(k)+v(k+1)

其中 α∈[0,1)\alpha \in [0,1)α∈[0,1) 为阻尼系数（通常取 0.9），γ\gammaγ 为步长。

**作用**：抑制震荡，加速一致下降方向上的进展。在深度学习中几乎是标配；变体有 Adagrad、Adam、RMSprop 等。

### 随机梯度下降（SGD）

**动机**：典型的机器学习问题需要最小化经验风险：  
R(θ):=1N∑i=1NL(yi,pθ(xi))R(\boldsymbol{\theta}) := \frac{1}{N}\sum_{i=1}^N L(y_i, p_{\boldsymbol{\theta}}(\mathbf{x}_i))R(θ):=N1​∑i=1N​L(yi​,pθ​(xi​))  
全梯度每步需要计算 NNN 个梯度，代价极高。

**关键思想**：单个数据点的梯度是全梯度的无偏估计：  
E[∇L(yi,pθ(xi))]=∇R(θ)\mathbb{E}[\nabla L(y_i, p_{\boldsymbol{\theta}}(\mathbf{x}_i))] = \nabla R(\boldsymbol{\theta})E[∇L(yi​,pθ​(xi​))]=∇R(θ)

**SGD 更新**（随机选取索引 iki_kik​）：  
θ(k+1)=θ(k)−γ(k)∇L ⁣(yik,pθ(xik))⊤\boldsymbol{\theta}^{(k+1)} = \boldsymbol{\theta}^{(k)} - \gamma^{(k)} \nabla L\!\left(y_{i_k}, p_{\boldsymbol{\theta}}(\mathbf{x}_{i_k})\right)^\topθ(k+1)=θ(k)−γ(k)∇L(yik​​,pθ​(xik​​))⊤

- 每步代价 O(1)O(1)O(1) 而非 O(N)O(N)O(N)
- 方向"平均而言"正确，但有噪声
- 实践中使用**小批量（Mini-Batch）**（通常 32-256 个样本）

**SGD 是训练所有现代神经网络的基础！**

---

## 六、有约束优化

### 问题形式

min⁡xf(x)s.t.gi(x)=0, i=1,…,m;hj(x)≤0, j=1,…,p\min_{\mathbf{x}} f(\mathbf{x}) \quad \text{s.t.} \quad g_i(\mathbf{x}) = 0,\ i=1,\ldots,m;\quad h_j(\mathbf{x}) \le 0,\ j=1,\ldots,pminx​f(x)s.t.gi​(x)=0, i=1,…,m;hj​(x)≤0, j=1,…,p

### Lagrange 乘数法

**几何直觉**：在约束 g(x)=0g(\mathbf{x}) = 0g(x)=0 上的最优点处，∇f\nabla f∇f 平行于 ∇g\nabla g∇g，即 ∃ λ\exists\, \lambda∃λ 使得 ∇f(x⋆)+λ∇g(x⋆)=0\nabla f(\mathbf{x}^\star) + \lambda \nabla g(\mathbf{x}^\star) = \mathbf{0}∇f(x⋆)+λ∇g(x⋆)=0。

**Lagrange 函数**：  
L(x,λ)=f(x)+λ⊤g(x)\mathcal{L}(\mathbf{x}, \boldsymbol{\lambda}) = f(\mathbf{x}) + \boldsymbol{\lambda}^\top \mathbf{g}(\mathbf{x})L(x,λ)=f(x)+λ⊤g(x)

**必要条件（Lagrange）**：在约束最优点 x⋆\mathbf{x}^\starx⋆ 处，存在 λ⋆\boldsymbol{\lambda}^\starλ⋆ 使得：  
∇xL(x⋆,λ⋆)=0⊤且g(x⋆)=0\nabla_{\mathbf{x}} \mathcal{L}(\mathbf{x}^\star, \boldsymbol{\lambda}^\star) = \mathbf{0}^\top \quad \text{且} \quad \mathbf{g}(\mathbf{x}^\star) = \mathbf{0}∇x​L(x⋆,λ⋆)=0⊤且g(x⋆)=0

**例**：最小化 f(x,y)=x2+y2f(x,y) = x^2 + y^2f(x,y)=x2+y2 使得 x+y=1x + y = 1x+y=1，解为 x⋆=y⋆=12x^\star = y^\star = \frac{1}{2}x⋆=y⋆=21​，λ⋆=−1\lambda^\star = -1λ⋆=−1，f(x⋆,y⋆)=12f(x^\star, y^\star) = \frac{1}{2}f(x⋆,y⋆)=21​。

### KKT 条件（含不等式约束）

**Karush-Kuhn-Tucker（KKT）条件**：在最优点 x⋆\mathbf{x}^\starx⋆ 处：

1. **驻定性**：∇f(x⋆)+∑iλi∇gi(x⋆)+∑jμj∇hj(x⋆)=0⊤\nabla f(\mathbf{x}^\star) + \sum_i \lambda_i \nabla g_i(\mathbf{x}^\star) + \sum_j \mu_j \nabla h_j(\mathbf{x}^\star) = \mathbf{0}^\top∇f(x⋆)+∑i​λi​∇gi​(x⋆)+∑j​μj​∇hj​(x⋆)=0⊤
2. **原始可行性**：gi(x⋆)=0g_i(\mathbf{x}^\star) = 0gi​(x⋆)=0，hj(x⋆)≤0h_j(\mathbf{x}^\star) \le 0hj​(x⋆)≤0
3. **对偶可行性**：μj≥0\mu_j \ge 0μj​≥0
4. **互补松弛性**：μjhj(x⋆)=0\mu_j h_j(\mathbf{x}^\star) = 0μj​hj​(x⋆)=0（对每个 jjj）

**互补松弛含义**：对每个不等式约束，要么 hj(x⋆)<0h_j(\mathbf{x}^\star) < 0hj​(x⋆)<0（不活跃）⇒μj=0\Rightarrow \mu_j = 0⇒μj​=0，要么 μj>0⇒hj(x⋆)=0\mu_j > 0 \Rightarrow h_j(\mathbf{x}^\star) = 0μj​>0⇒hj​(x⋆)=0（活跃，"起约束作用"）。

### 统计应用：正则化回归

岭回归/Lasso 可表述为有约束优化：

惩罚形式：min⁡β∥y−Xβ∥22+λ∥β∥pp\text{惩罚形式：}\min_{\boldsymbol{\beta}} \|\mathbf{y} - \mathbf{X}\boldsymbol{\beta}\|_2^2 + \lambda\|\boldsymbol{\beta}\|_p^p惩罚形式：minβ​∥y−Xβ∥22​+λ∥β∥pp​  
约束形式：min⁡β∥y−Xβ∥22s.t.∥β∥pp≤c\text{约束形式：}\min_{\boldsymbol{\beta}} \|\mathbf{y} - \mathbf{X}\boldsymbol{\beta}\|_2^2 \quad \text{s.t.} \quad \|\boldsymbol{\beta}\|_p^p \le c约束形式：minβ​∥y−Xβ∥22​s.t.∥β∥pp​≤c

KKT 驻定性条件（岭回归，p=2p=2p=2）：  
β^Ridge=(X⊤X+μI)−1X⊤y\hat{\boldsymbol{\beta}}_{\text{Ridge}} = (\mathbf{X}^\top\mathbf{X} + \mu\mathbf{I})^{-1}\mathbf{X}^\top\mathbf{y}β^​Ridge​=(X⊤X+μI)−1X⊤y

**几何对比**：

- 岭回归（p=2p=2p=2）：圆形约束区域 → 解在圆弧上
- Lasso（p=1p=1p=1）：菱形约束区域 → 解常在角点处 → **稀疏性**（βj=0\beta_j = 0βj​=0）

### KKT 与凸性

**定理（充分条件）**：若 fff 和所有 hjh_jhj​ 是凸函数，所有 gig_igi​ 是仿射函数，则 KKT 条件不仅是全局最优的必要条件，也是充分条件。

- 对凸优化问题：KKT 点 = 全局最优
- 许多统计问题是凸的（最小二乘、指数族的 MLE 等）
- 非凸问题（如神经网络）：KKT 仅给出局部最优

---

## 七、R 中的优化

### `optimize()`（单变量）

```r
f <- function(x) (x - 3)^2
optimize(f, interval = c(0, 10))
# $minimum: 3   $objective: 0
```

使用黄金分割搜索（二分法的变体）。

### `optim()`（多变量）

```r
# Nelder-Mead（默认）
optim(c(-1, -0.5), f, method = "Nelder-Mead")

# BFGS（推荐，有梯度时）
optim(c(-1, -0.5), f, method = "BFGS")

# L-BFGS-B（有上下界约束）
optim(c(-1, 0.5), f, gr = grad_f, method = "L-BFGS-B",
      lower = c(-1, -1), upper = c(1, 1))
```

**注意事项**：

- 结果可能收敛到**局部**而非全局最优（取决于初始值）
- 迭代次数耗尽时不一定给出警告（需检查 `convergence` 字段：0 表示收敛）
- 提供解析梯度比数值近似更快更精确

---

## 八、总结

|方法|适用场景|
|---|---|
|Nelder-Mead|无导数、低维（n≤10n \le 10n≤10）|
|梯度下降|大规模问题、实现简单|
|拟牛顿（BFGS）|中等规模问题，**默认推荐**|
|牛顿法|小规模问题，需快速收敛|
|动量梯度下降|梯度下降 + 病态条件数问题|
|SGD|超大数据集与高维参数|
|Lagrange/KKT|有约束优化|
