函数逼近、插值与光滑化深度解析 https://www.coze.cn/s/CSD1R3R59OE/
## 🎯 预备知识

> [!info] 线性代数
> - 线性方程组 $\mathbf{Ba} = \mathbf{y}$ 及其可解性
> - 矩阵条件数（数值稳定性度量）
> - 基与线性无关性

> [!info] 分析
> - 多项式及其导数
> - 连续性与可微性
> - 分段定义函数
> - 范数（如 $\|\cdot\|_\infty$）

> [!info] 统计
> - 最小二乘与伪逆
> - 偏差-方差分解与 MSE
> - 交叉验证、信息准则

---

## 📑 目录

```

1. 引言：三种逼近变体
2. 基表示与插值
3. 多项式插值及其问题
4. 样条与 B 样条
5. 样条的理论性质
6. 光滑化与回归
7. 偏差-方差权衡
8. 多变量函数逼近

```

---

**也就是要拟合采样点，有插值法（精准）和 光滑化两大类**！！！！！！！！！！！

**插值相信数据，光滑化怀疑数据**


# 一、引言：三种逼近变体

## 1.1 问题概述

> [!important] 三种变体：寻找 $\hat{f}$，使得……
> 
> | 逼近 | 插值 | 光滑化 |
> |:---:|:---:|:---:|
> | $\|f - \hat{f}\| \approx 0$ | $f(x_i) = \hat{f}(x_i) \;\forall i$ | $y_i = \hat{f}(x_i) + \varepsilon_i \;\forall i$ |
> | 整体误差小 | 精确通过数据点 | 允许噪声存在 |

> [!note] 三者联系
> - 逼近问题通常通过转化为插值或光滑化问题来求解
> - 光滑化是"最难"的变体，因为误差 $\varepsilon_i$ 通常是随机的
> - 本讲重点关注**插值**

---

## 1.2 应用场景

> [!success] 重要应用
> 
> **数值计算**
> - 近似但更快速地求值（如 $f(x) = \int_0^x g(y)\,dy$ 或 $f(x) = g^{-1}(x)$）
> - 逼近 $f$ 的导数或积分
> 
> **机器学习与数据科学**
> - 计算机图形：图像缩放/旋转时的像素值插值
> - 时间序列：填补传感器或金融数据中的缺失值
> - 生成模型：VAE/GAN 中的潜空间插值
> - NLP：Transformer 中的位置编码（如 RoPE）

---

## 1.3 插值问题的非唯一性

> [!warning] 关键观察
> 对插值问题 $y_i = \hat{f}(x_i),\; i=1,\ldots,n$，存在**无穷多个**函数 $\hat{f}$ 可以解该问题！

> [!example] 无穷多解示例
> 
> 考虑 3 个点：$(0,1), (1,2), (2,5)$，以下均为插值函数：
> 
> | 函数 | 表达式 |
> |:---|:---|
> | 抛物线 | $\hat{f}_1(x) = 1 + x^2$ |
> | 分段线性 | $\hat{f}_2(x) = \begin{cases} x+1, & x\in[0,1]\\ 3x-1, & x\in[1,2]\end{cases}$ |
> | 三次多项式 | $\hat{f}_3(x) = x^3 - 2x^2 + 2x + 1$ |
> | 带振荡 | $\hat{f}_4(x) = 1 + x^2 + 0.5\sin(2\pi x)$ |
> 
> 所有函数都满足 $\hat{f}(x_i) = y_i$，但形态截然不同！

> [!tip] 解决方案
> 我们需要**基系统**（多项式、样条）来限制解的范围

---

# 二、基表示与插值

## 2.1 函数空间视角

> [!important] 核心思想
> 
> 函数全体 $f:[a,b]\to\mathbb{R}$ 构成一个**向量空间**
> - 加法、数乘按点定义
> - 该空间是**无穷维**的
> 
> **解决方案**：限制到有限维子空间

---

## 2.2 基函数表示

> [!important] 基函数展开
> 
> 选取**基函数** $\phi_1, \ldots, \phi_K$，只考虑
> $$
> \mathcal{F}_K = \left\{ f(x) = \sum_{k=1}^K a_k \phi_k(x) \;\Big|\; a_k \in \mathbb{R} \right\}
> $$
> 
> $\mathcal{F}_K$ 中的每个 $f$ 由 $K$ 个系数 $(a_1, \ldots, a_K)$ **唯一确定**

> [!example] 常用基函数
> 
> | 类型 | 基函数 | 维度 |
> |:---|:---|:---|
> | 多项式（次数 $\leq n-1$） | $\phi_k(x) = x^{k-1}$ | $K = n$ |
> | 样条（$m+1$ 节点，$q$ 次） | B 样条 | $K = m + q$ |

---

## 2.3 插值问题的线性方程组形式

> [!important] 转化为线性方程组
> 
> 设 $\hat{f}(x) = \sum_{k=1}^K a_k \phi_k(x)$，则插值条件
> $$
> y_i = \sum_{k=1}^K a_k \phi_k(x_i), \quad i = 1, \ldots, n
> $$
> 
> 可写成 $\mathbf{Ba} = \mathbf{y}$，其中
> $$
> B_{ik} = \phi_k(x_i), \quad \mathbf{a} = (a_1, \ldots, a_K)^\top, \quad \mathbf{y} = (y_1, \ldots, y_n)^\top
> $$

> [!example] 具体例子
> 
> 3 个数据点 $(0,1), (1,2), (2,5)$，单项式基 $\phi_k(x) = x^{k-1}$，$K=3$：
> $$
> \mathbf{B} = \begin{pmatrix}1&0&0\\1&1&1\\1&2&4\end{pmatrix}, \quad \mathbf{y} = \begin{pmatrix}1\\2\\5\end{pmatrix}
> $$
> 
> 求解得 $a_1 = 1, a_2 = 0, a_3 = 1$，即 $\hat{f}(x) = 1 + x^2$ ✓

---

# 三、多项式插值及其问题

## 3.1 唯一性定理

> [!important] 多项式插值唯一性
> 
> 对 $n$ 对点 $(x_i, y_i)_{i=1}^n$（$x_i$ 两两不同），恰好存在**唯一**一个次数**至多**为 $n-1$ 的多项式 $p$ 满足 $p(x_i) = y_i$

> [!warning] 常见误解
> 是次数**至多** $n-1$，而非恰好 $n-1$！
> 
> 例：3 个共线点 $(0,0), (1,1), (2,2)$ 的插值多项式是 $p(x) = x$（1次，而非2次）

**最高可能**需要 2 次，但**有时候更低次就够了**

---

## 3.2 问题一：条件数

> [!danger] Vandermonde 矩阵的病态性
> 
> 单项式基导致基矩阵 $\mathbf{B}$ 条件数极差：
> 
> | $n$ | 多项式次数 | $\kappa(\mathbf{B})$ |
> |:---:|:---:|:---:|
> | 5 | 4 | $\approx 10^3$ |
> | 10 | 9 | $\approx 10^8$ |
> | 15 | 14 | $\approx 10^{12}$ |
> | 20 | 19 | $\approx 10^{16}$ |
> 
> 双精度浮点（约16位有效数字）在 $n \geq 20$ 时几乎丧失全部精度！

> [!tip] 解决方案
> 使用**正交化基**（Legendre、Chebyshev、Hermite 多项式）
> 
> 在 R 中用 `poly(x, 3)` 代替 `cbind(1, x, x^2, x^3)`

**单项式基在高次时数值不稳定，换成正交多项式基就好了**
**也就是在各个基上的系数不一样，但是展开后一样**

---

## 3.3 问题二：Runge 现象

> [!danger] Runge 现象（1901）
> 
> 用 $n$ 个等距节点插值
> $$
> f(x) = \frac{1}{1 + 25x^2}
> $$
> 于 $[-1, 1]$
> 
> 尽管 $f \in \mathcal{C}^\infty$，但
> $$
> \|f - p_n\|_\infty \to \infty \quad (n \to \infty)
> $$
> 
> 边界处出现剧烈振荡！

你想用多项式去拟合它。

直觉上：点越多 → 多项式次数越高 → 拟合越准

但实际上相反！

**实际发生了什么？**

用 5 个等距点插值 → 还行

用 10 个等距点插值 → 边缘开始振荡

用 20 个等距点插值 → 边缘剧烈振荡，完全跑飞

点越多，**边界处误差越大**，趋向无穷！

**根本原因**

全局多项式的问题：**牵一发动全身 **     改动一个点的 yyy 值 → 整条曲线都变

> [!abstract] 根本原因
> 改变任一 $y_i$，**整个函数**都会改变
> 
> → 方法不稳定，这是多项式基的根本缺陷

> [!success] 解决方案
> 用**分段多项式**代替全局多项式 → **样条（Splines）**

---

# 四、样条与 B 样条

## 4.1 样条的定义

> [!important] 定义：$q$ 次多项式样条
> 
> $[a,b]$ 上的 **$q$ 次多项式样条**是函数 $s:[a,b]\to\mathbb{R}$：
> 
> 1. 定义在节点网格 $a = \xi_0 < \xi_1 < \cdots < \xi_m = b$ 上
> 2. 在每个子区间 $[\xi_{k-1}, \xi_k)$ 上为 $q$ 次多项式 $p_k$
> 3. 具有 $q-1$ 阶连续导数，即 $s \in \mathcal{C}^{q-1}$

> [!note] 通俗理解
> 样条是在节点 $\xi_1, \ldots, \xi_{m-1}$ 处将多项式"光滑拼接"在一起的函数

节点网格把 [a,b] 分成若干小段     每段是 q 次多项式局部用简单多项式   拼接处足够光滑     ===>样条就是分段多项式，每段简单，拼接处光滑

---

## 4.2 参数计数

> [!important] 自由度计算
> 
> $q$ 次、$m+1$ 个节点的样条需要 $m + q$ 个参数
> 
> **推导**：
> - 无连续性条件时：$m(q+1)$ 个参数
> - 在 $m-1$ 个内部节点各施加 $q$ 个条件（函数值及 $q-1$ 阶导数的连续性）
> - 故 $m(q+1) - (m-1) \cdot q = m + q$

- 假如4个节点，也就是有3段曲线，形如 $ax^3+bx^2+cx+d$ 每个曲线是4个参数
- 3段曲线就是12个参数
- 4个节点不算两头，中间两个节点
- 因为曲线接头要连续，所以每个内部节点要求：位置连续，一阶导数（斜率）连续，二阶导数（弯曲程度）连续
- 所以每个节点有3个条件（约束），一共6个约束
- **自由度**就是"你能自由选择的东西的个数"
- 例如某个内部节点：
	- 值相等：$a_{1}x^3 + b_{1}x^2 + c_{1}x + d_{1} = a_{2}x^3 + b_{2}x^2 + c_{2}x + d_{2}$
	- 一阶导数相等：$3a_{1}x^2 + 2b_{1}x + c_{1} = 3a_{2}x^2 + 2b_{2}x + c_{2}$
	- 二阶单数相等：$6a_1x + 2b_{1} = 6a_{2}x + 2b_{2}$
	- 想象， 左边曲线一旦确定，那么：公式改写为
		- $a_{2}x^3 + b_{2}x^2 + c_{2}x + d_{2} = 固定值$
		- $3a_{2}x^2 + 2b_{2}x + c_{2} = 固定值$
		- $6a_{2}x + 2b_{2} = 固定值$
		- $b_{2},c_{2},d_{2}$ 都可以用 $a_{2}$ 表达，所以，可以自由调整的只有 $a_{2}$ 也就是只有 1 个自由度 
- 所以，m(q+1) 代表 一共多少参数， (m-1)q 代表 减去多少自由度（条件），**两个相减就是需要多少方程约束**

---

## 4.3 三次样条的边界条件

> [!tip] 边界条件类型
> 
> 对 $n = m+1$ 个节点的插值，自由度为 $m+3$，插值条件为 $n = m+1$，还差 **2 个条件**：
> 
> | 类型 | 条件 |
> |:---|:---|
> | **自然**（Natural） | $s''(a) = s''(b) = 0$ |
> | **夹紧**（Clamped） | $s'(a), s'(b)$ 给定 |
> | **周期**（Periodic） | $s(a) = s(b),\; s'(a) = s'(b)$ |
> 
> 无边界信息时，**自然样条**是标准选择

- 三次样条就是 q = 3
- 每个点 x，y 开始的方程  应该是  $y_{i} = a_{i}x^3+b_{i}x^2+c_{i}x+d_{i}$，也就是几个点就是几个方程，**几个方程就是几个条件**
- 由于自由度是 m + 3（对于三次样条），所以还剩下 m + 3 - (m + 1) = 2 个条件（参数）没有约束，所以需要再补充2个条件
- 有三种方法
	- 自然（两头放松，要求曲线在两端的二阶导数为 0，也就是两头不弯曲，自然地伸出去）
	- 夹紧（两头"钉死"，告诉你曲线在两端的斜率是多少）
	- 周期样条（要求起点和终点的值相等、斜率也相等）
---

## 4.4 B 样条

> [!important] B 样条基
> 
> 每个 $q$ 次样条都可以用 $m+q$ 个 **B 样条基函数** $B_k$ 的线性组合表示：（m 是样条曲线的**段数**）
> $$
> s(x) = \sum_{k=1}^{m+q} a_k B_k(x)
> $$

- 打个比方：B1​,B2​,… 是基本颜料（红、黄、蓝……）
- a1​,a2​,… 是每种颜料放多少
- 最后混出来的颜色就是你的样条曲线
- 数学家找到了 m+q 个特殊的曲线，叫 $B_{1},B_{2},\dots,\dots, B_{m+q}$
- 任何一条样条曲线，都可以写成这些基函数的"**加权组合**"
- $s(x) = a_{1}B_{1} + a_{2}B_{2} + \dots + a_{m+q}B_{m+q}$
- 权重 $a_1, a_2, \ldots, a_{m+q}$ 就是你要选的 $m+q$ 个参数
- **你有多少个点，就设多少个节点（或更多）**
- 点越多，m 越大，B 样条基函数的个数 m+q 也越多，能调的参数就越多
- 这个基函数和 q （次数）和 点序号有关的迭代，较为复杂，不用管


> [!abstract] Cox-de Boor 递推
> 
> **零次**：
> $$
> B_k^{(0)}(x) = \mathbf{1}(\tau_k \leq x < \tau_{k+1})
> $$
> 
> **递推**：
> $$
> B_k^{(q)}(x) = \frac{x - \tau_k}{\tau_{k+q} - \tau_k} B_k^{(q-1)}(x) + \frac{\tau_{k+q+1} - x}{\tau_{k+q+1} - \tau_{k+1}} B_{k+1}^{(q-1)}(x)
> $$

**举例：**

节点：τ₀=0, τ₁=1, τ₂=2, τ₃=3

参数：因为是1次，所以 q = 1， 由于 4 个节点，所以，m = 4 - 1 = 3 个段， 要根据定理，这个样条函数 需要用 m + q = 4 个 基函数 

**要计算1次的B函数，根据递推公式需要算0次的**

我们来算 **1 次** B 样条基函数 B₁⁽¹⁾(x)。

---

**第一步：写出 0 次基函数**

B₁⁽⁰⁾(x) = 1  当 1 ≤ x < 2，否则为 0

**第二步：用递推公式算 1 次**

B₁⁽¹⁾(x) = [(x - τ₁)/(τ₂ - τ₁)] B₁⁽⁰⁾(x) + [(τ₃ - x)/(τ₃ - τ₂)] B₂⁽⁰⁾(x)

代入节点值：

B₁⁽¹⁾(x) = (x - 1) B₁⁽⁰⁾(x) + (3 - x) B₂⁽⁰⁾(x)

**第三步：分段写出来**

- 当 1 ≤ x < 2：只有 B₁⁽⁰⁾=1，所以 B₁⁽¹⁾(x) = x - 1
- 其他地方：B₁⁽¹⁾(x) = 0

**结果**

B₁⁽¹⁾(x) = 
- x - 1，当 1 ≤ x < 2

同理计算 $B_{2}^{(1)}$

...
## 4.5 B 样条的数值优势

> [!success] 条件数对比（$n=20$ 个点）
> 
> | 基 | 多项式次数 | 条件数 |
> |:---|:---:|:---:|
> | 单项式基 | 19 | $\approx 10^{16}$ |
> | B 样条（三次，20 节点） | 3 | $\approx 10^{2}$ |
> 
> B 样条的条件数优 **14 个数量级**！

> [!tip] 计算效率
> 带状稀疏结构（每行仅 $q+1$ 个非零元素）使线性方程组求解从 $O(n^3)$ 降至 $O(nq^2)$

---

## 4.6 (B-)样条的主要性质

> [!success] 关键性质总结
> 
> 1. **逼近性**：节点间距足够小时，可任意精确地逼近任何连续函数
> 
> 2. **导数/积分封闭性**：$q$ 次样条的导数是 $q-1$ 次样条，积分是 $q+1$ 次样条
> 
> 3. **逼近传递性**：样条的导数/积分同时逼近原函数的导数/积分
> 
> 4. **数值稳定性**：B 样条条件数低
> 
> 5. **局部支撑**：$B_k(x) = 0$ 对 $x \notin [\tau_k, \tau_{k+q+1}]$
>    - 使逼近具有局部性（更稳定）
>    - 基矩阵具有带状结构（可高效求解）


```r
# 模拟数据点
set.seed(42)
x_data <- c(0, 1, 2, 3, 4, 5)
y_data <- c(1, 3, 2, 5, 4, 2) + rnorm(6, 0, 0.2)

# 使用 splines 包
library(splines)

# 创建三次 B 样条基（q = 3）
x_plot <- seq(0, 5, length.out = 200)

# 用 bs() 创建 B 样条基，然后拟合
B <- bs(x_data, df = 6, degree = 3)
model <- lm(y_data ~ B - 1)

# 预测曲线
B_plot <- bs(x_plot, df = 6, degree = 3, 
             Boundary.knots = attr(B, "Boundary.knots"),
             knots = attr(B, "knots"))
coefs <- coef(model)

# 把 NA 替换成 0
B_plot[is.na(B_plot)] <- 0
coefs[is.na(coefs)] <- 0

y_plot <- as.vector(B_plot %*% coefs)

# 计算加权基函数
weighted <- matrix(0, nrow = length(x_plot), ncol = ncol(B_plot))
for (i in 1:ncol(B_plot)) {
  weighted[, i] <- coefs[i] * B_plot[, i]
}

# 画图：三张图
par(mfrow = c(1, 3), mar = c(4, 4, 3, 1))

# 图1：数据点和拟合曲线
plot(x_data, y_data, pch = 19, cex = 1.5, col = "red",
     xlab = "x", ylab = "y", main = "1. B样条拟合",
     xlim = c(0, 5), ylim = c(0, 6))
lines(x_plot, y_plot, col = "blue", lwd = 2)

# 图2：各个 B 样条基函数
colors <- c("red", "orange", "green", "blue", "purple", "brown")
plot(NULL, xlim = c(0, 5), ylim = c(0, 1),
     xlab = "x", ylab = "B(x)", main = "2. B样条基函数")
for (i in 1:ncol(B_plot)) {
  lines(x_plot, B_plot[, i], col = colors[i], lwd = 2)
}
legend("topright", legend = paste0("B", 1:ncol(B_plot)), 
       col = colors, lty = 1, lwd = 2, cex = 0.8)

# 图3：加权后的基函数 + 总和
plot(NULL, xlim = c(0, 5), ylim = c(-1, 7),
     xlab = "x", ylab = "a × B(x)", main = "3. 加权叠加")

for (i in 1:ncol(B_plot)) {
  lines(x_plot, weighted[, i], col = colors[i], lwd = 2)
}

# 总和曲线（黑色粗线）
lines(x_plot, y_plot, col = "black", lwd = 4)

# 加上数据点
points(x_data, y_data, pch = 19, cex = 1.5, col = "red")

# 打印结果
cat("\n拟合系数：\n")
print(round(coefs, 3))
```
---

# 五、样条的理论性质

## 5.1 最小曲率性

> [!important] 定理：三次样条具有最小曲率
> 
> 设 $s$ 是插值 $(x_1, y_1), \ldots, (x_n, y_n)$ 的**自然三次样条**（即 $s''(a) = s''(b) = 0$）。
> 
> 若 $g \in \mathcal{C}^2[a,b]$ 是另一个满足 $g(x_i) = y_i\;\forall i$ 的函数，则
> $$
> \int_a^b |s''(x)|^2\,dx \leq \int_a^b |g''(x)|^2\,dx
> $$

> [!note] 直观理解
> 三次样条产生"最光滑"的曲线——在所有插值函数中**弯曲程度最小**

> [!example] 数值验证
> 
> 点 $(0,0), (1,1), (2,0)$ 的比较：
> 
> | 插值函数 | $\int_0^2 |f''(x)|^2\,dx$ |
> |:---|:---:|
> | 自然三次样条 $s$ | 6 |
> | 抛物线 $p(x) = -x^2 + 2x$ | 8 |
> 
> 样条的曲率更小！

---

## 5.2 逼近误差

> [!important] 定理：三次样条的逼近误差
> 
> 设 **网格步长** $h := \max_i |x_i - x_{i-1}|$。
> 
> 对每个 $f \in \mathcal{C}^4[a,b]$，存在三次样条 $s$ 满足 $f(x_i) = s(x_i)\;\forall i$ 且
> $$
> \max_{x \in [a,b]} |f(x) - s(x)| \leq C \cdot h^4 \max_{x \in [a,b]} |f^{(4)}(x)|
> $$
> 
> 其中 $C$ 为小常数（自然样条约为 $C = \frac{5}{384}$）

定理说的是：**在采样点之间**，样条曲线和原函数的误差有多大
- 你有一条真实的曲线 f(x)f(x)f(x)（比如 sin⁡x\sin xsinx）
- 你在几个点上采样，然后用三次样条把这些点连起来
- 在采样点上：样条和原函数完全重合，误差 = 0
- 在采样点之间：样条是猜的，可能和原函数有偏差
- 定理说：这个偏差最多是多少

> [!success] 实际意义
> 
> - **网格越密**（$h$ 越小）→ 逼近越好
> - **函数越光滑**（$f^{(4)}$ 越小）→ 逼近越好
> - **局部误差分析**建议在 $|f^{(4)}|$ 大的地方加密网格
 
> [!example] 数值验证
> 
> $f(x) = \sin(2\pi x)$，$\max|f^{(4)}(x)| = (2\pi)^4 \approx 1559$：
> 
> | 节点数 | 网格步长 $h$ | 误差上界 | 实际误差 |
> |:---:|:---:|:---:|:---:|
> | 5 | 0.25 | $\approx 0.08$ | $\approx 0.020$ |
> | 9 | 0.125 | $\approx 0.005$ | $\approx 0.001$ |
> | 17 | 0.0625 | $\approx 0.0003$ | $\approx 0.00006$ |
> 
> 观察：$h$ 减半 → 误差缩小为原来的 $2^4 = 16$ 分之一！

![[53013a9d-bd14-41a6-9b2f-accc28b572c7.png]]

```r
# 设定范围
a <- 0
b <- 2 * pi

# 8个不等间距的采样点
set.seed(123)
x_data <- sort(c(a, b, runif(6, a, b)))  # 确保包含端点
y_data <- sin(x_data)

# 计算网格步长 h
h <- max(diff(x_data))
cat("网格步长 h =", round(h, 3), "\n")

# 使用 splines 包
library(splines)

# 创建细密的 x 用于画图
x_plot <- seq(a, b, length.out = 500)
y_true <- sin(x_plot)

# 三次样条插值
# 使用 splinefun 做精确插值（经过所有点）
spline_fit <- splinefun(x_data, y_data, method = "natural")
y_spline <- spline_fit(x_plot)

# 计算误差
error <- y_true - y_spline
max_error <- max(abs(error))

# sin(x) 的四阶导数还是 sin(x)，最大值为 1
# 理论误差上界：C * h^4 * max|f^(4)|
C <- 5/384
theoretical_bound <- C * h^4 * 1

cat("实际最大误差 =", round(max_error, 6), "\n")
cat("理论误差上界 =", round(theoretical_bound, 6), "\n")

# 画图：三张图
par(mfrow = c(2, 2), mar = c(4, 4, 3, 1))

# 图1：原函数 vs 样条拟合
plot(x_plot, y_true, type = "l", col = "blue", lwd = 2,
     xlab = "x", ylab = "y", main = "sin(x) vs 三次样条")
lines(x_plot, y_spline, col = "red", lwd = 2, lty = 2)
points(x_data, y_data, pch = 19, cex = 1.5, col = "black")
legend("topright", 
       legend = c("sin(x)", "三次样条", "采样点"),
       col = c("blue", "red", "black"), 
       lty = c(1, 2, NA), pch = c(NA, NA, 19), lwd = 2)

# 图2：误差曲线
plot(x_plot, error, type = "l", col = "purple", lwd = 2,
     xlab = "x", ylab = "误差", main = "逼近误差 f(x) - s(x)")
abline(h = 0, lty = 3)
abline(h = c(-max_error, max_error), col = "red", lty = 2)

# 图3：放大看差异
plot(x_plot, y_true, type = "l", col = "blue", lwd = 2,
     xlab = "x", ylab = "y", main = "放大看（间隔最大处）",
     xlim = c(x_data[which.max(diff(x_data))], 
              x_data[which.max(diff(x_data)) + 1]))
lines(x_plot, y_spline, col = "red", lwd = 2, lty = 2)
points(x_data, y_data, pch = 19, cex = 1.5, col = "black")

# 图4：采样点分布
plot(x_data, rep(0, length(x_data)), pch = "|", cex = 2,
     xlab = "x", ylab = "", main = "采样点位置（不等间距）",
     xlim = c(a, b), yaxt = "n")
segments(x_data[-length(x_data)], 0, x_data[-1], 0, col = "gray")
text(x_data, rep(0.1, length(x_data)), round(x_data, 2), cex = 0.8)

# 标出最大间隔
idx <- which.max(diff(x_data))
segments(x_data[idx], -0.1, x_data[idx+1], -0.1, col = "red", lwd = 3)
text(mean(x_data[idx:(idx+1)]), -0.2, paste("h =", round(h, 2)), col = "red")
```

---

# 六、光滑化与回归

## 6.1 问题设定

> [!important] 光滑化模型
> $$
> y_i = f(x_i) + \epsilon_i
> $$
> 
> 其中 $\epsilon_i$ 独立同分布，$\mathbb{E}[\epsilon_i | x_i] = 0$

- 观测值 = 真实值 + 噪声(**是随机误差/噪声（秤不准、站的位置不同等）**)
- 每次测量的误差互不影响，而且误差的大小服从同一个规律
- 因为我们假设真实函数 $f(x)$ **是光滑的曲线**，而数据 $y_i$​ 因为有噪声所以上下跳动。我们的目标是从跳动的数据中，把那条光滑的曲线找出来。

---

## 6.2 求解方法

> [!important] 最小二乘求解
> 
> 令 $\hat{f}(x) = \sum_{k=1}^K a_k \phi_k(x)$，解
> $$
> \min_{\mathbf{a}} \|\mathbf{y} - \mathbf{Ba}\|_2^2
> $$

> [!abstract] 插值 vs. 光滑化
> 
> | 方法 | 基函数数量 | 特点 |
> |:---|:---:|:---|
> | **插值** | $K = n$ | 精确拟合所有点，但会拟合噪声 |
> | **光滑化** | $K < n$ | 允许 $\hat{f}(x_i) \neq y_i$，获得更光滑、可推广的函数 |

- **插值**：相信每个数据点都是对的。
- **光滑化**：知道数据有误差，找一条"差不多"的光滑曲线

---

## 6.3 应用场景选择

> [!tip] 方法选择指南
> 
> | 场景 | 推荐方法 |
> |:---|:---|
> | 精确、无噪声数据 | 插值 |
> | 有噪声测量 | 光滑化 |
> | 对新数据预测 | 光滑化 |

---

# 七、偏差-方差权衡

## 7.1 偏差分析

> [!important] 偏差（Bias）
> 
> 假设等距节点、$f \in \mathcal{C}^4$、三次样条，$K$ 个基函数对应 $h = O(1/K)$
> 
> 逼近误差（偏差）满足
> $$
> \max_{x \in [a,b]} |f(x) - \hat{f}(x)| = O(h^4) = O(K^{-4})
> $$
> 
> → 偏差小需要 $K$ 尽量**大**

- 基函数越多，曲线越能"跟上"真实函数的弯曲，偏差越小
- 意思是 K(**基函数个数**) 翻倍，偏差变成原来的 $\frac{1}{16}$
- 用乐高拼一条曲线,​ 用 5 块乐高, 只能拼个大概轮廓，细节丢失 用 500 块乐高  几乎完美复刻

---

## 7.2 方差分析

> [!important] 方差（Variance）
> 
> 假设 $\text{Var}[\epsilon_i] = \sigma^2$（同方差），可证明
> $$
> \text{Var}[\hat{f}(x)] = O(K/n)
> $$
> 
> → 方差小需要 $K$ 尽量**小**

- 因为 $\hat{f}(x)$ 是用**带噪声的数据**算出来的,  数据有随机性 → 算出的曲线也有随机性 → 所以有方差
- n - 采集数据点个数
- K - 选择的基函数个数
- 数据越多，方差越小，因为噪声被平均掉了
- O(K/n) 就是说 方差 和 K/n 成正比

---

## 7.3 MSE 分解

> [!important] MSE 的偏差-方差分解
> $$
> \mathbb{E}\left[(\hat{f}(x) - f(x))^2\right] = \underbrace{\left(\mathbb{E}[\hat{f}(x)] - f(x)\right)^2}_{\text{偏差}^2} + \underbrace{\text{Var}[\hat{f}(x)]}_{\text{方差}} = O(K^{-8} + K/n)
> $$

> [!success] 最优选择
> 理论最优 $K \sim n^{1/9}$ 平衡两者，此时 $\text{MSE} = O(n^{-8/9})$
> 
> 但这是渐近阶，实践中合适的 $K$ 通常远大于此

- 总误差 = 偏差² + 方差   这两个东西此消彼长。
- 偏差²  - 模型表达能力不够  K 变大 ==》 偏差变小  $O(K^{-8})$     (根据公式 -4 次方分之1的平方)
- 方差 - 数据噪声带来的波动   K 太大 》方差大（**过拟合**）
- 要找一个中间点，让总误差最小
- 打个比方，画人像
	- 用 5 笔画（K 小）：只能画个轮廓，不像（偏差大）
	- 用 500 笔画（K 大）：手一抖就画歪了（方差大）
	- 用 50 笔画（K 适中）：刚好，又像又稳

---

## 7.4 数值示例

> [!example] 偏差-方差权衡示例
> 
> $f(x) = \sin(3x)$，$n = 100$，$\sigma = 0.3$：
> 
> | 模型 | $K$ | 偏差² | 方差 | MSE |
> |:---|:---:|:---:|:---:|:---:|
> | **欠拟合** | 5 | **0.42** | 0.02 | 0.44 |
> | **最优** | 15 | 0.03 | 0.05 | **0.07** |
> | **过拟合** | 40 | 0.00 | **0.12** | 0.12 |

---

## 7.5 实践中的模型选择

> [!tip] 选择 $K$ 的方法
> 
> - **交叉验证**
> - **信息准则**：AIC、BIC
> - **广义交叉验证**：
> $$
> \text{GCV}(K) = \frac{\text{RSS}_K / n}{(1 - K/n)^2}
> $$

> [!success] 实用方案
> 选择**足够大**的 $K$，通过**惩罚项**控制光滑度
> 
> → P 样条、广义加性模型（GAM）
> 
> 在 R 中：`mgcv::gam()`

---

# 八、多变量函数逼近

- 多维情况下，把每个维度的基函数乘起来，组成新的基函数。系数还是用最小二乘求
- 张量积就是把一维的方法"乘"到多维，最小二乘照样能用，但维度高了就炸了。

## 8.1 张量积基

> [!important] 张量积基构造
> 
> 对多变量函数 $f: \mathbb{R}^p \to \mathbb{R}$：
> $$
> \hat{f}(\mathbf{x}) = \sum_{j_1=1}^K \cdots \sum_{j_p=1}^K a_{j_1, \ldots, j_p} \left(\phi_{j_1}(x_1) \times \cdots \times \phi_{j_p}(x_p)\right)
> $$
> 
> 系数 $\mathbf{A} = (a_{j_1, \ldots, j_p}) \in \mathbb{R}^{K \times \cdots \times K}$ 仍可通过普通最小二乘求解

- 想象你在分析一个业务指标，比如“用户活跃度”。
	- **一维情况：** 你认为活跃度只和“注册时长”有关。你找了一组“基函数”（比如简单的 $1, x, x^2$），试图用它们的组合去拟合曲线。这就是你熟悉的单变量回归
	- **多维情况：** 现实是，活跃度既受“注册时长 ($x_1$​)”影响，也受“APP打开次数 ($x_2$​)”影响。
- **问题来了：** 我们已经有了一套处理 $x_1$​ 的基函数，也有了一套处理 $x_2$​ 的基函数。**怎么把这两套独立的“工具箱”组合起来，去描述 $x_1$​ 和 $x_2$​ 共同作用下的复杂曲面呢？
- 最简单的思维方式就是：**让每一维度的可能性“互相碰撞”**
- 这就是所谓的“张量积基”。我们可以这样理解:
	- 假设 $x_1$​ 方向有 2 个基础形状：`常数(2)` 和 `线性(x₁)`
	- 假设 $x_2$​ 方向也有 2 个基础形状：`常数(3)` 和 `线性(x₂)`
	- 当我们把它们“乘”起来时，就得到了 **2 × 2 = 4** 个新的形状
		- 2×3=6 （整体偏置）
		- $2 \times x_2 = x_2$ （$x_2$​ 的独立影响）
		- $x_1 \times 3 = x_1$ （$x_1$​ 的独立影响）
		- $x_1 \times x_2 = x_1x_{2}$​ （**重点：这是两者产生的交互影响**）
	- **这就是张量积的本质：** 它通过把各维度的基函数两两相乘，确保拟合出来的不仅有每个维度的“独奏”，还有维度之间的“合唱”（交互作用）。
	- 写出最终的拟合函数: 你的拟合函数就是把这些“新积木”配上系数（$a_1, a_2, a_3, a_4$​）加起来:
		- $\hat{f}(x_1, x_2) = a_1(\mathbf{6}) + a_2(\mathbf{2x_2}) + a_3(\mathbf{3x_1}) + a_4(\mathbf{x_1x_2})$
		- 因为最后有系数 a1 .。。 a4 存在，所以，两个参数的 常数项目 不需要 2 和 3，用 1 即可，最后都要算出 a1 .。 a4
		-  $\hat{f}(x_1, x_2) = a_1 + a_2(\mathbf{x_2}) + a_3(\mathbf{x_1}) + a_4(\mathbf{x_1x_2})$
		- 将其写成 **$A\mathbf{x} = \mathbf{b}$** 的形式：$$ \underbrace{ \begin{bmatrix} 1 & x_{12} & x_{11} & x_{11}x_{12} \\ 1 & x_{22} & x_{21} & x_{21}x_{22} \\ \vdots & \vdots & \vdots & \vdots \\ 1 & x_{n2} & x_{n1} & x_{n1}x_{n2} \end{bmatrix} }_{\text{矩阵 } A \ (n \times 4)} \underbrace{ \begin{bmatrix} a_1 \\ a_2 \\ a_3 \\ a_4 \end{bmatrix} }_{\text{待求向量 } \mathbf{x}} = \underbrace{ \begin{bmatrix} y_1 \\ y_2 \\ \vdots \\ y_n \end{bmatrix} }_{\text{观测结果 } \mathbf{b}} $$
		- **注意，上面矩阵里面不同行代表不同组的观测数据，例如第二行代表第二组观测数据里面的 x2，x1**  ==》那不同参数要求观测数量要一致，不能一个100组，一个10组
		- **如果不同参数里面参数数量不一样，没有关系，矩阵知识被拉长了，例如第一个参数有2个参数，第二个有3个参数，那一共有6个参数**， 构建成了一个 2 x 3 的矩阵（数组），也就是二阶张量
		- 这就可以继续用二乘法或者梯度下降来计算 a1...a4   
			- 正规方程：$\mathbf{x} = (A^T A)^{-1} A^T \mathbf{b}$
	- 这个转换过程完美展示了**“张量积”**在实际运算中是如何落地的：
		- 它把多维的复杂关系，通过**“列扩展”**的方式，转变成了你在线性代数初步里学过的最简单的**线性方程组求解**问题。
		- **维度炸弹就在 A 的列数里：** 如果你有 10 个变量，每个变量有 10 个基函数，那么这个 A 矩阵就会有 $10^{10}$ 列——这就是为什么说维度高了内存会炸。
	
---

## 8.2 维数诅咒

> [!danger] 存储问题
> 
> $K = 10$ 时：
> 
> | 维度 $p$ | 系数数量 $K^p$ | 内存 |
> |:---:|:---:|:---:|
> | 1 | 10 | 80 字节 |
> | 2 | 100 | 800 字节 |
> | 5 | 100,000 | 800 KB |
> | 10 | $10^{10}$ | **80 GB** |

> [!danger] 收敛问题
> 
> 最优 $K \sim n^{1/(8+p)}$ 时
> $$
> \text{MSE} = O(n^{-8/(8+p)})
> $$
> 
> 随 $p$ 增大收敛越来越慢，高维问题需要**指数级更多数据**！

---

## 8.3 解决方案：广义加性模型（GAM）

> [!success] GAM 的加性结构
> 
> 用加性结构代替完整张量积：
> $$
> \hat{f}(\mathbf{x}) = \beta_0 + \sum_{i=1}^p f_i(x_i) \quad (pK \text{ 个系数，而非 } K^p)
> $$

> [!tip] GAM 的优势
> 
> | 方面 | 说明 |
> |:---|:---|
> | **可扩展** | $O(pK)$ 而非 $O(K^p)$ 个参数 |
> | **可解释** | 每个变量的效应可单独可视化 |
> | **自动调参** | 在 R 中：`mgcv::gam()` 自动选择光滑参数 |

---

# 九、总结

## 📌 方法对比

> [!tip] 插值方法选择
> 
> | 方法 | 优点 | 缺点 |
> |:---|:---|:---|
> | **多项式插值** | 理论简单 | 条件数差，Runge 现象 |
> | **样条插值** | 稳定，局部性好 | 需要选择节点 |
> | **B 样条** | 数值稳定，高效 | 实现稍复杂 |

---

## 📝 核心公式速查

> [!summary] 关键公式
> 
> **插值条件的矩阵形式**
> $$
> \mathbf{Ba} = \mathbf{y}, \quad B_{ik} = \phi_k(x_i)
> $$
> 
> **三次样条逼近误差**
> $$
> \max |f(x) - s(x)| \leq C \cdot h^4 \max |f^{(4)}(x)|
> $$
> 
> **MSE 分解**
> $$
> \text{MSE} = \text{偏差}^2 + \text{方差} = O(K^{-8} + K/n)
> $$
> 
> **GCV**
> $$
> \text{GCV}(K) = \frac{\text{RSS}_K / n}{(1 - K/n)^2}
> $$

---

## ✅ 自测清单

- [ ] 能区分逼近、插值、光滑化三种问题
- [ ] 理解为什么插值问题有无穷多解
- [ ] 能解释多项式插值的条件数问题
- [ ] 能描述 Runge 现象及其原因
- [ ] 理解样条的定义和参数计数
- [ ] 能解释 B 样条的局部支撑性质
- [ ] 理解三次样条的最小曲率性质
- [ ] 能写出偏差-方差分解公式
- [ ] 理解维数诅咒及 GAM 的解决思路
