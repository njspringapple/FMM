
## 故事：你要造一个房价预测机器

### 第一章：你有什么数据

你收集了1000套房子的资料。

每套房子你记录了3个信息：面积、楼层、房龄。

每套房子你也知道真实成交价。

现在定义几个词：

- **样本数量**（$N$）：你有多少套房子的数据。这里 $N = 1000$。
- **特征数量**（$d$）：每套房子你记录了几个信息。这里 $d = 3$（面积、楼层、房龄）。
- **输出值数量**（$m$）：你想预测几个东西。如果只预测房价，$m = 1$。如果同时预测房价和租金，$m = 2$。
- **第 $i$ 个样本的特征**（$\mathbf{x}_i$）：一个包含 $d$ 个数字的向量。比如第1套房子是（面积80，楼层5，房龄10），写成 $\mathbf{x}_1 = (80, 5, 10)$。
- **第 $i$ 个样本的真实值**（$y_i$）：第 $i$ 套房子的真实成交价。

---

### 第二章：目标

你想造一台机器：

- 输入：一套房子的 $d$ 个特征 $\mathbf{x}$
- 输出：这套房子的预测房价

你给它特征，它吐出预测值，这就是**预测**（$f(\mathbf{x}; \theta)$）。

这里 $\theta$ 是所有旋钮的集合，$f$ 是机器内部的计算规则。

---

### 第三章：机器的构造

机器内部有几个旋钮，每个旋钮控制一个特征的重要程度。

- 旋钮1（$w_1$）：面积有多重要
- 旋钮2（$w_2$）：楼层有多重要
- 旋钮3（$w_3$）：房龄有多重要

这些旋钮的数值就叫**权重**（$\theta$ 或 $\mathbf{w}$）。

最简单的机器是这样算的：

$$
\text{预测房价} = w_1 \times \text{面积} + w_2 \times \text{楼层} + w_3 \times \text{房龄}
$$

写成数学符号：

$$
f(\mathbf{x}; \theta) = w_1 x_1 + w_2 x_2 + w_3 x_3 = \mathbf{w}^\top \mathbf{x}
$$

**旋钮数量 = 特征数量 × 输出值数量 = $d \times m$**

- 3个特征、1个输出：3个旋钮
- 3个特征、2个输出：6个旋钮
- 10000个特征、10个输出：100000个旋钮

**问题来了**：这些旋钮应该调到多少？你不知道。

---

### 第四章：训练——让机器自己学会

你有 $N = 1000$ 个样本。

**训练**（Training）就是：让机器用这 $N$ 个样本，自己找到最好的旋钮位置 $\theta$。

---

### 第五章：损失函数——衡量机器有多差

你先随便调旋钮，让机器预测这 $N$ 套房子的价格。

对于第 $i$ 个样本：

- 预测值：$f(\mathbf{x}_i; \theta)$
- 真实值：$y_i$
- 单个样本的误差：$\ell(f(\mathbf{x}_i; \theta), y_i)$

把所有样本的误差加起来取平均，就是**损失函数**（Loss Function）：

$$
L(\theta) = \frac{1}{N} \sum_{i=1}^{N} \ell(f(\mathbf{x}_i; \theta), y_i)
$$

用人话说：遍历第1个到第 $N$ 个样本，每个样本算一个误差，加起来除以 $N$。

**损失函数**就是一个评分器：

- 输入：当前的旋钮位置 $\theta$
- 输出：一个数字 $L(\theta)$，告诉你机器有多差

预测越准，$L(\theta)$ 越小。预测越差，$L(\theta)$ 越大。

---

### 第六章：优化目标

训练的目标就是：

$$
\min_{\theta \in \mathbb{R}^d} L(\theta)
$$

用人话说：在所有可能的旋钮位置中，找一个让损失最小的。

$\theta \in \mathbb{R}^d$ 的意思是：$\theta$ 是 $d$ 个实数组成的向量。

---

### 第七章：怎么调旋钮？

现在你知道机器有多差了，但往哪个方向调旋钮能变好？

**方案1：瞎调**

随便试，碰运气。

缺点：太慢，旋钮多了根本试不完。

**方案2：挨个试（朴素方法）**

每个旋钮单独试：往左拧一点看损失变大还是变小，往右拧一点再看。

对于 $d$ 个旋钮，要试 $d$ 次。

缺点：如果 $d = 100000$，要试100000次，太慢。

这种笨办法就叫**朴素方法**（Naive Method）。

**方案3：算导数（梯度）**

导数直接告诉你：每个旋钮往哪个方向拧，损失会变小。

对于 $d$ 个旋钮，一次算出 $d$ 个导数：

$$
\nabla_\theta L = \left( \frac{\partial L}{\partial w_1}, \frac{\partial L}{\partial w_2}, \ldots, \frac{\partial L}{\partial w_d} \right)
$$

这个向量叫**梯度**（Gradient），包含 $d$ 个偏导数，就是**高维导数**。

选方案3。

---

### 第八章：梯度下降——一步一步调

知道方向后，怎么调？

1. 算出当前的梯度 $\nabla_\theta L$
2. 沿着梯度的反方向，把旋钮拧一小步：$\theta_{\text{new}} = \theta_{\text{old}} - \eta \cdot \nabla_\theta L$
3. 重复很多次

这里 $\eta$ 叫**学习率**（Learning Rate），控制每一步走多大。

这个过程叫**梯度下降**（Gradient Descent），也是**训练**的核心。

---

### 第九章：新问题——山不是简单的碗

如果 $L(\theta)$ 是一个简单的碗形，只有一个最低点，那你一直往下走，一定能到。这种函数叫**凸函数**（Convex）。

但真实的 $L(\theta)$ 可能坑坑洼洼，有很多小坑。这种函数叫**非凸函数**（Non-convex）。

- **局部最小值**（Local Minimum）：一个小坑，周围都比它高，但不是全局最低。
- **鞍点**（Saddle Point）：从某些方向看是最低点，从另一些方向看是最高点，像马鞍中间。梯度为零，但不是最低点。

---

### 第十章：新问题——样本太多

你有 $N$ 个样本。每走一步，要用全部 $N$ 个样本算一次梯度。

如果 $N = 1000000$，每一步都算100万次，太慢了。

**方案：Minibatch（小批量）**

每一步不用全部 $N$ 个样本，只随机拿 $B$ 个样本来算。$B$ 通常是32、64、128。

梯度的近似：

$$
\nabla_\theta L \approx \frac{1}{B} \sum_{i \in \text{batch}} \nabla_\theta \ell(f(\mathbf{x}_i; \theta), y_i)
$$

算出来的方向不完全准确，但大致对。走很多步，平均下来效果差不多，而且快得多。

---

### 第十一章：新问题——数值不稳定

你在算的时候，数据本身可能有一点点测量误差。

比如真实面积是100平米，但测量时记成了100.01平米。这个小变化叫**扰动**（Perturbation）。

记作 $\mathbf{x} + \Delta \mathbf{x}$，其中 $\Delta \mathbf{x}$ 是扰动。

有些问题对扰动很敏感：$\Delta \mathbf{x}$ 很小，但结果变化很大。这种问题叫**病态**（Ill-conditioned）。

衡量病态程度的指标叫**条件数**（Condition Number）。条件数越大，越病态，越不稳定。

---

### 第十二章：旋钮变多了——真实模型有多大

刚才只有 $d = 3$ 个特征、$m = 1$ 个输出，所以只有3个旋钮。

真实的机器学习模型有多少旋钮？

| 模型 | 参数量（旋钮数） | 样本数量 $N$ |
|------|------------------|--------------|
| **逻辑回归**（Logistic Regression） | 几千 | 几万 |
| **ResNet-50** | 2500万 | 上百万张图片 |
| GPT-3 | 1750亿 | 上万亿个词 |

旋钮越多，能力越强，但也越难训练。

如果用**朴素方法**，$d = 25000000$ 个旋钮要算2500万次，太慢了。

---

### 第十三章：张量——数据的形状

刚才的权重 $\mathbf{w}$ 是 $d$ 个数字，排成一列，叫**向量**（Vector）。

但复杂模型里，权重可能是：

- **向量**：一列数字，形状是 $(d,)$
- **矩阵**（Matrix）：一个表格，形状是 $(m, n)$
- **张量**（Tensor）：三维或更高维的数据块，比如形状是 $(a, b, c)$

比如一张彩色图片是三维张量：宽 × 高 × 颜色通道（RGB），形状是 $(224, 224, 3)$。

处理张量的函数，就叫**张量函数**（Tensor Function）。

神经网络里到处都是张量，所以需要**张量函数的导数**。

---

### 第十四章：完整流程回顾

| 步骤 | 做什么 | 涉及的概念和符号 |
|------|--------|------------------|
| 0 | 收集数据 | 样本数量 $N$，特征数量 $d$，输出值数量 $m$，特征 $\mathbf{x}_i$，真实值 $y_i$ |
| 1 | 造一台机器，有 $d$ 个旋钮 | 权重 $\theta$ 或 $\mathbf{w}$ |
| 2 | 给输入特征，机器吐出预测值 | 预测 $f(\mathbf{x}; \theta)$ |
| 3 | 比较 $N$ 个预测和真实的差距，算平均分 | 损失函数 $L(\theta) = \frac{1}{N} \sum_{i=1}^{N} \ell(f(\mathbf{x}_i; \theta), y_i)$ |
| 4 | 算出每个旋钮往哪拧能让损失变小 | 梯度 $\nabla_\theta L$，高维导数 |
| 5 | 拧一小步，重复很多次 | 梯度下降 $\theta \leftarrow \theta - \eta \nabla_\theta L$，训练 |
| 6 | 山不是碗，可能卡住 | 非凸，鞍点，局部最小值 |
| 7 | 样本太多，每次只用 $B$ 个 | Minibatch |
| 8 | 数据有误差，结果可能不稳定 | 扰动 $\Delta \mathbf{x}$，病态，条件数 |
| 9 | 旋钮太多，笨方法太慢 | 朴素方法 |
| 10 | 需要聪明的数学方法处理高维数据 | 张量，张量函数 |

---

### 第十五章：那个公式完整解释

$$
\min_{\theta \in \mathbb{R}^d} L(\theta) = \frac{1}{N} \sum_{i=1}^{N} \ell(f(\mathbf{x}_i; \theta), y_i)
$$

| 符号 | 含义 |
|------|------|
| $\theta$ | 所有权重（旋钮），共 $d$ 个 |
| $\theta \in \mathbb{R}^d$ | $\theta$ 是 $d$ 个实数组成的向量 |
| $\mathbf{x}_i$ | 第 $i$ 个样本的特征向量 |
| $y_i$ | 第 $i$ 个样本的真实值 |
| $f(\mathbf{x}_i; \theta)$ | 用当前旋钮 $\theta$ 对第 $i$ 个样本的预测 |
| $\ell(\cdot, \cdot)$ | 单个样本的损失（预测和真实的差距） |
| $\sum_{i=1}^{N}$ | 从第1个样本加到第 $N$ 个样本 |
| $\frac{1}{N}$ | 除以样本数量，取平均 |
| $\min$ | 找让这个值最小的 $\theta$ |

**用人话说**：在 $d$ 维空间里找一组旋钮位置 $\theta$，让机器在 $N$ 个样本上的平均预测误差最小。
