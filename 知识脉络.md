## 全局总览

本课程围绕一个核心问题展开：**如何在计算机上可靠、高效地求解统计学和数据科学中的数学问题？** 全部知识沿四条主线展开，最终在"优化"和"函数逼近"两大应用中汇合。

```
主线一：数值基础        →→→  "计算能不能信？"
主线二：矩阵代数与分解  →→→  "方程怎么解？"
主线三：多元微分学      →→→  "函数怎么变化？"
主线四：凸性与优化      →→→  "最优解怎么找？"

            ╲        ╲       ╱        ╱
             ╲        ╲     ╱        ╱
              ╲        ╲   ╱        ╱
               ▼        ▼ ▼        ▼
        ┌──────────────────────────────┐
        │  终极应用：                    │
        │  · 统计估计（MLE、回归）       │
        │  · 机器学习（神经网络训练）     │
        │  · 函数逼近（插值、样条、GAM） │
        └──────────────────────────────┘
```

---

## 主线一：数值基础——"计算能不能信？"

> **核心命题**：计算机的一切计算都是近似的，所以"怎么算"比"算什么"更重要。

### 层级 1.1：浮点运算与算法概念（第2章）

**你需要先理解：**
- 计算机只能表示有限多个数、做有限次运算

**核心知识点：**
- 舍入误差与累积
- 灾难性消去（两个几乎相等的大数相减）
- 浮点加法不满足结合律
- 算法的定义与分类（精确/近似/迭代/概率）
- 时间复杂度与空间复杂度

**目的：** 建立"计算不是精确的"这一根本意识，为后续一切误差分析奠基。

---

### 层级 1.2：Landau 记号（第2章）

**前置：** 层级 1.1（复杂度概念）

**核心知识点：**
- 大 $O$：增长上界（至多同阶）
- 小 $o$：严格低阶（可忽略）
- 计算规则（加法、乘法、吸收）

**目的：** 提供描述"增长趋势"和"误差阶数"的通用语言。后续几乎所有章节都要用到。

---

### 层级 1.3：误差度量（第4章前半）

**前置：** 层级 1.2 + 向量范数概念（层级 2.1）

**核心知识点：**
- 绝对误差与相对误差的严格定义
- 误差界
- 误差分解：总误差 = 算法误差 + 输入传播误差

**目的：** 将"误差"从模糊概念变成可量化、可分析的对象。

---

### 层级 1.4：条件数（第4章后半）

**前置：** 层级 1.3（误差度量）+ 层级 2.2（矩阵范数）

**核心知识点：**
- 绝对条件数与相对条件数
- 矩阵条件数 $\kappa(\mathbf{A}) = \|\mathbf{A}\|\cdot\|\mathbf{A}^{-1}\|$
- 良态 vs 病态 vs 不适定
- 向量加法的条件数分析（灾难性消去的理论解释）

**目的：** 回答"问题本身有多敏感？"——这决定了无论用什么算法，误差的下限在哪里。

---

### 层级 1.5：稳定性（第4章末尾）

**前置：** 层级 1.4（条件数）

**核心知识点：**
- 算法稳定性的含义
- 复合算法的稳定性定理
- "条件数差的步骤应尽早执行"
- 方差两种算法的稳定性对比

**目的：** 回答"算法本身是否放大误差？"——这与条件数共同决定计算结果的可靠性。

---

### 主线一的依赖链

```
浮点运算 + 算法概念
        │
        ▼
   Landau 记号 ──────────────────────────────► 贯穿全课程
        │
        │    ┌─── 向量/矩阵范数（主线二）
        │    │
        ▼    ▼
    误差度量（绝对/相对误差）
        │
        ▼
     条件数 ─────────────────────────────────► 最小二乘条件分析
        │                                     ► 多项式插值病态性
        ▼                                     ► 优化收敛分析
     稳定性 ─────────────────────────────────► 算法选择的核心判据
```

---

## 主线二：矩阵代数与分解——"方程怎么解？"

> **核心命题**：统计学的大量计算归结为求解线性方程组，而高效稳定的求解依赖矩阵分解。

### 层级 2.1：矩阵迹与范数（第3章）

**前置：** 线性代数基础（特征值、正交性）+ Landau 记号

**核心知识点：**

**迹（Trace）：**
- 定义：对角元之和
- 循环性质：$\text{tr}(\mathbf{AB}) = \text{tr}(\mathbf{BA})$
- 迹 = 特征值之和

**Frobenius 范数：**
- 定义与计算
- 与迹的联系：$\|\mathbf{A}\|_F^2 = \text{tr}(\mathbf{A}^\top\mathbf{A})$
- 局限性：忽略变换结构

**算子范数：**
- 谱范数、列和范数、行和范数
- 几何意义：最大拉伸倍数

**正交矩阵：**
- 保长度、条件数为1
- 逆等于转置

**Schatten 范数：**
- 统一框架：谱范数($p=\infty$)、Frobenius($p=2$)、核范数($p=1$)

**目的：** 建立度量矩阵"大小"的工具箱。范数是条件数、误差分析、优化正则化的共同基础。

---

### 层级 2.2：矩阵分解——LU 与 Cholesky（第6章）

**前置：** 层级 2.1（范数、条件数）+ 层级 1.1（算法复杂度）

**核心知识点：**

**LU 分解：**
- $\mathbf{A} = \mathbf{LU}$（高斯消元的矩阵表述）
- 前向替换 + 回代求解
- 选主元保证稳定性
- 复杂度：分解 $O(n^3)$，求解 $O(n^2)$

**Cholesky 分解（SPD 矩阵专用）：**
- $\mathbf{A} = \mathbf{LL}^\top$
- 比 LU 快约一倍、天然稳定
- 应用：多元正态分布采样

**核心原则：永远不要直接求逆矩阵！**

**目的：** 提供求解线性方程组的高效稳定算法。Cholesky 分解尤其重要，因为统计学中大量出现 SPD 矩阵（协方差矩阵、Gram 矩阵、Fisher 信息矩阵）。

---

### 层级 2.3：SVD——奇异值分解（第8章）

**前置：** 层级 2.1（谱范数、谱分解）+ 层级 2.2（矩阵分解思想）

**核心知识点：**
- $\mathbf{A} = \mathbf{U}\boldsymbol{\Sigma}\mathbf{V}^\top$：旋转-拉伸-旋转
- 奇异值 = $\sqrt{\lambda_i(\mathbf{A}^\top\mathbf{A})}$
- 简化 SVD 与 Moore-Penrose 伪逆
- 四个基本子空间的显式正交基
- 最优低秩近似（Eckart-Young 定理）
- 谱范数 = 最大奇异值

**目的：** SVD 是全课程的枢纽。它同时连接：
- 条件数（$\kappa = \sigma_{\max}/\sigma_{\min}$）
- 最小二乘（伪逆）
- 数据压缩与降维（PCA）
- 推荐系统

---

### 层级 2.4：最小二乘法（第5章）

**前置：** 层级 2.2（LU/Cholesky）+ 层级 2.3（SVD/伪逆）+ 层级 1.4（条件数）

**核心知识点：**
- 问题定义：最小化 $\|\mathbf{Ax} - \mathbf{b}\|_2$
- 几何解释：投影到列空间
- 正规方程：$\mathbf{A}^\top\mathbf{A}\hat{\mathbf{x}} = \mathbf{A}^\top\mathbf{b}$
- 条件数分析：$\kappa(\mathbf{A}^\top\mathbf{A}) \approx \kappa(\mathbf{A})^2$（条件数平方化！）
- 三种求解方法对比：
  - Cholesky（快但可能不稳定）
  - QR（通用首选）
  - SVD（最稳定）

**目的：** 最小二乘是线性回归的数学基础，也是统计学中最核心的计算任务之一。三种方法的对比体现了速度-稳定性的权衡。

---

### 层级 2.5：迭代与概率方法（第7章）

**前置：** 层级 2.3（SVD/特征值）+ 层级 1.4（条件数/收敛性）

**核心知识点：**

**迭代方法：**
- 幂方法（求最大特征值）
- QR 迭代（求所有特征值）
- 线性方程组迭代求解（Richardson、Jacobi、Gauss-Seidel）
- 收敛分析：$\|\mathbf{x}^{(k)} - \mathbf{x}\| \leq \rho^k\|\mathbf{x}^{(0)} - \mathbf{x}\|$

**概率方法：**
- 矩阵草图（matrix sketching）
- 随机嵌入：高概率保持距离
- Johnson-Lindenstrauss 型定理

**目的：** 当问题规模太大（$n > 10^6$）或矩阵稀疏时，直接方法不可行。迭代方法以"精度换速度"，概率方法以"确定性换效率"。应用于 PageRank、PCA、大规模最近邻搜索。

---

### 主线二的依赖链

```
矩阵迹 + 范数 + 正交矩阵
        │
        ├─────────────────────┐
        ▼                     ▼
   LU / Cholesky            SVD
   (直接方程组求解)     (分解 + 伪逆 + 低秩近似)
        │                     │
        ├──────┬──────────────┤
        ▼      ▼              ▼
    最小二乘（三种方法）   迭代/概率方法
        │                     │
        ▼                     ▼
    线性回归              PCA、PageRank
    正则化回归            大规模数据处理
```

---

## 主线三：多元微分学——"函数怎么变化？"

> **核心命题**：优化需要导数，而统计学中的函数输入输出可以是向量、矩阵甚至张量。需要一套统一的导数理论。

### 层级 3.1：张量与多重线性映射（第9章）

**前置：** 线性代数基础（基、线性映射）+ 层级 2.3（SVD 的外积形式）

**核心知识点：**
- 多重线性映射的定义（对每个参数分别线性）
- 张量的定义（多维数组）
- 外积 $\mathbf{v}\otimes\mathbf{w} = \mathbf{v}\mathbf{w}^\top$：秩-1 矩阵
- 张量积与 Kronecker 积
- 向量空间的张量积：$\dim(V\otimes W) = \dim V \cdot \dim W$

**目的：**
- 理解深度学习中的数据结构（图像 = 3阶张量，批次 = 4阶张量）
- 为高阶导数（Hessian = 2阶张量）和多元函数逼近（张量积基）提供语言
- SVD 的外积形式 $\mathbf{A} = \sum \sigma_i \mathbf{u}_i \otimes \mathbf{v}_i$ 统一了分解与近似

---

### 层级 3.2：Fréchet 导数——统一的导数概念（第10章）

**前置：** 层级 2.1（赋范空间、算子范数）+ 层级 1.2（小 $o$ 记号）

**核心知识点：**

**核心定义：**
$$f(\mathbf{x}+\mathbf{h}) = f(\mathbf{x}) + D_\mathbf{x}f(\mathbf{h}) + o(\|\mathbf{h}\|)$$
导数 = 最佳线性近似

**特殊情况：**

| 输入→输出 | 导数形式 | 记号 |
|:---|:---|:---|
| 向量→标量 | 梯度 | $\nabla f \in \mathbb{R}^{1\times n}$ |
| 向量→向量 | Jacobi 矩阵 | $\mathbf{J}_f \in \mathbb{R}^{m\times n}$ |
| 标量→矩阵 | 矩阵导数 | $\partial\mathbf{F}/\partial x$ |
| 矩阵→标量 | 矩阵梯度 | $\partial f/\partial\mathbf{X}$ |

**重要公式：**
- $\nabla(\mathbf{a}^\top\mathbf{x}) = \mathbf{a}^\top$
- $\nabla(\mathbf{x}^\top\mathbf{A}\mathbf{x}) = 2\mathbf{x}^\top\mathbf{A}$（$\mathbf{A}$ 对称时）
- 行列式导数公式

**目的：** 为梯度下降、牛顿法、反向传播提供统一的数学框架。一个定义涵盖所有情况。

---

### 层级 3.3：运算法则（第11章前半）

**前置：** 层级 3.2（Fréchet 导数定义）

**核心知识点：**
- 可微 $\Rightarrow$ 连续（反之不成立）
- 导数算子的线性性
- **乘积法则**：$D_\mathbf{x}[fg] = (D_\mathbf{x}f)\cdot g + f\cdot(D_\mathbf{x}g)$
- **链式法则**：$D_\mathbf{x}(g\circ f) = D_{f(\mathbf{x})}g \circ D_\mathbf{x}f$
- 应用：逻辑回归梯度、欧氏范数梯度

**目的：** 链式法则是反向传播的数学本质。乘积法则用于推导二次型、迹函数的导数。

---

### 层级 3.4：高阶导数与 Taylor 展开（第11章后半）

**前置：** 层级 3.3（链式法则）+ 层级 3.1（张量概念）

**核心知识点：**

**Hessian 矩阵：**
- $\mathbf{H}_f(\mathbf{x})_{i,j} = \frac{\partial^2 f}{\partial x_i \partial x_j}$
- Schwarz 定理：混合偏导相等 → Hessian 对称
- 临界点分类：正定→极小，负定→极大，不定→鞍点

**Taylor 展开：**
- 一阶：$f(\mathbf{x}+\mathbf{h}) \approx f(\mathbf{x}) + \nabla f(\mathbf{x})\mathbf{h}$
- 二阶：$f(\mathbf{x}+\mathbf{h}) \approx f(\mathbf{x}) + \nabla f(\mathbf{x})\mathbf{h} + \frac{1}{2}\mathbf{h}^\top\mathbf{H}_f(\mathbf{x})\mathbf{h}$

**Fisher 信息矩阵：**
- $\mathbf{I}(\boldsymbol{\theta}) = -\mathbb{E}[\mathbf{H}_\ell(\boldsymbol{\theta})]$
- 曲率大 = 信息多 = 估计精确

**目的：**
- Hessian 直接决定凸性判断和临界点分类
- 二阶 Taylor 展开推导出牛顿法
- Fisher 信息连接微分学与统计推断

---

### 主线三的依赖链

```
赋范空间 + 小o记号           张量/多重线性映射
        │                           │
        ▼                           │
   Fréchet 导数                     │
   (统一定义)                       │
        │                           │
        ├───────────┐               │
        ▼           ▼               ▼
    乘积法则    链式法则       高阶导数(Hessian)
        │           │               │
        │           ▼               │
        │     反向传播              │
        │    (神经网络)              │
        │                           │
        └───────────┬───────────────┘
                    ▼
              Taylor 展开
                    │
          ┌─────────┼─────────┐
          ▼         ▼         ▼
      牛顿法    凸性判据   Fisher 信息
```

---

## 主线四：凸性与优化——"最优解怎么找？"

> **核心命题**：统计学和机器学习的核心计算任务是优化。凸性保证我们能找到全局最优，优化算法告诉我们怎么找。

### 层级 4.1：凸集与凸函数（第12章）

**前置：** 层级 3.4（Hessian 矩阵）+ 层级 2.1（范数、正定性）

**核心知识点：**

**凸集：**
- 定义：任意两点的线段都在集合内
- 重要例子：闭球、半空间、单纯形、半正定矩阵集合
- 凸性保持运算：交集、线性映射、Minkowski 和
- 投影定理：闭凸集上的投影唯一

**凸函数：**
- 定义：弦在图像上方
- 三个等价刻画：
  1. 定义（弦不等式）
  2. 切线在图像下方
  3. Hessian 半正定
- Jensen 不等式：$f(\mathbb{E}[X]) \leq \mathbb{E}[f(X)]$
- 凸性保持运算：加法、非负数乘、逐点最大

**严格凸函数 → 极小值唯一**

**目的：**
- **凸 = 局部最优即全局最优**，这是优化理论的基石
- Jensen 不等式证明方差非负、KL 散度非负
- 判断统计问题是否"容易求解"的理论工具

---

### 层级 4.2：无约束优化（第13章前半）

**前置：** 层级 4.1（凸性）+ 层级 3.4（Taylor 展开、Hessian）+ 层级 3.3（链式法则）

**核心知识点：**

**基础理论：**
- 最优性条件：$\nabla f(\mathbf{x}^\star) = \mathbf{0}$，$\mathbf{H}_f(\mathbf{x}^\star) \succ 0$
- 鞍点问题：高维中几乎所有临界点都是鞍点

**梯度下降：**
- $\mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} - \gamma\nabla f(\mathbf{x}^{(k)})^\top$
- 步长选择（太大震荡、太小慢）
- 收敛速率：一般凸 $O(1/k)$，强凸 $O(\rho^k)$
- Armijo 回溯线搜索

**牛顿法：**
- $\mathbf{x}^{(k+1)} = \mathbf{x}^{(k)} - \mathbf{H}_f^{-1}\nabla f^\top$（二阶 Taylor 的精确最小化）
- 二次收敛，但每步 $O(n^3)$

**拟牛顿法（BFGS）：**
- 用梯度信息迭代近似 $\mathbf{H}_f^{-1}$
- 超线性收敛，每步 $O(n^2)$

**目的：** 提供从理论到实践的完整优化工具链。梯度下降用于大规模问题，牛顿法用于小规模高精度需求，BFGS 是通用折中方案。

---

### 层级 4.3：随机优化（第13章中段）

**前置：** 层级 4.2（梯度下降）+ 概率论基础

**核心知识点：**
- 动量法：积累惯性，抑制震荡
- SGD：单样本梯度是全梯度的无偏估计
- 小批量 SGD：实践中的标准选择（batch size 32-256）

**目的：** SGD 是训练所有现代神经网络的基础。当数据量 $N$ 极大时，全梯度每步 $O(N)$ 不可接受，SGD 每步 $O(1)$。

---

### 层级 4.4：有约束优化（第13章后半）

**前置：** 层级 4.1（凸性）+ 层级 4.2（最优性条件）

**核心知识点：**
- Lagrange 乘数法：等式约束
- KKT 条件：不等式约束（驻定性 + 原始可行 + 对偶可行 + 互补松弛）
- 凸优化中 KKT 是充要条件
- 岭回归与 Lasso 的约束形式及几何解释（Lasso 菱形角点 → 稀疏性）

**目的：** 正则化回归（岭回归、Lasso、弹性网）是现代统计学的核心方法，其数学本质是有约束优化。

---

### 主线四的依赖链

```
凸集 + 凸函数（需要 Hessian 判据）
        │
        ├── Jensen 不等式 ──► 方差非负、KL散度
        │
        ▼
  全局最优性定理
  (凸函数：临界点 = 全局最优)
        │
        ├─────────────────────────────┐
        ▼                             ▼
  无约束优化                      有约束优化
  ┌─────┬─────┐                  ┌────┴────┐
  ▼     ▼     ▼                  ▼         ▼
梯度  牛顿  拟牛顿           Lagrange    KKT
下降   法    法                乘数法     条件
  │                                       │
  ▼                                       ▼
 SGD                               岭回归/Lasso
 动量法                            (稀疏性)
  │
  ▼
神经网络训练
```

---

## 应用汇合点：函数逼近（第14章）

> 函数逼近是四条主线知识的综合应用。

### 层级 5.1：插值基础

**需要主线二的：** 线性方程组求解（层级 2.2）、条件数（层级 1.4）

**核心知识点：**
- 三种变体：逼近 / 插值 / 光滑化
- 基函数展开：$\hat{f}(x) = \sum a_k\phi_k(x)$
- 插值条件 → 线性方程组 $\mathbf{Ba} = \mathbf{y}$

---

### 层级 5.2：多项式插值及其问题

**需要层级 5.1 + 层级 1.4（条件数）**

**核心知识点：**
- 唯一性定理（$n$ 个点确定至多 $n-1$ 次多项式）
- Vandermonde 矩阵条件数指数增长
- Runge 现象：等距节点高次多项式插值发散
- 根本问题：全局多项式的任何局部改变影响全局

**目的：** 揭示"朴素方法"为何失败，激发对样条的需求。

---

### 层级 5.3：样条与 B 样条

**需要层级 5.2（多项式插值的问题）+ 层级 2.2（带状方程组求解）**

**核心知识点：**
- 样条定义：分段多项式 + 光滑拼接
- 自由度计算：$m+q$ 个参数
- B 样条基：局部支撑 + 带状稀疏 + 条件数低
- Cox-de Boor 递推
- 最小曲率性质（自然三次样条）
- 逼近误差 $O(h^4)$

**目的：** B 样条是函数逼近的"最优实践"——稳定、高效、局部性好。

---

### 层级 5.4：光滑化与回归

**需要层级 5.3（样条基）+ 层级 2.4（最小二乘）+ 统计学基础**

**核心知识点：**
- 光滑化模型：$y_i = f(x_i) + \epsilon_i$
- 最小二乘拟合：$\min_\mathbf{a}\|\mathbf{y} - \mathbf{Ba}\|_2^2$
- 插值（$K=n$）vs 光滑化（$K < n$）
- 偏差-方差权衡：$\text{MSE} = O(K^{-8} + K/n)$
- 模型选择：交叉验证、GCV、AIC/BIC

---

### 层级 5.5：多变量函数逼近与维数诅咒

**需要层级 5.4 + 层级 3.1（张量积基）**

**核心知识点：**
- 张量积基：$K^p$ 个系数（指数爆炸）
- 维数诅咒：MSE 收敛速率 $O(n^{-8/(8+p)})$
- 解决方案：GAM（加性结构，$pK$ 个参数）

**目的：** 理解高维统计问题的本质困难，以及 GAM 如何在可解释性和灵活性之间取得平衡。

---

### 函数逼近的依赖链

```
线性方程组(主线二)  条件数(主线一)   最小二乘(主线二)   张量积(主线三)
        │                │                │                │
        ▼                ▼                │                │
    插值基础 ──► 多项式插值问题           │                │
                    │                     │                │
                    ▼                     │                │
               样条 / B样条              │                │
                    │                     │                │
                    └────────┬────────────┘                │
                             ▼                             │
                      光滑化与回归                         │
                    (偏差-方差权衡)                         │
                             │                             │
                             └──────────┬──────────────────┘
                                        ▼
                                  多变量逼近
                                  维数诅咒 → GAM
```

---

## 完整依赖关系总图

```
                         ┌─────────────────────┐
                         │  大一基础：          │
                         │  线性代数 + 微积分   │
                         │  + 概率统计          │
                         └──────────┬──────────┘
                                    │
              ┌─────────────────────┼─────────────────────┐
              ▼                     ▼                     ▼
    ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
    │ 浮点运算     │     │ 矩阵迹+范数  │     │ 张量/多重    │
    │ Landau记号   │     │ 正交矩阵     │     │ 线性映射     │
    │ [第2章]      │     │ [第3章]      │     │ [第9章]      │
    └──────┬───────┘     └──────┬───────┘     └──────┬───────┘
           │                    │                     │
           ▼                    │                     ▼
    ┌──────────────┐            │              ┌──────────────┐
    │ 误差分析     │◄───────────┤              │ Fréchet导数  │
    │ 条件数       │            │              │ 梯度/Jacobi  │
    │ 稳定性       │            │              │ [第10章]     │
    │ [第4章]      │            │              └──────┬───────┘
    └──────┬───────┘            │                     │
           │                    ▼                     ▼
           │         ┌──────────────┐          ┌──────────────┐
           │         │ LU/Cholesky  │          │ 链式/乘积    │
           │         │ 分解         │          │ 法则         │
           │         │ [第6章]      │          │ Hessian      │
           │         └──────┬───────┘          │ Taylor展开   │
           │                │                  │ [第11章]     │
           │                ▼                  └──────┬───────┘
           │         ┌──────────────┐                 │
           │         │ SVD          │                 │
           │         │ 伪逆/低秩   │                 │
           │         │ [第8章]      │                 │
           │         └──────┬───────┘                 │
           │                │                         │
           ▼                ▼                         ▼
    ┌──────────────────────────────┐          ┌──────────────┐
    │        最小二乘法             │          │ 凸性理论     │
    │  (条件数分析+三种求解方法)    │          │ Jensen不等式 │
    │        [第5章]               │          │ [第12章]     │
    └──────────────┬───────────────┘          └──────┬───────┘
                   │                                  │
                   │         ┌────────────────────────┤
                   │         │                        │
                   │         ▼                        ▼
                   │  ┌──────────────┐         ┌──────────────┐
                   │  │ 梯度下降     │         │ 有约束优化   │
                   │  │ 牛顿法       │         │ Lagrange     │
                   │  │ BFGS/SGD     │         │ KKT条件      │
                   │  │ [第13章]     │         │ [第13章]     │
                   │  └──────┬───────┘         └──────┬───────┘
                   │         │                        │
                   │         │                        ▼
                   │         │                  岭回归/Lasso
                   │         │                  (稀疏性)
                   │         ▼
                   │   神经网络训练
                   │
    ┌──────────────┴───────────────────────────────────────┐
    │                                                       │
    ▼                                                       ▼
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│ 迭代/概率    │      │ 函数逼近      │      │ 多变量逼近   │
│ 方法         │      │ 样条/B样条    │      │ 维数诅咒     │
│ [第7章]      │      │ 光滑化        │      │ GAM          │
│              │      │ 偏差-方差     │      │ [第14章]     │
│ PageRank     │      │ [第14章]      │      │              │
│ 大规模PCA    │      │              │      │              │
└──────────────┘      └──────────────┘      └──────────────┘
```

---

## 一句话总结每章的地位

| 章 | 主题 | 在课程中的角色 |
|:---:|:---|:---|
| 2 | 算法与复杂度 | **地基**：建立"计算是近似的"意识和复杂度语言 |
| 3 | 迹与范数 | **度量工具**：所有误差分析和条件数的前提 |
| 4 | 误差分析 | **质量标准**：判断问题和算法好坏的理论框架 |
| 5 | 最小二乘 | **核心任务**：统计学最常见的计算问题 |
| 6 | 矩阵分解 | **求解引擎**：线性方程组的高效稳定算法 |
| 7 | 迭代与概率 | **扩展能力**：应对大规模问题的现代方法 |
| 8 | SVD | **万能工具**：连接范数、伪逆、降维、压缩的枢纽 |
| 9 | 张量 | **语言扩展**：为高阶导数和多变量逼近提供数学语言 |
| 10 | 导数 | **变化描述**：统一框架处理所有类型的导数 |
| 11 | 运算法则 | **计算引擎**：链式法则 = 反向传播的数学本质 |
| 12 | 凸性 | **理论保障**：凸 = 全局最优可达 |
| 13 | 优化 | **最终方法**：统计学和 ML 中几乎所有问题归结于此 |
| 14 | 函数逼近 | **综合应用**：四条主线知识的集大成 |
