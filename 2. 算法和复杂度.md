## 数值问题（Numerische Probleme）

- 一个数值问题的解是一个或多个数字。
- 例如：
  - 积分的值
  - 线性方程组（LGS）的解
  - 函数逼近中的基函数系数
- 一般来说：给定一个带输入 x 的问题 f，计算它的解 f(x)。

---

## 数值问题：困难（Numerische Probleme: Schwierigkeiten）

- **计算机几乎从来都不是精确的**。
- 它们只能：
  - **表示有限位数**、**并且数量有限的数字**。  
    例如：  
    π = 3.141592653589793238462643383279…（无限不循环）:(  
  - 进行有限次数的计算。  
    例如：  
    e^x = Σ_(n=0)^∞ x^n / n!（无穷级数）:(  
- 因此，我们“如何计算”是非常重要的！


## 数值问题：困难（Numerische Probleme: Schwierigkeiten）

- 计算机只能表示有限多个数。
- 这会导致：
  - 舍入误差（四舍五入误差），并且这些**误差可能会累积**。
  - 计算操作的执行顺序会产生影响。
- **单次运算的误差往往可以忽略，但它们可能会累积**。
- 建议：**尽量避免出现特别大或特别小的数**。


## 灾难性消去（Catastrophic Cancellation）

### 现象

- 在浮点数计算中，**两个几乎相等的巨大数相减**时，结果的有效数字会大量丢失，误差可能被放大，甚至得到离谱结果（如算成 0）。

### 例子：方差的两种等价公式（代数等价，数值不等价）

- 不稳定（容易灾难性消去）：
$$
Var(x)=\frac{1}{n}\sum_{i=1}^n x_i^2-\left(\frac{1}{n}\sum_{i=1}^n x_i\right)^2
$$
- 更稳定（推荐）：
$$
Var(x)=\frac{1}{n}\sum_{i=1}^n (x_i-\bar x)^2
$$

### 为什么不稳定

- 当数据整体很大（比如都在 \(10^9\) 附近），但波动很小（差异只有十几）时：
  - \(\frac{1}{n}\sum x_i^2\) 和 \(\bar x^2\) 都是 \(10^{18}\) 量级
  - 它们的差（方差）却只有 \(10^1\) 量级  
- **计算机只能存有限精度**：两大数相减会“把高位相同部分消掉”，剩下的低位受舍入影响大 → 结果不可靠。

### 结论/建议
- **代数恒等式 ≠ 数值稳定**。
- **计算方差尽量用 “先中心化再平方”的形式，或使用数值稳定算法（如 Welford 在线算法）。**
- 避免让程序做“非常大与非常大但很接近的数”的减法（尤其是产生很小差值时）。

## 加法不满足结合律（浮点数）

**标题：** Beispiel: Assoziativität verletzt  
**结论：** 浮点数加法 **不是结合的**，即一般不保证  
$$
(x+y)+z = x+(y+z).
$$

**例子：** 设  
$$
x=10^{30},\quad y=-10^{30},\quad z=1.
$$

- 按这种顺序：
$$
(x+y)+z=(10^{30}+(-10^{30}))+1=0+1=1.
$$

- 换一种顺序：
$$
x+(y+z)=10^{30}+((-10^{30})+1)\approx 10^{30}+(-10^{30})=0.
$$

**为什么第二个变成 0：** 因为在浮点数里，相对 \(10^{30}\) 来说，\(1\) 太小，计算 \((-10^{30})+1\) 时，\(1\) 可能被舍入“吃掉”，结果仍是 \(-10^{30}\)。  
**要点：** 求和的**运算顺序**会影响结果。

---

## 算法（Algorithmus）

**定义：**  
算法是一种过程（由若干步骤组成），对输入 \(x\) 计算一个（可能的）输出/解 $(\tilde f(x)$)。

**算法类型（Arten von Algorithmen）：**
- **精确/直接（exakt/direkt）**：\(\tilde f(x)=f(x)\)（除了舍入误差）。
- **近似（approximativ）**：只要求 \(\tilde f(x)\approx f(x)\)。
- **迭代（iterativ）**：重复应用同一规则多次（逐步逼近）。
- **概率（probabilistisch）**：依赖随机性（用随机方法得到解或近似解）。

---

### 一句总结（适合写在页边）
- 浮点运算会引入舍入误差，**顺序很重要**；因此需要设计**数值稳定**的算法。

## 机器学习 / 统计中的算法类型（Algorithmenarten in ML / Statistik）

- **精确/直接**（Exakt/direkt）：
  - 用高斯消元法求解线性方程组（后面会讲）
  - 矩阵乘法

- **近似**（Approximativ）：
  - 蒙特卡洛积分（通过抽样来估计积分）
  - 神经网络（优化直到达到某个容忍误差水平）

- **迭代**（Iterativ）：
  - 机器学习中的梯度下降
  - 用牛顿法求零点

- **概率型/随机化**（Probabilistisch）：
  - 随机化的快速排序变体
  - 贝叶斯统计中的 MCMC 方法

常见情况是组合使用：机器学习算法往往同时是“**迭代 + 近似 + 概率型**”！

## 类比（Analogie）

一个类比：
- 问题：做出像奶奶做的一样的烤猪肉（Schweinsbraten）。
- 算法 ↔ 菜谱
- 计算机 ↔ 按菜谱做菜、但会犯错误的厨师

---

## 好算法（Gute Algorithmen）
我们想要的算法：
- **尽可能给出精确的结果**
- **速度快**
- **占用内存少**

## 数值问题与算法：代价与复杂度（Aufwand und Komplexität）

- 计算机执行算法时，会用基本算术运算
  $$f_i \in \{+,-,\cdot,/\}.$$

- 算法的时间代价（Zeitaufwand）是这些基本运算的次数。
  - **代价越高 ≈ 运行时间越长**
- 算法的空间代价（Speicheraufwand）大致是需要存储的（中间）数字的数量。
  - **代价越高 = 需要更多内存空间**

## 解读：例子——矩阵-向量乘法（Matrix-Vektor-Multiplikation）

### 1) 问题
计算
$$
y = A x,
$$
其中
- A 是 3×2 矩阵
- x 是 2 维向量（所以 y 是 3 维向量）

具体给出：
$$
A=
\begin{pmatrix}
1&2\\
3&4\\
5&6
\end{pmatrix},
\quad
x=
\begin{pmatrix}
7\\
8
\end{pmatrix}.
$$

---

### 2) 计算（按行做点积）
- 第 1 行：
$$
y1 = 1·7 + 2·8 = 7 + 16 = 23
$$
（2 次乘法 + 1 次加法）

- 第 2 行：
$$
y2 = 3·7 + 4·8 = 21 + 32 = 53
$$

- 第 3 行：
$$
y3 = 5·7 + 6·8 = 35 + 48 = 83
$$

所以
$$
y = (23, 53, 83)^T.
$$
### 3) 时间开销（Zeitaufwand）
这例子里：
- 每个 yi 需要 2 次乘法 + 1 次加法
- 一共 3 个分量

因此总运算量：
- 3×2 = 6 次乘法
- 3×1 = 3 次加法
- 合计 9 次基本运算

一般情况：A 是 n×d
- 乘法次数：n d
- 加法次数：n(d-1)
- 总操作数约为
$$
2 n d
$$
（忽略常数和低阶项时经常写成“约 2nd”）

### 4) 空间开销（Speicheraufwand）
需要存储的数字个数大致是：
- 矩阵 A：n d 个数
- 向量 x：d 个数
- 结果 y：n 个数

总计：
$$
nd + d + n
$$
个数。




这段是在把某个循环算法的“时间开销”按迭代次数拆开数清楚。

设：
- n = 向量长度 / 矩阵 A 的行数（每次要处理 n 个元素）
- d = 迭代次数（i 从 1 到 d）
- ai = A 的第 i 列（长度为 n）
- xi, yi = 标量
- f = 长度为 n 的向量（累加用）

---

## 每次迭代 i（i = 1,...,d）做了什么、各要多少次基本运算

1) 计算差值
$$
(xi - yi)
$$
- 1 次减法 → 1 operation

2) 缩放（把标量乘到向量列上）
$$
(xi - yi) · ai
$$
- ai 有 n 个分量，每个分量乘一次
- n 次乘法 → n operations

3) 累加到 f
$$
f <- f + (...)
$$
- f 是 n 维向量，对每个分量加一次
- n 次加法 → n operations

---

## 所以每次迭代的总开销
$$
1 + n + n = 2n + 1 \quad \text{operations}
$$

---

## 总开销（做 d 次）
$$
d · (2n + 1) = 2nd + d \quad \text{operations}
$$

---

### 一句话笔记
“每轮：1 次标量减法 + n 次乘法 + n 次加法；做 d 轮 ⇒ 总共 2nd + d 次基本运算。”


## 复杂度（Komplexität）

- **知道“精确的”运算次数和需要存多少个数，并不是最重要的。**
- 我们主要关心的是：**当问题规模变大时，开销如何随规模增长而变化。**
- 这就叫算法的（运行时间/内存）复杂度。

## 例子（Beispiel）

- 某算法需要的运算次数是
$$
4n^3 + 16n^2 + 239.
$$

- 当 n 很大时，和主项 4n^3 相比，16n^2 + 239 可以忽略不计。

因此：
- 该算法的增长“像” 4n^3 一样
- 复杂度是三次（立方）阶
- 记作：
$$
O(n^3).
$$

## 复杂度类别（Komplexitätsklassen）

### 不同复杂度如何随规模 n 增长（Interpretation）
- O(1)（常数）：运算次数与 n 无关
- O(log n)（对数）：n 翻倍时，运算次数大约只多 1 个“单位”（增加一个常数级别）
- O(n)（线性）：n 翻倍，运算次数也翻倍
- O(n^2)（平方）：n 翻倍，运算次数变为 4 倍
- O(2^n)（指数）：n 翻倍，运算次数会呈指数爆炸式增长

---

### 注意（Vorsicht）
- O( ) 记号会忽略常数因子和低阶项。

例如：
- 1000n + 10000 次运算仍然是 O(n)
- 但它在实际中可能（明显）比 n^2 次运算（O(n^2)）慢或快要看 n 的范围：  
  对 n < 100 时，1000n + 10000 可能仍然比 n^2 大很多，因此“虽然同是/不同阶”，实际速度未必如直觉。

## Landau 符号（小 o / 大 O）

### 1) 定义（对数列 a_n, b_n）
- 小 o：
  a_n = o(b_n) 当且仅当
$$
lim(n→∞) a_n / b_n = 0
$$
直观：a_n 相比 b_n 可以忽略（严格小一阶）。

- 大 O：
  a_n = O(b_n) 当且仅当
$$
limsup(n→∞) |a_n / b_n| < ∞
$$
等价直观：a_n / b_n 最终有界，也就是 a_n 至多是 b_n 的常数倍量级。

---

### 2) 解释（Interpretation）
- a_n = o(b_n)：**a_n 增长得比 b_n 慢，和 b_n 比起来“可忽略小**”
- a_n = O(b_n)：**a_n 增长速度不超过 b_n 的常数倍；b_n 相对 a_n “不一定可忽略”**

并且常用关系：
- a_n = o(b_n)  ⇒  a_n = O(b_n)
- 反过来不一定成立

---

## 计算例子（Rechenbeispiele）

### 例 1：证明 3n^2 + 5n = O(n^2)
算比值：
$$
lim(n→∞) (3n^2 + 5n) / n^2
= lim(n→∞) (3 + 5/n)
= 3 < ∞
$$
所以 3n^2 + 5n = O(n^2)。

---

### 例 2：证明 5n = o(n^2)
$$
lim(n→∞) 5n / n^2
= lim(n→∞) 5/n
= 0
$$
所以 5n = o(n^2)。

---

### 例 3：n^2 是不是 O(n)？不是
$$
lim(n→∞) n^2 / n
= lim(n→∞) n
= ∞
$$
比值无界，因此 n^2 不是 O(n)。

## 解读：Landau 符号的“计算规则”与例子

### 这一页的主旨
- 我们通常想找一个简单的 b_n，使得
  - a_n = O(b_n)（上界同阶）
  - 但 a_n 不是 o(b_n)（不要选得太松，尽量贴近主导项）

---

## Lemma：一些计算规则（Ein paar Rechenregeln）

设 f_n, g_n 是两列，a_n, b_n 是参考序列。

### 规则 1
如果
- f_n = O(a_n)
- g_n = O(b_n)

那么
- 加法：
  $$
  f_n + g_n = O(a_n + b_n)
  $$
- 乘法：
  $$
  f_n g_n = O(a_n b_n)
  $$

### 规则 2
如果
- f_n = O(a_n)
- g_n = o(b_n)

那么
$$
f_n g_n = o(a_n b_n)
$$
（直观：一个因子“可忽略小”，乘出来仍“可忽略小”。）

### 规则 3
如果
- f_n = O(a_n + b_n) 
- 并且 a_n = O(b_n)  -- **a_n 被 b_n 支配**

那么 --  **a_n + b_n 仍是 b_n 量级**
$$
f_n = O(b_n)
$$
（直观：a_n 被 b_n 支配，所以 a_n + b_n 仍是 b_n 量级。）

---

## 应用例子 1：加法与乘法

给定：
- f_n = 3n^2 = O(n^2)
- g_n = 5n = O(n)

### 加法
$$
f_n + g_n = 3n^2 + 5n = O(n^2 + n) = O(n^2)
$$
最后一步用规则 3，因为
$$
n = O(n^2).
$$

### 乘法
$$
f_n g_n = (3n^2)(5n) = 15n^3 = O(n^2 \cdot n) = O(n^3).
$$

---

## 应用例子 2：复杂表达式化简（只保留主导项）

算法需要的运算次数：
$$
4n^3 + 16n^2 + 239
$$

写成：
$$
4n^3 + 16n^2 + 239 = O(n^3 + n^2 + 1).
$$

并且因为
$$
n^2 = O(n^3), \quad 1 = O(n^3),
$$
所以
$$
O(n^3 + n^2 + 1) = O(n^3).
$$

---


**“我们可以把复杂表达式简化为由主导项决定的增长阶。”**


## 斐波那契：复杂度分析 1（迭代算法）

图上给的是一个迭代版本（R 代码思想）：
- 先分配长度为 n 的向量 x
- 设定 x2 = 1
- 循环 i = 2 到 n-1：令 x(i+1) = x(i) + x(i-1)

### 时间复杂度（Zeitkomplexität）
- 初始化：x <- numeric(n)  
  分配 n 个位置，开销是 O(n)
- 循环：n-2 次迭代  
  每次做常数次操作（2 次加法 + 1 次赋值，整体仍是常数）  
  总计 (n-2) * 常数 = O(n)
- 总时间：O(n)（线性）

### 空间复杂度（Speicherkomplexität）
- 需要存一个长度为 n 的向量 x  
  空间 O(n)

---

## 斐波那契：复杂度分析 2（朴素递归）

朴素递归：
- fib(n) = fib(n-1) + fib(n-2)
- fib(0)=0, fib(1)=1（或类似基例）

### 时间复杂度
- 每次调用会再产生 2 个调用，调用树规模呈指数增长
- 总时间复杂度：O(2^n)（指数级）
- 讲义评价：非常慢（katastrophal langsam）

更直观的原因：大量重复计算，例如 fib(40) 会反复算很多次 fib(20)、fib(19) 等。

### 空间复杂度
- 递归深度最大大约是 n
- 调用栈需要 O(n) 空间

---

## 结论（图下那句）
迭代解法在时间上“戏剧性”更高效：
- 迭代：O(n)
- 朴素递归：O(2^n)
空间两者都大致是 O(n)（这里按讲义的口径：递归是栈深度 O(n)，迭代是存整个向量 O(n)）。




## 练习

### 回顾（Recap）

- 为了确定一个算法的时间开销（即运行时间），我们统计**基本算术运算**（`+`、`-`、`·`、`/`）的次数。  
- 空间开销是（临时）**存储的数字**的数量。  
- 我们通常不关心精确次数，而关心**复杂度**：即算法如何随着问题规模（输入大小）增长而变化。  
- 例如：当数据量翻倍时，算法用时会不会也翻倍？还是会变成四倍？  
- 用 **Landau 记号（小 o 与大 O）**形式化：  
  - 若 $\lim_{n\to\infty}\frac{a_n}{b_n}=0$，则 $a_n=o(b_n)$。  
  - 若 $\limsup_{n\to\infty}\left|\frac{a_n}{b_n}\right|<\infty$，则 $a_n=O(b_n)$。  
	- 例子：  
	  - $10^6 + 100n + 2n^2 = O(n^2) = o(n^3)$。  
	  - $\frac{1}{n^2}+\frac{1}{n^3}=O\!\left(\frac{1}{n^2}\right)=o\!\left(\frac{1}{n}\right)$。  
	  - 结论：把复杂表达式**化简为其主导项（dominant term）**。  
	- 重要特例：  
	  - $a_n=O(1)$：即 $a_n$ **有界**。  
	  - $a_n=o(1)$：即 $a_n$ **收敛到 0**。  
- Landau 记号会**忽略常数因子**。例如：  
  - $a_n=1000n=O(n)$。  
  - $b_n=0.1n^2=O(n^2)$。  
  - 注意：虽然 $b_n$ 只有在 $n$ 很大时才会超过 $a_n$，但在实践中，**复杂度更差的算法仍可能比复杂度更好的算法更快**（受常数、实现、硬件等因素影响）。

### 例子

1. **给定** $X_1,\dots,X_n\in\mathbb{R}$

	- **均值**： $\frac{1}{n}\sum_{i=1}^n X_i$：对 $n$ 个数求和需要 $n-1$ 次加法，然后再做 $1$ 次除法 $\Rightarrow$ **$O(n)$**。
	
	- **距离矩阵**： $D\in\mathbb{R}^{n\times n}$，其中 $D_{ij}=|X_i-X_j|$：  一共有 $n^2$ 个元素；每个元素需要一次减法再取绝对值 $\Rightarrow$ **$O(n^2)$**。  

2. **矩阵乘法** $A\in\mathbb{R}^{m\times n}$、$B\in\mathbb{R}^{n\times \ell}$，计算 $AB\in\mathbb{R}^{m\times \ell}$

	- 标准算法：$(AB)_{ij}=\sum_{k=1}^n A_{ik}B_{kj}$。  每个输出元素需要 $n$ 次乘法和 $n-1$ 次加法 $\Rightarrow O(n)$，共有 $m\ell$ 个输出元素  $\Rightarrow$ 总复杂度 **$O(mn\ell)$**。
	
3. **矩阵的迹**（trace）是对角线元素之和。给定 $A,B\in\mathbb{R}^{n\times n}$、$c\in\mathbb{R}$，计算下列量的复杂度：
	
	- $tr(A)=\sum_{i=1}^n A_{ii}$：需要对 $n$ 个对角元素求和 $\Rightarrow$ **$O(n)$**。  
	- $tr(A+B)$：可以先算 $tr(A)+tr(B)$（各 $O(n)$）再相加（$O(1)$）  
	  $\Rightarrow$ **$O(n)$**。  
	  （若先显式计算 $A+B$ 需要 $O(n^2)$，但并非必要。）  
	- $tr(cA)=c\,tr(A)$：先算 $tr(A)$ 为 $O(n)$，再乘以标量 $c$ 为 $O(1)$  
	  $\Rightarrow$ **$O(n)$**。

### 练习1.1

#### 问题：

对给定的数列 $a_n$，找两个更简单的数列 $b_n$ 和 $c_n$，使得 $a_n = O(b_n)$ 且 $a_n = o(c_n)$。

(a) $a_n = 100n^{-1} + 20n^{0.5} + 5n^{1.5}$。

(b) $a_n = \frac{2\log(n)}{n} + \frac{\log(2)}{n^2} + \log\!\left(1+\frac{1}{n}\right)$。  提示：利用 $\log\!\left(1+\frac{1}{n}\right)\le \frac{1}{n}$。

#### a) 解答

**定义描述**：

- 当 $\limsup_{n\to\infty}\lvert a_n/b_n\rvert<\infty$ 时，$a_n=O(b_n)$
- 当 $\lim_{n\to\infty} \lvert a_n/c_n \rvert =0$ 时，$a_n=O(c_n)$。

(a) 

- 因为在 $a_n=100n^{-1}+20n^{0.5}+5n^{1.5}$ 里，$n^{1.5}$ **是主导项**（增长最快），所以用 $b_n=n^{1.5}$ 去做比较时， $\frac{a_n}{b_n}$ 里的主导项会变成常数： $\frac{5n^{1.5}}{n^{1.5}}=5$， 其他更慢的项会变成趋于 0 的小量： $\frac{20n^{0.5}}{n^{1.5}}=20n^{-1}\to 0$， $\frac{100n^{-1}}{n^{1.5}}=100n^{-2.5}\to 0$。
- 取 $b_n=n^{1.5}$ 且 $c_n=n^2$，因此
$$
\frac{a_n}{b_n}
=\frac{100n^{-1}+20n^{0.5}+5n^{1.5}}{n^{1.5}}
=100n^{-2.5}+2n^{-1}+5 \xrightarrow[n\to\infty]{} 5.
$$
类似地可以证明 $\frac{a_n}{c_n}\to 0$。

另外也可以用 $O$ 记号改写：
$$
a_n=100n^{-1}+20n^{0.5}+5n^{1.5}
=O(n^{-1})+O(n^{0.5})+O(n^{1.5}).
$$
接着因为当 $n\to\infty$ 时有
$$
n^{-1}\le n^{1.5},\qquad n^{0.5}\le n^{1.5},
$$
所以
$$
O(n^{-1})\subseteq O(n^{1.5}),\qquad O(n^{0.5})\subseteq O(n^{1.5}).
$$
因此
$$
a_n\in O(n^{1.5})+O(n^{1.5})+O(n^{1.5})=O(n^{1.5})=O(b_n),
$$
其中 $b_n=n^{1.5}$。

**此题目让找两个序列，第一个序列满足了上界小于正无穷，满足了一个条件，根据这个，我们也就可以找一个更一般的，例如 $n^2$ 来正面极限为0。**

(b) 

例如取 $b_n=\log(n)/n$，并取 $c_n=1$。有
$$
\frac{2\log(n)/n}{\log(n)/n}=2 \quad\Rightarrow\quad 2\log(n)/n=O(b_n),
$$
以及
$$
\frac{\log(2)/n^2}{\log(n)/n}=\frac{\log(2)}{\log(n)}\cdot\frac{1}{n}\to 0
\quad\Rightarrow\quad \log(2)/n^2=o(b_n).
$$

另外，$\log(1+1/n)\le 1/n$，因此
$$
\frac{\log(1+1/n)}{\log(n)/n}\le \frac{1/n}{\log(n)/n}=\frac{1}{\log(n)}\to 0
\quad\Rightarrow\quad \log(1+1/n)=o(b_n).
$$

综上，$a_n=O(b_n)$。由于 $b_n\to 0$，所以 $b_n=o(1)$，从而也有 $a_n=o(1)$。

**经典结论**， 序列极限为0，就是 o(1)

当然，这里任何满足 $b_n\to 0$ 的 $c_n$ 也都是一个合法的选择，但我们倾向于寻找一个 $b_n$，使得 $a_n=O(b_n)$，但 $a_n$ 不是 $o(b_n)$。

**通用结论**：$$ a_n = O(b_n),\ b_n = o(c_n)\ \Rightarrow\ a_n = o(c_n) $$

### 练习1.2

## 题目（Aufgabe 1.2）
设 e_n 是一个数列，并且
$$
a_n = O(e_n + e_n^2).
$$
证明：
1) 若 e_n → ∞，则
$$
a_n = O(e_n^2).
$$
2) 若 e_n → 0，则
$$
a_n = O(e_n).
$$

---

## 解读 + 证明思路（按讲义）

关键点：比较 e_n + e_n^2 与 e_n^2 或 e_n 的大小关系，谁是主导项。

---

### 情况 1：e_n → ∞
看比值：
$$
(e_n + e_n^2) / e_n^2 = 1/e_n + 1 → 1.
$$
因此
$$
e_n + e_n^2 = O(e_n^2).
$$

又因为已知
$$
a_n = O(e_n + e_n^2),
$$
利用传递性（如果 a_n = O(b_n) 且 b_n = O(c_n)，则 a_n = O(c_n)），得到
$$
a_n = O(e_n^2).
$$

---

### 情况 2：e_n → 0
先看
$$
(e_n + e_n^2) / e_n^2 = 1/e_n + 1 → ∞,
$$
所以不能说 e_n + e_n^2 = O(e_n^2)（因为这时 e_n^2 反而更小）。

再看与 e_n 的比值：
$$
(e_n + e_n^2) / e_n = 1 + e_n → 1.
$$
因此
$$
e_n + e_n^2 = O(e_n).
$$

同样用传递性推出
$$
a_n = O(e_n).
$$

---

## 一句话总结
- $e_n$ 很大时，$e_n^2$ 主导，所以 $e_n + e_n^2$ 和 $e_n^2$ 同阶
- $e_n$ 很小时，$e_n$ 主导，所以 $e_n + e_n^2$ 和 $e_n$ 同阶

## 练习 1.3

#### 背景

这是**投资组合方差**或**线性组合方差**的估计问题，在统计学和金融学中非常常见。

**实际应用场景**

- **投资组合理论**：$X_i$ 是 $d$ 种资产的收益率向量，$\beta_i$ 是投资在第 $i$ 种资产的比例，$\sigma^2(\beta)$ 就是整个投资组合的风险（方差）

	- 我们买了3支ETF
	- $X_1$ 是第一天的 3 支ETF的收益率，如（+2%, -1%, -0.5%)
	- ... ...
	- $X_{200}$ 是第一天的 3 支ETF的收益率，如（+2%, -1%, -0.5%)
	- $\beta = (\beta_1, \beta_2, \ldots, \beta_d)^T$ 是一个 **权重向量** 或 **系数向量**吗，它表示如何将 $d$ 个随机变量组合起来：
	- 例如 $\beta$ 是三个 ETF 的购买比例，例如 30%, 40%, 30%

- **主成分分析**：为了减少风险，我们需要寻找使 $\sigma^2(\beta)$ 最大或最小的方向 $\beta$
    
**公式整体逻辑**

1. 理论上，线性组合的方差是 $\sigma^2(\beta) = \beta^T \Sigma \beta$
2. 但真实的 $\Sigma$ 未知，所以用 **样本估计** $\hat{\Sigma}$
3. 进而得到方差的估计 $\hat{\sigma}^2(\beta) = \beta^T \hat{\Sigma} \beta$

简单说，这套公式就是在回答：**给定一组多维数据和一个权重向量，如何估计加权组合的波动性**？

#### 题目

设 $X_1, X_2, …, X_n$ 是取值于 $R^d$ 的独立同分布随机向量，且 $Var(X)=Σ$  ，对任意参数 beta都有
$$
\sigma^2(\beta) = Var(\beta^T X) = \beta^T Σ \beta.
$$
- $X_i \in \mathbb{R}^d$
- $\beta \in \mathbb{R}^d$

**协方差矩阵** Σ 可以按如下方式估计：
$$
\hat{\Sigma} = \frac{1}{n} \sum_{i=1}^{n} (X_i - \hat{\mu})(X_i - \hat{\mu})^T,
\quad
\hat{\mu} = \frac{1}{n} \sum_{i=1}^{n} X_i
$$


**相应地我们定义**（用样本估计来推测）
$$
\hat{\sigma}^2(\beta) = \beta^T \hat{\Sigma} \beta
$$
由这些公式我们可以推导出下面用于计算 $\hat{\sigma}^2(\beta)$ 的算法：

```r
#' @param X Matrix mit Zeilen X_1, ..., X_n.
#' @param beta Parametervektor
# 计算投资组合方差的函数
# X: n × d 矩阵，n个观测（如交易日），d个资产（如ETF）
# beta: 长度为d的向量，表示各资产的投资权重
sigma_2 <- function(X, beta) {
  
  n <- nrow(X)  # 观测数量（天数）
  d <- ncol(X)  # 资产数量
  
  # 初始化协方差矩阵为零矩阵
  Sigma <- matrix(0, d, d)
  
  # 计算样本协方差矩阵 Σ̂ = (1/n) Σ(Xi - μ̂)(Xi - μ̂)^T
  for(i in 1:n) {
    # X[i,] - colMeans(X): 第i个观测减去均值向量
    # %*% t(...): 外积，得到 d × d 矩阵
    Sigma <- Sigma + (X[i,] - colMeans(X)) %*% t(X[i,] - colMeans(X))
  }
  
  # 返回组合方差：σ̂²(β) = β^T Σ̂ β
  as.numeric(t(beta) %*% Sigma %*% beta / n)
}

# ========== 示例：3个ETF，200天的收益率数据 ==========

days <- 200  # 观测天数
set.seed(123)  # 固定随机种子，保证结果可重复

# 模拟3个ETF的日收益率
# ETF1: 年化收益约7.6%，年化波动约15.9%
# ETF2: 年化收益约12.6%，年化波动约23.8%
# ETF3: 年化收益约25.2%，年化波动约39.7%
X <- cbind(
  rnorm(days, mean = 0.0003, sd = 0.01),   # ETF1: 低风险低收益
  rnorm(days, mean = 0.0005, sd = 0.015),  # ETF2: 中等
  rnorm(days, mean = 0.001, sd = 0.025)    # ETF3: 高风险高收益
)

# 投资权重：30% ETF1，40% ETF2，30% ETF3
beta <- c(0.3, 0.4, 0.3)

# 计算组合的日方差
var_daily <- sigma_2(X, beta)

# 日波动率（百分比）= sqrt(方差) × 100
sd_daily <- sqrt(var_daily) * 100
cat("日波动率:", round(sd_daily, 2), "%\n")

# 年化波动率（百分比）= 日波动率 × sqrt(252)
# 252是一年的交易日数，波动率按sqrt(T)放大
sd_annual <- sqrt(var_daily) * sqrt(252) * 100
cat("年化波动率:", round(sd_annual, 2), "%\n")
```


(a) 用 Landau 记号给出该算法的运行时间复杂度和存储空间复杂度。

**存储空间复杂度**

- X 的存储空间：$O(nd)$  - n 天 * d个ETF
- β 的存储空间：$O(d)$ - d个ETF
- Σ̂ 的存储空间：$O(d^2)$ - 协方差矩阵 d * d

**总体空间复杂度：$O(nd + d^2)$**

**运行时间复杂度**

- 计算 `colMeans(X)` 的运行时间：$O(nd)$（对每个 d 列各做 n−1 次加法，用于求均值）
    
- 单次计算 `(X[i,] - colMeans(X)) %*% t(X[i,] - colMeans(X))` 的运行时间：$O(nd + d^2)$（其中 $O(nd)$ 用于 `colMeans(X)`，2d 次减法，$d^2$ 次乘法用于**外积**）
    
- 对 i 的 n 次迭代：计算 Σ̂ 的总时间为 $O(n(nd + d^2)) = O(n^2d + nd^2)$
    
- 计算 $\beta^T \hat{\Sigma} \beta$ 的时间：$O(d^2)$（$2d^2$ 次运算用于 $\beta^T \hat{\Sigma}$：对每个 d 列各做 d 次乘法与加法；以及 d 次运算用于 $(\beta^T \hat{\Sigma}) \beta$：$\mathbb{R}^d$ 中两个向量的内积）

	**$\beta^T \hat{\Sigma}$：**
		- $\beta$ 是 1 × d 的行向量
		- $\hat{\Sigma}$ 是 d × d 的矩阵
		- 结果是 1 × d 的行向量
		- 运算量：d 列，每列做 d 次乘法和 d-1 次加法，共 O(d²)
	
	**$(\beta^T \hat{\Sigma}) \beta$：**
		- 1 × d 向量与 d × 1 向量的内积
		- 运算量：d 次乘法 + d-1 次加法，共 O(d)
    

**总体时间复杂度**：$O(n^2d + nd^2)$  - 第四步 $d^2$ 被 第三步所吸收，第一步被第二步所吸收，第二步被第三步吸收，所以保留第三步

(b) 用数值实验检验运行时间的性质。为此生成多个数据集，其中 n 取 [10,1000]，d 取 [10,1000]，并对算法做基准测试（例如用 microbenchmark::microbenchmark()）。


```r
library(microbenchmark)
library(ggplot2)

grid <- expand.grid(
  n = seq.int(10, 1000, length = 4),
  d = seq.int(10, 1000, length = 4)
)

times <- numeric(nrow(grid))

for (k in 1:nrow(grid)) {
  n <- grid[k, 1]
  d <- grid[k, 2]
  X <- replicate(d, rnorm(n))
  cat("n = ", n, ", d = ", d, ", time = ", sep = "")
  times[k] <- median(microbenchmark(sigma_2(X, rep(1, d)), times = 10)$time)
  cat(times[k] / 1e9, "\n")
}

# n = 10, d = 10, time = 6.17295e-05
# n = 340, d = 10, time = 0.003412121
# n = 670, d = 10, time = 0.009470589
# n = 1000, d = 10, time = 0.01863533
# n = 10, d = 340, time = 0.003610478
# n = 340, d = 340, time = 0.113568
# n = 670, d = 340, time = 0.3226099
# n = 1000, d = 340, time = 0.6329988
# n = 10, d = 670, time = 0.01052881
# n = 340, d = 670, time = 0.4429608
# n = 670, d = 670, time = 1.070812
# n = 1000, d = 670, time = 1.959374
# n = 10, d = 1000, time = 0.02686237
# n = 340, d = 1000, time = 1.143939
# n = 670, d = 1000, time = 2.537492
# n = 1000, d = 1000, time = 3.985535

cbind(grid, time = times) |>
  ggplot(aes(n, time, color = as.factor(d))) +
  geom_line()
```


这个公式表示线性组合 $\beta^T X$ 的方差。

具体来说：

- $X$ 是一个随机向量（比如 $d$ 维）
- $\beta$ 是一个系数向量
- $\Sigma$ 是 $X$ 的协方差矩阵
- $\beta^T X = \sum_{i=1}^{d} \beta_i X_i$ 是各分量的线性组合

公式 $\sigma^2(\beta) = \beta^T \Sigma \beta$ 是多元统计中的基本结论：随机向量经过线性变换后的方差，等于系数向量与协方差矩阵的二次型。

举个简单例子：如果 $d=2$，$\beta = (1, 1)^T$，那么 $\beta^T X = X_1 + X_2$，其方差为 $Var(X_1) + Var(X_2) + 2Cov(X_1, X_2)$，这正好等于 $\beta^T \Sigma \beta$。

