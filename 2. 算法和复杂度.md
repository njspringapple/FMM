## 🎯 本章核心问题

> [!question] 核心问题
> - 计算机如何求解数学问题？
> - 为什么"怎么算"比"算什么"更重要？
> - 如何评价一个算法的好坏？

---

## 📑 目录

```

1. 数值问题概述
2. 浮点数的困难
3. 算法基础
4. 复杂度分析
5. Landau 记号
6. 案例分析
7. 练习与总结

```

---

# 一、数值问题概述

## 1.1 什么是数值问题

> [!important] 定义：数值问题
> 
> 一个数值问题的解是一个或多个数字。
> 
> 一般形式：给定带输入 $x$ 的问题 $f$，计算它的解 $f(x)$。

> [!example] 常见的数值问题
> - 积分的值
> - 线性方程组（LGS）的解
> - 函数逼近中的基函数系数

---

## 1.2 一个类比

> [!tip] 类比：做菜
> 
> | 概念 | 类比 |
> |:---|:---|
> | 问题 | 做出像奶奶做的一样的烤猪肉 |
> | 算法 | 菜谱 |
> | 计算机 | 按菜谱做菜、但会犯错误的厨师 |

---

# 二、浮点数的困难

## 2.1 计算机的局限性

> [!warning] 计算机几乎从来都不是精确的
> 
> 它们只能：
> - **表示有限位数**、**并且数量有限的数字**
> - **进行有限次数的计算**

> [!example] 无法精确表示的例子
> - $\pi = 3.141592653589793238462643383279...$（无限不循环）
> - $e^x = \sum_{n=0}^{\infty} \frac{x^n}{n!}$（无穷级数）

> [!danger] 关键结论
> 因此，我们"**如何计算**"是非常重要的！

---

## 2.2 舍入误差的累积

> [!warning] 舍入误差问题
> - 计算机只能表示有限多个数
> - 这会导致：
>   - 舍入误差（四舍五入误差）
>   - **误差可能会累积**
>   - 计算操作的执行顺序会产生影响

> [!tip] 建议
> **尽量避免出现特别大或特别小的数**

---

## 2.3 灾难性消去

> [!danger] 灾难性消去（Catastrophic Cancellation）
> 
> 在浮点数计算中，**两个几乎相等的巨大数相减**时，结果的有效数字会大量丢失，误差可能被放大，甚至得到离谱结果（如算成 0）。

> [!example] 方差的两种等价公式
> 
> **不稳定**（容易灾难性消去）：
> $$
> Var(x) = \frac{1}{n}\sum_{i=1}^n x_i^2 - \left(\frac{1}{n}\sum_{i=1}^n x_i\right)^2
> $$
> 
> **更稳定**（推荐）：
> $$
> Var(x) = \frac{1}{n}\sum_{i=1}^n (x_i - \bar{x})^2
> $$

> [!note] 为什么不稳定
> 
> 当数据整体很大（比如都在 $10^9$ 附近），但波动很小（差异只有十几）时：
> - $\frac{1}{n}\sum x_i^2$ 和 $\bar{x}^2$ 都是 $10^{18}$ 量级
> - 它们的差（方差）却只有 $10^1$ 量级
> - 计算机只能存有限精度：两大数相减会"把高位相同部分消掉"，剩下的低位受舍入影响大

> [!success] 结论
> - **代数恒等式 ≠ 数值稳定**
> - 计算方差尽量用"先中心化再平方"的形式
> - 避免"非常大与非常大但很接近的数"的减法

---

## 2.4 加法不满足结合律

> [!danger] 浮点数加法不是结合的
> 
> 一般不保证 $(x+y)+z = x+(y+z)$

> [!example] 例子
> 
> 设 $x = 10^{30}$，$y = -10^{30}$，$z = 1$
> 
> 按第一种顺序：
> $$
> (x+y)+z = (10^{30} + (-10^{30})) + 1 = 0 + 1 = 1
> $$
> 
> 按第二种顺序：
> $$
> x+(y+z) = 10^{30} + ((-10^{30}) + 1) \approx 10^{30} + (-10^{30}) = 0
> $$

> [!note] 为什么第二个变成 0
> 
> 在浮点数里，相对 $10^{30}$ 来说，$1$ 太小，计算 $(-10^{30})+1$ 时，$1$ 被舍入"吃掉"，结果仍是 $-10^{30}$。

> [!warning] 要点
> 求和的**运算顺序**会影响结果！

---

# 三、算法基础

## 3.1 算法的定义

> [!important] 定义：算法
> 
> 算法是一种过程（由若干步骤组成），对输入 $x$ 计算一个（可能的）输出/解 $\tilde{f}(x)$。

---

## 3.2 算法类型

> [!abstract] 算法类型
> 
> | 类型 | 说明 | 例子 |
> |:---|:---|:---|
> | **精确/直接** | $\tilde{f}(x) = f(x)$（除了舍入误差） | 高斯消元法、矩阵乘法 |
> | **近似** | 只要求 $\tilde{f}(x) \approx f(x)$ | 蒙特卡洛积分、神经网络 |
> | **迭代** | 重复应用同一规则多次 | 梯度下降、牛顿法 |
> | **概率** | 依赖随机性 | 随机快排、MCMC |

> [!tip] 常见组合
> 机器学习算法往往同时是"**迭代 + 近似 + 概率型**"！

---

## 3.3 好算法的标准

> [!success] 我们想要的算法
> - **尽可能给出精确的结果**
> - **速度快**
> - **占用内存少**

---

## 3.4 代价的衡量

> [!important] 代价的定义
> 
> - **时间代价**：基本算术运算（$+$、$-$、$\cdot$、$/$）的次数
>   - 代价越高 ≈ 运行时间越长
> - **空间代价**：需要存储的（中间）数字的数量
>   - 代价越高 = 需要更多内存空间

---

## 3.5 例子：矩阵-向量乘法

> [!example] 计算 $y = Ax$
> 
> $$
> A = \begin{pmatrix} 1 & 2 \\ 3 & 4 \\ 5 & 6 \end{pmatrix}, \quad x = \begin{pmatrix} 7 \\ 8 \end{pmatrix}
> $$
> 
> **计算过程**：
> - $y_1 = 1 \cdot 7 + 2 \cdot 8 = 23$
> - $y_2 = 3 \cdot 7 + 4 \cdot 8 = 53$
> - $y_3 = 5 \cdot 7 + 6 \cdot 8 = 83$

> [!note] 时间开销
> 
> | 运算 | 次数 |
> |:---|:---|
> | 乘法 | $3 \times 2 = 6$ |
> | 加法 | $3 \times 1 = 3$ |
> | 合计 | $9$ |
> 
> **一般情况**：$A$ 是 $n \times d$ 矩阵
> - 乘法次数：$nd$
> - 加法次数：$n(d-1)$
> - 总操作数约为 $2nd$

> [!note] 空间开销
> 
> | 项目 | 存储量 |
> |:---|:---|
> | 矩阵 $A$ | $nd$ |
> | 向量 $x$ | $d$ |
> | 结果 $y$ | $n$ |
> | **总计** | $nd + d + n$ |

---

# 四、复杂度分析

## 4.1 复杂度的概念

> [!important] 复杂度
> 
> - 知道"精确的"运算次数和需要存多少个数，**并不是最重要的**
> - 我们主要关心的是：**当问题规模变大时，开销如何随规模增长而变化**
> - 这就叫算法的（运行时间/内存）**复杂度**

> [!example] 例子
> 
> 某算法需要的运算次数是 $4n^3 + 16n^2 + 239$
> 
> 当 $n$ 很大时，和主项 $4n^3$ 相比，$16n^2 + 239$ 可以忽略不计
> 
> 因此：
> - 该算法的增长"像" $4n^3$ 一样
> - 复杂度是**三次（立方）阶**
> - 记作：$O(n^3)$

---

## 4.2 复杂度类别

> [!abstract] 常见复杂度类别
> 
> | 复杂度 | 名称 | 含义 |
> |:---|:---|:---|
> | $O(1)$ | 常数 | 运算次数与 $n$ 无关 |
> | $O(\log n)$ | 对数 | $n$ 翻倍时，运算次数只增加常数 |
> | $O(n)$ | 线性 | $n$ 翻倍，运算次数也翻倍 |
> | $O(n^2)$ | 平方 | $n$ 翻倍，运算次数变为 4 倍 |
> | $O(2^n)$ | 指数 | $n$ 翻倍，运算次数呈指数爆炸 |

> [!warning] 注意
> 
> $O(\cdot)$ 记号会**忽略常数因子和低阶项**
> 
> 例如：$1000n + 10000$ 次运算仍然是 $O(n)$
> 
> 但它在实际中可能比 $n^2$ 次运算慢或快——要看 $n$ 的范围

---

# 五、Landau 记号

## 5.1 定义

> [!important] Landau 符号定义
> 
> 设 $a_n$、$b_n$ 是数列：
> 
> **小 $o$**：
> $$
> a_n = o(b_n) \iff \lim_{n \to \infty} \frac{a_n}{b_n} = 0
> $$
> 直观：$a_n$ 相比 $b_n$ 可以忽略（严格小一阶）
> 
> **大 $O$**：
> $$
> a_n = O(b_n) \iff \limsup_{n \to \infty} \left|\frac{a_n}{b_n}\right| < \infty
> $$
> 直观：$a_n$ 至多是 $b_n$ 的常数倍量级

> [!note] 重要关系
> - $a_n = o(b_n) \Rightarrow a_n = O(b_n)$
> - 反过来不一定成立

---

## 5.2 重要特例

> [!tip] 重要特例
> 
> - $a_n = O(1)$：即 $a_n$ **有界**
> - $a_n = o(1)$：即 $a_n$ **收敛到 0**

---

## 5.3 计算规则

> [!abstract] Lemma：计算规则
> 
> 设 $f_n, g_n$ 是两列，$a_n, b_n$ 是参考序列：
> 
> **规则 1**：若 $f_n = O(a_n)$，$g_n = O(b_n)$，则
> - 加法：$f_n + g_n = O(a_n + b_n)$
> - 乘法：$f_n g_n = O(a_n b_n)$
> 
> **规则 2**：若 $f_n = O(a_n)$，$g_n = o(b_n)$，则
> $$
> f_n g_n = o(a_n b_n)
> $$
> 
> **规则 3**：若 $f_n = O(a_n + b_n)$ 且 $a_n = O(b_n)$，则
> $$
> f_n = O(b_n)
> $$

---

## 5.4 计算例子

> [!example] 例 1：证明 $3n^2 + 5n = O(n^2)$
> 
> $$
> \lim_{n \to \infty} \frac{3n^2 + 5n}{n^2} = \lim_{n \to \infty} \left(3 + \frac{5}{n}\right) = 3 < \infty
> $$

> [!example] 例 2：证明 $5n = o(n^2)$
> 
> $$
> \lim_{n \to \infty} \frac{5n}{n^2} = \lim_{n \to \infty} \frac{5}{n} = 0
> $$

> [!example] 例 3：$n^2$ 不是 $O(n)$
> 
> $$
> \lim_{n \to \infty} \frac{n^2}{n} = \lim_{n \to \infty} n = \infty
> $$

> [!example] 复杂表达式化简
> 
> $$
> 4n^3 + 16n^2 + 239 = O(n^3 + n^2 + 1) = O(n^3)
> $$
> 
> 因为 $n^2 = O(n^3)$，$1 = O(n^3)$

---

# 六、案例分析

## 6.1 斐波那契数列

### 迭代算法

> [!example] 迭代版本
> 
> ```r
> fib_iter <- function(n) {
>   x <- numeric(n)
>   x[2] <- 1
>   for (i in 2:(n-1)) {
>     x[i+1] <- x[i] + x[i-1]
>   }
>   return(x)
> }
> ```

> [!success] 复杂度分析
> 
> | 项目 | 复杂度 |
> |:---|:---|
> | 时间 | $O(n)$（线性） |
> | 空间 | $O(n)$ |

### 朴素递归

> [!example] 递归版本
> 
> ```r
> fib_rec <- function(n) {
>   if (n <= 1) return(n)
>   return(fib_rec(n-1) + fib_rec(n-2))
> }
> ```

> [!danger] 复杂度分析
> 
> | 项目 | 复杂度 |
> |:---|:---|
> | 时间 | $O(2^n)$（指数级）— **非常慢！** |
> | 空间 | $O(n)$（递归栈深度） |

> [!success] 结论
> 迭代解法在时间上"戏剧性"更高效！

---

## 6.2 常见操作的复杂度

> [!abstract] 常见操作复杂度
> 
> | 操作 | 复杂度 |
> |:---|:---|
> | $n$ 个数求和 | $O(n)$ |
> | 距离矩阵 $D_{ij} = |X_i - X_j|$ | $O(n^2)$ |
> | 矩阵乘法 $A \in \mathbb{R}^{m \times n}$，$B \in \mathbb{R}^{n \times \ell}$ | $O(mn\ell)$ |
> | 矩阵的迹 $\text{tr}(A)$ | $O(n)$ |
> | $\text{tr}(A+B)$（利用 $\text{tr}(A) + \text{tr}(B)$） | $O(n)$ |
> | $\text{tr}(cA)$（利用 $c \cdot \text{tr}(A)$） | $O(n)$ |

---

# 七、练习与总结

## 练习 1.1：Landau 记号

> [!question] 题目
> 
> 对给定的数列 $a_n$，找简单的 $b_n$ 和 $c_n$，使得 $a_n = O(b_n)$ 且 $a_n = o(c_n)$。
> 
> (a) $a_n = 100n^{-1} + 20n^{0.5} + 5n^{1.5}$
> 
> (b) $a_n = \frac{2\log(n)}{n} + \frac{\log(2)}{n^2} + \log\left(1+\frac{1}{n}\right)$

> [!success] 解答
> 
> **(a)** 主导项是 $n^{1.5}$
> 
> 取 $b_n = n^{1.5}$，$c_n = n^2$
> $$
> \frac{a_n}{b_n} = 100n^{-2.5} + 20n^{-1} + 5 \to 5
> $$
> 
> **(b)** 主导项是 $\frac{\log n}{n}$
> 
> 取 $b_n = \frac{\log n}{n}$，$c_n = 1$
> 
> 由于 $\log(1+\frac{1}{n}) \le \frac{1}{n}$，所有项都是 $o(1)$

---

## 练习 1.2：条件复杂度

> [!question] 题目
> 
> 设 $a_n = O(e_n + e_n^2)$，证明：
> 1. 若 $e_n \to \infty$，则 $a_n = O(e_n^2)$
> 2. 若 $e_n \to 0$，则 $a_n = O(e_n)$

> [!success] 解答
> 
> **情况 1**：$e_n \to \infty$
> $$
> \frac{e_n + e_n^2}{e_n^2} = \frac{1}{e_n} + 1 \to 1
> $$
> 所以 $e_n + e_n^2 = O(e_n^2)$
> 
> **情况 2**：$e_n \to 0$
> $$
> \frac{e_n + e_n^2}{e_n} = 1 + e_n \to 1
> $$
> 所以 $e_n + e_n^2 = O(e_n)$

> [!tip] 一句话总结
> - $e_n$ 很大时，$e_n^2$ 主导
> - $e_n$ 很小时，$e_n$ 主导

---

## 练习 1.3：投资组合方差

> [!example] 问题背景
> 
> 计算投资组合方差：$\hat{\sigma}^2(\beta) = \beta^T \hat{\Sigma} \beta$
> 
> 其中 $\hat{\Sigma} = \frac{1}{n} \sum_{i=1}^{n} (X_i - \hat{\mu})(X_i - \hat{\mu})^T$

> [!success] 复杂度分析
> 
> **空间复杂度**：
> - $X$：$O(nd)$
> - $\beta$：$O(d)$
> - $\hat{\Sigma}$：$O(d^2)$
> - **总计**：$O(nd + d^2)$
> 
> **时间复杂度**：
> - 计算 `colMeans(X)`：$O(nd)$
> - 计算协方差矩阵（$n$ 次迭代）：$O(n(nd + d^2)) = O(n^2d + nd^2)$
> - 计算 $\beta^T \hat{\Sigma} \beta$：$O(d^2)$
> - **总计**：$O(n^2d + nd^2)$

---

## 总结

> [!summary] 本章要点
> 
> 1. **浮点数问题**：舍入误差、灾难性消去、运算顺序影响结果
> 
> 2. **算法类型**：精确、近似、迭代、概率
> 
> 3. **复杂度分析**：关注增长趋势而非精确次数
> 
> 4. **Landau 记号**：
>    - $O(\cdot)$：上界同阶
>    - $o(\cdot)$：严格小一阶
> 
> 5. **实践建议**：
>    - 避免大数相减
>    - 注意运算顺序
>    - 选择合适的算法

---

## ✅ 自测清单

- [ ] 理解为什么计算顺序很重要
- [ ] 能解释灾难性消去
- [ ] 能区分四种算法类型
- [ ] 能使用 Landau 记号分析复杂度
- [ ] 能简化复杂度表达式找出主导项
- [ ] 能分析简单算法的时间和空间复杂度
