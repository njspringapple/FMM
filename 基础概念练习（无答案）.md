### 向量

1. 向量 v 默认我们认为是 （______）向量

2. 向量 v 我们可以写成 $(v_1,v_2,...,v_n) \in \mathbb{R}^n$，其中 n 我们称为向量的 （______）

3. 有一些度量向量的工具，称为范数：

   - L1范数（曼哈顿范数）$\|v\|_1$ - 是向量的各个分量的（______），数学符号记为 $\|v\|_1 = \sum_{i=1}^n |v_i|$
   - L2范数（欧几里得范数）$\|v\|_2$ - 是向量的各个分量的（______），数学符号记为 $\|v\|_2 = \sqrt{\sum_{i=1}^n v_i^2}$
   - L∞范数（无穷范数/切比雪夫范数）$\|v\|_\infty$ - 是向量各个分量的（______），数学符号记为 $\|v\|_\infty = \max_i |v_i|$
   - Lp范数 $\|v\|_p$ - 是向量各个分量的（______），数学符号记录为 $\|v\|_p = \left(\sum_{i=1}^n |v_i|^p\right)^{1/p}$，其中 $p \geq 1$

4. 向量的**内积**为一个（______）向量乘以（______）向量，即 $\mathbf{v} \cdot \mathbf{w} = \mathbf{v}^T\mathbf{w}$，是两个（______）相同的向量 v, w 的各个（______）分量相乘后再（______），数学符号记为 $\mathbf{v} \cdot \mathbf{w} = \sum_{i=1}^n v_i w_i$，内积是一个（______）量
	1. 当n维向量 v 和 x， 这两个向量 内积 也可以看作是一个 (______)，即 $f(x) = v^Tx = v_{1}x_{1} + \dots \dots \dots + v_{n}x_{n}$

5. 向量的**外积**为一个（______）向量乘以（______）向量，即 $\mathbf{v} \otimes \mathbf{w} = \mathbf{v}\mathbf{w}^T$，如果 v 是 m 维，w 是 n 维，那么产生的矩阵维度是（______），这个矩阵的秩为（______），这个矩阵的每一列都是向量（______）的倍数，每一行都是（______）的倍数

6. 如果向量 v 和 w，他们的（______）为 0，我们称向量 v 和 w（______），记为 $\mathbf{v} \perp \mathbf{w} \Leftrightarrow \mathbf{v}^T\mathbf{w} = 0$

7. 向量 v 和 w，如果 $\mathbf{w} = k\mathbf{v}$，我们说向量 v 和 w（______），几何上，向量 v 和 w（______），一个向量是另一个向量的（______）

8. 向量集合 $V = \{\mathbf{v}_1, \mathbf{v}_2, ..., \mathbf{v}_n\}$，我们用数学符号（______）表示向量集合 V（______）的空间，即 V 中所有向量的线性组合构成的集合

9. 如果一组向量 $\mathbf{v}_1, ..., \mathbf{v}_n$，一组实数 $k_1, ..., k_n$，我们称（______）为这组向量的（______）

10. 单位向量为（______）（L2范数）为（______）的向量，任意非零向量 v 都可以**单位化**为 $\hat{\mathbf{v}} = \frac{\mathbf{v}}{\|\mathbf{v}\|_2}$

11. 零向量是所有分量都为（______）的向量，它与任何向量都（______）

12. 在 $\mathbb{R}^n$ 空间中，（______）是（______）的（______）条件，但是基（______）。**标准基**特指坐标轴方向的单位向量 $\{\mathbf{e}_1, ..., \mathbf{e}_n\}$，其中 $\mathbf{e}_i$ 的第 i 个分量为 1，其余为 0。如果某组基（______）且都是（______），则称为（______）

13. **向量投影**：向量 v 在向量 u 上的投影记录为 $\text{proj}_{\mathbf{u}}\mathbf{v} = \frac{\mathbf{v} \cdot \mathbf{u}}{\|\mathbf{u}\|^2}\mathbf{u}$，投影结果与（______）共线。如果 u 和 v 之间夹角小于 90°，投影后的向量与 u（______），否则（______）

### 矩阵

1. 矩阵 A（m* n），如果 m = n ，称为（______），否则称为（______）

2. 矩阵常见运算：
	1. 标量k乘以矩阵A，等于矩阵每个元素都（______）k
	2. 矩阵A和B相加，满足交换律和结合率，即（A + B = B + A），（A + （B + C）） = （（A + B）+ C ）
	3. 矩阵满足**乘法分配律** A(X+H) = AX + AH
	4. 矩阵 A （m * n ） 乘以 矩阵 B （n * p）得到矩阵C （m * p），矩阵乘法（______）交换律
	5. 矩阵 A  （m * n ） 乘以 向量 x （n维度）可以看作矩阵A的列向量的一个（______），x 存放了线性组合的（______），得到一个线性组合后的向量 b，因此 b 的维度是  （______）维
	6. 矩阵 A（m × n）乘以矩阵 B（n × p），可以把 B 看成 p 个（______）并排（ $[\mathbf{b}_1, \mathbf{b}_2, \dots, \mathbf{b}_p]$），则： $AB = A[\mathbf{b}_1, \mathbf{b}_2, \dots, \mathbf{b}_p] = [A\mathbf{b}_1, A\mathbf{b}_2, \dots, A\mathbf{b}_p]$ 即 A 对 B 的每一列分别做（线性变换），结果按列拼成新矩阵。这可以理解为**批量线性变换**。
	7. 对角矩阵D和矩阵相乘：AD（左乘）- A的（______）缩放  DA (右乘) - A 的（______）缩放
	
3. **主对角线**指的是矩阵中位置满足（______）等于（______）的元素，即a11，a22，。。。

4. 矩阵 U 通常代表 （______），L 通常代表 （______），上三角矩阵是指主对角线以下全都是 （______）的矩阵，同理 下三角矩阵是指主对角线以上元素都是（______）的元素，上下三角矩阵未必是方阵，但是实际应用中是方阵

5. 把矩阵A的行变成（______），（______）变成行，得到A的（______），记录为（______) ，有 $(A^T)^T = A$

6. 单位矩阵是（______）阵，其主对角线元素均为1，记为 （______）

7. 我们称矩阵A是 **正交矩阵**，那满足 （______) = （______）= I

8. 对于矩阵A，存在一个矩阵 $A^{-1}$ 使得 $A^{-1} A = AA^{-1} = I$, 则 $A^{-1}$ 称为A的（______），由此可见，正交矩阵的逆 $A^{-1}$ = （______）

9. 如果矩阵 A 的转置 $A^T$ 相等，我们称矩阵A 是 **（______）矩阵**，对称矩阵一定是（______）阵

10. 如果A不是方阵，可以通过 $A^T A$ 或者 $AA^T$ 构造一个（______），这两个（______）由相同的（______）特征值，也叫（______）矩阵

11. 矩阵A（m * n ）可以看成由一组列向量 v1，。。。vn 构成，这组向量里面**最大线性无关向量个数** 称为 （______），数学上记做 （______）如果 r = n 则矩阵 A （______）秩，否则 （______）秩

12. 矩阵A的（______）一定等于（______）

13. 由矩阵A（m * n ）的列向量中的线性无关向量 (______) 的空间称为矩阵A的（______），记作（______）

14. 所有满足 Ax = 0 的向量 x 构成的集合 称为 （______） 或者称为 （______），记作 （______），Null（A）= Ker（A）= { x | Ax = 0 }，零空间一定包含（______）

15. 矩阵A的列向量个数 （______）矩阵的 （______），得到矩阵A的 （______）或者称为 （______） 的维度，这叫做 **秩-零化度定理**

16. 矩阵 A（m* n）从线性方程组的视角看，如果 m > n ，是（______）方程，即 约束条件 比 未知数 多，（______）精确解，如果 m < n，是（______）方程，无（______）解，如果 m = n，且矩阵 A ( ______ )秩，有（______）解

17. 矩阵 Q 的所有列向量（______）且 都是（______）向量，即构成（______）则称矩阵为 **正交矩阵**，满足 $Q^T Q = I$

18. 方阵 A 的行列式记作 $\det(A)$ 或 $|A|$，是一个标量。几何意义是：矩阵 A 对空间做线性变换后，**体积的缩放因子**。如果 det(A)  > 0 体积变大， det(A) < 0 体积缩小，det(A) = 0 空间被（______）到 （______）这时候矩阵（______）

19. 用线性变换视角看：
	1. 如果矩阵 A列向量线性相关，则（______）秩，则（______）逆，行列式（______）零，零空间维度（______）0，称为（______）矩阵。
	2. 反之，如果矩阵 A列向量线性无关，则（______）秩，则（______）逆，行列式（______）零，零空间维度（______）0，称为（______）矩阵。
	
20. 只有主对角线上由非零元素的（______）阵，称为（______），记作（______）

21. 对于方阵，矩阵的 （______） 是(______)的和，记作：tr(A)

22. 矩阵的条件数是 最大（______）（______）最小（______），记作：$\kappa(A) = \frac{\sigma_{\max}}{\sigma_{\min}}$，可以用于衡量矩阵的（______），如果条件数为1的矩阵叫做（______），不会（______）误差，$\kappa(A) \gg 1$：矩阵（______），数值计算不稳定，输入的微小扰动会导致输出的大幅变化

23. （______）矩阵的条件数为（______），因为所有（______）都等于 1。

### 张量

1. **张量**（Tensor）是向量和矩阵的推广：
	1. 标量是 （______） 阶张量，向量是 （______） 阶张量，矩阵是 （______) 阶张量
	2. 更高阶的是(______) $T(\mathbf{x}) = A\mathbf{x}$，这里 T 指 (______)
	3. 张量数学上记为：$A = (a_{i_1, \ldots, i_k} : 1 \leq i_1 \leq n_1, \ldots, 1 \leq i_k \leq n_k) \in \mathbb{R}^{n_1 \times \cdots \times n_k}$
	4. 数 k 称为张量的**阶**（order）
	
2.  $v \otimes w$ 算子在v和w为向量的时候是（______），即 $vw^T$，如果是张量场景下，称为（______），即 （______） 是 （______）的特殊场景

3. 两个张量 $\mathbf{A} \in \mathbb{R}^{m_1 \times \cdots \times m_p}$ 和 $\mathbf{B} \in \mathbb{R}^{n_1 \times \cdots \times n_q}$ 的**张量积**定义为 （______），即张量积的维度**直接进行拼接**，例如 2 * 3 阶矩阵 张量积 3 * 4阶矩阵结果是  2* * 3 * 3 * 4 阶矩阵

4. 形如 $\mathbf{u} \otimes \mathbf{v} \otimes \mathbf{w}$ 的张量**秩为（______）** -- $\mathbf{u} \otimes \mathbf{v}$ 的秩为1，然后再

5. **Kronecker 积**：$A \otimes B$ 是把 A 的每个元素乘以整个 B 矩阵并替换原位置，得到更大的分块矩阵，维度从 (m×n) ⊗ (p×q) 变为 (mp × nq)。

6. k 元，次数 d <= 1的多项式可以表示为 不同元的 （______）的 （______），表示为：$B_{1} \otimes B_{2}$ 这个多项式的 维度为 ( ______)  即 维度随  ______增长 ，也叫 （______）


### 矩阵变换

1. 线性方程组 $A\mathbf{x} = \mathbf{b}$ 求解，可以使用 A 的（______）求解，即 $\mathbf{x} = A^{-1}\mathbf{b}$（前提是 A 可逆）。但是由于矩阵求逆运算量（______），且 A 不一定（______），也不一定（______），因此需要对矩阵做分解。
	- QR 分解记作：$A = QR$，Q 是（______），R 是（______） 
	- SVD 分解记作：$A = U\Sigma V^T$，U、V 是（______），$\Sigma$ 是(______)（对角元素为(______））

2. 不论是方阵，还是厂房矩阵，都可以做（______）分解和（______）分解，记作 

3. 所有方阵，可以做（______）分解（某些情况需要行置换，记作 （______）

4. 如果方阵可以对角化，可以做（______）分解，记作：（______）

5. 如果方阵是个（______）方阵（这种矩阵一定可以（______））可以做（______）分解，这个分解是特征值分解的一个**特例**，记作：（______）

6. 如果方阵是个（______）方阵，且（______），可以做（______）分解，记作：（______）

7. 正定矩阵是指所有特征值（______），半正定矩阵指所有特征值（______）

8. 对于方阵 A，如果存在非零向量 $\mathbf{v}$ 和标量 $\lambda$，使得 $A\mathbf{v} = \lambda\mathbf{v}$，则 $\lambda$ 称为（______），$\mathbf{v}$ 称为（______），可见：矩阵对特征向量的作用只是（______），不改变（______）（特征值为负时方向反转）

9. SVD分解：
	1. SVD 分解后，$\Sigma$ 中对角线的奇异值按（______）序排列
	2. 矩阵的秩等于（______）的个数
	3. 矩阵A的奇异值是 矩阵 $A^T A$ 的特征值的（______），因此，奇异值一定（______）零（回顾：$A^TA$ 是半正定矩阵，所以特征值大于等于0，所以才能开根号）
	4. SVD分解的求和形式：矩阵A可以分解多个（______）"层"的(______)，每层为用 (______) 乘以 （______） 外积 （______），记作：（______）
	
10. n 阶方阵有（______）个特征值，含（______），所有特征值之（______）等于矩阵的（______），所有特征值之（______）等于矩阵的（______）

### 矩阵范数

1. 矩阵的逐元素范数意思是把矩阵所有元素看成一个（______），有以下几种逐元素范数：
	- **Frobenius 范数**（L2 范数）：$\|A\|_F = \sqrt{\sum_{i,j} a_{ij}^2}$，即所有元素平方和的平方根 
		- **特性**：矩阵 A( m * n) ,  通过  $A^TA$  构成的方阵，对角线的元素正好是矩阵 A 对应的列的每个元素的平方的和
		- 所以，矩阵 A 所有元素的平方和可以写为  $A^TA$  这个方阵的对角线元素之和，也就是  $tr(A^TA)$ 
		- 所以，$\|A\|_F = \sqrt{tr(A^TA)}$
	- **L1 范数**：$\|A\|_1 = \sum_{i,j} |a_{ij}|$，即所有元素绝对值之和 
	- **L∞ 范数**（最大范数）：$\|A\|_\infty = \max_{i,j} |a_{ij}|$，即所有元素绝对值的最大值

2. 矩阵的诱导范数（算子范数）是从向量范数诱导出来的，衡量矩阵对向量的（______）程度： 
	- **谱范数**（L2 诱导范数）：$\|A\|_2 = \sigma_{\max}$，即最大（______） 
	- **列和范数**（L1 诱导范数）：每列元素（______）的（______） 
	- **行和范数**（L∞ 诱导范数）：每行元素(______)的（______）