## 🎯 预备知识

> [!info] 分析基础
> - **导数**：$f'(x) = \lim_{h \to 0} \frac{f(x+h) - f(x)}{h}$
> - **偏导数**：$\frac{\partial f(\mathbf{x})}{\partial x_i}$
> - **线性近似**：$f(x+h) \approx f(x) + f'(x)h$
> - **泰勒级数基础**

> [!info] 线性代数基础
> - **矩阵迹**：$\text{tr}(\mathbf{A}) = \sum_i a_{ii}$
> - **矩阵范数**：$\|\mathbf{A}\|_F = \sqrt{\text{tr}(\mathbf{A}^\top \mathbf{A})}$
> - **赋范向量空间**
> - **有界线性算子**

> [!info] Landau 记号
> $$
> g(h) = o(h) \quad \Leftrightarrow \quad \lim_{h \to 0} \frac{g(h)}{h} = 0
> $$
> 即 $g(h)$ 比 $h$ **更快地趋于零**

---

## 📑 目录

```

1. 一元函数的导数回顾
2. 一般导数概念（Fréchet导数）
3. 特殊情况：向量→标量（梯度）
4. 特殊情况：向量→向量（Jacobi矩阵）
5. 特殊情况：标量→矩阵
6. 特殊情况：矩阵→标量
7. 总结

```

---

# 一、一元函数的导数回顾

## 1.1 可微性定义

> [!important] 定义：可微性
> 设 $f: S \subseteq \mathbb{R} \to \mathbb{R}$。若下式成立，则称 $f$ 在点 $x \in S$ 处**可微**，导数为 $f'(x)$：
> 
> $$
> \lim_{h \to 0} \frac{f(x+h) - f(x)}{h} = f'(x)
> $$

> [!tip] 几何意义
> - $f'(x)$ 是 $f$ 在点 $x$ 处**切线的斜率**
> - 若斜率不唯一，则 $f$ 在该点不可微

---

## 1.2 导数的等价刻画

> [!question] 小测验
> 设 $f$ 在 $x$ 处可微，导数为 $f'(x)$。哪些陈述正确？
> 
> 1. $f'$ 是线性函数
> 2. $h \mapsto f'(x)h$ 是线性函数（固定 $x$）
> 3. $\lim_{h \to 0} \frac{|f(x+h) - f(x) - f'(x)h|}{h} = 0$
> 4. $f(x+h) = f(x) + f'(x)h + o(h)$

> [!success] 答案：2、3、4 正确 ✓
> 
> | 选项 | 正误 | 说明 |
> |:---:|:---:|:---|
> | 1 | ✗ | $f'$ 可以是任意函数（如 $f(x)=x^2$ 时 $f'(x)=2x$） |
> | 2 | ✓ | 对固定的 $x$，$h \mapsto f'(x) \cdot h$ 显然是线性的 |
> | 3 | ✓ | 这是导数定义的等价形式 |
> | 4 | ✓ | **线性近似 + 高阶小量**，这是推广的关键！ |

---

# 二、一般导数概念（Fréchet导数）

## 2.1 核心思想

> [!abstract] 推广导数的关键
> 
> **导数 = 线性近似**
> 
> 一元情形的核心公式：
> $$
> f(x+h) = f(x) + \underbrace{f'(x) \cdot h}_{\text{线性部分}} + \underbrace{o(h)}_{\text{误差项}}
> $$
> 
> 推广到任意赋范空间：用**有界线性映射**代替 $f'(x) \cdot h$

---

## 2.2 Fréchet可微的定义

> [!important] 定义：Fréchet 可微
> 设 $\mathbb{D}$ 和 $\mathbb{E}$ 是两个赋范空间，$f: \mathbb{D} \to \mathbb{E}$。
> 
> 若存在**有界线性映射** $D_x f: \mathbb{D} \to \mathbb{E}$，使得
> 
> $$
> f(x+h) = f(x) + D_x f(h) + o(\|h\|) \quad \text{对所有 } h \to 0
> $$
> 
> 则称 $f$ 在点 $x \in \mathbb{D}$ 处 **Fréchet 可微**。

> [!note] 关键要素解读
> 
> | 要素 | 说明 |
> |:---|:---|
> | **有界性** | 存在 $M < \infty$，使得 $\|D_x f(h)\| \leq M\|h\|$ 对所有 $h$ 成立 |
> | **线性性** | $h \mapsto D_x f(h)$ 对 $h$ 是线性的 |
> | **依赖于 $x$** | $D_x f$ 可以以任意方式依赖于 $x$ |
> | **误差项** | $o(\|h\|)$ 当 $h \to 0$ 时相对于 $\|h\|$ 变得任意小 |

---

## 2.3 具体例子：$f(x) = x^2$

> [!example] 验证 $f(x) = x^2$ 的 Fréchet 可微性
> 
> **目标**：证明 $D_x f(h) = 2xh$
> 
> **展开计算**：
> $$
> f(x+h) = (x+h)^2 = x^2 + 2xh + h^2 = f(x) + 2xh + h^2
> $$
> 
> **验证三个条件**：
> 
> ✅ **线性性**：$D_x f(h) = 2xh$ 关于 $h$ 是线性的
> 
> ✅ **有界性**：$|D_x f(h)| = |2xh| = 2|x| \cdot |h| \leq M|h|$，其中 $M = 2|x| < \infty$
> 
> ✅ **误差项**：$\lim_{h \to 0} \frac{|h^2|}{|h|} = \lim_{h \to 0} |h| = 0 \Rightarrow h^2 = o(|h|)$
> 
> **结论**：$D_x f(h) = f'(x)h = 2x \cdot h$，与经典导数一致 ✓

---

## 2.4 重要特殊情况总览

> [!tip] 导数类型速查表
> 
> | 输入 | 输出 | 导数名称 | 记号 | 维度 |
> |:---:|:---:|:---:|:---:|:---:|
> | 标量 $x$ | 标量 $y$ | 导数 | $\frac{dy}{dx}$ | $1 \times 1$ |
> | 向量 $\mathbf{x}$ | 标量 $y$ | **梯度** | $\nabla f(\mathbf{x})$ | $1 \times n$ |
> | 向量 $\mathbf{x}$ | 向量 $\mathbf{y}$ | **Jacobi矩阵** | $\mathbf{J}_f(\mathbf{x})$ | $m \times n$ |
> | 标量 $x$ | 矩阵 $\mathbf{Y}$ | 矩阵导数 | $\frac{d\mathbf{Y}}{dx}$ | $m \times n$ |
> | 矩阵 $\mathbf{X}$ | 标量 $y$ | 矩阵梯度 | $\frac{\partial y}{\partial \mathbf{X}}$ | $m \times n$ |

---

# 三、特殊情况：向量→标量（梯度）

## 3.1 梯度的定义

> [!important] 定义：梯度
> 设 $f: \mathbb{R}^n \to \mathbb{R}$ 可微。$f$ 在 $\mathbf{x} \in \mathbb{R}^n$ 处的**梯度**定义为行向量：
> 
> $$
> \frac{\partial f(\mathbf{x})}{\partial \mathbf{x}} = \nabla f(\mathbf{x}) = \left(\frac{\partial f(\mathbf{x})}{\partial x_1}, \cdots, \frac{\partial f(\mathbf{x})}{\partial x_n}\right) \in \mathbb{R}^{1 \times n}
> $$
> 
> 此时 Fréchet 导数为：
> $$
> D_\mathbf{x} f(\mathbf{h}) = \nabla f(\mathbf{x}) \mathbf{h}
> $$

> [!warning] 注意
> 本课程中梯度定义为**行向量**（$\in \mathbb{R}^{1 \times n}$），便于矩阵运算。有些教材定义为列向量。

---

## 3.2 几何意义

> [!abstract] 梯度的几何直观
> 
> 对于 $f: \mathbb{R}^n \to \mathbb{R}$：
> 
> - **梯度方向**：$f$ 在点 $\mathbf{x}$ 处**上升最快的方向**
> - **梯度大小**：该方向上的变化率
> - **切平面**：$\mathbf{h} \mapsto f(\mathbf{x}) + \nabla f(\mathbf{x})\mathbf{h}$ 是 $f$ 在 $\mathbf{x}$ 处的切平面

```

```
    z
    ↑
    │    ∇f(x)  ← 指向最陡上升方向
    │   ↗
    │  /
────●─────→ x
   /│
  / │
 ↙  │
y   │
```

```

---

## 3.3 例：二次函数的梯度

> [!example] 计算 $f(\mathbf{x}) = x_1^2 + 3x_1 x_2 + 2x_2^2$ 的梯度
> 
> **步骤1：计算偏导数**
> $$
> \frac{\partial f(\mathbf{x})}{\partial x_1} = 2x_1 + 3x_2, \quad \frac{\partial f(\mathbf{x})}{\partial x_2} = 3x_1 + 4x_2
> $$
> 
> **步骤2：组成梯度向量**
> $$
> \nabla f(\mathbf{x}) = (2x_1 + 3x_2, \; 3x_1 + 4x_2)
> $$
> 
> **注意**：$\nabla f: \mathbb{R}^2 \to \mathbb{R}^{1 \times 2}$

---

## 3.4 重要梯度公式

> [!success] 常用梯度恒等式
> 
> **线性函数**
> $$
> f(\mathbf{x}) = \mathbf{a}^\top \mathbf{x} \quad \Rightarrow \quad \nabla f(\mathbf{x}) = \mathbf{a}^\top
> $$
> 
> **二次型**（$\mathbf{A}$ 对称）
> $$
> f(\mathbf{x}) = \mathbf{x}^\top \mathbf{A} \mathbf{x} \quad \Rightarrow \quad \nabla f(\mathbf{x}) = 2\mathbf{x}^\top \mathbf{A}
> $$
> 
> **特例：欧氏范数平方**
> $$
> f(\mathbf{x}) = \|\mathbf{x}\|_2^2 = \mathbf{x}^\top \mathbf{x} \quad \Rightarrow \quad \nabla f(\mathbf{x}) = 2\mathbf{x}^\top
> $$

---

## 3.5 二次型梯度的推导

> [!note] 详细推导：$\nabla(\mathbf{x}^\top \mathbf{A} \mathbf{x})$
> 
> 设 $\mathbf{A} \in \mathbb{R}^{n \times n}$ 对称，$f(\mathbf{x}) = \mathbf{x}^\top \mathbf{A} \mathbf{x}$
> 
> **展开为求和形式**：
> $$
> f(\mathbf{x}) = \sum_{i=1}^n \sum_{j=1}^n x_i a_{ij} x_j
> $$
> 
> **对 $x_k$ 求偏导**（注意 $x_k$ 可能出现在 $i=k$ 或 $j=k$ 的项中）：
> $$
> \frac{\partial f}{\partial x_k} = \sum_{i=1}^n x_i a_{ik} + \sum_{j=1}^n a_{kj} x_j
> $$
> 
> **利用对称性** $a_{ik} = a_{ki}$：
> $$
> \frac{\partial f}{\partial x_k} = \sum_{i=1}^n a_{ki} x_i + \sum_{j=1}^n a_{kj} x_j = 2\sum_{i=1}^n a_{ki} x_i = 2(\mathbf{A}\mathbf{x})_k
> $$
> 
> **结论**：$\nabla f(\mathbf{x}) = 2\mathbf{x}^\top \mathbf{A}$

---

## 3.6 应用：梯度下降

> [!tip] 梯度下降算法
> 
> **问题**：最小化损失函数 $L(\boldsymbol{\theta})$
> 
> **迭代公式**：
> $$
> \boldsymbol{\theta}^{(t+1)} = \boldsymbol{\theta}^{(t)} - \alpha \nabla L(\boldsymbol{\theta}^{(t)})^\top
> $$
> 
> | 符号 | 含义 |
> |:---:|:---|
> | $\alpha > 0$ | 学习率（步长） |
> | $-\nabla L$ | 负梯度，指向**最陡下降方向** |
> | $\boldsymbol{\theta}$ | 待优化的模型参数 |

> [!success] 应用场景
> - 神经网络训练
> - 逻辑回归
> - 支持向量机
> - 各种机器学习模型优化

---

# 四、特殊情况：向量→向量（Jacobi矩阵）

## 4.1 Jacobi矩阵的定义

> [!important] 定义：Jacobi矩阵
> 设 $f: \mathbb{R}^n \to \mathbb{R}^m$ 可微，$f(\mathbf{x}) = (f_1(\mathbf{x}), \ldots, f_m(\mathbf{x}))^\top$。
> 
> $f$ 在 $\mathbf{x}$ 处的 **Jacobi矩阵** $\mathbf{J}_f(\mathbf{x}) \in \mathbb{R}^{m \times n}$ 为：
> 
> $$
> \mathbf{J}_f(\mathbf{x}) := \frac{\partial f(\mathbf{x})}{\partial \mathbf{x}} = \begin{pmatrix} \partial f_1/\partial x_1 & \cdots & \partial f_1/\partial x_n \\ \vdots & \ddots & \vdots \\ \partial f_m/\partial x_1 & \cdots & \partial f_m/\partial x_n \end{pmatrix}
> $$
> 
> 此时 Fréchet 导数为：$D_\mathbf{x} f(\mathbf{h}) = \mathbf{J}_f(\mathbf{x})\mathbf{h}$

> [!note] 与梯度的关系
> Jacobi矩阵的**每一行**是对应分量函数的梯度：
> $$
> \mathbf{J}_f(\mathbf{x}) = \begin{pmatrix} \nabla f_1(\mathbf{x}) \\ \vdots \\ \nabla f_m(\mathbf{x}) \end{pmatrix}
> $$

---

## 4.2 几何直观

> [!abstract] Jacobi矩阵的几何意义
> 
> $$
> f(\mathbf{x} + \mathbf{h}) \approx f(\mathbf{x}) + \mathbf{J}_f(\mathbf{x})\mathbf{h}
> $$
> 
> **Jacobi矩阵描述了函数的局部线性近似**
> 
> - $f$ 在局部**弯曲和拉伸**空间
> - $\mathbf{J}_f$ 给出这种变形的**一阶近似**
> - 类似于切平面，但是从 $\mathbb{R}^n$ 到 $\mathbb{R}^m$

---

## 4.3 重要Jacobi矩阵公式

> [!success] 常用恒等式
> 
> | 函数 | Jacobi矩阵 |
> |:---|:---|
> | $f(\mathbf{x}) = \mathbf{c}$（常向量） | $\mathbf{J}_f = \mathbf{0}_{m \times n}$ |
> | $f(\mathbf{x}) = \mathbf{x}$（恒等映射） | $\mathbf{J}_f = \mathbf{I}_n$ |
> | $f(\mathbf{x}) = \mathbf{A}\mathbf{x}$（线性变换） | $\mathbf{J}_f = \mathbf{A}$ |
> | $f(\mathbf{x}) = \mathbf{x}^\top \mathbf{A}$ | $\mathbf{J}_f = \mathbf{A}^\top$ |

---

## 4.4 例：线性函数的Jacobi矩阵

> [!example] 证明 $f(\mathbf{x}) = \mathbf{A}\mathbf{x}$ 的 Jacobi矩阵是 $\mathbf{A}$
> 
> 设 $\mathbf{A} \in \mathbb{R}^{m \times n}$
> 
> **逐分量表示**：
> $$
> f_i(\mathbf{x}) = \sum_{j=1}^n a_{ij} x_j
> $$
> 
> **计算偏导数**：
> $$
> \frac{\partial f_i(\mathbf{x})}{\partial x_k} = \frac{\partial}{\partial x_k}\left(\sum_{j=1}^n a_{ij} x_j\right) = a_{ik}
> $$
> 
> **组成Jacobi矩阵**：
> $$
> \mathbf{J}_f(\mathbf{x}) = \begin{pmatrix} a_{11} & \cdots & a_{1n} \\ \vdots & \ddots & \vdots \\ a_{m1} & \cdots & a_{mn} \end{pmatrix} = \mathbf{A} \quad \checkmark
> $$

> [!tip] 重要结论
> - 线性函数的Jacobi矩阵是**常数矩阵**
> - 对于线性函数：$f(\mathbf{x}+\mathbf{h}) = f(\mathbf{x}) + \mathbf{J}_f(\mathbf{x})\mathbf{h}$（**精确相等**，无误差项）

---

## 4.5 应用：神经网络中的反向传播

> [!tip] 反向传播原理
> 
> **神经网络结构**：
> $$
> \text{输入 } \mathbf{x} \xrightarrow{f_1} \mathbf{z}_1 \xrightarrow{f_2} \mathbf{z}_2 \xrightarrow{\cdots} \cdots \xrightarrow{f_K} \text{输出 } \hat{\mathbf{y}}
> $$
> 
> - 每层 $f_k$ 有参数 $\boldsymbol{\theta}_k$
> - 损失函数 $L(\hat{\mathbf{y}}; \boldsymbol{\theta})$ 衡量输出质量
> - 需要计算 $\frac{\partial L}{\partial \boldsymbol{\theta}_k}$ 来优化
> 
> **使用Jacobi矩阵的链式法则**：
> $$
> \frac{\partial L}{\partial \boldsymbol{\theta}_k} = \frac{\partial L}{\partial \hat{\mathbf{y}}} \cdot \mathbf{J}_{f_K}(\mathbf{z}_{K-1}) \cdot \mathbf{J}_{f_{K-1}}(\mathbf{z}_{K-2}) \cdots \mathbf{J}_{f_k}(\mathbf{z}_{k-1}) \cdot \frac{\partial \mathbf{W}_k}{\partial \boldsymbol{\theta}_k}
> $$

> [!success] 核心思想
> **反向传播 = 高效计算这个Jacobi矩阵链！**

---

# 五、特殊情况：标量→矩阵

## 5.1 定义

> [!important] 定义：标量对矩阵的导数
> 设 $\mathbf{F}: \mathbb{R} \to \mathbb{R}^{m \times n}$，$\mathbf{F}(x) = (f_{ij}(x))$
> 
> 定义：
> $$
> \frac{\partial \mathbf{F}(x)}{\partial x} = \begin{pmatrix} \frac{\partial f_{11}(x)}{\partial x} & \cdots & \frac{\partial f_{1n}(x)}{\partial x} \\ \vdots & \ddots & \vdots \\ \frac{\partial f_{m1}(x)}{\partial x} & \cdots & \frac{\partial f_{mn}(x)}{\partial x} \end{pmatrix} \in \mathbb{R}^{m \times n}
> $$
> 
> Fréchet 导数：$D_x \mathbf{F}(h) = \frac{\partial \mathbf{F}(x)}{\partial x} h$

---

## 5.2 例题

> [!example] 计算矩阵函数的导数
> 
> $$
> \mathbf{F}(x) = \begin{pmatrix} x^2 & 2e^x & 0 \\ 0 & x & \ln(x) \end{pmatrix}
> $$
> 
> **逐元素求导**：
> $$
> \frac{\partial \mathbf{F}(x)}{\partial x} = \begin{pmatrix} 2x & 2e^x & 0 \\ 0 & 1 & \frac{1}{x} \end{pmatrix}
> $$

---

## 5.3 重要恒等式

> [!success] 标量→矩阵的导数公式
> 
> **逆矩阵的导数**
> $$
> \frac{\partial \mathbf{F}(x)^{-1}}{\partial x} = -\mathbf{F}(x)^{-1} \frac{\partial \mathbf{F}(x)}{\partial x} \mathbf{F}(x)^{-1}
> $$
> 
> **行列式的导数**
> $$
> \frac{\partial \det(\mathbf{F}(x))}{\partial x} = \det(\mathbf{F}(x)) \cdot \text{tr}\left[\mathbf{F}(x)^{-1} \frac{\partial \mathbf{F}(x)}{\partial x}\right]
> $$
> 
> **迹的导数**
> $$
> \frac{\partial \text{tr}(\mathbf{F}(x))}{\partial x} = \text{tr}\left(\frac{\partial \mathbf{F}(x)}{\partial x}\right)
> $$

---

## 5.4 迹导数公式的证明

> [!note] 证明：$\frac{\partial \text{tr}(\mathbf{F}(x))}{\partial x} = \text{tr}\left(\frac{\partial \mathbf{F}(x)}{\partial x}\right)$
> 
> **利用迹的定义**：
> $$
> \text{tr}(\mathbf{F}(x)) = \sum_{i=1}^n f_{ii}(x)
> $$
> 
> **对 $x$ 求导**：
> $$
> \frac{\partial}{\partial x} \text{tr}(\mathbf{F}(x)) = \frac{\partial}{\partial x} \sum_{i=1}^n f_{ii}(x) = \sum_{i=1}^n \frac{\partial f_{ii}(x)}{\partial x} = \text{tr}\left(\frac{\partial \mathbf{F}(x)}{\partial x}\right) \quad \checkmark
> $$

---

## 5.5 例：行列式导数公式验证

> [!example] 验证行列式导数公式
> 
> 设 $\mathbf{F}(x) = \begin{pmatrix} x & 0 \\ 0 & 2x \end{pmatrix}$
> 
> **直接计算**：
> $$
> \det(\mathbf{F}(x)) = x \cdot 2x = 2x^2 \quad \Rightarrow \quad \frac{\partial}{\partial x} \det(\mathbf{F}(x)) = 4x
> $$
> 
> **用公式验证**：
> 
> $\frac{\partial \mathbf{F}(x)}{\partial x} = \begin{pmatrix} 1 & 0 \\ 0 & 2 \end{pmatrix}$，$\mathbf{F}(x)^{-1} = \begin{pmatrix} 1/x & 0 \\ 0 & 1/(2x) \end{pmatrix}$
> 
> $$
> \mathbf{F}(x)^{-1} \frac{\partial \mathbf{F}(x)}{\partial x} = \begin{pmatrix} 1/x & 0 \\ 0 & 1/x \end{pmatrix}, \quad \text{tr}(\cdots) = \frac{2}{x}
> $$
> 
> $$
> \det(\mathbf{F}(x)) \cdot \text{tr}[\cdots] = 2x^2 \cdot \frac{2}{x} = 4x \quad \checkmark
> $$

> [!success] 应用
> 这个公式在统计学中计算**似然函数的导数**时非常有用！

---

# 六、特殊情况：矩阵→标量

## 6.1 定义

> [!important] 定义：矩阵对标量的导数
> 设 $f: \mathbb{R}^{m \times n} \to \mathbb{R}$
> 
> 定义：
> $$
> \frac{\partial f(\mathbf{X})}{\partial \mathbf{X}} = \begin{pmatrix} \partial f/\partial x_{11} & \cdots & \partial f/\partial x_{1n} \\ \vdots & \ddots & \vdots \\ \partial f/\partial x_{m1} & \cdots & \partial f/\partial x_{mn} \end{pmatrix} \in \mathbb{R}^{m \times n}
> $$
> 
> Fréchet 导数：
> $$
> D_\mathbf{X} f(\mathbf{H}) = \text{tr}\left(\left(\frac{\partial f(\mathbf{X})}{\partial \mathbf{X}}\right)^\top \mathbf{H}\right)
> $$

---

## 6.2 例：双线性型

> [!example] 计算 $f(\mathbf{X}) = \mathbf{a}^\top \mathbf{X} \mathbf{b}$ 的导数
> 
> 设 $\mathbf{X} \in \mathbb{R}^{m \times n}$，$\mathbf{a} \in \mathbb{R}^m$，$\mathbf{b} \in \mathbb{R}^n$
> 
> **展开为求和**：
> $$
> f(\mathbf{X}) = \sum_{i=1}^m \sum_{j=1}^n a_i x_{ij} b_j
> $$
> 
> **求偏导**：
> $$
> \frac{\partial f(\mathbf{X})}{\partial x_{ij}} = a_i b_j
> $$
> 
> **结论**：
> $$
> \frac{\partial (\mathbf{a}^\top \mathbf{X} \mathbf{b})}{\partial \mathbf{X}} = \mathbf{a}\mathbf{b}^\top
> $$

---

## 6.3 重要恒等式

> [!success] 矩阵→标量的导数公式
> 
> **二次型**（$\mathbf{A}$ 对称）
> $$
> f(\mathbf{X}) = \text{tr}(\mathbf{X}\mathbf{A}\mathbf{X}^\top) \quad \Rightarrow \quad \frac{\partial f}{\partial \mathbf{X}} = 2\mathbf{X}\mathbf{A}
> $$
> 
> **迹-乘积**
> $$
> f(\mathbf{X}) = \text{tr}(\mathbf{A}^\top \mathbf{X}) \quad \Rightarrow \quad \frac{\partial f}{\partial \mathbf{X}} = \mathbf{A}
> $$
> 
> **Frobenius范数平方**
> $$
> f(\mathbf{X}) = \|\mathbf{X}\|_F^2 = \text{tr}(\mathbf{X}^\top \mathbf{X}) \quad \Rightarrow \quad \frac{\partial f}{\partial \mathbf{X}} = 2\mathbf{X}
> $$

---

## 6.4 应用：矩阵分解与矩阵补全

> [!tip] 矩阵补全问题
> 
> **给定**：仅在索引 $(i,j) \in \Omega$ 处观测到的不完整矩阵 $\mathbf{Y} \in \mathbb{R}^{m \times n}$（如用户对电影的评分）
> 
> **目标**：找到低秩近似 $\mathbf{Y} \approx \mathbf{U}\mathbf{V}^\top$
> 
> **损失函数**：
> $$
> L(\mathbf{U}, \mathbf{V}) = \frac{1}{2} \sum_{(i,j) \in \Omega} (y_{ij} - [\mathbf{U}\mathbf{V}^\top]_{ij})^2 = \frac{1}{2} \|\mathbf{P}_\Omega \odot (\mathbf{Y} - \mathbf{U}\mathbf{V}^\top)\|_F^2
> $$
> 
> **梯度**：
> $$
> \frac{\partial L}{\partial \mathbf{U}} = -(\mathbf{P}_\Omega \odot (\mathbf{Y} - \mathbf{U}\mathbf{V}^\top))\mathbf{V}
> $$
> $$
> \frac{\partial L}{\partial \mathbf{V}} = -(\mathbf{P}_\Omega \odot (\mathbf{Y} - \mathbf{U}\mathbf{V}^\top))^\top \mathbf{U}
> $$

> [!success] 实际应用
> - 推荐系统（Netflix问题）
> - 缺失数据填补
> - 协同过滤

---

# 七、总结

## 📌 核心概念脑图

```

```
                    ┌─────────────────┐
                    │   Fréchet 导数   │
                    │  f(x+h)=f(x)+   │
                    │  Dₓf(h)+o(‖h‖)  │
                    └────────┬────────┘
                             │
    ┌────────────────────────┼────────────────────────┐
    ↓                        ↓                        ↓
```

┌───────────────┐ ┌───────────────┐ ┌───────────────┐  
│ 向量→标量 │ │ 向量→向量 │ │ 矩阵相关 │  
│ (梯度) │ │ (Jacobi矩阵) │ │ │  
│ ∇f ∈ R^{1×n} │ │ J ∈ R^{m×n} │ │ │  
└───────────────┘ └───────────────┘ └───────────────┘  
│ │ │  
↓ ↓ ↓  
梯度下降 反向传播 矩阵分解优化

```

---

## 📝 必背公式速查

> [!summary] 核心公式
> 
> **Fréchet可微定义**
> $$
> f(x+h) = f(x) + D_x f(h) + o(\|h\|)
> $$
> 
> **梯度**
> $$
> \nabla(\mathbf{a}^\top \mathbf{x}) = \mathbf{a}^\top, \quad \nabla(\mathbf{x}^\top \mathbf{A} \mathbf{x}) = 2\mathbf{x}^\top \mathbf{A}
> $$
> 
> **Jacobi矩阵**
> $$
> \mathbf{J}_{\mathbf{Ax}} = \mathbf{A}, \quad \mathbf{J}_{\mathbf{x}} = \mathbf{I}
> $$
> 
> **行列式导数**
> $$
> \frac{\partial \det(\mathbf{F})}{\partial x} = \det(\mathbf{F}) \cdot \text{tr}\left[\mathbf{F}^{-1} \frac{\partial \mathbf{F}}{\partial x}\right]
> $$
> 
> **Frobenius范数导数**
> $$
> \frac{\partial \|\mathbf{X}\|_F^2}{\partial \mathbf{X}} = 2\mathbf{X}
> $$

---

## ✅ 自测清单

- [ ] 能写出 Fréchet 可微的定义
- [ ] 能计算二次型 $\mathbf{x}^\top \mathbf{A} \mathbf{x}$ 的梯度
- [ ] 能计算线性变换 $\mathbf{A}\mathbf{x}$ 的 Jacobi 矩阵
- [ ] 理解梯度下降算法的数学原理
- [ ] 能解释反向传播与 Jacobi 矩阵的关系
- [ ] 能计算迹函数和行列式的导数

---

> [!quote] 下次课预告
> **高等微分学（二）** — 链式法则、高阶导数（Hessian矩阵）、泰勒展开
