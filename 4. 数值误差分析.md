## 🎯 前置知识

> [!info] 算法基础
> - 算法概念与复杂度分析
> - 理解算法（通常）计算的是**近似**解

> [!info] 线性代数 I
> - 向量范数和矩阵范数
> - 范数的性质：正定性、齐次性、三角不等式
> - 矩阵运算和矩阵-向量乘积

> [!info] 分析 I
> - 极限和收敛
> - 连续性
> - 泰勒近似基础

---

## 📑 目录

```

1. 误差分析基础
2. 误差分解
3. 条件数
4. 稳定性
5. 总结

```

---

# 一、误差分析基础

## 1.1 误差的来源

> [!important] 计算机存储/计算时会产生误差
> 
> **绝对误差**：
> $$
> \text{绝对误差} = \text{误差值} - \text{真实值}
> $$
> 
> **相对误差**（更常用）：
> $$
> \text{相对误差} = \frac{\text{误差值} - \text{真实值}}{\text{真实值}}
> $$

> [!tip] 相对误差的特点
> - **优点**：无量纲，可用百分比表示
> - **缺点**：真实值不能为 0

> [!note] 误差关系
> $$
> \text{误差值} = \text{真实值} \times (1 + \text{相对误差})
> $$

---

## 1.2 误差度量的严格定义

> [!important] 定义：误差度量
> 
> 设 $V$ 是带有（半）范数 $\|\cdot\|$ 的向量空间，$\tilde{\mathbf{v}} \in V$ 是 $\mathbf{v} \in V$ 的近似。则：
> 
> **绝对误差**：
> $$
> \Delta_{\mathbf{v}} = \tilde{\mathbf{v}} - \mathbf{v}
> $$
> 
> **相对误差**：
> $$
> \delta_{\mathbf{v}} = \frac{\|\tilde{\mathbf{v}} - \mathbf{v}\|}{\|\mathbf{v}\|} = \frac{\|\Delta_{\mathbf{v}}\|}{\|\mathbf{v}\|}
> $$
> 
> 若 $\|\Delta_{\mathbf{v}}\| \leq \varepsilon$ 或 $\delta_{\mathbf{v}} \leq \varepsilon$，则称 $\varepsilon$ 为**绝对误差界**或**相对误差界**。

---

## 1.3 误差界引理

> [!abstract] 引理：误差界
> 
> 有如下结论：
> $$
> \|\mathbf{z}\|(1 - \delta_{\mathbf{z}}) \leq \|\tilde{\mathbf{z}}\| \leq \|\mathbf{z}\|(1 + \delta_{\mathbf{z}})
> $$

> [!note] 证明
> 
> 由相对误差定义：$\delta_{\mathbf{z}} = \frac{\|\tilde{\mathbf{z}} - \mathbf{z}\|}{\|\mathbf{z}\|}$
> 
> 因此：$\|\tilde{\mathbf{z}} - \mathbf{z}\| = \delta_{\mathbf{z}}\|\mathbf{z}\|$
> 
> 由逆三角不等式：$\big| \|\tilde{\mathbf{z}}\| - \|\mathbf{z}\| \big| \leq \delta_{\mathbf{z}}\|\mathbf{z}\|$
> 
> 由绝对值定义：$-\delta_{\mathbf{z}}\|\mathbf{z}\| \leq \|\tilde{\mathbf{z}}\| - \|\mathbf{z}\| \leq \delta_{\mathbf{z}}\|\mathbf{z}\|$
> 
> 整理得：$\|\mathbf{z}\|(1 - \delta_{\mathbf{z}}) \leq \|\tilde{\mathbf{z}}\| \leq \|\mathbf{z}\|(1 + \delta_{\mathbf{z}})$

---

## 1.4 示例：向量的误差度量

> [!example] 计算示例
> 
> 设 $\mathbf{v} = \begin{pmatrix} 3 \\ 4 \end{pmatrix}$，$\tilde{\mathbf{v}} = \begin{pmatrix} 3.2 \\ 4.3 \end{pmatrix}$，使用欧几里得范数 $\|\cdot\|_2$。
> 
> **绝对误差**：
> $$
> \Delta_{\mathbf{v}} = \tilde{\mathbf{v}} - \mathbf{v} = \begin{pmatrix} 0.2 \\ 0.3 \end{pmatrix}
> $$
> 
> **绝对误差的范数**：
> $$
> \|\Delta_{\mathbf{v}}\|_2 = \sqrt{0.2^2 + 0.3^2} = \sqrt{0.13} \approx 0.361
> $$
> 
> **相对误差**：
> $$
> \delta_{\mathbf{v}} = \frac{\|\Delta_{\mathbf{v}}\|_2}{\|\mathbf{v}\|_2} = \frac{\sqrt{0.13}}{5} \approx 0.072 = 7.2\%
> $$
> 
> **误差界**：当 $\varepsilon = 0.1$ 时，$\delta_{\mathbf{v}} \approx 0.072 < 0.1$，所以 $\varepsilon = 0.1$ 是一个相对误差界。

---

# 二、误差分解

## 2.1 误差的来源分析

> [!important] 计算流程
> 
> 设 $f$ 是一个具有理想输入 $\mathbf{x}$ 的问题，$\tilde{f}$ 是对应的算法，$\tilde{\mathbf{x}}$ 是实际输入。
> 
> ```
> 输入 x̃ → 算法 f̃ → 结果 f̃(x̃)
> ```
> 
> 结果中的误差 $\tilde{f}(\tilde{\mathbf{x}}) \neq f(\mathbf{x})$ 可能来源于：
> - **输入误差**（测量/存储 $\mathbf{x}$ 时产生）— 不可避免
> - **算法误差**（舍入/近似产生）— 可以控制

---

## 2.2 误差分解公式

> [!important] 核心公式
> 
> $$
> \text{总误差} = \tilde{f}(\tilde{\mathbf{x}}) - f(\mathbf{x})
> $$
> 
> $$
> = \underbrace{\tilde{f}(\tilde{\mathbf{x}}) - f(\tilde{\mathbf{x}})}_{\text{算法误差（机器算术）}} + \underbrace{f(\tilde{\mathbf{x}}) - f(\mathbf{x})}_{\text{输入引起的传播误差（精确算术）}}
> $$

> [!tip] 两项的含义
> - **第一项**不依赖于输入误差。若它"很小"，则称该算法是**稳定的**
> - **第二项**不依赖于算法 $\tilde{f}$。若它"很小"，则称问题 $f$ 在点 $\mathbf{x}$ 处是**良态的（well-conditioned）**

---

## 2.3 示例：误差分解

> [!example] 计算 $e^\pi$ 的误差分解
> 
> 已知 $e^x = \sum_{n=0}^{\infty} \frac{x^n}{n!}$
> 
> 假设我们想计算 $f(\pi) = e^\pi$，并使用近似 $\tilde{\pi} = 3$ 和 $\tilde{f}(x) = \sum_{n=0}^{2} x^n/n!$
> 
> **误差分解**：
> $$
> \tilde{f}(3) - f(\pi) = \left[\tilde{f}(3) - f(3)\right] + \left[f(3) - f(\pi)\right]
> $$
> 
> $$
> = \text{算法误差} + \text{输入引起的传播误差}
> $$
> 
> $$
> = \left[\sum_{n=3}^{\infty} 3^n/n!\right] + \left[e^3 - e^\pi\right]
> $$
> 
> $$
> \approx \left[e^3 - \left(1 + \frac{3}{1} + \frac{9}{2}\right)\right] + [20.1 - 23.1]
> $$
> 
> $$
> \approx [-11.6] + [-3]
> $$

---

# 三、条件数

## 3.1 直观理解

> [!important] 条件数的含义
> 
> - 输入误差被视为*不可避免的*
> - 有些问题难以求解，因为它们*对输入过于敏感*：
>   - 输出集合 $R = f(E)$ 相对于误差输入集合 $E = \{\tilde{\mathbf{x}}: \|\tilde{\mathbf{x}} - \mathbf{x}\|/\|\mathbf{x}\| \leq \delta\}$ 来说*很大*
> - 我们称这样的问题为**病态的（ill-conditioned）**

> [!example] 病态问题示例
> 
> 设 $f(x) = x^{-1}$，$x = 10^{-17}$，$\tilde{x} = x + \varepsilon$，其中 $\varepsilon \in (-10^{-17}, 10^{-17})$
> 
> $$
> \Longrightarrow f(\tilde{x}) \in (1/2 \cdot 10^{17}, \infty)
> $$
> 
> 且 $\left|\frac{f(\tilde{x}) - f(x)}{f(x)}\right| \in [0, \infty)$

---

## 3.2 条件数的严格定义

> [!important] 定义：条件数
> 
> 设 $f: \mathbb{R}^n \rightarrow \mathbb{R}^m$ 是一个输入为 $\mathbf{x}$ 的问题。
> 
> **绝对条件数**是满足以下条件的最小数 $\kappa_{abs} = \kappa_{abs}(f, \mathbf{x})$：
> $$
> \|f(\tilde{\mathbf{x}}) - f(\mathbf{x})\| \leq \kappa_{abs}\|\tilde{\mathbf{x}} - \mathbf{x}\|, \quad \text{当 } \tilde{\mathbf{x}} \rightarrow \mathbf{x}
> $$
> 
> **相对条件数**是满足以下条件的最小数 $\kappa_{rel} = \kappa_{rel}(f, \mathbf{x})$：
> $$
> \frac{\|f(\tilde{\mathbf{x}}) - f(\mathbf{x})\|}{\|f(\mathbf{x})\|} \leq \kappa_{rel}\frac{\|\tilde{\mathbf{x}} - \mathbf{x}\|}{\|\mathbf{x}\|}, \quad \text{当 } \tilde{\mathbf{x}} \rightarrow \mathbf{x}
> $$

---

## 3.3 条件数的解释

> [!tip] 条件数分类
> 
> | 条件 | 含义 |
> |:---|:---|
> | **良态** ($\kappa \leq 1$) | 输入误差导致相同或更小的输出误差 |
> | **病态** ($\kappa \gg 1$) | 任何输入误差都会导致更大的输出误差 |
> | **不适定** ($\kappa = \infty$) | 存在输入误差时无法求解 |

---

## 3.4 矩阵条件数

> [!example] 示例：矩阵求逆的条件数
> 
> 设 $f(\mathbf{x}) = \mathbf{A}^{-1}\mathbf{x} = \mathbf{y}$
> 
> **绝对条件数**：
> $$
> \|f(\tilde{\mathbf{x}}) - f(\mathbf{x})\| = \|\mathbf{A}^{-1}(\tilde{\mathbf{x}} - \mathbf{x})\| \leq \|\mathbf{A}^{-1}\| \|\tilde{\mathbf{x}} - \mathbf{x}\|
> $$
> 
> 因此 $\kappa_{abs} = \|\mathbf{A}^{-1}\|$
> 
> **相对条件数**：设 $\mathbf{h} = \tilde{\mathbf{x}} - \mathbf{x}$
> $$
> \kappa_{rel} \leq \|\mathbf{A}^{-1}\| \|\mathbf{A}\|
> $$

> [!important] 矩阵的条件数
> 
> $$
> \kappa(\mathbf{A}) = \|\mathbf{A}^{-1}\| \|\mathbf{A}\|
> $$
> 
> 对于算子范数 $\|\cdot\|_2$（*谱范数*），有
> $$
> \kappa_2(\mathbf{A}) = \frac{\sqrt{\lambda_{\max}(\mathbf{A}^\top\mathbf{A})}}{\sqrt{\lambda_{\min}(\mathbf{A}^\top\mathbf{A})}} = \frac{\sigma_{\max}(\mathbf{A})}{\sigma_{\min}(\mathbf{A})}
> $$

---

## 3.5 测验

> [!question] 测验
> 
> 设
> $$
> \mathbf{A} = \begin{pmatrix} 2 & 0 & 0 \\ 0 & 5 & 0 \\ 0 & 0 & 0.1 \end{pmatrix}
> $$
> 
> 该矩阵关于 2-范数的条件数是多少？
> 
> 1. 2
> 2. 20
> 3. 5
> 4. 50

> [!success] 答案
> **4. 50**
> 
> 对于对角矩阵，$\kappa_2(\mathbf{A}) = \frac{\sigma_{\max}}{\sigma_{\min}} = \frac{5}{0.1} = 50$

---

## 3.6 练习：向量加法的条件数

> [!example] 练习
> 
> 设 $f(\mathbf{x}) = x_1 + x_2 = \begin{pmatrix} 1 \\ 1 \end{pmatrix}^\top \mathbf{x} = \mathbf{1}^\top\mathbf{x}$
> 
> 1. 条件数是多少？
> 2. 对于哪些 $\mathbf{x}$，该问题是病态的？

> [!success] 解答
> 
> 有
> $$
> |f(\tilde{\mathbf{x}}) - f(\mathbf{x})| = |\mathbf{1}^\top(\tilde{\mathbf{x}} - \mathbf{x})| \leq \|\mathbf{1}\| \|\tilde{\mathbf{x}} - \mathbf{x}\| = \sqrt{2}\|\tilde{\mathbf{x}} - \mathbf{x}\|
> $$
> 
> 因此 $\kappa_{abs} \leq \sqrt{2}$，且
> $$
> \kappa_{rel} = \kappa_{abs}\frac{\|\mathbf{x}\|}{|x_1 + x_2|} \leq \sqrt{2}\frac{\|\mathbf{x}\|}{|x_1 + x_2|}
> $$
> 
> 当 $\|\mathbf{x}\| \gg |x_1 + x_2| \approx 0$ 时，$\kappa_{rel} \gg 1$，因此问题是病态的。
> 
> （即当两个分量大小相近但符号相反时，会发生**灾难性抵消**）

---

# 四、稳定性

## 4.1 稳定性的含义

> [!important] 稳定性概念
> 
> 类似于条件数的分析，我们可以研究算法误差 $\tilde{f}(\tilde{\mathbf{x}}) - f(\tilde{\mathbf{x}})$
> 
> 如果算法误差"小"，我们称 $\tilde{f}$ 是**稳定的**

---

## 4.2 示例：随机梯度下降（SGD）

> [!example] SGD 的稳定性
> 
> 优化损失函数 $L(\theta)$，更新规则：$\theta_{k+1} = \theta_k - \alpha\hat{\nabla}L(\theta_k)$
> 
> | 类型 | 学习率 | 效果 |
> |:---|:---|:---|
> | **稳定算法** | 小学习率 $\alpha$（如 0.001） | 误差缓慢累积，可靠收敛 |
> | **不稳定算法** | 过大学习率 $\alpha$（如 1.0） | 放大舍入/估计误差，振荡或发散 |

> [!tip] 稳定化技术
> - **梯度裁剪（Gradient Clipping）**：防止大更新
> - **批归一化（Batch Normalization）**：控制随机误差
> - **混合精度训练（Mixed Precision Training）**：控制舍入误差

---

## 4.3 复合算法的稳定性定理

> [!important] 定理：复合算法的稳定性
> 
> 设 $f$ 是一个问题，输入为 $\mathbf{x}$，其中 $f(\mathbf{x}) = (h \circ g)(\mathbf{x})$，由两个子问题 $g$ 和 $h$ 组成。
> 
> 对于算法 $\tilde{f} = \tilde{h} \circ \tilde{g}$，有：
> $$
> \frac{\|\tilde{f}(\mathbf{x}) - f(\mathbf{x})\|}{\|f(\mathbf{x})\|} \leq \frac{\|\tilde{h}(\tilde{\mathbf{y}}) - h(\tilde{\mathbf{y}})\|}{\|h(\mathbf{y})\|} + \kappa_{rel}(h, g(\mathbf{x})) \cdot \frac{\|\tilde{g}(\mathbf{x}) - g(\mathbf{x})\|}{\|g(\mathbf{x})\|}
> $$
> 
> 其中 $\mathbf{y} = g(\mathbf{x})$，$\tilde{\mathbf{y}} = \tilde{g}(\mathbf{x})$

> [!danger] 关键结论
> - 一个**条件数差**的子问题 $h$（在 $\mathbf{y} = g(\mathbf{x})$ 处）会放大 $\tilde{g}$ 中的误差，是 $\tilde{f}$ 不稳定的**迹象**
> - 因此，应该**尽早执行条件数差的步骤**！

---

## 4.4 示例：方差的稳定性

> [!example] 计算样本方差
> 
> **问题**：计算样本方差 $f(\mathbf{x}) = \frac{1}{n} \sum(x_i - \bar{x})^2$
> 
> **算法1（稳定）**：
> $$
> \tilde{f}_1(\mathbf{x}) = \frac{1}{n} \sum(x_i - \bar{x})^2
> $$
> 
> **算法2（不稳定）**：
> $$
> \tilde{f}_2(\mathbf{x}) = \frac{1}{n} \sum x_i^2 - \bar{x}^2
> $$

> [!warning] 算法2的问题
> 
> 在 $\tilde{f}_2$ 中，最后计算差值 $h(a, b) = a - b$，其中 $a = \frac{1}{n} \sum x_i^2$ 和 $b = \bar{x}^2$
> 
> 条件数为：
> $$
> \kappa_{rel}(h, (a, b)) = \sqrt{2} \cdot \frac{\sqrt{a^2 + b^2}}{|a - b|}
> $$
> 
> 如果 $a, b$ 很大，但 $a \approx b$，则 $\kappa_{rel}(h, (a, b))$ 很大，$h$ 会放大所有之前的误差！

> [!danger] R代码验证
> 
> ```r
> set.seed(5)
> x <- rnorm(10^3, mean = 0, sd = 1) + 10^10
> mean((x - mean(x))^2)
> ## [1] 1.02
> 
> mean(x^2) - mean(x)^2
> ## [1] 16384
> ```
> 
> 两种算法对同一数据给出了完全不同的结果！

---

# 五、总结

## 5.1 核心概念

> [!summary] 关键定义
> 
> | 概念 | 定义 |
> |:---|:---|
> | **绝对误差** | $\Delta_{\mathbf{v}} = \tilde{\mathbf{v}} - \mathbf{v}$ |
> | **相对误差** | $\delta_{\mathbf{v}} = \|\tilde{\mathbf{v}} - \mathbf{v}\| / \|\mathbf{v}\|$ |
> | **条件数** | 问题对输入误差的敏感性 |
> | **矩阵条件数** | $\kappa(\mathbf{A}) = \|\mathbf{A}\| \cdot \|\mathbf{A}^{-1}\|$ |
> | **稳定性** | 算法误差小时，算法是稳定的 |

---

## 5.2 误差分解

> [!important] 核心公式
> 
> $$
> \text{总误差} = \underbrace{\tilde{f}(\tilde{\mathbf{x}}) - f(\tilde{\mathbf{x}})}_{\text{算法误差}} + \underbrace{f(\tilde{\mathbf{x}}) - f(\mathbf{x})}_{\text{输入传播误差}}
> $$

---

## 5.3 核心要点

> [!danger] 今日所学
> 
> - **条件（Kondition）**描述问题对输入误差的**敏感性**
> - **稳定性（Stabilität）**描述算法对误差的**鲁棒性**
> - **条件数差的步骤应尽早执行！**

---

## ✅ 自测清单

- [ ] 能定义绝对误差和相对误差
- [ ] 理解误差分解公式
- [ ] 能区分算法误差和输入传播误差
- [ ] 能定义绝对条件数和相对条件数
- [ ] 理解矩阵条件数的含义
- [ ] 能计算简单矩阵的条件数
- [ ] 理解稳定性的含义
- [ ] 知道复合算法稳定性的关键结论
- [ ] 能解释方差计算两种算法的稳定性差异
