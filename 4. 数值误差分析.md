
# 前置知识

- 算法概念
- 复杂度分析
- 理解算法（通常）计算的是**近似**解

**线性代数 I**

- 向量范数和矩阵范数
- 范数的性质：正定性、齐次性、三角不等式
- 矩阵运算和矩阵-向量乘积

**分析 I**

- 极限和收敛
- 连续性
- 泰勒近似基础

---

# 误差分析

- 计算机存储/计算时会产生误差
- **绝对误差**：

绝对误差=误差值−真实值\text{绝对误差} = \text{误差值} - \text{真实值}绝对误差=误差值−真实值

- 更常用的是**相对误差**：

相对误差=误差值−真实值真实值\text{相对误差} = \frac{\text{误差值} - \text{真实值}}{\text{真实值}}相对误差=真实值误差值−真实值​

> - 优点：无量纲，可用百分比表示
> - 缺点：真实值不能为 0

- 因此有：

误差值=真实值×(1+相对误差)\text{误差值} = \text{真实值} \times (1 + \text{相对误差})误差值=真实值×(1+相对误差)

---

# 误差度量

**定义：误差度量**

设 $V$ 是带有（半）范数 $\|\cdot\|$ 的向量空间，$\tilde{\mathbf{v}} \in V$ 是 $\mathbf{v} \in V$ 的近似。则：

- **绝对误差**：

$$\Delta_{\mathbf{v}} = \tilde{\mathbf{v}} - \mathbf{v}$$

- **相对误差**：

$$\delta_{\mathbf{v}} = \frac{\|\tilde{\mathbf{v}} - \mathbf{v}\|}{\|\mathbf{v}\|} = \frac{\|\Delta_{\mathbf{v}}\|}{\|\mathbf{v}\|}$$

- 若 $\|\Delta_{\mathbf{v}}\| \leq \varepsilon$ 或 $\delta_{\mathbf{v}} \leq \varepsilon$，则称 $\varepsilon$ 为**绝对误差界**或**相对误差界**。


# 误差度量

**引理：误差界**

有如下结论：

$$\|\mathbf{z}\|(1 - \delta_{\mathbf{z}}) \leq \|\tilde{\mathbf{z}}\| \leq \|\mathbf{z}\|(1 + \delta_{\mathbf{z}})$$

**证明：**

$$\delta_{\mathbf{z}} = \frac{\|\tilde{\mathbf{z}} - \mathbf{z}\|}{\|\mathbf{z}\|}$$

$$\|\tilde{\mathbf{z}} - \mathbf{z}\| = \delta_{\mathbf{z}}\|\mathbf{z}\|$$

$$\big| \|\tilde{\mathbf{z}}\| - \|\mathbf{z}\| \big| \leq \delta_{\mathbf{z}}\|\mathbf{z}\| \quad \text{[逆三角不等式]}$$

$$-\delta_{\mathbf{z}}\|\mathbf{z}\| \leq \|\tilde{\mathbf{z}}\| - \|\mathbf{z}\| \leq \delta_{\mathbf{z}}\|\mathbf{z}\| \quad \text{[绝对值定义]}$$

$$\|\mathbf{z}\| - \delta_{\mathbf{z}}\|\mathbf{z}\| \leq \|\tilde{\mathbf{z}}\| \leq \|\mathbf{z}\| + \delta_{\mathbf{z}}\|\mathbf{z}\|$$

---

# 示例：向量的误差度量

设 $\mathbf{v} = \begin{pmatrix} 3 \\ 4 \end{pmatrix}$，$\tilde{\mathbf{v}} = \begin{pmatrix} 3.2 \\ 4.3 \end{pmatrix}$，使用欧几里得范数 $\|\cdot\|_2$。

- **绝对误差：**

$$\Delta_{\mathbf{v}} = \tilde{\mathbf{v}} - \mathbf{v} = \begin{pmatrix} 3.2 \\ 4.3 \end{pmatrix} - \begin{pmatrix} 3 \\ 4 \end{pmatrix} = \begin{pmatrix} 0.2 \\ 0.3 \end{pmatrix}$$

- **绝对误差的范数：**

$$\|\Delta_{\mathbf{v}}\|_2 = \sqrt{0.2^2 + 0.3^2} = \sqrt{0.04 + 0.09} = \sqrt{0.13} \approx 0.361$$

- **相对误差：**

$$\delta_{\mathbf{v}} = \frac{\|\Delta_{\mathbf{v}}\|_2}{\|\mathbf{v}\|_2} = \frac{\sqrt{0.13}}{\sqrt{3^2 + 4^2}} = \frac{\sqrt{0.13}}{5} \approx \frac{0.361}{5} \approx 0.072 = 7.2\%$$

- **误差界：** 当 $\varepsilon = 0.1$ 时，$\delta_{\mathbf{v}} \approx 0.072 < 0.1$，所以 $\varepsilon = 0.1$ 是一个相对误差界。

---

# 误差分解

- 设 $f$ 是一个具有理想输入 $\mathbf{x}$ 的问题，$\tilde{f}$ 是对应的算法，$\tilde{\mathbf{x}}$ 是实际输入。
- 算法遵循以下模式：
  - 输入 $\tilde{\mathbf{x}}$ $\longrightarrow$ 算法 $\tilde{f}$ $\longrightarrow$ 结果 $\tilde{f}(\tilde{\mathbf{x}})$
- 结果中的误差 $\tilde{f}(\tilde{\mathbf{x}}) \neq f(\mathbf{x})$ 可能来源于：
  - 输入误差（测量/存储 $\mathbf{x}$ 时产生）
    - 不可避免
  - 算法误差（舍入/近似产生）
    - 可以控制

---

# 误差分解

$$\text{总误差} = \tilde{f}(\tilde{\mathbf{x}}) - f(\mathbf{x})$$

$$= \underbrace{\tilde{f}(\tilde{\mathbf{x}}) - f(\tilde{\mathbf{x}})}_{\text{算法误差（机器算术）}} + \underbrace{f(\tilde{\mathbf{x}}) - f(\mathbf{x})}_{\text{输入引起的传播误差（精确算术）}}$$

- 第一项不依赖于输入误差。若它"很小"，则称该算法是**稳定的**。
- 第二项不依赖于算法 $\tilde{f}$。若它"很小"，则称问题 $f$ 在点 $\mathbf{x}$ 处是**良态的（well-conditioned）**。

# 示例：误差分解

- 已知 $e^x = \sum_{n=0}^{\infty} \frac{x^n}{n!}$
- 假设我们想计算 $f(\pi) = e^\pi$，并使用近似 $\tilde{\pi} = 3$ 和 $\tilde{f}(x) = \sum_{n=0}^{2} x^n/n!$

将总误差分解为上述两部分。

**解：**

$$\tilde{f}(3) - f(\pi) = \left[\tilde{f}(3) - f(3)\right] + \left[f(3) - f(\pi)\right]$$

$$= \text{算法误差} + \text{输入引起的传播误差}$$

$$= \left[\sum_{n=3}^{\infty} 3^n/n!\right] + \left[e^3 - e^\pi\right]$$

$$\approx \left[e^3 - \left(1 + \frac{3}{1} + \frac{9}{2}\right)\right] + [20.1 - 23.1]$$

$$\approx [-11.6] + [-3]$$

---

# 条件数

（图示：输入空间 $E$ 中的点 $x$ 通过映射 $f$ 到达输出空间 $R$ 中的 $f(x)$）

- 输入误差被视为*不可避免的*。
- 有些问题难以求解，因为它们*对输入过于敏感*：
  - $\rightarrow$ 输出集合 $R = f(E)$ 相对于误差输入集合 $E = \{\tilde{\mathbf{x}}: \|\tilde{\mathbf{x}} - \mathbf{x}\|/\|\mathbf{x}\| \leq \delta\}$ 来说*很大*。
- 我们称这样的问题为**病态的（ill-conditioned）**。

---

# 条件数：示例

**示例：**

- 设 $f(x) = x^{-1}$
- 设 $x = 10^{-17}$，$\tilde{x} = x + \varepsilon$，其中 $\varepsilon \in (-10^{-17}, 10^{-17})$
- $\Longrightarrow f(\tilde{x}) \in (1/2 \cdot 10^{17}, \infty)$，且

$$\left|\frac{f(\tilde{x}) - f(x)}{f(x)}\right| \in [0, \infty)$$

---

# 条件数

- 下面我们考虑映射 $f: \mathbb{R}^n \rightarrow \mathbb{R}^m$
- $\|\cdot\|$ 表示任意向量范数（或由其诱导的矩阵*算子范数*）

**定义：条件数**

设 $f$ 是一个输入为 $\mathbf{x}$ 的问题。

- **绝对条件数**是满足以下条件的最小数 $\kappa_{abs} = \kappa_{abs}(f, \mathbf{x})$：

$$\|f(\tilde{\mathbf{x}}) - f(\mathbf{x})\| \leq \kappa_{abs}\|\tilde{\mathbf{x}} - \mathbf{x}\|, \quad \text{当 } \tilde{\mathbf{x}} \rightarrow \mathbf{x}$$

- **相对条件数**是满足以下条件的最小数 $\kappa_{rel} = \kappa_{rel}(f, \mathbf{x})$：

$$\frac{\|f(\tilde{\mathbf{x}}) - f(\mathbf{x})\|}{\|f(\mathbf{x})\|} \leq \kappa_{rel}\frac{\|\tilde{\mathbf{x}} - \mathbf{x}\|}{\|\mathbf{x}\|}, \quad \text{当 } \tilde{\mathbf{x}} \rightarrow \mathbf{x}$$

# 解释

$$\|f(\tilde{\mathbf{x}}) - f(\mathbf{x})\| \leq \kappa_{abs}\|\tilde{\mathbf{x}} - \mathbf{x}\|, \quad \frac{\|f(\tilde{\mathbf{x}}) - f(\mathbf{x})\|}{\|f(\mathbf{x})\|} \leq \kappa_{rel}\frac{\|\tilde{\mathbf{x}} - \mathbf{x}\|}{\|\mathbf{x}\|}$$

- **良态**（$\kappa \leq 1$）：输入误差导致相同或更小的输出误差
- **病态**（$\kappa \gg 1$）：任何输入误差都会导致更大的输出误差
- **不适定**（$\kappa = \infty$）：存在输入误差时无法求解

---

# 示例

- 设 $f(\mathbf{x}) = \mathbf{A}^{-1}\mathbf{x} = \mathbf{y}$
- 绝对条件数：

$$\|f(\tilde{\mathbf{x}}) - f(\mathbf{x})\| = \|\mathbf{A}^{-1}(\tilde{\mathbf{x}} - \mathbf{x})\| \leq \|\mathbf{A}^{-1}\| \|\tilde{\mathbf{x}} - \mathbf{x}\|$$

$$\Longrightarrow \kappa_{abs} = \|\mathbf{A}^{-1}\|$$

- 相对条件数：设 $\mathbf{h} = \tilde{\mathbf{x}} - \mathbf{x}$，则

$$\frac{\|\mathbf{A}^{-1}\mathbf{h}\|}{\|\mathbf{A}^{-1}\mathbf{x}\|} \leq \frac{\|\mathbf{A}^{-1}\| \|\mathbf{h}\|}{\|\mathbf{A}^{-1}\mathbf{x}\|} \leq \kappa_{rel}\frac{\|\mathbf{h}\|}{\|\mathbf{x}\|}$$

$$\frac{\kappa_{abs}\|\mathbf{x}\|}{\|\mathbf{A}^{-1}\mathbf{x}\|} \leq \kappa_{rel} \Longrightarrow \kappa_{rel} = \frac{\kappa_{abs}\|\mathbf{A}\mathbf{y}\|}{\|\mathbf{y}\|} \leq \kappa_{abs}\|\mathbf{A}\| = \|\mathbf{A}^{-1}\| \|\mathbf{A}\|$$

- $\kappa(\mathbf{A}) = \|\mathbf{A}^{-1}\| \|\mathbf{A}\|$ 在线性代数中也称为**矩阵 $\mathbf{A}$ 的条件数**
- 对于算子范数 $\|\cdot\|_2$（*谱范数*），有

$$\kappa_2(\mathbf{A}) = \frac{\sqrt{\lambda_{\max}(\mathbf{A}^\top\mathbf{A})}}{\sqrt{\lambda_{\min}(\mathbf{A}^\top\mathbf{A})}}$$

---

# 测验

设

$$\mathbf{A} = \begin{pmatrix} 2 & 0 & 0 \\ 0 & 5 & 0 \\ 0 & 0 & 0.1 \end{pmatrix}$$

该矩阵关于 2-范数的条件数是多少？

1. 2
2. 20
3. 5
4. **50** ✓

**解析：** 对于对角矩阵，$\kappa_2(\mathbf{A}) = \frac{\sigma_{\max}}{\sigma_{\min}} = \frac{5}{0.1} = 50$

---

# 练习

设 $f(\mathbf{x}) = x_1 + x_2 = \begin{pmatrix} 1 \\ 1 \end{pmatrix}^\top \mathbf{x} = \mathbf{1}^\top\mathbf{x}$

1. 条件数是多少？
2. 对于哪些 $\mathbf{x}$，该问题是病态的？

**解：**

- 有

$$|f(\tilde{\mathbf{x}}) - f(\mathbf{x})| = |\mathbf{1}^\top(\tilde{\mathbf{x}} - \mathbf{x})| \leq \|\mathbf{1}\| \|\tilde{\mathbf{x}} - \mathbf{x}\| = \sqrt{2}\|\tilde{\mathbf{x}} - \mathbf{x}\|$$

因此 $\kappa_{abs} \leq \sqrt{2}$，且

$$\kappa_{rel} = \kappa_{abs}\frac{\|\mathbf{x}\|}{|x_1 + x_2|} \leq \sqrt{2}\frac{\|\mathbf{x}\|}{|x_1 + x_2|}$$

- 当 $\|\mathbf{x}\| \gg x_1 + x_2 \approx 0$ 时，$\kappa_{rel} \gg 1$，因此问题是病态的

（即当两个分量大小相近但符号相反时，会发生**灾难性抵消**）

# 稳定性（Stabilität）

## 概念图示

从输入空间 E 到输出空间 R 的映射：

- f 表示精确函数
- f̃ 表示近似算法
- x̃ 是带误差的输入
- f(x̃) 是精确输出，f̃(x̃) 是近似输出

## 总误差分解

```
总误差 = f̃(x̃) - f(x)
       = [f̃(x̃) - f(x̃)] + [f(x̃) - f(x)]
         \_____________/   \___________/
           算法误差         输入传播误差
```

- 类似于条件数的分析，我们可以研究算法误差 f̃(x̃) - f(x̃)
- 如果算法误差"小"，我们称 f̃ 是**稳定的**

---

# 示例：随机梯度下降（SGD）

优化损失函数 L(θ)，更新规则：θ_{k+1} = θ_k - α∇̂L(θ_k)

## 稳定算法

- 小学习率 α（如 0.001）
- （数值）误差缓慢累积
- → 可靠收敛到最优解

## 不稳定算法

- 过大学习率 α（如 1.0）
- 放大梯度 ∇̂L(θ_k) 的舍入/估计误差
- → 振荡或发散

## 对应稳定性概念

|概念|对应内容|
|---|---|
|问题 f|求 L(θ) 的最小值|
|算法 f̃|有限精度的随机梯度下降|
|算法误差|在迭代 k 上累积的舍入/估计误差|

## 额外的稳定化技术

- 梯度裁剪（Gradient Clipping）：防止大更新
- 批归一化（Batch Normalization）：控制随机误差
- 混合精度训练（Mixed Precision Training）：控制舍入误差

---

# 稳定性：核心定理

## 定理：复合算法的稳定性

设 f 是一个问题，输入为 x，其中 f(x) = (h ∘ g)(x)，由两个子问题 g 和 h 组成。

对于算法 f̃ = h̃ ∘ g̃，有：

```
‖f̃(x) - f(x)‖     ‖h̃(ỹ) - h(ỹ)‖                    ‖g̃(x) - g(x)‖
______________ ≤ ______________ + κ_rel(h, g(x)) · ______________
   ‖f(x)‖            ‖h(y)‖                            ‖g(x)‖
```

其中 y = g(x)，ỹ = g̃(x)

### 关键结论

- 一个**条件数差**的子问题 h（在 y = g(x) 处）会放大 g̃ 中的误差，是 f̃ 不稳定的**迹象**
- 因此，应该**尽早执行条件数差的步骤**！

---

# 稳定性：核心定理证明

```
‖f̃(x) - f(x)‖     ‖h̃(g̃(x)) - h(g(x))‖
______________ = ______________________
   ‖f(x)‖            ‖h(g(x))‖

                 ‖h̃(g̃(x)) - h(g̃(x)) + h(g̃(x)) - h(g(x))‖
               = _________________________________________
                              ‖h(g(x))‖

                 ‖h̃(g̃(x)) - h(g̃(x))‖   ‖h(g̃(x)) - h(g(x))‖
               ≤ ____________________ + ____________________
                      ‖h(g(x))‖              ‖h(g(x))‖

                 ‖h̃(ỹ) - h(ỹ)‖                    ‖g̃(x) - g(x)‖
               ≤ ______________ + κ_rel(h, g(x)) · ______________
                    ‖h(y)‖                            ‖g(x)‖
```

---

# 示例：方差的稳定性

## 问题

计算样本方差：

```
f(x) = (1/n) Σ(x_i - x̄)²
```

## 两种算法

**算法1（稳定）：**

```
f̃₁(x) = (1/n) Σ(x_i - x̄)²
```

**算法2（不稳定）：**

```
f̃₂(x) = (1/n) Σx_i² - x̄²
```

## 算法2的问题

在 f̃₂ 中，最后计算差值 h(a, b) = a - b，其中：

```
a = (1/n) Σx_i²  和  b = x̄²
```

条件数为：

```
κ_rel(h, (a, b)) = √2 · √(a² + b²) / |a - b|
```

- 如果 a, b 很大，但 a ≈ b，则 κ_rel(h, (a, b)) 很大
- 此时 h 会放大所有之前的误差！

## R代码验证

```r
set.seed(5)
x <- rnorm(10^3, mean = 0, sd = 1) + 10^10
mean((x - mean(x))^2)
## [1] 1.02

mean(x^2) - mean(x)^2
## [1] 16384
```

---

# 总结（Übersicht）

- **绝对误差**：Δ_v = ṽ - v
- **相对误差**：δ_v = ‖ṽ - v‖ / ‖v‖
- **误差分解**：
    - 总误差 = 算法误差 (f̃(x̃) - f(x̃)) + 输入传播误差 (f(x̃) - f(x))
- **条件（Kondition）**：问题对输入误差的敏感性
- **条件数**：绝对和相对（κ_abs, κ_rel）
- **矩阵条件数**：κ(A) = ‖A‖ · ‖A⁻¹‖
- **稳定性**：算法误差小时，算法是稳定的
- **复合算法的稳定性**：误差在算法子步骤中传播

---

# 总结要点（Wrap-up）

## 今日所学

- **条件（Kondition）**描述问题对输入误差的**敏感性**
- **稳定性（Stabilität）**描述算法对误差的**鲁棒性**
- **条件数差的步骤应尽早执行！**

## 下节课预告

- 线性方程组（LGS）的数值求解