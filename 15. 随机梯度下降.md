随机梯度下降（SGD）：从原理到深度学习
===

## 🎯 预备知识

> [!info] 多元微积分
> - **梯度** $\nabla f(\mathbf{x}) = \left(\frac{\partial f}{\partial x_1}, \ldots, \frac{\partial f}{\partial x_n}\right)^\top$
> - **链式法则**：复合函数的偏导数
> - **方向导数**：$\nabla f(\mathbf{x})^\top \mathbf{d}$，其中 $\|\mathbf{d}\| = 1$

> [!info] 线性代数
> - **向量范数**：$\|\mathbf{v}\|_2 = \sqrt{v_1^2 + \cdots + v_n^2}$
> - **矩阵乘法**：$\mathbf{X}\boldsymbol{\beta}$ —— 预测值

> [!info] 概率论
> - **数学期望**：$\mathbb{E}[X] = \sum_i x_i P(X = x_i)$
> - **无偏估计**：若 $\mathbb{E}[\hat{\theta}] = \theta$，则 $\hat{\theta}$ 是 $\theta$ 的无偏估计

---

## 📑 目录

```


1. 问题引入：为什么需要 SGD
2. 三种梯度下降：批量、随机、小批量
3. SGD 的数学推导
4. 逐步数值计算示例
5. 代码实现
6. 关键超参数的影响
7. 收敛性分析
8. 动量法：走出震荡困境
9. 自适应学习率：Adam 算法
10. 工程实践与调试
11. 知识串联
```

---

# 一、问题引入：为什么需要 SGD

## 1.1 从一个具体问题说起

> [!example] 线性回归的优化问题
>
> 有 $N$ 个数据点 $(x_i, y_i)$，拟合模型：
> $$
> \hat{y}_i = \beta_0 + \beta_1 x_i
> $$
>
> 损失函数（均方误差）：
> $$
> L(\boldsymbol{\beta}) = \frac{1}{N}\sum_{i=1}^{N}(y_i - \hat{y}_i)^2
> $$

**目标**：找到使 $L(\boldsymbol{\beta})$ 最小的 $\boldsymbol{\beta} = (\beta_0, \beta_1)^\top$。

::: {.callout-note title="思考一个问题"}
正规方程给出了 $\boldsymbol{\beta}^* = (\mathbf{X}^\top\mathbf{X})^{-1}\mathbf{X}^\top\mathbf{y}$，一步到位求出解析解。

那为什么我们还需要 SGD？这不是在多此一举吗？
:::

**答案**藏在问题规模里：

| 场景 | 数据量 $N$ | 参数维度 $d$ | 正规方程的困境 |
|:---|:---:|:---:|:---|
| 小数据集 | < 1,000 | < 10 | 勉强能用 |
| 中等数据集 | ~10万 | ~100 | 每次迭代 $O(Nd)$，尚可 |
| 大规模数据 | ~1000万 | ~1000 | 内存爆炸，$N d$ 矩阵存不下 |
| 深度学习 | ~10亿 | ~百万 | 正规方程彻底不可行 |

**核心矛盾**：当数据集变得巨大时，每次迭代都计算"全量梯度"（所有 $N$ 个样本的梯度之和）代价极高。

---

## 1.2 另一个更深层的问题

即使数据集不大，还有另一类问题——**模型本身无法写成矩阵形式**：

> [!important] 非线性模型无法用正规方程
>
> 神经网络：$\hat{y} = \text{NeuralNet}(\mathbf{x}; \boldsymbol{\theta})$
>
> 这个模型无法写成 $y = \mathbf{X}\boldsymbol{\beta}$ 的形式，因此没有解析解。
>
> 只能：**迭代优化**——从某个初始点出发，沿着下降方向一步步逼近最优解。

无论是大规模线性回归，还是神经网络，SGD 都是核心工具。接下来的所有内容，都围绕这个核心矛盾展开。

---

# 二、三种梯度下降：批量、随机、小批量

## 2.1 损失函数的结构

> [!important] 经验风险
>
> 对于监督学习，损失函数通常是**经验风险**：
> $$
> R_N(\boldsymbol{\theta}) = \frac{1}{N}\sum_{i=1}^{N} L(y_i, \hat{y}_i)
> $$
>
> 其中 $L$ 是单个样本的损失（如均方误差、交叉熵等）。

::: {.callout-tip title="关键洞察"}
梯度 $\nabla R_N(\boldsymbol{\theta})$ 可以分解为 $N$ 个**独立项的加和**：
$$
\nabla R_N(\boldsymbol{\theta}) = \frac{1}{N}\sum_{i=1}^{N} \nabla L(y_i, \hat{y}_i)
$$

这意味着：**我们不必一次性计算全部 $N$ 个梯度。**
:::

---

## 2.2 三种计算方式对比

> [!abstract] 三种梯度计算方式

| 方法 | 每次迭代用多少数据 | 梯度估计质量 | 单步计算代价 | 收敛稳定性 |
|:---|:---:|:---:|:---:|:---:|
| **批量 GD** (Batch GD) | $N$（全部） | 无噪声，精确 | $O(Nd)$，极高 | 非常稳定 |
| **随机 GD** (SGD) | $1$（单个） | 高噪声，无偏 | $O(d)$，极低 | 震荡 |
| **小批量 GD** (Mini-batch SGD) | $B$（$B \ll N$） | 低噪声，无偏 | $O(Bd)$，可控 | 较好 |

其中 $d$ 是参数维度，$B$ 是批量大小（通常 32-256）。

::: {.callout-note title="为什么 SGD 的梯度是无偏估计？"}
单个样本的梯度 $\nabla L(y_i, \hat{y}_i)$ 的期望等于全量梯度：
$$
\mathbb{E}_{i \sim \text{Uniform}}\left[\nabla L(y_i, \hat{y}_i)\right] = \frac{1}{N}\sum_{i=1}^{N}\nabla L(y_i, \hat{y}_i) = \nabla R_N(\boldsymbol{\theta})
$$

因为每个样本被选中的概率相等（都是 $1/N$），所以平均方向是正确的。只是每次只看到方向的一个" noisy 样本"，方向对但方差大。
:::

---

## 2.3 直观对比

```
假设要下山，目标是谷底。

Batch GD（批量）：        SGD（随机）：
有卫星地图，知道          闭着眼走，每步方向
精确的下山方向，          基本正确但有随机
但每一步都非常慢           偏差，容易走弯
      ↓                      ↓
    ●→→→→                   ●↘
  ↗  ●→→→                ●↗↘
↗ ↗    ●→→              ↗●↘↘
↗↗↗↗      ●          ↗↗↗  ↘●

Mini-batch SGD：
有一定视野（看几个方向），
比单眼好，比全知慢，
是实际工程中的默认选择。
      ↓
    ●→↓
  ↗  ●↘
↗ ↗  ●↓
↗↗↗   ●↓←最终到达
```

> [!warning] 噪声的双刃剑效应
>
> - **坏处**：收敛路径震荡，不是直线
> - **好处**：噪声本身是一种**正则化**，有助于逃离局部最优和鞍点

---

# 三、SGD 的数学推导

## 3.1 线性回归的梯度推导

> [!important] 符号定义
>
> - 数据：$(\mathbf{x}_i, y_i)$，其中 $\mathbf{x}_i = (1, x_i)^\top$（含偏置项）
> - 预测：$\hat{y}_i = \mathbf{x}_i^\top \boldsymbol{\beta}$
> - 残差：$r_i = y_i - \hat{y}_i$
> - 损失：$L_i = r_i^2$
> - 全局损失：$R_N(\boldsymbol{\beta}) = \frac{1}{N}\sum_{i=1}^{N} r_i^2$

**对单个样本 $i$ 求梯度**（对 $\beta_0$ 和 $\beta_1$）：

由 $r_i = y_i - \beta_0 - \beta_1 x_i$，有：
$$
\frac{\partial r_i}{\partial \beta_0} = -1, \qquad \frac{\partial r_i}{\partial \beta_1} = -x_i
$$

由链式法则：
$$
\frac{\partial L_i}{\partial \beta_j} = 2r_i \cdot \frac{\partial r_i}{\partial \beta_j} = -2r_i \cdot \frac{\partial \hat{y}_i}{\partial \beta_j}
$$

> [!success] 单样本梯度公式
>
> 单个样本 $i$ 对 $\boldsymbol{\beta}$ 的梯度为：
> $$
> \nabla L_i(\boldsymbol{\beta}) = \begin{pmatrix} -2(y_i - \mathbf{x}_i^\top\boldsymbol{\beta}) \\ -2x_i(y_i - \mathbf{x}_i^\top\boldsymbol{\beta}) \end{pmatrix} = -2r_i \begin{pmatrix} 1 \\ x_i \end{pmatrix}
> $$

---

## 3.2 SGD 迭代公式

> [!important] SGD 迭代
>
> 在第 $k$ 步，随机选取一个样本 $i_k$：
> $$
> \boldsymbol{\beta}^{(k+1)} = \boldsymbol{\beta}^{(k)} - \gamma^{(k)} \cdot \nabla L_{i_k}(\boldsymbol{\beta}^{(k)})
> $$
>
> 其中 $\gamma^{(k)} > 0$ 是**学习率**（步长）。

展开分量形式：
$$
\beta_0^{(k+1)} = \beta_0^{(k)} + 2\gamma^{(k)} \cdot (y_{i_k} - \beta_0^{(k)} - \beta_1^{(k)} x_{i_k})
$$
$$
\beta_1^{(k+1)} = \beta_1^{(k)} + 2\gamma^{(k)} \cdot x_{i_k}(y_{i_k} - \beta_0^{(k)} - \beta_1^{(k)} x_{i_k})
$$

> [!tip] 直观理解
>
> - 当残差 $y_i - \hat{y}_i > 0$（预测偏低）时，$\beta_0$ **增大**（截距上移）
> - 当残差 $y_i - \hat{y}_i > 0$ 且 $x_i > 0$ 时，$\beta_1$ **增大**（斜率变大）
> - 学习率 $\gamma$ 控制每一步调整的幅度

---

## 3.3 小批量梯度

> [!important] Mini-batch SGD 迭代
>
> 随机选取一个批次 $B_k \subseteq \{1, \ldots, N\}$：
> $$
> \boldsymbol{\beta}^{(k+1)} = \boldsymbol{\beta}^{(k)} - \gamma^{(k)} \cdot \frac{1}{|B_k|}\sum_{i \in B_k}\nabla L_i(\boldsymbol{\beta}^{(k)})
> $$
>
> 其中 $|B_k| = B$ 是批量大小。

展开：
$$
\boldsymbol{\beta}^{(k+1)} = \boldsymbol{\beta}^{(k)} - \frac{2\gamma^{(k)}}{B}\sum_{i \in B_k} r_i^{(k)} \begin{pmatrix} 1 \\ x_i \end{pmatrix}
$$

::: {.callout-tip title="为什么实践中用 mini-batch 而不是纯 SGD？"}
纯 SGD（$B=1$）虽然每步代价最低，但梯度方差太大，收敛路径极其震荡，需要极小的学习率，反而拖慢了整体收敛。

Mini-batch 在"计算效率"和"梯度稳定性"之间取得了良好平衡：
- $B=32\sim256$ 时，梯度方差大幅降低
- 可利用向量化（矩阵乘法），充分利用 GPU 并行能力
- 每个 epoch 可以做 $N/B$ 次参数更新（远多于 $B=1$ 的 $N$ 次）

这就是为什么深度学习框架中默认的 `batch_size` 通常是 32 或 64。
:::

---

# 四、逐步数值计算示例

## 4.1 问题设定

> [!example] 用具体数字理解 SGD 的每一步
>
> **数据集**（$N=4$，故意选少来手动计算）：
> | $i$ | $x_i$ | $y_i$ |
> |:---:|:---:|:---:|
> | 1 | 1.0 | 3.0 |
> | 2 | 2.0 | 5.1 |
> | 3 | 3.0 | 6.9 |
> | 4 | 4.0 | 9.2 |
>
> **真实参数**（为了便于验证）：$\beta_0^* = 1.0$，$\beta_1^* = 2.0$
>
> **初始参数**：$\boldsymbol{\beta}^{(0)} = (0.0, 0.0)^\top$
>
> **学习率**：$\gamma = 0.01$

---

## 4.2 Batch GD 精确梯度（对照）

先算全量梯度作对照：

每个样本的预测值（初始 $\beta=(0,0)$）：$\hat{y}_i = 0$

| $i$ | $x_i$ | $y_i$ | $r_i = y_i - \hat{y}_i$ | $r_i \cdot (1)$ | $r_i \cdot x_i$ |
|:---:|:---:|:---:|:---:|:---:|:---:|
| 1 | 1.0 | 3.0 | 3.0 | 3.0 | 3.0 |
| 2 | 2.0 | 5.1 | 5.1 | 5.1 | 10.2 |
| 3 | 3.0 | 6.9 | 6.9 | 6.9 | 20.7 |
| 4 | 4.0 | 9.2 | 9.2 | 9.2 | 36.8 |
| | | | **均值** | **6.05** | **17.675** |

精确梯度：$\nabla R_N = -2 \times (6.05, 17.675) = (-12.10, -35.35)$

第一步更新：$\beta_0^{(1)} = 0 + 0.01 \times 12.10 = 0.121$，$\beta_1^{(1)} = 0 + 0.01 \times 35.35 = 0.3535$

---

## 4.3 SGD 单步计算（选样本 $i=1$）

现在用 SGD，只用样本 1 的梯度来更新：

样本 1 的残差：$r_1 = y_1 - \hat{y}_1 = 3.0 - 0 = 3.0$

> [!success] 计算样本 1 的梯度
>
> $\nabla L_1 = -2 \times 3.0 \times \begin{pmatrix} 1 \\ 1.0 \end{pmatrix} = \begin{pmatrix} -6.0 \\ -6.0 \end{pmatrix}$

SGD 更新：
$$
\beta_0^{(1)} = 0 - 0.01 \times (-6.0) = 0 + 0.06 = 0.060
$$
$$
\beta_1^{(1)} = 0 - 0.01 \times (-6.0) = 0 + 0.06 = 0.060
$$

::: {.callout-note title="对比 Batch GD 和 SGD 的第一步"}
- **Batch GD**：$\beta^{(1)} = (0.121, 0.3535)$，方向是 $(-12.1, -35.4)$
- **SGD（样本1）**：$\beta^{(1)} = (0.060, 0.060)$，方向是 $(-6.0, -6.0)$

SGD 的方向完全不同！它只用了 $x=1$ 的数据，而 Batch GD 综合了 $x=1,2,3,4$ 的所有信息。SGD 的方向是大致正确的（$\beta_0, \beta_1$ 都应该增大），但幅度和比例严重偏离真实方向。
:::

---

## 4.4 SGD 单步计算（选样本 $i=4$）

再选样本 4 做一轮：

当前 $\boldsymbol{\beta}^{(0)} = (0.0, 0.0)$（重新从起点开始演示）

样本 4 的残差：$r_4 = y_4 - \hat{y}_4 = 9.2 - 0 = 9.2$

$\nabla L_4 = -2 \times 9.2 \times \begin{pmatrix} 1 \\ 4.0 \end{pmatrix} = \begin{pmatrix} -18.4 \\ -73.6 \end{pmatrix}$

SGD 更新：
$$
\beta_0^{(1)} = 0 - 0.01 \times (-18.4) = 0.184
$$
$$
\beta_1^{(1)} = 0 - 0.01 \times (-73.6) = 0.736
$$

::: {.callout-tip title="两个样本的对比"}
- 样本 1 → $\beta^{(1)} = (0.060, 0.060)$，$\beta_1/\beta_0 = 1$
- 样本 4 → $\beta^{(1)} = (0.184, 0.736)$，$\beta_1/\beta_0 = 4$

真实比值是 $\beta_1^*/\beta_0^* = 2$。样本 1 低估了，样本 4 高估了。

**这就是 SGD 噪声的来源**：不同样本给出的更新方向差异巨大！但随着样本越来越多，平均下来会趋近真实方向。
:::

---

## 4.5 Mini-batch SGD 单步计算（$B=2$，选样本 1 和 4）

批量大小为 2，用样本 1 和 4 的梯度**平均**：

样本 1 梯度：$\begin{pmatrix} -6.0 \\ -6.0 \end{pmatrix}$（上面已算）
样本 4 梯度：$\begin{pmatrix} -18.4 \\ -73.6 \end{pmatrix}$（上面已算）

批量平均梯度：
$$
\frac{1}{2}\left[\begin{pmatrix} -6.0 \\ -6.0 \end{pmatrix} + \begin{pmatrix} -18.4 \\ -73.6 \end{pmatrix}\right] = \begin{pmatrix} -12.2 \\ -39.8 \end{pmatrix}
$$

SGD 更新：
$$
\beta_0^{(1)} = 0 - 0.01 \times (-12.2) = 0.122
$$
$$
\beta_1^{(1)} = 0 - 0.01 \times (-39.8) = 0.398
$$

::: {.callout-note title="Mini-batch 的"中和"效应"}
- 样本 1 的方向 $\beta_1/\beta_0 = 1$（低估了）
- 样本 4 的方向 $\beta_1/\beta_0 = 4$（高估了）
- 两个样本的平均方向：$\beta_1/\beta_0 = 39.8/12.2 \approx 3.26$（比单个样本都更接近真实值 2）

**方差减小了**——两个噪声样本的平均，比单个噪声样本更稳定。
Batch GD 的精确方向 $(-12.1, -35.4)$ 对应 $\beta_1/\beta_0 \approx 2.92$，mini-batch 的方向 $(12.2, 39.8)$ 几乎相同！
:::

---

# 五、代码实现

## 5.1 模拟数据生成

```r
set.seed(42)

# ===== 1. 生成模拟数据 =====
n <- 200
true_beta0 <- 1.0
true_beta1 <- 2.0

x <- runif(n, 0, 5)
noise <- rnorm(n, 0, 0.5)       # 小噪声：信噪比高
y <- true_beta0 + true_beta1 * x + noise

cat("真实参数: β0 =", true_beta0, ", β1 =", true_beta1, "\n")
cat("数据量 N =", n, "\n\n")

# 设计矩阵（含偏置列）
X <- cbind(1, x)
```

---

## 5.2 三种方法对比实现

```r
cat("===== 三种梯度下降方法对比 =====\n\n")

# ---- 方法1：Batch GD ----
cat("=== 方法1: Batch GD (全量梯度) ===\n")
gamma_batch <- 0.001   # 批量 GD 可以用较大的学习率
n_iter_batch <- 200
beta_batch <- c(0, 0)
loss_batch <- numeric(n_iter_batch)

for (k in 1:n_iter_batch) {
  residuals <- y - X %*% beta_batch
  gradient  <- -2 * t(X) %*% residuals / n
  beta_batch <- beta_batch - gamma_batch * gradient
  loss_batch[k] <- mean(residuals^2)
}

cat("β0 =", round(beta_batch[1], 4), "\n")
cat("β1 =", round(beta_batch[2], 4), "\n")
cat("最终 MSE =", round(loss_batch[n_iter_batch], 4), "\n\n")

# ---- 方法2：SGD (B=1) ----
cat("=== 方法2: SGD (batch_size=1) ===\n")
gamma_sgd <- 0.005    # SGD 需要较小的学习率（噪声大）
n_epochs_sgd <- 50
beta_sgd <- c(0, 0)
loss_sgd <- numeric(n_epochs_sgd)

for (epoch in 1:n_epochs_sgd) {
  idx <- sample(1:n, replace = FALSE)   # 打乱顺序
  for (i in idx) {
    xi <- X[i, ]
    yi <- y[i]
    residual <- yi - sum(xi * beta_sgd)
    gradient  <- -2 * residual * xi       # 单样本梯度
    beta_sgd  <- beta_sgd - gamma_sgd * gradient
  }
  loss_sgd[epoch] <- mean((y - X %*% beta_sgd)^2)
}

cat("β0 =", round(beta_sgd[1], 4), "\n")
cat("β1 =", round(beta_sgd[2], 4), "\n")
cat("最终 MSE =", round(loss_sgd[n_epochs_sgd], 4), "\n\n")

# ---- 方法3：Mini-batch SGD ----
cat("=== 方法3: Mini-batch SGD (batch_size=32) ===\n")
gamma_mb   <- 0.01
batch_size <- 32
n_epochs_mb <- 50
beta_mb <- c(0, 0)
loss_mb <- numeric(n_epochs_mb)

for (epoch in 1:n_epochs_mb) {
  idx <- sample(1:n)
  X_shuf <- X[idx, ]
  y_shuf <- y[idx]

  for (b in seq(1, n, by = batch_size)) {
    batch_end <- min(b + batch_size - 1, n)
    Xb <- X_shuf[b:batch_end, , drop = FALSE]
    yb <- y_shuf[b:batch_end]
    m  <- nrow(Xb)

    residual <- yb - Xb %*% beta_mb
    gradient  <- -2 * t(Xb) %*% residual / m   # 批量平均梯度
    beta_mb  <- beta_mb - gamma_mb * gradient
  }
  loss_mb[epoch] <- mean((y - X %*% beta_mb)^2)
}

cat("β0 =", round(beta_mb[1], 4), "\n")
cat("β1 =", round(beta_mb[2], 4), "\n")
cat("最终 MSE =", round(loss_mb[n_epochs_mb], 4), "\n\n")
```

---

## 5.3 学习率衰减

```r
cat("===== 学习率衰减实验 =====\n\n")

gamma0 <- 0.05        # 初始学习率
decay_rate <- 0.95     # 每 epoch 衰减 5%
n_epochs_lr <- 80

beta_lr <- c(0, 0)
loss_lr <- numeric(n_epochs_lr)

for (epoch in 1:n_epochs_lr) {
  gamma <- gamma0 * decay_rate^(epoch - 1)   # 指数衰减

  idx <- sample(1:n)
  X_shuf <- X[idx, ]
  y_shuf <- y[idx]

  for (b in seq(1, n, by = 32)) {
    batch_end <- min(b + 31, n)
    Xb <- X_shuf[b:batch_end, , drop = FALSE]
    yb <- y_shuf[b:batch_end]
    m  <- nrow(Xb)

    residual <- yb - Xb %*% beta_lr
    gradient  <- -2 * t(Xb) %*% residual / m
    beta_lr   <- beta_lr - gamma * gradient
  }
  loss_lr[epoch] <- mean((y - X %*% beta_lr)^2)
}

cat("最终 β0 =", round(beta_lr[1], 4), ", β1 =", round(beta_lr[2], 4), "\n")
cat("最终 MSE =", round(loss_lr[n_epochs_lr], 4), "\n")
cat("最终学习率 =", round(gamma0 * decay_rate^(n_epochs_lr-1), 6), "\n")
```

---

## 5.4 动量 SGD 实现

```r
cat("\n===== 动量 SGD 实验 =====\n\n")

gamma_mom  <- 0.01
momentum   <- 0.9
n_epochs_m <- 50
beta_mom <- c(0, 0)
v <- c(0, 0)          # 速度（累积动量）
loss_mom <- numeric(n_epochs_m)

for (epoch in 1:n_epochs_m) {
  idx <- sample(1:n)
  X_shuf <- X[idx, ]
  y_shuf <- y[idx]

  for (b in seq(1, n, by = 32)) {
    batch_end <- min(b + 31, n)
    Xb <- X_shuf[b:batch_end, , drop = FALSE]
    yb <- y_shuf[b:batch_end]
    m  <- nrow(Xb)

    residual <- yb - Xb %*% beta_mom
    gradient  <- -2 * t(Xb) %*% residual / m

    # --- 动量更新 ---
    v <- momentum * v - gamma_mom * gradient    # 速度更新
    beta_mom <- beta_mom + v                    # 位置更新
  }
  loss_mom[epoch] <- mean((y - X %*% beta_mom)^2)
}

cat("β0 =", round(beta_mom[1], 4), "\n")
cat("β1 =", round(beta_mom[2], 4), "\n")
cat("最终 MSE =", round(loss_mom[n_epochs_m], 4), "\n")
```

---

## 5.5 Adam 算法实现

```r
cat("\n===== Adam 算法实现 =====\n\n")

gamma_adam <- 0.1
beta1 <- 0.9      # 一阶矩估计衰减
beta2 <- 0.999    # 二阶矩估计衰减
eps   <- 1e-8
n_epochs_a <- 50

beta_adam <- c(0, 0)
m <- c(0, 0)      # 一阶矩（梯度的指数加权平均）
v <- c(0, 0)      # 二阶矩（梯度平方的指数加权平均）
loss_adam <- numeric(n_epochs_a)

for (epoch in 1:n_epochs_a) {
  t <- epoch

  idx <- sample(1:n)
  X_shuf <- X[idx, ]
  y_shuf <- y[idx]

  for (b in seq(1, n, by = 32)) {
    batch_end <- min(b + 31, n)
    Xb <- X_shuf[b:batch_end, , drop = FALSE]
    yb <- y_shuf[b:batch_end]
    m_b <- nrow(Xb)

    residual <- yb - Xb %*% beta_adam
    gradient  <- -2 * t(Xb) %*% residual / m_b

    # --- Adam 更新 ---
    m <- beta1 * m + (1 - beta1) * gradient         # 更新一阶矩
    v <- beta2 * v + (1 - beta2) * (gradient^2)     # 更新二阶矩

    m_hat <- m / (1 - beta1^t)                       # 偏差修正
    v_hat <- v / (1 - beta2^t)

    beta_adam <- beta_adam - gamma_adam * m_hat / (sqrt(v_hat) + eps)
  }
  loss_adam[epoch] <- mean((y - X %*% beta_adam)^2)
}

cat("β0 =", round(beta_adam[1], 4), "\n")
cat("β1 =", round(beta_adam[2], 4), "\n")
cat("最终 MSE =", round(loss_adam[n_epochs_a], 4), "\n")
```

---

## 5.6 可视化

```r
#| fig-width: 14
#| fig-height: 10
#| fig-cap: "SGD 方法对比：拟合结果、收敛曲线和学习率衰减"

par(mfrow = c(2, 2), mar = c(4.5, 4.5, 3, 1))

# ===== 图1: 拟合结果对比 =====
plot(x, y, col = "grey60", pch = 16, cex = 0.7,
     main = "三种方法拟合对比", xlab = "x", ylab = "y")

x_line <- seq(0, 5, length.out = 100)
lines(x_line, true_beta0 + true_beta1 * x_line,
      col = "#2ecc71", lwd = 2.5, lty = 2)     # 真实线
lines(x_line, beta_batch[1] + beta_batch[2] * x_line,
      col = "#3498db", lwd = 2)                  # Batch GD
lines(x_line, beta_sgd[1] + beta_sgd[2] * x_line,
      col = "#e74c3c", lwd = 2, lty = 3)         # SGD
lines(x_line, beta_mom[1] + beta_mom[2] * x_line,
      col = "#9b59b6", lwd = 2, lty = 4)          # 动量 SGD

legend("topleft",
       legend = c("真实", "Batch GD", "SGD (B=1)", "Momentum"),
       col = c("#2ecc71", "#3498db", "#e74c3c", "#9b59b6"),
       lty = c(2, 1, 3, 4), lwd = 2, cex = 0.8, bg = "white")

# ===== 图2: 收敛曲线 =====
plot(1:n_iter_batch, loss_batch, type = "l", col = "#3498db", lwd = 2,
     main = "损失收敛对比", xlab = "Iteration / Epoch", ylab = "MSE")
lines(1:n_epochs_sgd, loss_sgd, col = "#e74c3c", lwd = 2)
lines(1:n_epochs_mb, loss_mb, col = "#f39c12", lwd = 2)
lines(1:n_epochs_m, loss_mom, col = "#9b59b6", lwd = 2)
lines(1:n_epochs_a, loss_adam, col = "#1abc9c", lwd = 2)
abline(h = min(loss_batch), col = "#3498db", lty = 2)
legend("topright",
       legend = c("Batch GD", "SGD (B=1)", "Mini-batch", "Momentum", "Adam"),
       col = c("#3498db", "#e74c3c", "#f39c12", "#9b59b6", "#1abc9c"),
       lty = rep(1, 5), lwd = 2, cex = 0.75, bg = "white")

# ===== 图3: 学习率衰减效果 =====
plot(1:n_epochs_lr, loss_lr, type = "l", col = "#e74c3c", lwd = 2,
     main = "学习率衰减的收敛效果", xlab = "Epoch", ylab = "MSE")
legend("topright",
       legend = c("LR 衰减 (γ₀=0.05, decay=0.95)"),
       col = "#e74c3c", lty = 1, lwd = 2, cex = 0.8, bg = "white")

# ===== 图4: 不同批量大小的收敛对比 =====
results_bs <- list()
for (bs in c(1, 8, 32, 128)) {
  beta_bs <- c(0, 0)
  for (epoch in 1:50) {
    idx <- sample(1:n)
    Xs <- X[idx, ]; ys <- y[idx]
    for (b in seq(1, n, by = bs)) {
      be <- min(b + bs - 1, n)
      Xb <- Xs[b:be, , drop = FALSE]
      yb <- ys[b:be]
      r <- yb - Xb %*% beta_bs
      g <- -2 * t(Xb) %*% r / nrow(Xb)
      beta_bs <- beta_bs - 0.01 * g
    }
    results_bs[[as.character(bs)]][epoch] <- mean((y - X %*% beta_bs)^2)
  }
}

plot(1:50, results_bs[["1"]], type = "l", col = "#e74c3c", lwd = 2,
     main = "不同批量大小的收敛", xlab = "Epoch", ylab = "MSE")
for (i in c("8", "32", "128")) {
  col_map <- c("8" = "#f39c12", "32" = "#3498db", "128" = "#2ecc71")
  lines(1:50, results_bs[[i]], col = col_map[[i]], lwd = 2)
}
legend("topright",
       legend = c("B=1 (SGD)", "B=8", "B=32", "B=128 (接近Batch)"),
       col = c("#e74c3c", "#f39c12", "#3498db", "#2ecc71"),
       lty = rep(1, 4), lwd = 2, cex = 0.75, bg = "white")

par(mfrow = c(1, 1))
```

---

# 六、关键超参数的影响

## 6.1 学习率 $\gamma$ 的影响

> [!important] 学习率是 SGD 中最重要的超参数

| 学习率 | 行为 | 后果 |
|:---:|:---|:---|
| 太小（如 $\gamma = 0.0001$） | 一步移动极小 | 收敛极慢，训练 10 万步仍停滞 |
| 适中（如 $\gamma = 0.01$） | 稳步下降 | 快速收敛 |
| 太大（如 $\gamma = 0.5$） | 越过最低点，来回震荡 | 不收敛甚至发散 |
| 极大学习率 | 直接爆炸 | 参数溢出（NaN） |

::: {.callout-tip title="经验法则"}
SGD（$B=1$）通常需要比 Batch GD 小 10-100 倍的学习率，因为梯度噪声放大了有效步长。
:::

---

## 6.2 批量大小 $B$ 的影响

| 批量大小 | 特点 | 适用场景 |
|:---:|:---|:---|
| $B=1$ | 噪声最大，但探索能力强 | 复杂非凸问题 |
| $B=32\sim64$ | 良好平衡，GPU 友好 | 深度学习默认 |
| $B=256\sim1024$ | 梯度更稳定 | 大模型，显存充足时 |
| $B=N$ | Batch GD，无噪声 | 小数据，可解析求解时 |

> [!warning] 深度学习中的批量大小悖论
>
> 传统观点认为"更大的批量 = 更精确的梯度 = 更好的效果"。但实践中发现：
> - 批量过大 → 泛化性能下降（泛化 gap）
> - 原因：噪声具有"正则化"效果，恰好防止过拟合
> - 因此，小批量训练往往泛化更好

---

## 6.3 迭代次数（Epoch）

> [!important] Epoch 的定义
>
> 1 个 Epoch = 遍历一遍所有 $N$ 个样本。
>
> 如果 batch_size = $B$，则每个 epoch 有 $N/B$ 次参数更新。

::: {.callout-note title="Epoch vs 迭代次数"}
- "训练 100 个 epoch" = 把数据看 100 遍
- "训练 1000 步" = 不知道看了多少数据（取决于批量大小）

实践中用 **Epoch** 更通用，因为不论批量大小如何，1 个 Epoch 总代表等量的数据处理。
:::

---

# 七、收敛性分析

## 7.1 为什么 SGD 能收敛？

> [!important] 收敛的直觉
>
> SGD 的每一步更新方向 $\nabla L_i(\boldsymbol{\beta})$ 是全量梯度 $\nabla R_N(\boldsymbol{\beta})$ 的**无偏估计**：
> $$
> \mathbb{E}_{i}\left[\nabla L_i(\boldsymbol{\beta})\right] = \nabla R_N(\boldsymbol{\beta})
> $$
>
> 这意味着：**平均而言，SGD 走在正确的方向上**。虽然每步有噪声，但噪声会随时间被"平均掉"。

---

## 7.2 凸函数的收敛速率

> [!success] 凸函数下的收敛速率
>
> 假设：
> - $R_N(\boldsymbol{\theta})$ 是**凸函数**
> - 梯度是 **$L$-Lipschitz** 的
> - 学习率 $\gamma^{(k)} = \gamma_0 / \sqrt{k+1}$（$\sqrt{k}$ 衰减）
>
> 则 SGD 的 excess risk 满足：
> $$
> \mathbb{E}\left[R_N(\boldsymbol{\theta}^{(k)}) - R_N(\boldsymbol{\theta}^*)\right] = O\!\left(\frac{1}{\sqrt{k}}\right)
> $$
>
> 即：**$O(1/\sqrt{k})$ 收敛速率**，慢于 Batch GD 的 $O(1/k)$。

::: {.callout-note title="为什么 SGD 收敛更慢？"}
Batch GD 每一步都用精确梯度，方向完全正确。

SGD 的每一步只用了 1 个样本的信息（噪声方差 $\sigma^2$），方向有 $O(\sigma)$ 的偏差。这个噪声不会随迭代减少（只要学习率恒定），所以收敛到最优后仍然会在最优解附近"震荡"。

学习率衰减 $\gamma^{(k)} \propto 1/\sqrt{k}$ 可以逐渐压低这个噪声，使 SGD 收敛到最优解——但因为噪声的累积效应，收敛速率无法达到 $O(1/k)$。
:::

---

## 7.3 强凸函数的情况

> [!abstract] 强凸函数的 SGD 收敛
>
> 若 $R_N(\boldsymbol{\theta})$ 是 $\mu$-**强凸**的，则：
> $$
> \mathbb{E}\left[\|\boldsymbol{\theta}^{(k)} - \boldsymbol{\theta}^*\|^2\right] = O\!\left(\frac{1}{k}\right)
> $$
>
> 这比一般凸函数的 $O(1/\sqrt{k})$ 快很多。

**但注意**：深度学习中的损失函数既不是凸的也不是强凸的，所以这些理论结果只是**定性指导**，不能直接用于预测收敛步数。

---

## 7.4 鞍点和局部最优

> [!warning] 高维空间中的鞍点问题
>
> 在高维非凸问题（如神经网络）中：
> - 局部最优点的数量可能远少于鞍点
> - 几乎所有临界点（梯度为零的点）都是鞍点
> - 但 SGD 的噪声有助于"逃离"鞍点

::: {.callout-tip title="SGD 噪声的意外好处"}
噪声让 SGD 不会精确停在鞍点，而是在鞍点附近震荡，偶尔就会翻越"山脊"继续下降。

理论上完全可以用确定性梯度下降（Batch GD），但实践中反而可能被困在鞍点附近出不来。SGD 的"不精确性"反而是它的优势。
:::

---

# 八、动量法：走出震荡困境

## 8.1 问题：SGD 在峡谷中震荡

> [!example] 病态条件数问题
>
> 考虑一个二次损失函数：
> $$
> f(x, y) = x^2 + 10y^2
> $$
>
> 梯度：$\nabla f = (2x, 20y)$
>
> 条件数 $\kappa = 10/1 = 10$，等高线是扁长的椭圆。
>
> 从 $(1, 0.8)$ 出发，Batch GD 每一步的更新：
> $$
> (x^{(k+1)}, y^{(k+1)}) = (x^{(k)}, y^{(k)}) - \gamma(2x^{(k)}, 20y^{(k)})
> $$
>
> 若 $\gamma = 0.1$：$x$ 方向每步变化 $0.2$，$y$ 方向每步变化 $2.0$，来回震荡。

---

## 8.2 动量法的物理直觉

> [!important] 动量法的迭代公式
>
> 引入"速度" $\mathbf{v}^{(k)}$：
> $$
> \mathbf{v}^{(k+1)} = \alpha \mathbf{v}^{(k)} - \gamma \nabla R_N(\boldsymbol{\theta}^{(k)})
> $$
> $$
> \boldsymbol{\theta}^{(k+1)} = \boldsymbol{\theta}^{(k)} + \mathbf{v}^{(k+1)}
> $$
>
> 其中 $\alpha \in [0, 1)$ 是**动量系数**（通常取 0.9）。

**物理类比**：滚下山坡的球不仅受重力（梯度），还积累了速度（惯性）。在平坦方向上速度越来越快，在陡峭方向上震荡被抑制。

---

## 8.3 逐步数值计算（动量法）

> [!example] 动量法逐步计算
>
> 函数：$f(x,y) = x^2 + 10y^2$
>
> 初始：$\boldsymbol{\theta}^{(0)} = (1, 0.8)$，$\mathbf{v}^{(0)} = (0, 0)$
>
> 参数：$\gamma = 0.1$，$\alpha = 0.9$

### 第 0 步（初始化）
$$
\mathbf{v}^{(0)} = (0, 0), \quad \boldsymbol{\theta}^{(0)} = (1, 0.8)
$$

### 第 1 步
梯度 $\nabla f(\boldsymbol{\theta}^{(0)}) = (2, 16)$

$$
\mathbf{v}^{(1)} = 0.9 \times (0, 0) - 0.1 \times (2, 16) = (-0.2, -1.6)
$$
$$
\boldsymbol{\theta}^{(1)} = (1, 0.8) + (-0.2, -1.6) = (0.8, -0.8)
$$

> [!note] 第一步相同
>
> $\mathbf{v}^{(0)} = (0, 0)$ 时，动量还没有积累，第一步和普通 SGD 完全相同。

### 第 2 步
梯度 $\nabla f(\boldsymbol{\theta}^{(1)}) = (1.6, -16)$

$$
\mathbf{v}^{(2)} = 0.9 \times (-0.2, -1.6) - 0.1 \times (1.6, -16)
$$

分开计算：
- $x$ 分量：$0.9 \times (-0.2) - 0.1 \times 1.6 = -0.18 - 0.16 = -0.34$
- $y$ 分量：$0.9 \times (-1.6) - 0.1 \times (-16) = -1.44 + 1.6 = 0.16$

$$
\mathbf{v}^{(2)} = (-0.34, 0.16)
$$
$$
\boldsymbol{\theta}^{(2)} = (0.8, -0.8) + (-0.34, 0.16) = (0.46, -0.64)
$$

### 第 3 步
梯度 $\nabla f(\boldsymbol{\theta}^{(2)}) = (0.92, -12.8)$

$$
\mathbf{v}^{(3)} = 0.9 \times (-0.34, 0.16) - 0.1 \times (0.92, -12.8)
$$
- $x$：$0.9 \times (-0.34) - 0.1 \times 0.92 = -0.306 - 0.092 = -0.398$
- $y$：$0.9 \times 0.16 - 0.1 \times (-12.8) = 0.144 + 1.28 = 1.424$

$$
\mathbf{v}^{(3)} = (-0.398, 1.424)
$$
$$
\boldsymbol{\theta}^{(3)} = (0.46, -0.64) + (-0.398, 1.424) = (0.062, 0.784)
$$

---

## 8.4 对比：普通 SGD vs 动量法

| 步数 | 普通 SGD $\boldsymbol{\theta}$ | 动量法 $\boldsymbol{\theta}$ | 普通 SGD $y$ 震荡 | 动量法 $y$ 震荡 |
|:---:|:---:|:---:|:---:|:---:|
| 0 | $(1.0, 0.8)$ | $(1.0, 0.8)$ | — | — |
| 1 | $(0.8, -0.8)$ | $(0.8, -0.8)$ | $-0.8$ | $-0.8$ |
| 2 | $(0.64, 0.8)$ | $(0.46, -0.64)$ | $+0.8$ | $+0.16$ |
| 3 | $(0.512, -0.64)$ | $(0.062, 0.784)$ | $-0.64$ | $+0.16$ |

::: {.callout-tip title="观察结果"}
- **普通 SGD**：$y$ 方向在 $\pm 0.8$ 来回大幅震荡，每步变化 $1.6$
- **动量法**：$y$ 方向速度逐渐增大但变化平缓（$0 \to -1.6 \to +0.16 \to +1.424$），从第 2 步开始就远小于 SGD 的震荡幅度

同时动量法在 $x$ 方向的进展更快：3 步后 $x = 0.062$（接近 0），而 SGD 只有 $0.512$。**收敛快了近一个数量级！**
:::

---

## 8.5 动量法的深层理解

> [!success] 动量法的等效形式
>
> 将动量更新展开：
> $$
> \mathbf{v}^{(k+1)} = \sum_{t=0}^{k} \alpha^{k-t} \nabla R_N(\boldsymbol{\theta}^{(t)})
> $$
>
> 这说明 $\mathbf{v}^{(k+1)}$ 是**历史梯度的指数加权平均**，越近的梯度权重越大（权重 $\alpha^t$）。
>
> 等价于：动量法在每一步用"滑动窗口内"的平均梯度，而非当前瞬时梯度，来更新参数。

::: {.callout-note title="为什么指数加权平均有效？"}
在 $f(x,y) = x^2 + 10y^2$ 的例子中：
- $y$ 方向的梯度符号交替变化（从 $+16$ 到 $-16$ 到 $+12.8$），指数加权平均后相互抵消
- $x$ 方向的梯度符号始终一致（始终负），指数加权平均后累积加强

**指数加权平均天然抑制了震荡方向，放大了持续下降方向。**
:::

---

# 九、自适应学习率：Adam 算法

## 9.1 问题：所有参数用同一个学习率

> [!warning] 固定学习率的局限性
>
> 在复杂模型中（如神经网络），不同参数的最优学习率可能差几个数量级：
> - 接近最优的参数只需要微调（学习率要小）
> - 远离最优的参数需要大步前进（学习率要大）
> - 不同方向（对应不同的 Hesse 特征值）需要不同的步长

**解决方案**：让学习率**自适应**地根据梯度历史调整。

---

## 9.2 Adam 的三个核心思想

### 思想一：一阶矩估计（梯度的指数加权平均）

$$
\mathbf{m}^{(k)} = \beta_1 \mathbf{m}^{(k-1)} + (1 - \beta_1) \nabla R_N(\boldsymbol{\theta}^{(k)})
$$

$\mathbf{m}$ 类似于动量法中的速度，追踪梯度的主要方向。

### 思想二：二阶矩估计（梯度平方的指数加权平均）

$$
\mathbf{v}^{(k)} = \beta_2 \mathbf{v}^{(k-1)} + (1 - \beta_2) \left(\nabla R_N(\boldsymbol{\theta}^{(k)})\right)^{\circ 2}
$$

$\mathbf{v}$ 追踪每个参数梯度的**幅度**（方差）。

::: {.callout-tip title="为什么用梯度平方？"}
梯度大 → $\mathbf{v}$ 大 → 学习率自动变小
梯度小 → $\mathbf{v}$ 小 → 学习率自动变大

这正是"自适应"的含义：对频繁变化的参数（高曲率方向）用小步长，对平坦方向的参数用大步长。
:::

### 思想三：偏差修正

> [!important] 为什么要修正偏差？
>
> 初始化 $\mathbf{m}^{(0)} = \mathbf{0}$，$\mathbf{v}^{(0)} = \mathbf{0}$。
>
> 前几步时，指数加权平均会偏向零（低估了）：
> $$
> \mathbf{m}^{(1)} = \beta_1 \cdot 0 + (1-\beta_1)\mathbf{g}^{(1)} = (1-\beta_1)\mathbf{g}^{(1)}
> $$
>
> 而 $1-\beta_1 \approx 0.1$（当 $\beta_1 = 0.9$ 时），前几步的矩估计只有真实梯度的约 10%。

**修正公式**：
$$
\hat{\mathbf{m}}^{(k)} = \frac{\mathbf{m}^{(k)}}{1 - \beta_1^k}, \qquad \hat{\mathbf{v}}^{(k)} = \frac{\mathbf{v}^{(k)}}{1 - \beta_2^k}
$$

当 $k$ 很大时，$1-\beta_1^k \approx 1$，修正量趋于零。

---

## 9.3 Adam 更新公式

> [!important] Adam 算法完整迭代
>
> 参数更新：
> $$
> \boldsymbol{\theta}^{(k+1)} = \boldsymbol{\theta}^{(k)} - \gamma \cdot \frac{\hat{\mathbf{m}}^{(k)}}{\sqrt{\hat{\mathbf{v}}^{(k)}} + \varepsilon}
> $$
>
> 其中 $\varepsilon = 10^{-8}$ 防止除零。

逐分量展开：
$$
\theta_j^{(k+1)} = \theta_j^{(k)} - \gamma \cdot \frac{\hat{m}_j^{(k)}}{\sqrt{\hat{v}_j^{(k)}} + \varepsilon}
$$

::: {.callout-note title="分量的直觉"}
每个参数 $\theta_j$ 有自己的学习率：
$$
\gamma_j^{(k)} = \frac{\gamma}{\sqrt{\hat{v}_j^{(k)}} + \varepsilon}
$$

- 若 $\theta_j$ 的梯度一直大 → $\hat{v}_j$ 大 → $\gamma_j$ 自动变小（保守更新）
- 若 $\theta_j$ 的梯度一直小 → $\hat{v}_j$ 小 → $\gamma_j$ 自动变大（加速更新）
- 若 $\theta_j$ 的梯度符号交替 → 分子 $\hat{m}_j$ 抵消 → 震荡被抑制

这就是 Adam 能"自适应"的原因。
:::

---

## 9.4 Adam vs SGD + 动量

| 属性 | SGD + 动量 | Adam |
|:---|:---|:---|
| **核心思想** | 累积速度抑制震荡 | 自适应调整每个参数的学习率 |
| **一阶矩** | 使用 $\mathbf{v}$ | 使用 $\hat{\mathbf{m}}$ |
| **二阶矩** | 不使用 | 使用 $\hat{\mathbf{v}}$ |
| **收敛速度（初期）** | 较慢 | 快 |
| **收敛速度（末期）** | 较快（精调能力强） | 可能有振荡 |
| **超参数数量** | 2（$\gamma$, $\alpha$） | 3（$\gamma$, $\beta_1$, $\beta_2$） |
| **泛化能力** | 往往更好 | 有时略差 |
| **调参难度** | 较难 | 较容易（默认参数效果好） |

::: {.callout-warning title="Adam 的泛化悖论"}
经验发现：**Adam 虽然收敛更快，但 SGD + 动量往往泛化更好**。

原因是 Adam 的自适应学习率在末期会让每个参数的学习率过小（因为累积的梯度平方使 $\hat{v}_j$ 偏大），导致精调能力不足。

这也是为什么在顶级深度学习比赛中，SGD + 动量 + 余弦退火仍是首选优化器，尽管它的调参更困难。
:::

---

# 十、工程实践与调试

## 10.1 实用超参数建议

> [!tip] 超参数推荐值
>
> | 超参数 | 推荐范围 | 说明 |
> |:---|:---|:---|
> | 学习率 $\gamma$ | $10^{-4}$ ~ $10^{-1}$ | 根据问题规模调整，大模型可偏大 |
> | 批量大小 $B$ | $32$ ~ $256$ | 深度学习默认 $32$ 或 $64$ |
> | 动量 $\alpha$ | $0.9$ ~ $0.999$ | 通常 $0.9$ |
> | Adam $\beta_1$ | $0.9$ | 梯度一阶矩 |
> | Adam $\beta_2$ | $0.999$ | 梯度二阶矩 |
> | 学习率衰减 | 初始的 $1/10$ ~ $1/100$ | 末期学习率 |

---

## 10.2 常见问题及诊断

### 问题一：Loss 不下降

| 可能原因 | 诊断方法 | 解决方案 |
|:---|:---|:---|
| 学习率太小 | loss 在缓慢下降但不收敛 | 增大学习率 10 倍 |
| 学习率太大 | loss 在震荡但不收敛 | 减小学习率 |
| 梯度消失 | 参数更新量极小 | 改用 ReLU、BatchNorm、残差连接 |
| 数据有问题 | 检查数据预处理 | 归一化、检查 NaN/Inf |

### 问题二：Loss 爆炸（变成 NaN）

| 可能原因 | 诊断方法 | 解决方案 |
|:---|:---|:---|
| 学习率太大 | 检查 loss 曲线的陡增 | 减小学习率 |
| 数据尺度不一致 | 检查各特征的方差 | 归一化数据 |
| 梯度爆炸 | 检查梯度范数 | 梯度裁剪（gradient clipping） |

### 问题三：训练集 loss 下降但测试集 loss 上升

> [!warning] 过拟合信号
>
> 这是经典的过拟合现象：模型记住了训练数据，泛化能力退化。
>
> 解决方案：
> - 增加正则化（L2、Dropout）
> - 用更大的批量（减少噪声的正则化效应）
> - 使用数据增强
> - 减少模型参数量

---

## 10.3 学习率调度策略

> [!important] 常用的学习率调度

### 阶梯衰减（Step Decay）
```r
# 每 30 个 epoch 学习率减半
if (epoch %% 30 == 0) gamma <- gamma * 0.5
```

### 指数衰减（Exponential Decay）
$$
\gamma^{(k)} = \gamma_0 \cdot \lambda^k, \quad \lambda \in (0, 1)
$$

### 余弦退火（Cosine Annealing）
$$
\gamma^{(k)} = \gamma_{\min} + \frac{1}{2}(\gamma_{\max} - \gamma_{\min})\left(1 + \cos\frac{\pi k}{T}\right)
$$
其中 $T$ 是周期，$\gamma_{\max} = \gamma_0$，$\gamma_{\min} = \gamma_0 / 100$。

::: {.callout-note title="余弦退火的直觉"}
余弦退火模拟了一个"加热-冷却"的物理过程：
- 前半段：学习率高，探索全局（类似加热）
- 中间段：学习率急剧下降，精细搜索（类似淬火）
- 周期性重启：跳出局部最优

配合 SGD + 动量使用时，这是目前泛化性能最好的调度策略之一。
:::

---

# 十一、知识串联

## 11.1 全文知识脉络

> [!abstract] 知识脉络表
>
> | 概念 | 角色 | 出现位置 |
> |:---|:---|:---:|
> | Batch GD | 基准方法，每步用全量梯度 | §2 |
> | SGD | 用单样本梯度代替全量梯度，引入噪声 | §2-3 |
> | Mini-batch SGD | 平衡计算效率和梯度稳定性 | §2, §3.3 |
> | 梯度无偏估计 | SGD 收敛的理论基础 | §2.2 |
> | 逐步数值计算 | 直观理解 SGD 的每次更新 | §4 |
> | 学习率敏感性 | SGD 最重要的超参数 | §6.1 |
> | 批量大小悖论 | 噪声正则化效应 | §6.2 |
> | $O(1/\sqrt{k})$ 收敛 | 凸函数下 SGD 的理论速率 | §7.2 |
> | 动量法 | 累积速度，抑制震荡，加速收敛 | §8 |
> | 指数加权平均 | 动量法和 Adam 的共同数学结构 | §8.5, §9.2 |
> | Adam | 自适应学习率，$\hat{m}/\sqrt{\hat{v}}$ 结构 | §9 |
> | 偏差修正 | Adam 中对初始化偏差的修正 | §9.2 |
> | 梯度裁剪 | 防止梯度爆炸的实用技巧 | §10.2 |
> | 余弦退火 | 最优泛化的学习率调度 | §10.3 |

---

## 11.2 方法选择指南

> [!tip] 从问题到方法
>
> ```
>
> 问题规模
>    │
>    ├─ 小数据（N < 1万）→ Batch GD 或 BFGS（直接求解析解）
>    │
>    └─ 大数据（N > 1百万）
>          │
>          ├─ 非深度学习 → Mini-batch SGD + 动量
>          │
>          └─ 深度学习
>                │
>                ├─ 通用场景 → Adam（默认，调参友好）
>                │
>                ├─ 追求泛化性能 → SGD + 动量 + 余弦退火
>                │
>                └─ 超大规模 → LAMB、LARS 等分布式优化器
> ```

---

## 11.3 自测清单

> [!summary]
>
> - [ ] 能写出 Batch GD、SGD、Mini-batch SGD 的迭代公式
> - [ ] 能解释为什么 SGD 的梯度是全量梯度的无偏估计
> - [ ] 能用具体数字手算 SGD 的一步更新
> - [ ] 能解释为什么 SGD 需要比 Batch GD 更小的学习率
> - [ ] 能解释动量法中 $\alpha$ 参数的作用
> - [ ] 能手算动量法在二次函数上的两到三步更新
> - [ ] 能解释 Adam 中 $\hat{m}/\sqrt{\hat{v}}$ 的含义
> - [ ] 能说出 Adam 和 SGD+动量的各自优缺点
> - [ ] 能诊断 SGD 的常见问题（不收敛、震荡、爆炸）
> - [ ] 能理解学习率调度策略的设计思路

---

> [!quote] 课程结束
> 从正规方程的解析解，到 SGD 的迭代解，我们完成了从"精确但不可扩展"到"近似但可扩展"的思维跨越。这条路，正是从传统机器学习走向深度学习的核心脉络。
