#### 快速记忆口诀

- **任何矩阵** 都可以用 **SVD变换**
- **任何方阵** 都可以用 **特征变换**

陀螺在房间里旋转，你换个位置看它，旋转速度不会变，变的只是你描述它的坐标。相似变换就是"换位置看"，**特征值** 就是那些 **不随** 观察角度改变的 **本质属性**。
#### 1. 相似变换

矩阵 A 和 B 描述的是 **同一个线性变换**，只是站在 **不同的坐标系** 下观察。相似变换就是 **换个角度看同一件事**（换套衣服，人还是同一个人）

**目的**：把 A 变成更简单的形式 B，同时 **保持特征值不变**， 任意一个可逆矩阵 $P$ 都可以进行相似变换

$B = P^{-1}AP$

```r
A <- matrix(rnorm(4),2)
eig_A <- eigen(A)
eig_A$values
P <- matrix(rnorm(4),2)
B <- solve(P) %*% A %*% P
eig_B <- eigen(B)
eig_B$values
```

**注意：相似变换是所有方阵都可以**，相似变换的目的是 **化简** 矩阵， 特征变换是相似变换的一个 **特例**

- $P$ - 从新基到旧基的 **过渡矩阵**
- $P^{-1}$ - 从旧基到新基 
- 整体含义：先把向量**从新坐标系换到旧坐标系**，用 A 作用，再换回新坐标系

**解读 $Bx$** ：

- $Px$ - 把 x 从新坐标换到旧坐标
- $A(Px)$ - 在旧坐标下 用 A 变换 
- $P^{-1}A(Px)$ - 把结果换回新坐标

**如果目标是对角阵（特征分解）**：

P 的列就是 A 的特征向量，通过解 $(A - \lambda I)x = 0$ 得到

**如果目标是上三角阵（舒尔分解）**：

P 是一个正交矩阵，通过 QR 迭代等数值算法逐步构造出来

#### 2. 特征变换

矩阵 A 是一个 **复杂动作**，可以拆解成三步：**换基 - 伸缩 - 复原基**，每个简单动作就是 "**沿某个方向伸缩**"，方向是 **特征向量**，伸缩倍数是 **特征值** （同一组基，原地往返）

**注意：特征变换要求 A 可以对角化**

$A = PDP^{-1}$：从这个定义可以看出 特征变换 的
#### 3. 奇异变换

矩阵 A 是一个 **复杂动作**，可以拆解成三步：**换基 → 伸缩 → 换到另一组基** 伸缩倍数是 **奇异值**，第一次换基由 **右奇异向量(V)** 决定，第二次由 **左奇异向量(U)** 决定 （两组不同的基，单程旅行）

$A=U\Sigma V^T$：

- $V^T$ - 把输入换到右奇异向量坐标系
- $\Sigma$ - 沿各轴独立伸缩
- $U$ - 换到左奇异向量坐标系（输出空间）
#### 4. 投影变换

把向量"压"到某个子空间上，只保留在该方向的分量，**丢掉 垂直分量**（**像影子落在墙上**）

**投影矩阵P满足**：$P^2 = P$（投两次和投一次结果一样）

**换基 = 旋转 = 在各个分量方向投影**

- $V^Tx$   是 投影，应为 $V^T$ 是正交的
- $P^{-1}x$ 是  **换基（求坐标）**，因为 $P^{-1}$ 不一定是正交矩阵

**举例：最小二乘法**

$Ax = b$ 可能无解，因为 $A^+$ 可以近似为 $A^{-1}$，因此：

$\hat{x} = A^+b$ 是最接近的解，所以：

$A\hat{x} = AA^+b$ 是用近似解计算出的 $\hat{b}$，就是 $b$ 在 A 的列空间 $Col(A)$ 中的投影，记作 $Proj_{col(A)}b$ 

所以：$AA^+b = Proj_{col(A)}b$

$Proj_{col(A)}$ 是"投影到A的列空间"的 **算子**
#### 5. 差异性

- SVD对 **任意矩阵** 都存在（m×n都行），而且一定能分解 （**万能工具**）
- 特征分解只对 **方阵** 有意义，而且还得可对角化才行
- **奇异值** = $\sqrt{ A^TA 的特征值 }$  = $\sqrt{ \lambda(A^TA)}$   - 因为 $A^TA$ 一定是 **对称半正定矩阵**，**特征值** 保证非负，**开根号** 才有意义
