
## 1. 伪逆

**直观理解**：伪逆（Pseudo-inverse）的是对 **逆矩阵的推广**，用来处理 **非方阵** 或 **奇异矩阵**的情况。

- **奇异矩阵（**退化矩阵**）**：

	- 一个方阵 $A$ 是奇异矩阵当且仅当：det(A)=0 或者
		- **秩不满**：$\text{rank}(A) < n$（$n \times n$ 矩阵）
		- **列向量线性相关**：列向量不是线性独立的
		- **不可逆**：不存在矩阵 $A^{-1}$ 使得 $AA^{-1} = I$
		- 奇异矩阵至少有**一个奇异值为0**

	**奇异矩阵意味着信息损失**：
	
	- 矩阵的列向量中有冗余或线性相关
	- 无法唯一地"反演"操作
	- 在数据分析中通常表示特征之间有共线性问题

	**与秩的关系**

	rank(A)=非零奇异值的个数：
	
	- **可逆矩阵**（$n \times n$）：所有 $n$ 个奇异值都非零
	- **奇异矩阵**（$n \times n$）：至少一个奇异值为0
	- **非方阵**（$m \times n$，$m > n$）：最多 $n$ 个非零奇异值

**最简单、最常用的方法。`qr()` 返回QR分解结果，其中 `$rank` 就是矩阵的秩**

```r
# 方法一：QR分解
A <- matrix(rnorm(100*100),nrow = 100)
qr(A)$rank
# 方法二：非零奇异值个数
sv <- svd(A)
sum(sv$d > 1e-10)

# 构造奇异矩阵
set.seed(42)

# 4x4 正交矩阵
Q <- qr.Q(qr(matrix(rnorm(4*4),nrow=4)))
# rank 为 2
sigma <- diag(c(5,3,0,0))
A <- Q %*% sigma %*% t(Q)
sum(svd(A)$d >  1e-10)
```

### 1.1 定义

对于 $m \times n$ 矩阵 $A$，其伪逆 $A^+$ 是 $n \times m$ 矩阵，$A^+ := (A^TA)^{-1}A^T$

满足以下性质（**不能用于确定违逆**）：

- $AA^+A=A$      "**往返一圈**"还能回到 $A$
- $A^+AA^+=A^+$ "**往返一圈**"还能回到 $A^+$

这叫做**Moore-Penrose伪逆**，是最标准的定义。

**注意：上面定义里面左乘的顺序，考虑 n x m 的矩阵是 伪逆**

### 1.2 与逆矩阵的关系

- 如果 $A$ 是 **可逆方阵**，则 $A^+ = A^{-1}$
- 如果 $A$ 不可逆或非方阵，伪逆仍 **然存在且唯一**

### 1.3 通过SVD计算伪逆

设 $A = U\Sigma V^T$，则：  $A^+ = V\Sigma^+ U^T$

其中 $\Sigma^+$ 是 $\Sigma$ 的伪逆：$\Sigma^+ = \text{diag}(1/\sigma_1, 1/\sigma_2, \ldots, 1/\sigma_r, 0, \ldots, 0)$

对于**非零奇异值** $\sigma_i$，取 $1/\sigma_i$；对于**零或极小的奇异值**，取0。

## 2. QR 分解
### 2.1 定义与基本概念

对于 $m \times n$ 矩阵 $A$（$m \geq n$，列满秩），QR分解将其分解为：$A = QR$

其中：

- $Q$ 是 $m \times n$ 矩阵，列向量正交（$Q^T Q = I$）
- $R$ 是 $n \times n$ 上三角矩阵，对角线元素为正

### 2.3 几何意义

将 $A$ 的列向量看作坐标系的基向量：

- $Q$ 通过**正交化**这些向量得到（旋转、翻转，但保持长度）
- $R$ 记录了**原向量和新向量的关系**

### 2.3 求解最小二乘

对于 $\text{min} |Ax - b|^2$：

利用 $Q$ 的正交性（$Q^T Q = I$）：  

$\|Ax - b\|^2 = \|QRx - b\|^2 = \|Rx - Q^T b\|^2$

**备注**：

- 对于正交矩阵 Q， 对任意向量 v 有：$|Q^Tv|^2=(Q^Tv)^T(Q^Tv)=v^TQQ^Tv=v^TIv=|v|^2$
- 也就是说，左乘 $Q^T$ 不改变向量的长度(**不改变范数**)

只需求解三角方程组：  

$Rx = Q^T b$      ---- ？？？ 推导过程有点复杂

用**回代法**求解（从下往上），计算量少。

### 2.4 优点与缺点

**优点**：

- 数值稳定性最好，条件数不被放大
- 适合各种规模的问题
- 自动处理列不满秩的情况

**缺点**：

- 计算量较大（约 $2mn^2$ 次浮点运算）
- 需要存储整个 $Q$ 矩阵

### 2.5 计算方法（简要）

常见算法：

- **Gram-Schmidt 正交化**：逐列正交化，但数值稳定性一般
- **Householder 反射**：利用反射矩阵，数值稳定（业界标准）
- **Givens 旋转**：用平面旋转，适合稀疏矩阵

### 2.6 应用场景

- 最小二乘问题（线性回归、数据拟合）
- 特征值问题的迭代算法
- 解决ill-conditioned线性方程组

## 3. Cholesky分解

### 3.1 定义与基本概念

对于**对称正定矩阵** $A$（$n \times n$），Cholesky分解将其分解为：

$A = LL^T$

其中 $L$ 是**下三角矩阵**，对角线元素为正。

### 3.2 前提条件

$A$ 必须满足两个条件：

1. **对称**：$A = A^T$
2. **正定**：所有特征值 > 0，或等价于对所有非零向量 $x$，都有 $x^T A x > 0$

### 3.3 为什么用在最小二乘中？

在正规方程 $A^T A x = A^T b$ 中，$A^T A$ 天然是对称正定的。

因此可以用 Cholesky 分解：  

$A^T A = LL^T$

方程变为：  

$LL^T x = A^T b$

### 3.4 求解步骤

分两步三角求解：

1. **前代**：求解 $Ly = A^T b$（从上往下）
2. **回代**：求解 $L^T x = y$（从下往上）

### 3.5 优点与缺点

**优点**：

- 只需一半的计算量（相比QR约快2倍）
- 存储空间小（只需存 $L$）
- 计算简单，容易编程

**缺点**：

- 数值稳定性一般（条件数是 $A$ 的平方）
- 数据有噪声时误差会放大
- 必须是对称正定矩阵

### 3.6 计算方法

直接法：对 \(A\) 逐列/逐行分解

$$
L_{ij} = \begin{cases}
\sqrt{A_{ii} - \sum_{k=1}^{i-1} L_{ik}^2} & \text{if } j = i \\
\frac{1}{L_{jj}}\left(A_{ij} - \sum_{k=1}^{j-1} L_{ik}L_{jk}\right) & \text{if } j < i
\end{cases}
$$

### 3.7 数值稳定性补充

虽然条件数会平方放大，但在**中等规模问题**（如特征数不超过1000）中，通常仍可接受。若要最高精度，改用QR或SVD。

### 3.8 应用场景

- 协方差矩阵求逆
- 生成多元高斯随机数
- 优化问题的Hessian矩阵分解
- 当性能比精度更重要时

## 4. SVD分解

### 4.1 定义与基本概念

对于**任意** $m \times n$ 矩阵 $A$ $(m \geq n$），SVD分解为：

$$
A = U \Sigma V^T
$$

其中：

- \(U\) 是 $m \times n$ 矩阵，**列向量正交** $(U^T U = I$）
- $\Sigma$ 是 $n \times n$ 对角矩阵，对角元素 $\sigma_1 \geq \sigma_2 \geq \cdots \geq \sigma_n \geq 0$（称为**奇异值**）
- $V$ 是 $n \times n$ 矩阵，**行向量正交** $(V^T V = I$）

### 4.2 奇异值的意义

奇异值反映了矩阵的"**能量**"或"**作用强度**"：

- $\sigma_1$ 最大：矩阵作用最强的方向
- $\sigma_i$ 递减：后续方向作用越来越弱
- $\sigma_i \approx 0$：该方向信息量很小（噪声或冗余）

### 4.3 与特征值分解的区别

| 特性        | SVD               | 特征值分解                 |
| --------- | ----------------- | --------------------- |
| **适用矩阵**  | 任意矩阵              | 必须方阵                  |
| **分解形式**  | $A = U\Sigma V^T$ | $A = Q\Lambda Q^{-1}$ |
| **几何意义**  | 旋转→缩放→旋转          | 沿特征向量方向缩放             |
| **数值稳定性** | 最佳                | 一般                    |

### 4.4 求解最小二乘

对于 $\min \|Ax - b\|^2$：

使用SVD的**伪逆**（pseudo-inverse）：

$$
A^+ = V\Sigma^+ U^T
$$

其中 $\Sigma^+$ 是 $\Sigma$ 的伪逆：对每个 $\sigma_i$，若 $\sigma_i > \text{阈值}$ 则取 $1/\sigma_i$，否则取0。

最小二乘解为：

$$
x = A^+ b
$$

### 4.5 处理列不满秩的情况

如果 \(A\) 列不满秩（有线性相关列），某些 $\sigma_i$ 会非常小或为0。

SVD 的伪逆**自动忽略**这些小奇异值，得到**最小范数解**（所有满足条件的解中范数最小的）。

### 4.6 优点与缺点

**优点**：

- 适用于任意矩阵（满秩、不满秩、超定、欠定都行）
- 数值稳定性最好
- 能自动处理ill-conditioned问题
- 提供矩阵秩的信息（非零奇异值个数）
- 可用于数据压缩和降维

**缺点**：

- 计算量最大（约 $2mn^2 + 11n^3$ 次浮点运算）
- 需要更多存储空间

### 4.7 应用场景

- **最高精度要求**的最小二乘问题
- **列不满秩**或**接近不满秩**的问题
- **主成分分析（PCA）**：取前几个大的奇异值进行降维
- **图像压缩**：低秩近似
- **伪逆计算**
- **矩阵秩的判定**

### 4.8 低秩近似

SVD的一个强大应用：用 $k$ 个最大的奇异值近似原矩阵

$$
A_k = U_k \Sigma_k V_k^T
$$

其中只取前 \(k\) 个奇异值。这样可以：

- 去噪（小奇异值往往是噪声）
- 压缩（用少得多的参数表示）

---

## 5. 三种方法对比表

| 指标        | QR分解  | Cholesky分解 | SVD分解 |
| --------- | ----- | ---------- | ----- |
| **适用矩阵**  | 任意m×n | 对称正定       | 任意m×n |
| **列满秩要求** | 是     | 是          | 否     |
| **计算量**   | 中等    | 少          | 最多    |
| **数值稳定性** | 好     | 中等         | 最好    |
| **编程复杂度** | 中等    | 简单         | 复杂    |
| **实际库支持** | ✅ 所有库 | ✅ 所有库      | ✅ 所有库 |

---

## 6. 实践选择建议

1. **快速解决问题，数据质量好** → QR分解
2. **对称正定矩阵，追求速度** → Cholesky分解
3. **数据有噪声、列不满秩、需要最高精度** → SVD分解
4. **不知道用什么** → 用SVD（最保险，库已优化）