#### 0. 逆矩阵

只有 **方阵** 才有普通意义上的逆矩阵，需要满足 $AA^{-1} = A^{-1}A = I$

```r
A_square <- matrix(rnorm(2*2),2)
solve(A_square) # 求逆
A_nonsquare <- matrix(rnorm(2*3),2)
solve(A_nonsquare) # 求逆，报错
```
#### 1. 列向量线性相关

"列向量线性相关"指的是矩阵的某一列可以写成其他列的线性组合，存在冗余
#### 2. 列空间

矩阵所有列向量的线性组合构成的空间，维度等于 **秩**

Ax = b 有解，把 A 想象一组列向量，Ax 是这些列向量的一个线性组合张成的空间，如果 Ax = b 有解，意味这 b 在这个空间内（例如在平面内）

如果 Ax = b 无解，意味这 b 不在这个空间内，如果 Ax = 0 有非零解，说明 A 的列向量线性相关，非零解可能不止一个，**所有非零解构成了零空间**

Ax = b 有解 等价于 b 属于 A 的列空间，等价于 b 和 A的左零空间**正交**

#### 3. 秩

描述了矩阵能 ”**真实有效**“ 达到多少 ”**维**“， 通过 **消元法** 可以消除 **线性相关的** 列向量（**保留线性无关的**）

- **零秩**：只有零矩阵的秩是 0

```r
A <- matrix(c(1, 0, 2, 
              2, 1, 5, 
              3, 1, 7), nrow=3, byrow=TRUE)
# 第三列 = 第一列 + 第二列 × 2，线性相关
qr(A)$rank  # 结果是 2，不是 3
```

- **亏秩**：存在冗余，零空间非平凡(零空间 **除了零向量还有其他向量**)

```r
A <- matrix(c(1, 2, 2, 4), nrow=2, byrow=TRUE)
qr(A)$rank # 结果是 1，亏秩
# 零空间: c(-2, 1) 满足 Ax = 0
A %*% c(-2, 1)  # 结果是 (0, 0)
```

- **满秩**：所有列（或行）都线性无关(零空间 **只有 零向量**)

```r
A <- matrix(c(1, 2, 3, 4), nrow=2, byrow=TRUE)
qr(A)$rank # 结果是 2，满秩
det(A) # 结果是 -2，非零
```
#### 4. 行列式

描述了 通过这个矩阵， ”**体积**“ 或者 ”**范数**“ 能变化多少， 行列式只针对 **方阵**

```r
A <- matrix(c(2, 0, 0, 3), nrow=2, byrow=TRUE)
det(A) # 结果是 6，单位正方形变成面积为 6 的矩形
```

- 行列式为 **零** 意味着变换后 ”**坍缩**“ 到更低的 维度，无法还原（通过逆变换），也即 **不可逆**

```r
A <- matrix(c(1, 2, 2, 4), nrow=2, byrow=TRUE)
det(A) # 结果是 0
solve(A) # 报错：矩阵是奇异的
```

- 行列式 **非零** 意味着 体积只是 **缩放/翻转** ，也即 **可逆**

```r
A <- matrix(c(1, 2, 3, 4), nrow=2, byrow=TRUE)
det(A)   # 结果是 -2，负号表示翻转
solve(A) # 可以求逆
```

- $|\det(A)|$ 单位超立方体变换后的 ”**体积**“

```r
A <- matrix(c(3, 1, 1, 2), nrow=2, byrow=TRUE)
abs(det(A)) # 结果是 5，单位正方形变成面积为 5 的平行四边形
```
#### 5. **左乘** 

- 向量左乘 A（即 Ax）：把向量映射到 A 的列空间 Col(A)

```r
A <- matrix(c(1, 0, 
			  0, 1, 
			  0, 0), nrow=3, byrow=TRUE)
x <- c(3, 4)
A %*% x  # 结果是 (3, 4, 0)，映射到 xy 平面
```

- 矩阵左乘 A（即 AB）：对 B 的每一列做 A 变换，相当于复合变换

```r
A <- matrix(c(0, -1, 1, 0), nrow=2, byrow=TRUE) # 旋转 90 度
B <- matrix(c(1, 0, 0, 1), nrow=2, byrow=TRUE) # 单位矩阵 
A %*% B # 结果等于 A，相当于旋转变换
```
#### 6. 正交

- **正交向量**：两个向量点积为零，互相垂直，$U,T \in \mathbb{R}^n, U^T \times T = 0$
- **正交矩阵**：满足 $Q^TQ = QQ^T = I$，列向量两两正交且长度为 1，左乘后，**保持长度和角度不变（旋转或反射）**
- **正交投影**：向量投影到子空间，投影矩阵 $P = A(A^TA)^{-1}A^T$

```r
# 正交向量 点积 
u <- c(1, 0)
v <- c(0, 1)
sum(u * v) # 结果是 0，正交

# 正交矩阵
n <- 4
A <- matrix(rnorm(n * n), nrow=n) # 随机矩阵 
Q <- qr.Q(qr(A)) # QR 分解取 Q
t(Q) %*% Q  # 验证是单位矩阵 det(Q) # 结果是 ±1
det(Q)      # 结果是 ±1，正交矩阵行列式只能是 1 或 -1

# 正交矩阵性质
Q <- matrix(c(cos(pi/4), -sin(pi/4), 
            sin(pi/4), cos(pi/4)), nrow=2, byrow=TRUE)
x <- rnorm(4)
sqrt(sum(x^2))         # 原向量长度
sqrt(sum((Q %*% x)^2)) # 变换后长度不变

# 正交投影
A <- matrix(c(1, 0, 
			  0, 1, 
			  0, 0), nrow=3, byrow=TRUE) 
# xy 平面的基 
P <- A %*% solve(t(A) %*% A) %*% t(A) # 投影矩阵 
x <- c(3, 4, 5) 
P %*% x # 结果是 (3, 4, 0)，投影到 xy 平面
```
#### 7. 伪逆

对于 **不可逆** 或 **非方阵**，提供"**最佳近似**"的逆，记作 $A^+$，当 A 可逆时，$A^- = A^+$

```r
A <- matrix(rnorm(6), nrow=3, byrow = TRUE) # 2 x 3 矩阵
A_plus <- MASS::ginv(A) # 3 x 2 矩阵，伪逆
A_plus %*% A # 原矩阵 左乘 伪逆矩阵，结果 近似 为单位矩阵
```

- **最小二乘应用**：Ax = b 无精确解时，$x = A^+b$ 给出最小二乘解

```r
A <- matrix(c(1, 1, 
			  1, 2,
			  1, 3), nrow=3, byrow=TRUE)
b <- c(1, 2, 2) 
x <- MASS::ginv(A) %*% b # 最小二乘解 
A %*% x # 接近 b，但不完全相等 
sum((A %*% x - b)^2) # 残差平方和最小
```
#### 8. 零空间（核）

也叫 ”**核**“ 是所有满足 Ax = 0 的 向量 x 的集合，零空间里的向量经过 A 变换后全被"**压没了**"，变成零向量
#### 9. 迹

方阵对 **角线元素之和**，等于 **特征值之和**

```r
A <- matrix(c(1, 2, 3, 4), nrow=2, byrow=TRUE)
sum(diag(A))       # 迹 = 5
sum(eigen(A)$values)  # 特征值之和也是 5
```
#### 10. 转置

$(AB)^T = B^T A^T$

```r
A <- matrix(rnorm(6), nrow=2)
B <- matrix(rnorm(6), nrow=3)
t(A %*% B)
t(B) %*% t(A)  # 结果一致
```
#### 11. 对称

- **形态**：方阵
- **定义**：满足 $A = A^T$，特征值 **全为实数**，**特征向量 正交**
- **构造对称矩阵**：
	- **方阵**：$A^T + A$  对称，未必正定，$A^T + A + k \times diag(n)$ 对角线加一个很大的数，**矩阵变成对角占优，所有特征值都是正的**。
	- **非方阵**：$AA^T$  $A^TA$对称，且 **半正定** 也称为**Gram矩阵， $A^TA$ 更常见

```r
A <- matrix(c(4, 2, 1,
              2, 5, 3,
              1, 3, 6), nrow=3, byrow=TRUE)  # 对称矩阵
e <- eigen(A)

e$values                    # 特征值全为实数
e$vectors                   # 特征向量矩阵

# 验证特征向量正交
t(e$vectors) %*% e$vectors  # 结果接近单位矩阵
round(t(e$vectors) %*% e$vectors, 10)  # 更清楚

# 单独验证两两正交
v1 <- e$vectors[,1]
v2 <- e$vectors[,2]
v3 <- e$vectors[,3]
sum(v1 * v2)  # 接近 0
sum(v1 * v3)  # 接近 0
sum(v2 * v3)  # 接近 0

# 方阵，未必正定，通过对角线加一个数值，构造正定矩阵
n <- 3
A <- matrix(rnorm(n*n), nrow=n)
B <- t(A) + A          # 对称，但不一定正定
eigen(B)$values        # 可能有负的

# 加足够大的对角元素
k <- 100
C <- B + k * diag(n)   # 对角占优
eigen(C)$values        # 全为正，正定

# 非方阵
A <- matrix(rnorm(6), nrow=2)  # 2×3 非方阵
G <- A %*% t(A)                 # 2×2 Gram 矩阵

# 验证对称
G
t(G)
all.equal(G, t(G))             # TRUE

# 验证半正定（特征值 >= 0）
eigen(G)$values                # 全 >= 0

# 验证 x'Gx >= 0
x <- rnorm(2)
t(x) %*% G %*% x               # 一定 >= 0

# 另一个方向 A'A 也是半正定
H <- t(A) %*% A                # 3×3 Gram 矩阵
eigen(H)$values                # 全 >= 0
```
#### 12. 正定

**对称** 且 **所有特征值 > 0**，等价于 $x^T A x > 0$ 对所有非零 x 成立  

- 表示 **向量 x 经过 A 变换后与自身的"内积"**，正定意味着这个值永远为正（除了 x=0）
- 正定矩阵是一个二次型，**函数就是一个"碗"的形状，有唯一的最小值点，优化算法能保证收敛**，因此：**性质好、计算稳定、有唯一解**。
- **半正定** ： 和正定类似，但条件放宽：对于任意非零向量 x，都有 $x^TAx ≥ 0$，**所有特征值都是 非负数，** 在优化里，半正定对应的二次函数是"平底碗"——可能有一条线或一个面都是最小值，而不是唯一的一个点

```r
A <- matrix(c(4, 2, 2, 3), nrow=2, byrow=TRUE)
eigen(A)$values    # 都是正数
chol(A)            # 能 Cholesky 分解说明正定
```
#### 13. 条件数

**最大奇异值 / 最小奇异值**，衡量矩阵的数值稳定性

```r
A <- matrix(c(1, 2, 3, 4), nrow=2, byrow=TRUE)
kappa(A)           # 条件数
s <- svd(A)
max(s$d) / min(s$d)  # 手动计算
```
#### 14. 向量范数

```r
x <- c(3, 4)
sqrt(sum(x^2))     # L2 范数 = 5
sum(abs(x))        # L1 范数 = 7
max(abs(x))        # 无穷范数 = 4
```
#### 15. 矩阵范数

```r
A <- matrix(c(1, 2, 3, 4), nrow=2, byrow=TRUE)
norm(A, "F")       # Frobenius 范数
norm(A, "2")       # 谱范数（最大奇异值）
```
#### 7. 关系

- $rank(A) = dim(Col(A)) = dim(Row(A))$
- $\det(A) \neq 0 \Leftrightarrow 满秩 \Leftrightarrow 可逆 \Leftrightarrow 零空间只有零向量$
- $\det(A) = 0 \Leftrightarrow 亏秩 \Leftrightarrow 不可逆 \Leftrightarrow 列向量线性相关$

```r
A <- matrix(c(1, 2, 3, 4, 5, 6), nrow=2, byrow=TRUE)
qr(A)$rank # 秩 = 2
qr(t(A))$rank # 转置后秩也是 2，行秩=列秩

A <- matrix(c(1, 2, 3, 4), nrow=2, byrow=TRUE)
det(A) != 0 # TRUE
qr(A)$rank == 2 # TRUE，满秩
solve(A) # 可逆，能求出逆矩阵

A <- matrix(c(1, 2, 2, 4), nrow=2, byrow=TRUE)
det(A) == 0 # TRUE
qr(A)$rank < 2 # TRUE，亏秩 
solve(A) # 会报错，不可逆
```
