
- **正定矩阵（Symmetric Positive Definite）**：**也称为SPD矩阵**。一个对称矩阵 A 是正定的，当且仅当对于任意非零向量 x 都有 $x^TAx > 0$，等价的说法：**所有特征值都是正数**
	- 如果 A 正定，**函数就是一个"碗"的形状，有唯一的最小值点，优化算法能保证收敛**
	- 简单说，正定 = **性质好、计算稳定、有唯一解**。
	- **半正定** ： 和正定类似，但条件放宽：对于任意非零向量 x，都有 $x^TAx ≥ 0$，**所有特征值都是 非负数，** 在优化里，半正定对应的二次函数是"平底碗"——可能有一条线或一个面都是最小值，而不是唯一的一个点
	- **正定矩阵一定可逆，也可以LU分解**
- **对称矩阵**：
	- 任何 **方阵** 加上自己的转置，结果一定是对称矩阵：$A^T + A$  对称，未必正定
	- $AA^T$ - 对称，且半正定 - 不需要**方阵**，也称为**Gram矩阵**
	- $A^T + A + diag(n)$ 对角线加一个很大的数，**矩阵变成对角占优，所有特征值都是正的**。
- **协方差矩阵**：协方差矩阵 Σ 描述变量之间的关系：对角线是各变量的方差，非对角线是变量两两之间的协方差。协方差矩阵也是对称矩阵。**协方差矩阵就是典型的 SPD 矩阵**。
- **行列式**：矩阵就是个线性变换，行列式表示变换后**面积/体积的缩放倍数**。如果行列式为0，意思就是被压扁了（三维变二维、二维变一条线），**信息丢了**，没法还原，所以行列式为 0 = 不可逆
- **奇异矩阵**：可以简单理解为不可逆矩阵，记法：**奇异 → 奇怪 → 有问题 → 不可逆**
- **向量** A 的 L 范数：
	- $\|A\|_{2} = \sqrt{ a_{1}^2 + \dots + a_{2}^2 }$
	- $\|A\|_{2}^2 = A^TA$ - 左乘自身转置
- **向量 r 垂直 A 的列空间**：$A^Tr = 0$, A 是 m x n 矩阵， r 是 m x 1 的向量，所以，$A^T$ 是 n x m 矩阵，维度上可以和 r 相乘，也就是 r 与 A 的列向量做 “点积”，判断点积为 0 ， 正交。
	- 向量正交：向量的 点积 为 0
- **最小二乘的正规方程**：残差 $b - A\hat{x}$ 垂直于 A 的列空间，所以：$A^T(b - A\hat{x}) = 0$，整理后得到正规方程：
					$A^TA\hat{x} = A^Tb$

	- 根据上面方程，如果 $A^TA$ 可逆(**列满秩**)，$\hat{x} = (A^TA)^{-1}A^Tb := A^+b$
- **A的列空间的正交投影矩阵**：$P = AA^+$
	- $P^2 = P$ - 投影两次等于投影一次
	- $P^T = P$ - 投影矩阵是对称矩阵
	- $Pv = v$ 如果 v 在列空间中
- **投影**：如果 $\hat{x}$ 是 最小二乘的解，$A\hat{x}$ 是 b 在 A 的列空间的**投影**，$Pb$ 也是 b 在 A 的列空间投影（通过投影矩阵P），所以 $A\hat{x} = Pb$
	- $\hat{x}$ - 找一个向量使得残差 $\|b - A\hat{x}\|$ 最小
	- 用投影方式，残差会最小，这个投影是 $A\hat{x}$
- **常见时间复杂度**：
	- 矩阵 x 向量：$O(n^2)$
	- 矩阵 x 矩阵：$O(n^3)$
	- n x n 矩阵 和 n x J 矩阵相乘：$O(n^2J)$
	- 矩阵求逆，LU分解：$O(n^3)$
	- 线性方程求解：$O(n^3)$ - 普通   $O(n^2)$ - 三角矩阵    
- **特征值和特征向量**：$Ax = \lambda x$ 看定义就是：设 n 阶方阵，如果存在非零向量 x 和标量 $\lambda$ 使得 向量（特征向量）x 在 A 变换后方向不变（**仍在原来所张成的一维子空间内**），但是会使得向量范数变化为原来的 $\|\lambda\|$ 倍（简单理解为长度伸缩）。
	- **奇数阶** 或 **对称** 矩阵一定有实数上的特征值（特征向量）
	- 正定矩阵/半正定矩阵 一定有实数上的特征值
	- 正交矩阵一定有实数上的特征值（实特征值只能是 ±1）
	- n 阶矩阵有 n 个特征值（计代数重数 ， 就是可以重复），但不同特征值的个数可以是 1 到 n 之间的任意值。

## 最小二乘核心概念汇总

| 符号             | 含义                             |
| -------------- | ------------------------------ |
| A              | **模型结构**，描述输入和输出之间的关系          |
| b              | 实际观测到的数据，**有噪声**               |
| x              | 想求的参数，但原方程 Ax = b 通常无解         |
| x̂             | **最小二乘解**，使误差最小的最佳参数           |
| Ax̂            | 用最佳参数得到的**预测值**，最接近观测数据的理想输出   |
| P = A(AᵀA)⁻¹Aᵀ | 投影矩阵，**把任何向量压到模型能表达的范围（列空间）内** |
| Pb             | **观测数据在列空间上的投影**，和 Ax̂ 是同一个东西  |
| r = b - Ax̂    | 残差，观测值和预测值的差距，**模型没法解释的部分**    |

### 关键关系

- Ax̂ = Pb（预测值就是投影）
- r ⊥ Col(A)（残差垂直于列空间，所以残差最短）


#### 常见矩阵特点

| 矩阵类型 | 求解时间复杂度          | 空间复杂度       | 计算稳定性        | 运算简易性        | 构造复杂性                 | 是否可逆           | 特征值性质         | 奇异值性质         |
| ---- | ---------------- | ----------- | ------------ | ------------ | --------------------- | -------------- | ------------- | ------------- |
| 普通矩阵 | O(n³)            | O(n²)       |              |              |                       |                |               |               |
| 三角矩阵 | **O(n²)**        | O(n²)       | **较好**       | **简单，前代/回代** |                       | **对角元素全非零时可逆** | **对角元素即为特征值** |               |
| 正交矩阵 | **O(n²)**        | O(n²)       | **极好，不放大误差** | **求逆即转置**    | 较复杂(**Householder等**) | **恒可逆，逆=转置**   | **模为1**       | **全为1**       |
| 对角矩阵 | **O(n)**         | **O(n)**    | **极好**       | **极简单**      |                       | **对角元素全非零时可逆** | **对角元素即为特征值** | **对角元素的绝对值**  |
| 正定矩阵 | O(n³/3)，Cholesky | O(n²)       | **很好，无需选主元** | **较简单**      |                       | **恒可逆**        | 全为正实数         | 全为正，**等于特征值** |
| 对称矩阵 | O(n³)            | **O(n²/2)** | **好，特征值为实数** | **较简单**      |                       |                | 全为实数，可正交对角化   | 特征值的绝对值       |








