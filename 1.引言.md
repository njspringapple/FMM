
在实践中，统计学/机器学习总是需要计算的。

在这门课程中，我们将学习有助于实际问题解决的数学概念。

这些概念是开发、理解和分析计算机算法所必需的。

因此需要学习：进阶（数值）线性代数，以及进阶微积分

Obsidian 支持 LaTeX，但语法是：

- 行内公式用单个 `$`：`$x^2$`
- 独立公式用 `$$` 并且要**单独成行**

试试这个：


**为什么需要这么多数学？一个例子**

**任务：** 训练一个线性模型 $y_i \approx \beta_1 x_{i1} + \cdots + \beta_p x_{ip}$，其中有 $p = 10000$ 个特征，$n = 1000$ 个观测值。

- 房价≈β1​×面积+β2​×房间数+⋯

![[fe645a56-2fd5-49ae-ae20-ed90eec5c92f.png]]

**方程比未知数少！** 这是一个欠定系统（underdetermined system），理论上有无穷多解。

最小二乘法找的是使 $|X\beta - y|^2$ 最小的那个解（**通常选范数最小的**）。

这也是为什么需要更高级的数学方法来处理。


⟹ **最小二乘问题：**

$$
\min_{\boldsymbol{\beta} \in \mathbb{R}^p} \left( \sum_{i=1}^{n} \left( y_i - \sum_{j=1}^{p} x_{ij} \beta_j \right)^2 \right) = \min_{\boldsymbol{\beta} \in \mathbb{R}^p} \| \mathbf{X}\boldsymbol{\beta} - \mathbf{y} \|_2^2
$$

**朴素解法：** $\hat{\boldsymbol{\beta}} = (\mathbf{X}^\top \mathbf{X})^{-1} \mathbf{X}^\top \mathbf{y}$

**定理**：求 $x = (A^TA)^{-1}A^Tb$ 得到**最佳近似的参数**

**实践中的问题：**

1. **条件数（Kondition）：** $\mathbf{X}^\top \mathbf{X}$ 可能条件数很差 ⟹ 数值不稳定，误差大

	**条件数**（Condition Number）衡量一个矩阵对误差的敏感程度

1. **复杂度（Komplexitat）：** 矩阵求逆需要 $O(p^3)$ 次运算 ⟹ 当 $p = 10000$ 时：约 $10^{12}$ 次浮点运算！
2. **存储：** $\mathbf{X}^\top \mathbf{X}$ 是 $p \times p$ 矩阵 ⟹ 当 $p = 10000$ 时：仅这个矩阵就需要约 800 MB

**更好的解法：** QR分解、SVD、迭代方法 → **这门课的内容**


- **条件数与复杂度**
    
    - 什么是好的算法？


- **数值线性代数**
    
    - 矩阵与张量运算
    - 计算 $Ax \approx b$ 等的（最小二乘）解


- **高级微分学/分析**
    
    - 如何用简单函数逼近复杂函数？
    - 高维、广义导数


- **数值优化**
    
    - 如何找到复杂函数的最小值/最大值？


## 例1：条件数

考虑线性方程组 $Ax = b$，其中

$A = \begin{pmatrix} 1 & 1 \\ 1 & 1.0001 \end{pmatrix}, \quad b = \begin{pmatrix} 2 \\ 2.0001 \end{pmatrix}$

**精确解：**

$x = \begin{pmatrix} 1 \\ 1 \end{pmatrix}$

**扰动问题**（例如由于舍入误差）：$Ax = \tilde{b}$，其中

$\tilde{b} = \begin{pmatrix} 2 \\ 2.0002 \end{pmatrix}$


（仅 0.01% 的误差！）

**扰动问题的解：**

$\tilde{x} = \begin{pmatrix} 0 \\ 2 \end{pmatrix}$

⟹ 数据中 0.01% 的误差导致 $x_1$​ 产生 **100% 的误差**！

⟹ 这个问题是（极其）**病态的**。

---

## 例2：复杂度

**矩阵乘法** C=AB，对于 $n \times n$ 矩阵：

| 算法         | 复杂度            | n=1000 时的运算次数             |
| ---------- | -------------- | ------------------------- |
| 朴素方法       | $O(n^3)$       | $\approx 10^9$次           |
| Strassen算法 | $O(n^{2.807})$ | $\approx 6 \times 10^8$次  |
| 优化算法       | $O(n^{2.373})$ | $\approx 2 \times 10^8$ 次 |


**解释**：

- 朴素方法：n x n矩阵相乘，结果矩阵的每个元素是：一个矩阵列 和 令一个矩阵行 做点积，那就是 n 次乘法 + （n-1）次加法。而结果有 n x n 个元素，所以，总运算次数是：n^2 x (n + n - 1) = n^2 x 2n (大O计算不考虑常数项，而且假定加法和乘法运算开销一样) = n ^ 3


**对于 n=10000：**

- 朴素方法：$\approx 10^{12}$ 次运算
- 优化方法：$\approx 2 \times 10^{10}$ 次运算

⟹ 通过更好的算法，**快50倍**（……潜在地）！

---

## 例3：高维导数

**AI示例：** 神经网络的梯度下降

**背景知识**：

**什么是神经网络**

神经网络就是一个"**可调节的函数**"。你给它输入（比如一张图片），它给你输出（比如"这是猫"）。网络里有很多参数（**权重 W**），决定了它怎么计算。**训练的目标是调整这些参数，让输出越来越准**。

**怎么调参数**

先用损失函数 L 衡量"**当前输出离正确答案差多远**"。然后算导数 $\frac{\partial L}{\partial W}$，它告诉你"每个参数往哪个方向调，能让误差变小"。最后按这个方向微调参数，重复很多次，网络就越来越准了。这就是梯度下降。

**问题在哪**

参数 W 是个矩阵，可能有成千上万个数。朴素方法要对每个数单独求导，比如 10000 个参数就要算 10000 次。

**矩阵微积分怎么帮忙**

用矩阵微积分，一个公式 $\frac{\partial L}{\partial W} = X^\top(XW - Y)$ 就能一次性算出所有导数，而且计算机算矩阵乘法非常快。

**总结**

神经网络参数多，逐个求导太慢。矩阵微积分把大量求导打包成一次矩阵运算，这是深度学习能实际运行的关键。


**损失函数：** $L(W): \mathbb{R}^{m \times n} \to \mathbb{R}$ （矩阵作为参数！）

#### 例如：MSE（均方误差）- 预测值和真实值之差的平方的平均值

**普通写法**：$L = \frac{1}{n}\sum_{i}(预测_i - 真实_i)^2$
**矩阵写法**：$L = \frac{1}{2}|XW - Y|_F^2$
$|XW - Y|_F^2$ 是 Frobenius 范数的平方，意思是把误差矩阵里所有元素平方后加起来。

- X 是输入数据矩阵，每一行是一个样本，每一列是一个特征，比如预测房价时，X 可能是 三套房子，两个特征（面积、楼层） 
- Y 是真实结果矩阵 每一行对应一个样本的真实值 比如 三套房子的真实价格
- W 是参数矩阵 模型要学习的权重 
- $XW$ 算出预测房价，$XW - Y$ 是预测和真实的差距，$|XW - Y|_F^2$ 把所有差距平方加起来。L 越小，预测越准。训练就是调整 W 让 L 变小

$$L(W) = \frac{1}{2}\|XW - Y\|_F^2$$

其中 $X \in \mathbb{R}^{N \times p}$，$W \in \mathbb{R}^{p \times K}$，$Y \in \mathbb{R}^{N \times K}$

- N - 样本数量（用来训练的数据的数量）
- p - 特征数量
- Y - 每个样本有 K 个真是输出值，例如给一个AI网络一个图片，输出 “是猫的概率 是狗的概率 是鸟的概率），这就是 K = 3，或者是预测房子的多个属性，例如 租金，销售价格，升值潜力等

- W - 特征数量 x 输出值 ，例如 10个特征数量，3个输出值，W = 10x

**损失函数 L 是一个数值，衡量"模型预测得有多差"。预测越准，L 越小；预测越离谱，L 越大**

**损失函数的作用**：训练模型就是调整 W，让 L 尽量小。怎么调？

**损失函数只负责评估**

给定一个 W，损失函数算出"当前预测有多差"，输出一个数值 L。它只是一个评分器，不负责调整任何东西。

**调整 W 是梯度下降的事**

梯度下降算法根据导数 $\frac{\partial L}{\partial W}$ 来调整 W。导数告诉你 W 往哪个方向改能让 L 变小，然后按这个方向更新 W。




**问题：** 如何计算 $\frac{\partial L}{\partial W}$？

**朴素方法：** 对所有 $i, j$ 分别计算 $\frac{\partial L}{\partial W_{ij}}$ ⟹ $p \times K$ 个导数！

对于 $p = 1000$，$K = 10$：**10000 个导数**！

**更好的方法：** 矩阵微积分

$$\frac{\partial L}{\partial W} = X^\top(XW - Y)$$

- 第一步，这告诉你当前位置 L 往哪个方向增长最快，求解结果也是个和W一样的矩阵
- 第二步，更新参数：

$W_{new} = W_{old} - \alpha \cdot \frac{\partial L}{\partial W}$

- a - 学习率，一个认为设定的正小数，比如0.1，0.001 ，他控制了每次调整的不服
梯度告诉你方向，$\alpha$ 决定你沿着这个方向走多远。

$\alpha$ 太大：步子迈太大，可能跨过最低点，L 反而变大，甚至发散。

$\alpha$ 太小：步子太小，要走很久才能到达最低点，训练很慢。

**打个比方**

你在山上想下山。梯度告诉你"最陡的上坡方向"，你就往反方向走（下坡）。走一小步，再算梯度，再走一小步，最终到达山底（L 最小）。



一行代替10000个！而且：**可高效实现**。

⟹ **矩阵/张量函数的导数对于深度学习、优化等至关重要**。

**朴素方法：算 10000 次**

W 是 $1000 \times 10$ 的矩阵，有 10000 个参数。

如果一个一个算偏导数 $\frac{\partial L}{\partial w_{11}}$、$\frac{\partial L}{\partial w_{12}}$、……，要写 10000 个公式，算 10000 次。

**矩阵微积分：一个公式搞定**



这一个公式直接输出一个 $1000 \times 10$ 的矩阵，里面就是全部 10000 个偏导数的结果。

**为什么说"可高效实现"**

计算机算矩阵乘法非常快（有专门优化的库，还能用 GPU 加速）。一次矩阵乘法比循环 10000 次快得多。

**总结**

不是说计算量变少了，而是把 10000 次零散计算打包成几次矩阵运算，利用计算机的并行能力大大加速。



## 什么是 MSE 损失函数

MSE 是衡量"模型预测得有多差"的一个数值。

$$L(W) = \frac{1}{2}\|XW - Y\|_F^2$$

这个公式的意思是：预测值和真实值的差距平方后加起来，再乘以 $\frac{1}{2}$。

---

## X、W、Y 分别是什么

- $X \in \mathbb{R}^{N \times p}$ 是输入数据，N 个样本，每个样本 p 个特征
- $W \in \mathbb{R}^{p \times K}$ 是模型参数，训练就是找最好的 W
- $Y \in \mathbb{R}^{N \times K}$ 是真实结果

---

## 朴素方法：逐个求偏导

W 有 $p \times K$ 个参数，每个要单独算。

如果 $p = 1000$，$K = 10$，要算 10000 次。

---

## 更好的方法：矩阵微积分

$$\frac{\partial L}{\partial W} = X^\top(XW - Y)$$

一个公式输出全部 10000 个偏导数。

---

## 最小例子

2 个样本，2 个特征，1 个输出。

**数据**

$$X = \begin{pmatrix} 1 & 2 \\ 3 & 4 \end{pmatrix}, \quad Y = \begin{pmatrix} 5 \\ 11 \end{pmatrix}, \quad W = \begin{pmatrix} w_1 \\ w_2 \end{pmatrix}$$

**第一步：计算预测值**

$$XW = \begin{pmatrix} w_1 + 2w_2 \\ 3w_1 + 4w_2 \end{pmatrix}$$

**第二步：计算误差**

$$XW - Y = \begin{pmatrix} w_1 + 2w_2 - 5 \\ 3w_1 + 4w_2 - 11 \end{pmatrix}$$

记 $e_1 = w_1 + 2w_2 - 5$，$e_2 = 3w_1 + 4w_2 - 11$

**第三步：计算损失函数**

损失函数定义是：

$$L = \frac{1}{2}\|XW - Y\|_F^2$$

Frobenius 范数的定义是：

$$\|A\|_F = \sqrt{\sum_{i,j} A_{ij}^2}$$

就是把矩阵里所有元素平方，加起来，再开根号。

但这里用的是 $\|\cdot\|_F^2$（范数的平方），开根号后又平方抵消了：

$$\|A\|_F^2 = \left(\sqrt{\sum_{i,j} A_{ij}^2}\right)^2 = \sum_{i,j} A_{ij}^2$$

所以 $\|\cdot\|_F^2$ 就是所有元素平方后直接加起来。

在这个例子里，$XW - Y$ 是：

$$XW - Y = \begin{pmatrix} e_1 \\ e_2 \end{pmatrix}$$

只有两个元素 $e_1$ 和 $e_2$，所以：

$$\|XW - Y\|_F^2 = e_1^2 + e_2^2$$

代入损失函数定义：

$$L = \frac{1}{2}(e_1^2 + e_2^2)$$

把 $e_1$、$e_2$ 的表达式代回去：

$$L = \frac{1}{2}\left[(w_1 + 2w_2 - 5)^2 + (3w_1 + 4w_2 - 11)^2\right]$$

**第四步：逐个求偏导**

$$\frac{\partial L}{\partial w_1} = e_1 + 3e_2$$

$$\frac{\partial L}{\partial w_2} = 2e_1 + 4e_2$$

**第五步：矩阵公式验证**

$$X^\top = \begin{pmatrix} 1 & 3 \\ 2 & 4 \end{pmatrix}$$

$$X^\top(XW - Y) = \begin{pmatrix} 1 & 3 \\ 2 & 4 \end{pmatrix} \begin{pmatrix} e_1 \\ e_2 \end{pmatrix} = \begin{pmatrix} e_1 + 3e_2 \\ 2e_1 + 4e_2 \end{pmatrix}$$

**结果一样，但矩阵公式只需一步。**




## 例4：函数近似：泰勒级数

**目标：**

用多项式来近似复杂函数 $f(x)$，在 $x_0$ 附近。

**二阶泰勒近似：**

$$f(x) \approx f(x_0) + f'(x_0)(x - x_0) + \frac{f''(x_0)}{2}(x - x_0)^2$$

**具体例子：** $f(x) = e^x$，在 $x_0 = 0$ 处展开

$$f(0) = f'(0) = f''(0) = 1 \implies e^x \approx 1 + x + \frac{x^2}{2}$$

**当 $x = 0.5$ 时的误差：**

- 精确值：$e^{0.5} = 1.6487...$
- 近似值：$1 + 0.5 + 0.125 = 1.625$
- 相对误差：< 2%


## 例5：机器学习中的优化

**典型问题：** 训练神经网络

$$\min_{\theta \in \mathbb{R}^d} L(\theta) = \frac{1}{N} \sum_{i=1}^{N} \ell(f(\mathbf{x}_i; \theta), y_i)$$

| 模型           | 参数量 d                      | 数据量 N              |
| ------------ | -------------------------- | ------------------ |
| 逻辑回归 (MNIST) | $\sim 10^3$                | $6 \times 10^4$    |
| ResNet-50    | $\sim 2.5 \times 10^7$     | $> 10^6$           |
| GPT-3        | $\sim 1.75 \times 10^{11}$ | $> 10^{12}$ Tokens |

**挑战：**

1. **非凸：** 损失函数有很多局部最小值/鞍点
2. **高维：** 朴素方法会失效
3. **随机性：** 只能访问小批量数据（minibatch）

$\implies$ 需要**智能算法**：随机梯度下降（SGD）、动量法（Momentum）等



## 所需前置知识

- **线性代数**（线性代数 I 课程）：
    - 主要是：矩阵运算、特征值和特征向量
- **微积分/分析**（分析 I 课程）：
    - 主要是：连续性与可微性、导数
- **R 语言编程**（R 语言入门课程）

**强烈推荐：** 通过 *3blue1brown* 等视频建立线性代数的直觉！

---

## （最简）自测：前置知识是否足够？

**线性代数 - 你应该知道：**

1. 什么是矩阵的*秩*？如何计算？
2. 矩阵 $A$ *可逆*意味着什么？
3. 什么是矩阵的*特征值/特征向量*？如何求？
4. 矩阵-向量乘法 $A\mathbf{x}$ 的几何意义是什么？

**微积分/分析 - 你应该知道：**

1. 函数 $f: \mathbb{R} \to \mathbb{R}$ *连续*是什么意思？
2. 导数 $f'(x_0)$ 的几何意义是什么？
3. 什么是局部最小值 vs. 全局最小值？




