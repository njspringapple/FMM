
# 1. 使用的预备知识

## 1.1 条件数与矩阵范数

- **条件数** $\kappa(\mathbf{A})$：衡量矩阵对输入扰动的敏感程度。定义为 $\kappa(\mathbf{A}) = |\mathbf{A}| \cdot |\mathbf{A}^{-1}|$。条件数越大，问题越"病态"。
- **矩阵范数**（特别是谱范数 $|\cdot|_2$）：$|\mathbf{A}|_2 = \sigma_{\max}(\mathbf{A})$，即最大奇异值。
- **正交矩阵**：满足 $\mathbf{Q}^T\mathbf{Q} = \mathbf{I}$ 的矩阵，保持向量长度和角度不变。

## 1.2 矩阵分解

- **LU分解**：$\mathbf{A} = \mathbf{LU}$，其中 $\mathbf{L}$ 是下三角矩阵，$\mathbf{U}$ 是上三角矩阵。
- **Cholesky分解**：对于正定矩阵，$\mathbf{A} = \mathbf{LL}^T$。
- **SVD（奇异值分解）**：$\mathbf{A} = \mathbf{U\Sigma V}^T$，其中 $\mathbf{U}, \mathbf{V}$ 是正交矩阵，$\mathbf{\Sigma}$ 是对角矩阵。
- **伪逆** $\mathbf{A}^+$：通过SVD定义，$\mathbf{A}^+ = \mathbf{V\Sigma}^+\mathbf{U}^T$。

# 2. 最小二乘法回顾

## 2.1 最小二乘问题（KQ问题）

**问题定义：**

- 给定矩阵 $\mathbf{A} \in \mathbb{R}^{m \times n}$ 和向量 $\mathbf{b} \in \mathbb{R}^m$
- 寻找向量 $\hat{\mathbf{x}} \in \mathbb{R}^n$，使得 $|\mathbf{A}\hat{\mathbf{x}} - \mathbf{b}|$ 最小

**注意：**  [LMU München](https://lsf.verwaltung.uni-muenchen.de/qisserver/rds?state=user&type=1&category=auth.login&startpage=portal.vm&breadCrumbSource=portal)

- 如果线性方程组 $\mathbf{Ax} = \mathbf{b}$ 有精确解 $\mathbf{x}^_2$，则 $|\mathbf{Ax}^_ - \mathbf{b}| = 0$，此时 $\mathbf{x}^*$ 也是最小二乘问题的解。
- 最小二乘方法特别适用于**没有精确解**的"方程组" $\mathbf{Ax}^* \approx \mathbf{b}$（即超定系统）。

# 2.2 动机

最小二乘方法是**统计学和机器学习中的基础方法**：

## 2.3 例子：线性回归

- **问题**：根据房屋面积预测房价
- **数据**：$n$ 个观测值 $(x_i, y_i)$，其中 $x_i$ = 面积（$m^2$），$y_i$ = 价格（€）
- **模型**：$y_i = \beta_0 + \beta_1 x_i + \epsilon_i$，其中 $\epsilon_i$ 是误差项
- **矩阵形式**：$\mathbf{b} = \mathbf{A}\boldsymbol{\beta}$

A=(1x11x2⋮⋮1xn),b=(y1y2⋮yn),β=(β0β1)\mathbf{A} = \begin{pmatrix} 1 & x_1 \\ 1 & x_2 \\ \vdots & \vdots \\ 1 & x_n \end{pmatrix}, \quad \mathbf{b} = \begin{pmatrix} y_1 \\ y_2 \\ \vdots \\ y_n \end{pmatrix}, \quad \boldsymbol{\beta} = \begin{pmatrix} \beta_0 \\ \beta_1 \end{pmatrix}A=​11⋮1​x1​x2​⋮xn​​​,b=​y1​y2​⋮yn​​​,β=(β0​β1​​)

- **解**：$\hat{\boldsymbol{\beta}} = (\mathbf{A}^T\mathbf{A})^{-1}\mathbf{A}^T\mathbf{b}$ 最小化 $\sum_{i=1}^{n}(y_i - \beta_0 - \beta_1 x_i)^2$

## 2.4 其他应用

- **广义线性模型、加性模型**：通过迭代最小二乘求近似解
- **深度学习**：神经网络训练（通常最小化平方误差）
- **计算机视觉**：相机标定、2D图像的3D重建
- **信号处理**：降噪、信号估计
- **大地测量**：GPS定位

# 3. 几何解释

最小二乘的核心是**投影**：

- $\text{col}(\mathbf{A})$：矩阵 $\mathbf{A}$ 的列空间（所有可能的 $\mathbf{Ax}$ 形成的子空间）
- $\hat{\mathbf{b}} = \mathbf{A}\hat{\mathbf{x}}$：$\mathbf{b}$ 在列空间上的投影
- $\mathbf{r} = \mathbf{b} - \hat{\mathbf{b}}$：残差向量，垂直于列空间

**定理：** 最小二乘问题的解集等于线性方程组 $\mathbf{A}\hat{\mathbf{x}} = \hat{\mathbf{b}}$ 的解集，其中 $\hat{\mathbf{b}} = \text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}$。

# 4. 正规方程

**定理：** 最小二乘问题的解集等于正规方程的解集：  
$\mathbf{A}^T\mathbf{A}\hat{\mathbf{x}} = \mathbf{A}^T\mathbf{b}$

**定理：** 如果 $\text{rank}(\mathbf{A}) = n$（即 $\mathbf{A}$ 的列满秩），则 $\mathbf{A}^T\mathbf{A}$ 可逆，最小二乘问题有**唯一解**：  

$\hat{\mathbf{x}} = (\mathbf{A}^T\mathbf{A})^{-1}\mathbf{A}^T\mathbf{b} =: \mathbf{A}^+\mathbf{b}$

这里 $\mathbf{A}^+ = (\mathbf{A}^T\mathbf{A})^{-1}\mathbf{A}^T$ 称为 $\mathbf{A}$ 的**Moore-Penrose伪逆**。

# 5. 小测验

以下哪些陈述是正确的？

1. $\mathbf{AA}^+ = \mathbf{I}_m$
2. $\mathbf{AA}^+\mathbf{b} = \text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}$
3. $\mathbf{r} = \mathbf{b} - \mathbf{A}\hat{\mathbf{x}} \perp \text{col}(\mathbf{A})$
4. $\mathbf{b} = \text{proj}_V\mathbf{b} + \text{proj}_{V^\perp}\mathbf{b}$ 对任意子空间 $V$ 成立

**答案：**

- **1. 错误**：一般情况下 $\mathbf{AA}^+ \neq \mathbf{I}_m$（除非 $\mathbf{A}$ 是方阵且可逆）。$\mathbf{AA}^+$ 是投影矩阵。
- **2. 正确**：$\mathbf{AA}^+$ 恰好是到 $\text{col}(\mathbf{A})$ 的正交投影矩阵。
- **3. 正确**：残差向量垂直于列空间，这是最小二乘解的几何特征。
- **4. 正确**：任意向量都可以分解为在子空间上的投影和在其正交补上的投影之和。


# 6. 条件数（Kondition）

## 6.1 预备说明

- 我们现在研究最小二乘问题 $\mathbf{Ax} \approx \mathbf{b}$ 的**条件数**，其中 $\mathbf{A} \in \mathbb{R}^{m \times n}$，$\mathbf{b} \in \mathbb{R}^m$。
- 我们限制在 $m \geq n$ 且 $\text{rank}(\mathbf{A}) = n$ 的情况下 $\Longrightarrow$ 存在唯一解。
- 以下使用 $|\cdot| = |\cdot|_2$（最小二乘问题的自然范数）。
- 对于不一定是方阵的矩阵，我们定义条件数为：

$\kappa(\mathbf{A}) = \begin{cases} \infty, & \text{rank}(\mathbf{A}) < n \\ \|\mathbf{A}\| \|\mathbf{A}^+\|, & \text{其他情况} \end{cases}$


**扩展说明：**

- 当 $\mathbf{A}$ 列满秩时，$\kappa_2(\mathbf{A}) = |\mathbf{A}|_2 |\mathbf{A}^+|_2 = \frac{\sigma_{\max}(\mathbf{A})}{\sigma_{\min}(\mathbf{A})}$，即最大奇异值与最小奇异值之比。
- 条件数衡量问题对输入扰动的敏感性：条件数大意味着小的输入误差可能导致大的输出误差。


# 7. 关于 $\mathbf{b}$ 的条件数

## 7.1 定理

设 $\mathbf{A} \in \mathbb{R}^{m \times n}$ 固定，$f$ 为求解问题，解为 $f(\mathbf{b}) = \mathbf{A}^+\mathbf{b}$。则有：

∥f(b~)−f(b)∥∥f(b)∥≤κ∥b~−b∥∥b∥其中κ=κ2(A)⋅∥b∥∥projcol(A)b∥\frac{\|f(\tilde{\mathbf{b}}) - f(\mathbf{b})\|}{\|f(\mathbf{b})\|} \leq \kappa \frac{\|\tilde{\mathbf{b}} - \mathbf{b}\|}{\|\mathbf{b}\|} \quad \text{其中} \quad \kappa = \kappa_2(\mathbf{A}) \cdot \frac{\|\mathbf{b}\|}{\|\text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}\|}∥f(b)∥∥f(b~)−f(b)∥​≤κ∥b∥∥b~−b∥​其中κ=κ2​(A)⋅∥projcol(A)​b∥∥b∥​

## 7.2 证明

∥A+b~−A+b∥∥A+b∥=∥A+(b~−b)∥∥A+b∥\frac{\|\mathbf{A}^+\tilde{\mathbf{b}} - \mathbf{A}^+\mathbf{b}\|}{\|\mathbf{A}^+\mathbf{b}\|} = \frac{\|\mathbf{A}^+(\tilde{\mathbf{b}} - \mathbf{b})\|}{\|\mathbf{A}^+\mathbf{b}\|}∥A+b∥∥A+b~−A+b∥​=∥A+b∥∥A+(b~−b)∥​

≤∥A+∥∥b~−b∥∥A+b∥[次可乘性]\leq \frac{\|\mathbf{A}^+\| \|\tilde{\mathbf{b}} - \mathbf{b}\|}{\|\mathbf{A}^+\mathbf{b}\|} \quad \text{[次可乘性]}≤∥A+b∥∥A+∥∥b~−b∥​[次可乘性]

=κ2(A)∥A∥⋅∥A+b∥∥b~−b∥[由 κ2(A) 的定义]= \frac{\kappa_2(\mathbf{A})}{\|\mathbf{A}\| \cdot \|\mathbf{A}^+\mathbf{b}\|} \|\tilde{\mathbf{b}} - \mathbf{b}\| \quad \text{[由 } \kappa_2(\mathbf{A}) \text{ 的定义]}=∥A∥⋅∥A+b∥κ2​(A)​∥b~−b∥[由 κ2​(A) 的定义]

≤κ2(A)∥AA+b∥∥b~−b∥[次可乘性]\leq \frac{\kappa_2(\mathbf{A})}{\|\mathbf{AA}^+\mathbf{b}\|} \|\tilde{\mathbf{b}} - \mathbf{b}\| \quad \text{[次可乘性]}≤∥AA+b∥κ2​(A)​∥b~−b∥[次可乘性]

# 8. 关于 $\mathbf{b}$ 的条件数——解释

$\kappa = \kappa_2(\mathbf{A}) \cdot \frac{\|\mathbf{b}\|}{\|\text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}$

## 8.1 解释

回忆勾股定理的分解：

∥b∥2=∥projcol(A)b∥2+∥projcol(A)⊥b∥2≥∥projcol(A)b∥2\|\mathbf{b}\|^2 = \|\text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}\|^2 + \|\text{proj}_{\text{col}(\mathbf{A})^\perp}\mathbf{b}\|^2 \geq \|\text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}\|^2∥b∥2=∥projcol(A)​b∥2+∥projcol(A)⊥​b∥2≥∥projcol(A)​b∥2

**问题：** $\kappa$ 的最好和最坏情况是什么？

- **最好情况：** $\mathbf{b} = \text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}$，即 $\mathbf{b} \in \text{col}(\mathbf{A})$
    
    - 此时 $\frac{|\mathbf{b}|}{|\text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}|} = 1$
    - $\Rightarrow \kappa = \kappa_2(\mathbf{A})$
    - **含义：** 当 $\mathbf{Ax} = \mathbf{b}$ 有精确解时，条件数最小。
- **最坏情况：** $\text{proj}_{\text{col}(\mathbf{A})}\mathbf{b} = \mathbf{0}$，即 $\mathbf{b} \perp \text{col}(\mathbf{A})$
    
    - 此时 $\frac{|\mathbf{b}|}{|\text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}|} = \frac{|\mathbf{b}|}{0} = \infty$
    - $\Rightarrow \kappa = \infty$
    - **含义：** 当 $\mathbf{b}$ 完全垂直于列空间时，问题病态。

---

# 9. 关于 $\mathbf{A}$ 的条件数

## 9.1 定理

设 $\mathbf{b} \in \mathbb{R}^m$ 固定，$f$ 为求解问题，解为 $f(\mathbf{A}) = \mathbf{A}^+\mathbf{b}$。则有：

$\frac{\|f(\tilde{\mathbf{A}}) - f(\mathbf{A})\|}{\|f(\mathbf{A})\|} \leq \kappa \frac{\|\tilde{\mathbf{A}} - \mathbf{A}\|}{\|\mathbf{A}\|} \quad \text{其中} \quad \kappa = \kappa_2(\mathbf{A}) + \kappa_2(\mathbf{A})^2 \cdot \frac{\|\mathbf{r}\|}{\|\text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}\|}$

_证明：非常繁琐的计算（略）。_

# 10. 关于 $\mathbf{A}$ 的条件数——解释

$\kappa = \kappa_2(\mathbf{A}) + \kappa_2(\mathbf{A})^2 \cdot \frac{\|\mathbf{r}\|}{\|\text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}\|}$

其中 $\mathbf{r} = \mathbf{b} - \mathbf{A}\hat{\mathbf{x}}$ 是残差向量。

## 10.1 解释

- **最好情况：** $\mathbf{r} = \mathbf{0}$，即 $\mathbf{Ax} = \mathbf{b}$ 有精确解
    
    - $\Rightarrow \kappa = \kappa_2(\mathbf{A})$
    - **含义：** 对于有精确解的线性方程组，条件数仅取决于矩阵本身。
    
- **最坏情况：** $\text{proj}_{\text{col}(\mathbf{A})}\mathbf{b} = \mathbf{0}$，但 $\mathbf{r} \neq \mathbf{0}$
    
    - $\Rightarrow \mathbf{Ax} = \mathbf{b}$ 不可解且 $\mathbf{b} \perp \text{col}(\mathbf{A})$
    - $\Rightarrow \kappa = \infty$
    - **含义：** 当 $\mathbf{b}$ 完全在列空间外时，问题极度病态。
    
- **一般情况：** $\mathbf{r} \neq \mathbf{0}$ 且 $\text{proj}_{\text{col}(\mathbf{A})}\mathbf{b} \neq \mathbf{0}$
    
    - $\Rightarrow \kappa = O(\kappa_2(\mathbf{A})^2)$
    - **含义：** 对于一般的最小二乘问题（无精确解），条件数可能是矩阵条件数的**平方量级**！

## 10.2 总结

| 情况                                               | 关于 $\mathbf{b}$ 的条件数                                                     | 关于 $\mathbf{A}$ 的条件数        |
| ------------------------------------------------ | ------------------------------------------------------------------------ | --------------------------- |
| 精确解存在 ($\mathbf{b} \in \text{col}(\mathbf{A})$)  | $\kappa_2(\mathbf{A})$                                                   | $\kappa_2(\mathbf{A})$      |
| 一般最小二乘                                           | $\kappa_2(\mathbf{A}) \cdot \frac{\|\mathbf{b}\|}{\|\hat{\mathbf{b}}\|}$ | $O(\kappa_2(\mathbf{A})^2)$ |
| 病态情况 ($\mathbf{b} \perp \text{col}(\mathbf{A})$) | $\infty$                                                                 | $\infty$                    |

**实际意义：** 最小二乘问题的数值稳定性不仅取决于矩阵 $\mathbf{A}$，还强烈依赖于 $\mathbf{b}$ 与列空间的关系。残差越大（即"拟合"越差），问题越敏感。

# 11. 问题：找出函数中的错误

为了计算最小二乘解 $\hat{\mathbf{x}} = (\mathbf{A}^T\mathbf{A})^{-1}\mathbf{A}^T\mathbf{b}$，给出以下R函数：

```r
kq_solve <- function(A, b) {
    solve(t(A) %*% A) %*% t(A) %*% b
}
```

**任务：** 找出函数中的错误。

## 11.1 答案：永远不要显式求逆矩阵！

这个函数虽然数学上正确，但在数值计算中是**错误的做法**：

- `solve(t(A) %*% A)` 显式计算了 $(\mathbf{A}^T\mathbf{A})^{-1}$
- **显式矩阵求逆在数值上不稳定，且计算量大**

**正确做法：** 使用 `solve(t(A) %*% A, t(A) %*% b)` 直接求解线性方程组，或更好地使用QR分解。

# 12. Cholesky分解方法

正规方程 $\mathbf{A}^T\mathbf{A}\mathbf{x} = \mathbf{A}^T\mathbf{b}$ 给出了一个简单的求解过程：

## 12.1 算法步骤

1. 计算 $\mathbf{M} = \mathbf{A}^T\mathbf{A}$ 和 $\mathbf{c} = \mathbf{A}^T\mathbf{b}$
2. 计算Cholesky分解 $\mathbf{M} = \mathbf{LL}^T$（$\mathbf{M}$ 是正定矩阵）
3. 通过前向替换求解 $\mathbf{Ly} = \mathbf{c}$
4. 通过回代求解 $\mathbf{L}^T\mathbf{x} = \mathbf{y}$

## 12.2 该方法的问题

- 方法高效，但**可能不稳定**：
    - $\mathbf{A}^T\mathbf{A}$ 中的误差会影响后续所有步骤
    - 可以证明，相对算法误差**最好情况**为 $O(\kappa_2(\mathbf{A})^2)$
    - 对比：问题的最佳条件数仅为 $O(\kappa_2(\mathbf{A}))$

**核心问题：** 计算 $\mathbf{A}^T\mathbf{A}$ 会使条件数平方化！

# 13. 替代求解方法

## 13.1 动机

- 我们希望**避免计算 $\mathbf{A}^T\mathbf{A}$**，直接对 $\mathbf{A}$ 操作
- 回忆：最小二乘解为  
    $x = arg⁡min⁡x∥Ax−b∥2\hat{\mathbf{x}} = \arg\min_{\mathbf{x}} \|\mathbf{Ax} - \mathbf{b}\|$

## 13.2 核心思想

- 用正交矩阵 $\mathbf{Q}$ 将问题 $\mathbf{Ax} \approx \mathbf{b}$ 转换为更简单的问题 $\mathbf{QAx} \approx \mathbf{Qb}$，同时**不改变解**

$\Longrightarrow$ $\mathbf{Q}$ 应该是**正交矩阵**，因为：  $\mathbf{QAx} - \mathbf{Qb}\| = \|\mathbf{Q}(\mathbf{Ax} - \mathbf{b})\| = \|\mathbf{Ax} - \mathbf{b}\|$

正交矩阵保持向量的2-范数不变！

# 14. 今日引入的概念总结

## 14.1 核心概念

- **最小二乘问题**：最小化 $|\mathbf{Ax} - \mathbf{b}|_2$
- **几何解释**：将 $\mathbf{b}$ 投影到 $\text{col}(\mathbf{A})$
    - 解满足：$\mathbf{A}\hat{\mathbf{x}} = \hat{\mathbf{b}}$，其中 $\hat{\mathbf{b}} = \text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}$
- **正规方程**：$\mathbf{A}^T\mathbf{A}\hat{\mathbf{x}} = \mathbf{A}^T\mathbf{b}$
    - 解：$\hat{\mathbf{x}} = (\mathbf{A}^T\mathbf{A})^{-1}\mathbf{A}^T\mathbf{b}$（当 $\text{rank}(\mathbf{A}) = n$ 时）

## 14.2 条件数分析

- **关于 $\mathbf{b}$**：取决于 $\mathbf{b}$ 与 $\text{col}(\mathbf{A})$ 的夹角
- **关于 $\mathbf{A}$**：典型情况下为 $O(\kappa_2(\mathbf{A})^2)$ — 条件数平方！

## 14.3 三种求解方法

1. **正规方程 + Cholesky**：快速但可能不稳定
2. **QR分解**：稳定，标准选择
3. **SVD**：最稳定，适用于秩亏矩阵 $\mathbf{A}$

## 14.4 算法比较

- 在**复杂度（速度）**和**稳定性**之间权衡

# 15. 重要性与联系

## 15.1 为什么需要最小二乘？

在统计学/机器学习中，我们通常有**更多数据点（观测）而非参数**：

- $m$ 个观测，$n$ 个参数（$m > n$）
- 方程组 $\mathbf{Ax} = \mathbf{b}$ 是**超定的**（没有精确解）
- 寻找最佳近似解：最小化 $|\mathbf{Ax} - \mathbf{b}|_2$
    - → 线性模型 $\mathbf{y} = \mathbf{X}\boldsymbol{\beta} + \boldsymbol{\epsilon}$
- **非线性问题**也常用此方法（迭代：线性近似 → 近似最小化 → 重复）

## 15.2 投影几何

- 解 $\hat{\mathbf{x}}$ 使 $\mathbf{A}\hat{\mathbf{x}}$ 等于 $\mathbf{b}$ 在 $\text{col}(\mathbf{A})$ 上的投影
- 理解投影对于几何直觉至关重要

## 15.3 正规方程的来源

- $\mathbf{A}^T\mathbf{A}\hat{\mathbf{x}} = \mathbf{A}^T\mathbf{b}$ 来自微积分——令 $\nabla|\mathbf{Ax} - \mathbf{b}|^2 = 0$
- $\mathbf{A}^T\mathbf{A}$ 至少是半正定的（当 $\text{rank}(\mathbf{A}) = n$ 时为正定）
    - $\Longrightarrow$ 可对 $\mathbf{A}^T\mathbf{A}$ 使用Cholesky分解

# 16. 今日学习要点

## 16.1 核心发现

**最小二乘问题的条件数**通常比线性方程组更差：

$\kappa(\mathbf{A}^T\mathbf{A}) \approx \kappa(\mathbf{A})^2 \Longrightarrow \text{朴素的正规方程方法可能不稳定！}$

## 16.2 三种求解方法详解

### 1. Cholesky分解（用于 $\mathbf{A}^T\mathbf{A}$）

- 求解 $(\mathbf{A}^T\mathbf{A})\mathbf{x} = \mathbf{A}^T\mathbf{b}$
- **复杂度**：$O(mn^2 + n^3)$
- **问题**：条件数平方化，潜在不稳定

### 2. QR分解

- $\mathbf{A} = \mathbf{Q}\begin{pmatrix}\mathbf{R}\\mathbf{0}\end{pmatrix}$，求解 $\mathbf{Rx} = \mathbf{c}_1$，其中 $\mathbf{Q}^T\mathbf{b} = \begin{pmatrix}\mathbf{c}_1\\mathbf{c}_2\end{pmatrix}$
- **复杂度**：$O(mn^2)$（当 $m \gg n$ 时等价于 $O(mn^2 + n^3)$）
- **优势**：稳定，不会平方化条件数

### 3. SVD

- $\hat{\mathbf{x}} = \mathbf{A}^+\mathbf{b}$，其中 $\mathbf{A}^+ = \mathbf{V}_r\mathbf{\Sigma}_r^{-1}\mathbf{U}_r^T$
- **复杂度**：$O(\min(m^2n, mn^2))$（当 $m \gg n$ 时等价于 $O(mn^2 + n^3)$）
- **优势**：最稳定，适用于奇异 $\mathbf{A}$（使用约化SVD），给出最小范数解
- **缺点**：最慢（常数因子大）

# 17. 总结

## 17.1 方法选择指南

| 场景                   | 推荐方法         |
| -------------------- | ------------ |
| $\mathbf{A}$ 良态，追求速度 | Cholesky     |
| 一般情况，需要稳定性           | **QR分解**（首选） |
| $\mathbf{A}$ 病态或秩亏   | SVD          |







