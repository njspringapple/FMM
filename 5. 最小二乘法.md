最小二乘法：从无解到最优解的智慧 https://www.coze.cn/s/8REkdPW2aGY/
## 🎯 预备知识

> [!info] 条件数与矩阵范数
> - **条件数** $\kappa(\mathbf{A}) = \|\mathbf{A}\| \cdot \|\mathbf{A}^{-1}\|$：衡量矩阵对输入扰动的敏感程度
> - **谱范数** $\|\mathbf{A}\|_2 = \sigma_{\max}(\mathbf{A})$：最大奇异值
> - **正交矩阵**：满足 $\mathbf{Q}^T\mathbf{Q} = \mathbf{I}$，保持向量长度和角度不变

> [!info] 矩阵分解
> - **LU分解**：$\mathbf{A} = \mathbf{LU}$
> - **Cholesky分解**：对于正定矩阵，$\mathbf{A} = \mathbf{LL}^T$
> - **SVD**：$\mathbf{A} = \mathbf{U\Sigma V}^T$
> - **伪逆**：$\mathbf{A}^+ = \mathbf{V\Sigma}^+\mathbf{U}^T$

---

## 📑 目录

```

1. 最小二乘问题
2. 几何解释
3. 正规方程
4. 条件数分析
5. 求解方法
6. 方法比较与总结

```

---

# 一、最小二乘问题

## 1.1 问题定义

> [!important] 最小二乘问题（KQ问题）
> 
> 给定矩阵 $\mathbf{A} \in \mathbb{R}^{m \times n}$ 和向量 $\mathbf{b} \in \mathbb{R}^m$，寻找向量 $\hat{\mathbf{x}} \in \mathbb{R}^n$，使得
> $$
> \|\mathbf{A}\hat{\mathbf{x}} - \mathbf{b}\|_2
> $$
> 最小。

> [!note] 与线性方程组的关系
> - 如果 $\mathbf{Ax} = \mathbf{b}$ 有精确解 $\mathbf{x}^*$，则 $\|\mathbf{Ax}^* - \mathbf{b}\| = 0$，此时 $\mathbf{x}^*$ 也是最小二乘问题的解
> - 最小二乘方法特别适用于**没有精确解**的超定系统

---

## 1.2 动机与应用

> [!success] 统计学和机器学习中的基础方法
> 
> **线性回归**
> - 问题：根据房屋面积预测房价
> - 数据：$n$ 个观测值 $(x_i, y_i)$
> - 模型：$y_i = \beta_0 + \beta_1 x_i + \epsilon_i$
> 
> **矩阵形式**：
> $$
> \mathbf{A} = \begin{pmatrix} 1 & x_1 \\ 1 & x_2 \\ \vdots & \vdots \\ 1 & x_n \end{pmatrix}, \quad \mathbf{b} = \begin{pmatrix} y_1 \\ y_2 \\ \vdots \\ y_n \end{pmatrix}, \quad \boldsymbol{\beta} = \begin{pmatrix} \beta_0 \\ \beta_1 \end{pmatrix}
> $$

> [!tip] 其他应用领域
> - 广义线性模型、加性模型
> - 深度学习（神经网络训练）
> - 计算机视觉（相机标定、3D重建）
> - 信号处理（降噪、信号估计）
> - 大地测量（GPS定位）

---

# 二、几何解释

## 2.1 投影视角

> [!important] 最小二乘的核心是投影
> 
> - $\text{col}(\mathbf{A})$：矩阵 $\mathbf{A}$ 的列空间（所有可能的 $\mathbf{Ax}$ 形成的子空间）
> - $\hat{\mathbf{b}} = \mathbf{A}\hat{\mathbf{x}}$：$\mathbf{b}$ 在列空间上的投影
> - $\mathbf{r} = \mathbf{b} - \hat{\mathbf{b}}$：残差向量，**垂直于列空间**

> [!abstract] 定理：几何特征
> 
> 最小二乘问题的解集等于线性方程组 $\mathbf{A}\hat{\mathbf{x}} = \hat{\mathbf{b}}$ 的解集，其中
> $$
> \hat{\mathbf{b}} = \text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}
> $$

---

# 三、正规方程

## 3.1 理论基础

> [!important] 定理：正规方程
> 
> 最小二乘问题的解集等于正规方程的解集：
> $$
> \mathbf{A}^T\mathbf{A}\hat{\mathbf{x}} = \mathbf{A}^T\mathbf{b}
> $$

> [!success] 定理：唯一解条件
> 
> 如果 $\text{rank}(\mathbf{A}) = n$（即 $\mathbf{A}$ 的列满秩），则 $\mathbf{A}^T\mathbf{A}$ 可逆，最小二乘问题有**唯一解**：
> $$
> \hat{\mathbf{x}} = (\mathbf{A}^T\mathbf{A})^{-1}\mathbf{A}^T\mathbf{b} =: \mathbf{A}^+\mathbf{b}
> $$
> 
> 这里 $\mathbf{A}^+ = (\mathbf{A}^T\mathbf{A})^{-1}\mathbf{A}^T$ 称为 $\mathbf{A}$ 的 **Moore-Penrose 伪逆**。

---

## 3.2 小测验

> [!question] 以下哪些陈述是正确的？
> 
> 1. $\mathbf{AA}^+ = \mathbf{I}_m$
> 2. $\mathbf{AA}^+\mathbf{b} = \text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}$
> 3. $\mathbf{r} = \mathbf{b} - \mathbf{A}\hat{\mathbf{x}} \perp \text{col}(\mathbf{A})$
> 4. $\mathbf{b} = \text{proj}_V\mathbf{b} + \text{proj}_{V^\perp}\mathbf{b}$ 对任意子空间 $V$ 成立

> [!success] 答案
> 
> | 陈述 | 正误 | 解释 |
> |:---|:---|:---|
> | 1 | ❌ | 一般情况下 $\mathbf{AA}^+ \neq \mathbf{I}_m$，$\mathbf{AA}^+$ 是投影矩阵 |
> | 2 | ✅ | $\mathbf{AA}^+$ 恰好是到 $\text{col}(\mathbf{A})$ 的正交投影矩阵 |
> | 3 | ✅ | 残差向量垂直于列空间，这是最小二乘解的几何特征 |
> | 4 | ✅ | 任意向量都可以分解为在子空间上的投影和在其正交补上的投影之和 |

---

# 四、条件数分析

## 4.1 预备说明

> [!note] 问题设定
> - 研究最小二乘问题 $\mathbf{Ax} \approx \mathbf{b}$ 的条件数
> - 限制在 $m \geq n$ 且 $\text{rank}(\mathbf{A}) = n$ 的情况 $\Longrightarrow$ 存在唯一解
> - 使用 $\|\cdot\| = \|\cdot\|_2$（最小二乘问题的自然范数）

> [!important] 条件数定义（一般矩阵）
> $$
> \kappa(\mathbf{A}) = \begin{cases} \infty, & \text{rank}(\mathbf{A}) < n \\ \|\mathbf{A}\| \|\mathbf{A}^+\|, & \text{其他情况} \end{cases}
> $$
> 
> 当 $\mathbf{A}$ 列满秩时：$\kappa_2(\mathbf{A}) = \|\mathbf{A}\|_2 \|\mathbf{A}^+\|_2 = \frac{\sigma_{\max}(\mathbf{A})}{\sigma_{\min}(\mathbf{A})}$

---

## 4.2 关于 $\mathbf{b}$ 的条件数

> [!important] 定理
> 
> 设 $\mathbf{A} \in \mathbb{R}^{m \times n}$ 固定，$f$ 为求解问题，解为 $f(\mathbf{b}) = \mathbf{A}^+\mathbf{b}$。则有：
> $$
> \frac{\|f(\tilde{\mathbf{b}}) - f(\mathbf{b})\|}{\|f(\mathbf{b})\|} \leq \kappa \frac{\|\tilde{\mathbf{b}} - \mathbf{b}\|}{\|\mathbf{b}\|}
> $$
> 其中 $\kappa = \kappa_2(\mathbf{A}) \cdot \frac{\|\mathbf{b}\|}{\|\text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}\|}$

> [!note] 解释
> 
> 回忆勾股定理的分解：
> $$
> \|\mathbf{b}\|^2 = \|\text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}\|^2 + \|\text{proj}_{\text{col}(\mathbf{A})^\perp}\mathbf{b}\|^2 \geq \|\text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}\|^2
> $$

> [!tip] 最好与最坏情况
> 
> | 情况 | 条件 | 结果 |
> |:---|:---|:---|
> | **最好** | $\mathbf{b} \in \text{col}(\mathbf{A})$（有精确解） | $\kappa = \kappa_2(\mathbf{A})$ |
> | **最坏** | $\mathbf{b} \perp \text{col}(\mathbf{A})$ | $\kappa = \infty$ |

---

## 4.3 关于 $\mathbf{A}$ 的条件数

> [!important] 定理
> 
> 设 $\mathbf{b} \in \mathbb{R}^m$ 固定，$f$ 为求解问题，解为 $f(\mathbf{A}) = \mathbf{A}^+\mathbf{b}$。则有：
> $$
> \frac{\|f(\tilde{\mathbf{A}}) - f(\mathbf{A})\|}{\|f(\mathbf{A})\|} \leq \kappa \frac{\|\tilde{\mathbf{A}} - \mathbf{A}\|}{\|\mathbf{A}\|}
> $$
> 其中 $\kappa = \kappa_2(\mathbf{A}) + \kappa_2(\mathbf{A})^2 \cdot \frac{\|\mathbf{r}\|}{\|\text{proj}_{\text{col}(\mathbf{A})}\mathbf{b}\|}$
> 
> 这里 $\mathbf{r} = \mathbf{b} - \mathbf{A}\hat{\mathbf{x}}$ 是残差向量。

> [!tip] 最好与最坏情况
> 
> | 情况 | 条件 | 结果 |
> |:---|:---|:---|
> | **最好** | $\mathbf{r} = \mathbf{0}$（有精确解） | $\kappa = \kappa_2(\mathbf{A})$ |
> | **一般** | $\mathbf{r} \neq \mathbf{0}$ 且 $\text{proj}_{\text{col}(\mathbf{A})}\mathbf{b} \neq \mathbf{0}$ | $\kappa = O(\kappa_2(\mathbf{A})^2)$ |
> | **最坏** | $\mathbf{b} \perp \text{col}(\mathbf{A})$ 但 $\mathbf{r} \neq \mathbf{0}$ | $\kappa = \infty$ |

---

## 4.4 条件数总结

> [!summary] 条件数对比表
> 
> | 情况 | 关于 $\mathbf{b}$ | 关于 $\mathbf{A}$ |
> |:---|:---|:---|
> | 精确解存在 | $\kappa_2(\mathbf{A})$ | $\kappa_2(\mathbf{A})$ |
> | 一般最小二乘 | $\kappa_2(\mathbf{A}) \cdot \frac{\|\mathbf{b}\|}{\|\hat{\mathbf{b}}\|}$ | $O(\kappa_2(\mathbf{A})^2)$ |
> | 病态情况 | $\infty$ | $\infty$ |

> [!warning] 实际意义
> 最小二乘问题的数值稳定性不仅取决于矩阵 $\mathbf{A}$，还强烈依赖于 $\mathbf{b}$ 与列空间的关系。**残差越大（即"拟合"越差），问题越敏感。**

---

# 五、求解方法

## 5.1 永远不要显式求逆！

> [!danger] 找出错误
> 
> ```r
> kq_solve <- function(A, b) {
>     solve(t(A) %*% A) %*% t(A) %*% b
> }
> ```

> [!warning] 问题所在
> - `solve(t(A) %*% A)` 显式计算了 $(\mathbf{A}^T\mathbf{A})^{-1}$
> - **显式矩阵求逆在数值上不稳定，且计算量大**

> [!success] 正确做法
> 使用 `solve(t(A) %*% A, t(A) %*% b)` 直接求解线性方程组，或更好地使用 QR 分解。

---

## 5.2 方法一：Cholesky 分解

> [!abstract] 算法步骤
> 
> 1. 计算 $\mathbf{M} = \mathbf{A}^T\mathbf{A}$ 和 $\mathbf{c} = \mathbf{A}^T\mathbf{b}$
> 2. 计算 Cholesky 分解 $\mathbf{M} = \mathbf{LL}^T$
> 3. 通过前向替换求解 $\mathbf{Ly} = \mathbf{c}$
> 4. 通过回代求解 $\mathbf{L}^T\mathbf{x} = \mathbf{y}$

> [!warning] 问题
> - 方法高效，但**可能不稳定**
> - $\mathbf{A}^T\mathbf{A}$ 中的误差会影响后续所有步骤
> - 相对算法误差**最好情况**为 $O(\kappa_2(\mathbf{A})^2)$
> 
> **核心问题**：计算 $\mathbf{A}^T\mathbf{A}$ 会使条件数平方化！

---

## 5.3 方法二：QR 分解

> [!important] 核心思想
> 
> 用正交矩阵 $\mathbf{Q}$ 将问题 $\mathbf{Ax} \approx \mathbf{b}$ 转换为更简单的问题 $\mathbf{QAx} \approx \mathbf{Qb}$，同时**不改变解**。
> 
> 因为 $\mathbf{Q}$ 是正交矩阵：$\|\mathbf{QAx} - \mathbf{Qb}\| = \|\mathbf{Q}(\mathbf{Ax} - \mathbf{b})\| = \|\mathbf{Ax} - \mathbf{b}\|$
> 
> **正交矩阵保持向量的 2-范数不变！**

> [!success] 算法步骤
> 
> 1. 计算 QR 分解：$\mathbf{A} = \mathbf{Q}\begin{pmatrix}\mathbf{R}\\\mathbf{0}\end{pmatrix}$
> 2. 计算 $\mathbf{Q}^T\mathbf{b} = \begin{pmatrix}\mathbf{c}_1\\\mathbf{c}_2\end{pmatrix}$
> 3. 求解 $\mathbf{Rx} = \mathbf{c}_1$

---

## 5.4 方法三：SVD

> [!abstract] 算法
> 
> $$
> \hat{\mathbf{x}} = \mathbf{A}^+\mathbf{b} = \mathbf{V}_r\mathbf{\Sigma}_r^{-1}\mathbf{U}_r^T\mathbf{b}
> $$

> [!success] 优势
> - 最稳定
> - 适用于奇异 $\mathbf{A}$（使用约化 SVD）
> - 给出最小范数解

> [!note] 缺点
> 最慢（常数因子大）

---

# 六、方法比较与总结

## 6.1 三种方法对比

> [!tip] 算法比较
> 
> | 方法 | 复杂度 | 稳定性 | 适用场景 |
> |:---|:---|:---|:---|
> | **Cholesky** | $O(mn^2 + n^3)$ | 可能不稳定（条件数平方化） | $\mathbf{A}$ 良态，追求速度 |
> | **QR 分解** | $O(mn^2)$ | 稳定 | **一般情况（首选）** |
> | **SVD** | $O(\min(m^2n, mn^2))$ | 最稳定 | $\mathbf{A}$ 病态或秩亏 |

---

## 6.2 核心要点

> [!summary] 今日学习要点
> 
> **最小二乘问题**
> - 最小化 $\|\mathbf{Ax} - \mathbf{b}\|_2$
> - 几何解释：将 $\mathbf{b}$ 投影到 $\text{col}(\mathbf{A})$
> 
> **正规方程**
> $$
> \mathbf{A}^T\mathbf{A}\hat{\mathbf{x}} = \mathbf{A}^T\mathbf{b}
> $$
> 
> **条件数**
> - 关于 $\mathbf{b}$：取决于 $\mathbf{b}$ 与 $\text{col}(\mathbf{A})$ 的夹角
> - 关于 $\mathbf{A}$：典型情况下为 $O(\kappa_2(\mathbf{A})^2)$

> [!danger] 核心发现
> $$
> \kappa(\mathbf{A}^T\mathbf{A}) \approx \kappa(\mathbf{A})^2
> $$
> 
> **朴素的正规方程方法可能不稳定！**

---

## 6.3 方法选择指南

> [!success] 推荐策略
> 
> | 场景 | 推荐方法 |
> |:---|:---|
> | $\mathbf{A}$ 良态，追求速度 | Cholesky |
> | 一般情况，需要稳定性 | **QR 分解**（首选） |
> | $\mathbf{A}$ 病态或秩亏 | SVD |

---

## ✅ 自测清单

- [ ] 能定义最小二乘问题
- [ ] 理解最小二乘的几何解释（投影）
- [ ] 能写出正规方程
- [ ] 理解伪逆 $\mathbf{A}^+$ 的定义
- [ ] 能分析最小二乘问题关于 $\mathbf{b}$ 和 $\mathbf{A}$ 的条件数
- [ ] 理解为什么条件数会平方化
- [ ] 能描述三种求解方法的步骤
- [ ] 能比较三种方法的复杂度和稳定性
- [ ] 知道何时选择哪种方法
