## 🎯 前置知识

> [!info] 线性代数
> - 矩阵运算（加法、乘法、转置）
> - 特征值与特征向量
> - 正交性与内积
> - 基本向量范数（欧几里得范数 $\|\mathbf{x}\|_2$、$p$-范数）

> [!info] 数学分析
> - 极限与上确界
> - 函数的最大值/最小值

> [!info] 数值分析基础
> - 范数作为误差度量
> - 条件数

---

## 📑 目录

```

1. 矩阵的迹
2. Frobenius 范数
3. 矩阵范数
4. 算子范数
5. 正交矩阵
6. 谱范数与谱分解
7. Schatten 范数
8. 范数的性质与应用
9. 总结

```

---

# 一、矩阵的迹

## 1.1 定义

> [!important] 定义：迹（Trace）
> 
> 一个 $n \times n$ 方阵 $\mathbf{A} \in \mathbb{R}^{n \times n}$ 的*迹*是其对角元素之和：
> $$
> \text{tr}(\mathbf{A}) = \sum_{i=1}^{n} a_{ii}
> $$

> [!note] 备注
> - **仅对方阵有定义**
> - 是矩阵的**标量**不变量

> [!example] 示例
> - $\text{tr}(\mathbf{I}_n) = n$
> - $\text{tr}\begin{pmatrix} 1 & 2 \\ 3 & 4 \end{pmatrix} = 1 + 4 = 5$
> - $\text{tr}\begin{pmatrix} 2 & -1 & 0 \\ 0 & 3 & 5 \\ 1 & 0 & -2 \end{pmatrix} = 2 + 3 + (-2) = 3$

---

## 1.2 基本性质

> [!important] 迹的性质
> 
> 对于矩阵 $\mathbf{A}, \mathbf{B} \in \mathbb{R}^{n \times n}$ 和 $c \in \mathbb{R}$：
> 
> | 性质 | 公式 |
> |:---|:---|
> | **线性** | $\text{tr}(\mathbf{A} + \mathbf{B}) = \text{tr}(\mathbf{A}) + \text{tr}(\mathbf{B})$ |
> | **齐次性** | $\text{tr}(c\mathbf{A}) = c \cdot \text{tr}(\mathbf{A})$ |
> | **循环性** | $\text{tr}(\mathbf{AB}) = \text{tr}(\mathbf{BA})$ |
> | **相似不变性** | $\text{tr}(\mathbf{PAP}^{-1}) = \text{tr}(\mathbf{A})$ |
> | **转置不变性** | $\text{tr}(\mathbf{A}^\top) = \text{tr}(\mathbf{A})$ |

> [!danger] 最重要的性质：循环置换
> 
> $$
> \text{tr}(\mathbf{ABC}) = \text{tr}(\mathbf{BCA}) = \text{tr}(\mathbf{CAB})
> $$
> 
> **注意**：$\text{tr}(\mathbf{AB}) = \text{tr}(\mathbf{BA})$，但 $\text{tr}(\mathbf{AB}) \neq \text{tr}(\mathbf{A}) \cdot \text{tr}(\mathbf{B})$！

---

## 1.3 迹与特征值

> [!important] 定理：迹是特征值之和
> 
> 对于一个具有特征值 $\lambda_1, \ldots, \lambda_n$ 的矩阵 $\mathbf{A} \in \mathbb{R}^{n \times n}$：
> $$
> \text{tr}(\mathbf{A}) = \sum_{i=1}^{n} \lambda_i
> $$

> [!note] 证明概要
> - 设 $\mathbf{A} = \mathbf{PDP}^{-1}$ 为一个对角化分解
> - $\Longrightarrow \mathbf{D} = \text{diag}(\lambda_1, \ldots, \lambda_n)$
> - $\text{tr}(\mathbf{A}) = \text{tr}(\mathbf{PDP}^{-1}) \overset{\text{相似不变}}{=} \text{tr}(\mathbf{D}) = \sum_{i=1}^{n} \lambda_i$

---

# 二、Frobenius 范数

## 2.1 定义

> [!important] 定义：Frobenius 范数
> 
> 矩阵 $\mathbf{A} \in \mathbb{R}^{m \times n}$ 的 *Frobenius 范数* 定义为
> $$
> \|\mathbf{A}\|_F := \sqrt{\sum_{i=1}^{m} \sum_{j=1}^{n} a_{ij}^2}
> $$

> [!note] 备注
> - 类似于欧几里得向量范数
> - 也称为 **Hilbert-Schmidt 范数**
> - **衡量的是矩阵的"大小"**

---

## 2.2 为什么需要矩阵范数？

> [!tip] 矩阵范数的用途
> - **测量矩阵的大小**
> - **量化计算中的误差**
> - 分析算法的**收敛性**
> - 理解问题的**条件数**

---

## 2.3 Frobenius 范数的问题

> [!warning] 问题
> - 忽略了 $\mathbf{A}$ 所表示的**线性变换** $\mathbf{x} \mapsto \mathbf{Ax}$
> - **将矩阵当作长向量处理**：对元素排列顺序不变
> - 没有直接的几何解释

> [!question] 问题：我们能否定义考虑变换性质的范数？

---

# 三、矩阵范数

## 3.1 定义

> [!important] 定义：矩阵范数
> 
> 函数 $\| \cdot \| : \mathbb{R}^{m \times n} \to [0, \infty)$ 是一个*矩阵范数*，如果对所有 $\mathbf{A}, \mathbf{B} \in \mathbb{R}^{m \times n}$ 和 $c \in \mathbb{R}$：
> 
> 1. **正定性**：$\|\mathbf{A}\| = 0 \iff \mathbf{A} = \mathbf{0}$
> 2. **绝对齐次性**：$\|c\mathbf{A}\| = |c| \cdot \|\mathbf{A}\|$
> 3. **三角不等式**：$\|\mathbf{A} + \mathbf{B}\| \le \|\mathbf{A}\| + \|\mathbf{B}\|$

> [!note] 备注
> - 与*向量范数*的公理相同
> - *向量范数*是 $\mathbb{R}^{n \times 1}$ 上的矩阵范数

---

## 3.2 从向量范数导出的矩阵范数

> [!abstract] 方法：向量化
> 
> 1. 将矩阵写成长向量
> $$
> \text{vec}(\mathbf{A}) = \begin{pmatrix} a_{11} \\ \vdots \\ a_{m1} \\ \vdots \\ a_{1n} \\ \vdots \\ a_{mn} \end{pmatrix} \in \mathbb{R}^{mn}
> $$
> 
> 2. 应用向量范数

> [!example] 示例
> - **Frobenius 范数**：$\|\mathbf{A}\|_F = \|\text{vec}(\mathbf{A})\|_2$
> - **和范数**：$\|\mathbf{A}\|_S = \|\text{vec}(\mathbf{A})\|_1 = \sum_{i,j} |a_{ij}|$
> - **最大范数**：$\|\mathbf{A}\|_M = \|\text{vec}(\mathbf{A})\|_\infty = \max_{i,j} |a_{ij}|$

---

## 3.3 逐元素范数的问题

> [!warning] 变换解释的丢失
> 
> 考虑 $\mathbf{A} = \mathbf{I}_n$（单位矩阵）：
> - $\|\mathbf{I}_n\|_F = \sqrt{n}$（随维度增长！）
> - $\|\mathbf{I}_n\|_S = n$
> - $\|\mathbf{I}_n\|_M = 1$
> 
> **问题**：变换 $\mathbf{x} \mapsto \mathbf{I}_n \mathbf{x} = \mathbf{x}$ 什么都不改变，但范数随 $n$ 增长！

> [!success] 解决方案
> **算子范数**，直接测量变换

---

## 3.4 相同范数，不同变换

> [!example] 示例
> 
> $$
> \mathbf{A}_1 = \begin{pmatrix} 1 & 0 \\ 0 & 1 \end{pmatrix}, \quad \mathbf{A}_2 = \begin{pmatrix} 0 & 1 \\ 1 & 0 \end{pmatrix}, \quad \mathbf{A}_3 = \begin{pmatrix} \sqrt{2} & 0 \\ 0 & 0 \end{pmatrix}
> $$
> 
> 相同的 Frobenius 范数：$\|\mathbf{A}_1\|_F = \|\mathbf{A}_2\|_F = \|\mathbf{A}_3\|_F = \sqrt{2}$
> 
> 但变换完全不同！设 $\mathbf{x} = \begin{pmatrix} 1 \\ 0 \end{pmatrix}$：
> - $\mathbf{A}_1 \mathbf{x} = \begin{pmatrix} 1 \\ 0 \end{pmatrix}$（恒等变换）
> - $\mathbf{A}_2 \mathbf{x} = \begin{pmatrix} 0 \\ 1 \end{pmatrix}$（旋转 90°）
> - $\mathbf{A}_3 \mathbf{x} = \begin{pmatrix} \sqrt{2} \\ 0 \end{pmatrix}$（拉伸 $\sqrt{2}$ 倍的投影）

---

# 四、算子范数

## 4.1 定义

> [!important] 定义：算子范数
> 
> 设 $\| \cdot \|_V$ 是 $\mathbb{R}^n$ 上的一个*向量范数*。$\mathbf{A}$ 的*算子范数*为：
> $$
> \|\mathbf{A}\|_V := \max_{\mathbf{x} \neq 0} \frac{\|\mathbf{Ax}\|_V}{\|\mathbf{x}\|_V} = \max_{\|\mathbf{x}\|_V = 1} \|\mathbf{Ax}\|_V
> $$
> 
> **直观解释**：矩阵 $\mathbf{A}$ 对于向量的"最大拉伸倍数"，即 $\mathbf{A}$ 最多能把一个单位向量拉伸多长。

---

## 4.2 几何解释

> [!tip] 理解算子范数
> 
> - 测量 $\mathbf{A}$ 的最大**拉伸**（在向量范数意义下）
>   - 大范数 → 强"拉伸"
>   - 小范数 → 弱"拉伸" / "压缩"
> - *Rayleigh 商*：$R_{\mathbf{A}}(\mathbf{x}) = \frac{\|\mathbf{Ax}\|_V}{\|\mathbf{x}\|_V}$

> [!note] 可视化
> 
> **回顾**：矩阵乘上向量会把向量变形。**特征向量**是那些乘完之后方向不变的特殊向量，**特征值**是它被拉伸或压缩的倍数。

---

## 4.3 重要的算子范数

> [!important] 基于不同 $p$-范数的算子范数
> 
> | 名称 | 定义 | 计算公式 |
> |:---|:---|:---|
> | **谱范数** ($p = 2$) | $\|\mathbf{A}\|_2 = \max_{\|\mathbf{x}\|_2=1} \|\mathbf{Ax}\|_2$ | $\sqrt{\lambda_{\max}(\mathbf{A}^\top\mathbf{A})}$ |
> | **列和范数** ($p = 1$) | $\|\mathbf{A}\|_1 = \max_{\|\mathbf{x}\|_1=1} \|\mathbf{Ax}\|_1$ | $\max_{1 \leq j \leq n} \sum_{i=1}^{m} \|a_{ij}\|$ |
> | **行和范数** ($p = \infty$) | $\|\mathbf{A}\|_\infty = \max_{\|\mathbf{x}\|_\infty=1} \|\mathbf{Ax}\|_\infty$ | $\max_{1 \leq i \leq m} \sum_{j=1}^{n} \|a_{ij}\|$ |

> [!tip] 直观理解
> - **谱范数**：用欧几里得距离衡量，单位向量最多被拉伸到多长
> - **列和范数**：用 L1 长度（分量绝对值之和）衡量
> - **行和范数**：用 L∞ 长度（分量绝对值最大者）衡量

---

# 五、正交矩阵

## 5.1 定义

> [!important] 定义：正交矩阵
> 
> 一个矩阵 $\mathbf{Q} \in \mathbb{R}^{n \times n}$ 称为*正交矩阵*，当其列向量为*标准正交向量*时，即
> $$
> \mathbf{Q}^\top \mathbf{Q} = \mathbf{I}
> $$

---

## 5.2 重要性质

> [!success] 正交矩阵的性质
> 
> | 性质 | 说明 |
> |:---|:---|
> | **几何上：旋转/反射** | $\mathbf{Q}$ 描述 $\mathbb{R}^n$ 中的旋转和/或反射，形状和大小都不变 |
> | **保长度** | $\|\mathbf{Qx}\|_2 = \|\mathbf{x}\|_2$ 对所有 $\mathbf{x} \in \mathbb{R}^n$ 成立 |
> | **逆就是转置** | $\mathbf{Q}^{-1} = \mathbf{Q}^\top$ |
> | **条件数为 1** | $\|\lambda_1(\mathbf{Q})\| = \cdots = \|\lambda_n(\mathbf{Q})\| = 1 \implies \kappa_2(\mathbf{Q}) = 1$ |
> | **乘积仍正交** | $\mathbf{Q}_1 \cdots \mathbf{Q}_n$ 也是正交的 |

> [!tip] 为什么条件数为 1 很重要？
> 
> 条件数衡量矩阵对误差的敏感程度。条件数等于 1 是最理想的情况，意味着误差不会被放大。
> 
> 与 $\mathbf{Q}$ 或 $\mathbf{Q}^{-1}$ 的乘法总是良态的，不会恶化算法的稳定性。

---

# 六、谱范数与谱分解

## 6.1 核心定理

> [!important] 定理：谱范数的计算
> 
> 对于 $\mathbf{A} \in \mathbb{R}^{m \times n}$，秩为 $r$，且谱分解 $\mathbf{A}^\top \mathbf{A} = \mathbf{P} \mathbf{\Lambda} \mathbf{P}^\top$，其中 $\mathbf{\Lambda} = \text{diag}(\lambda_1, \ldots, \lambda_r, 0, \ldots, 0)$，且 $\lambda_1 \geq \lambda_2 \geq \cdots \geq \lambda_r > 0$：
> $$
> \|\mathbf{A}\|_2 = \sqrt{\lambda_1} = \sqrt{\lambda_{\max}(\mathbf{A}^\top \mathbf{A})}
> $$

> [!note] 关键点
> - 只有对称矩阵才能谱分解
> - 任何矩阵 $\mathbf{A}$，$\mathbf{A}^\top \mathbf{A}$ 都是对称矩阵，都能谱分解
> - $\mathbf{A}^\top \mathbf{A}$ 的特征值开根号后，就是 $\mathbf{A}$ 的奇异值

---

## 6.2 谱分解的用途

> [!tip] 谱分解能做什么？
> 
> - **算条件数**，知道问题有多病态
> - **算范数**，知道最大拉伸能力
> - **算矩阵的逆**，对角矩阵求逆特别简单
> - **算矩阵的幂**，比如 $\mathbf{A}$ 的 100 次方
> - **降维**，把不重要的方向扔掉

---

## 6.3 算子范数计算示例

> [!example] 示例
> 
> 设 $\mathbf{A} = \begin{pmatrix} 2 & 1 \\ 0 & 3 \end{pmatrix}$
> 
> - **列和范数**：$\|\mathbf{A}\|_1 = \max\{|2| + |0|, |1| + |3|\} = 4$
> - **行和范数**：$\|\mathbf{A}\|_\infty = \max\{|2| + |1|, |0| + |3|\} = 3$
> - **谱范数**：$\|\mathbf{A}\|_2 = \sqrt{\lambda_{\max}(\mathbf{A}^\top \mathbf{A})} \approx 3.26$
> 
> 单位矩阵 $\mathbf{I}_n$：
> - $\|\mathbf{I}_n\|_1 = \|\mathbf{I}_n\|_2 = \|\mathbf{I}_n\|_\infty = 1$ ✓
> - 这是合理的：$\mathbf{I}_n$ 不改变任何东西！

---

# 七、Schatten 范数

## 7.1 定义

> [!important] 定义：Schatten-p-范数
> 
> 对于矩阵 $\mathbf{A} \in \mathbb{R}^{m \times n}$，其 $\mathbf{A}^\top \mathbf{A}$ 的特征值为 $\boldsymbol{\lambda} = (\lambda_1, \ldots, \lambda_n)$，*Schatten-p-范数* 定义为：
> $$
> \|\mathbf{A}\|_{S,p} := \|\boldsymbol{\lambda}^{1/2}\|_p = \left(\sum_{i=1}^{n} \lambda_i^{p/2}\right)^{1/p}
> $$

> [!tip] 直观理解
> 
> 算子范数只看最极端的那个方向，但有时候你想知道整体情况。Schatten 范数就是把每个方向的拉伸倍数（奇异值）收集起来，用 $p$ 范数统一度量。

---

## 7.2 重要特例

> [!important] 三种特殊的 Schatten 范数
> 
> | $p$ 值 | 名称 | 公式 |
> |:---|:---|:---|
> | $p = \infty$ | **谱范数** | $\|\mathbf{A}\|_{S,\infty} = \sqrt{\lambda_{\max}} = \|\mathbf{A}\|_2$ |
> | $p = 2$ | **Frobenius 范数** | $\|\mathbf{A}\|_{S,2} = \|\mathbf{A}\|_F$ |
> | $p = 1$ | **核范数** | $\|\mathbf{A}\|_{S,1} = \|\mathbf{A}\|_* = \sum_{i=1}^{r} \sqrt{\lambda_i}$ |

> [!success] 惊喜
> Frobenius 范数是一种 Schatten 范数！看似无脑的定义，背后藏着深刻的结构。

---

## 7.3 Frobenius 范数与迹的联系

> [!important] 定理
> $$
> \|\mathbf{A}\|_F^2 = \text{tr}(\mathbf{A}^\top \mathbf{A})
> $$

> [!note] 证明
> 由谱分解 $\mathbf{A}^\top \mathbf{A} = \mathbf{P} \mathbf{\Lambda} \mathbf{P}^\top$：
> $$
> \|\mathbf{A}\|_F^2 = \text{tr}(\mathbf{A}^\top \mathbf{A}) = \text{tr}(\mathbf{P}^\top \mathbf{\Lambda} \mathbf{P}) = \text{tr}(\mathbf{\Lambda}) = \sum_{i=1}^{r} \lambda_i
> $$

> [!tip] 总结
> 矩阵 $\mathbf{A}$ 的 F 范数的平方 = $\mathbf{A}^T \mathbf{A}$ 的迹 = $\mathbf{A}^T \mathbf{A}$ 的特征值的和

---

# 八、范数的性质与应用

## 8.1 范数等价性

> [!important] 定理：所有矩阵范数都是等价的
> 
> 对于任意两个矩阵范数 $\|\cdot\|_a$ 和 $\|\cdot\|_b$，存在常数 $c, C > 0$ 使得：
> $$
> c\|\mathbf{A}\|_a \leq \|\mathbf{A}\|_b \leq C\|\mathbf{A}\|_a
> $$

> [!example] 显式常数
> - $\|\mathbf{A}\|_2 \leq \|\mathbf{A}\|_F \leq \sqrt{\min(m, n)} \|\mathbf{A}\|_2$
> - $\|\mathbf{A}\|_F \leq \|\mathbf{A}\|_* \leq \sqrt{\min(m, n)} \|\mathbf{A}\|_F$
> - $\frac{1}{\sqrt{n}} \|\mathbf{A}\|_\infty \leq \|\mathbf{A}\|_2 \leq \sqrt{m} \|\mathbf{A}\|_\infty$

> [!warning] 重要
> - **等价性 $\neq$ 相等性**
> - 不同的范数可以对矩阵产生**不同的排序**
> - 每种范数都有其自身的**解释**

---

## 8.2 次可乘性

> [!important] 定义：次可乘范数
> 
> 一个矩阵范数 $\|\cdot\|$ 称为*次可乘的*，当：
> $$
> \|\mathbf{AB}\| \leq \|\mathbf{A}\| \cdot \|\mathbf{B}\|
> $$

> [!tip] 直观理解
> 
> $\mathbf{A}$ 能把向量最多拉伸 3 倍，$\mathbf{B}$ 能把向量最多拉伸 5 倍。那 $\mathbf{AB}$ 连着作用，最多拉伸 15 倍，但通常更小。

> [!success] 重要性
> - 控制矩阵乘积中的**误差传播**
> - 使迭代方法的**收敛性分析**成为可能
> - 所有*算子范数*和*Schatten范数*都是次可乘的

---

## 8.3 相容性

> [!important] 定义：相容范数
> 
> 一个矩阵范数 $\|\cdot\|$ 与向量范数 $\|\cdot\|_V$ *相容*，当：
> $$
> \|\mathbf{Ax}\|_V \leq \|\mathbf{A}\| \cdot \|\mathbf{x}\|_V
> $$

> [!success] 相容性规则
> 1. **每个次可乘范数**与自身相容
> 2. **每个算子范数**与其诱导的向量范数相容
> 3. **Frobenius范数**与欧几里得范数相容

> [!tip] 应用
> 相容性使**误差估计**成为可能：如果 $\|\mathbf{e}\|_V$ 很小，则 $\|\mathbf{Ae}\|_V$ 也是可控的。

---

## 8.4 矩阵范数与条件数

> [!important] 条件数公式
> $$
> \kappa(\mathbf{A}) = \|\mathbf{A}\| \cdot \|\mathbf{A}^{-1}\|
> $$
> 
> 对于谱范数：
> $$
> \kappa_2(\mathbf{A}) = \|\mathbf{A}\|_2 \cdot \|\mathbf{A}^{-1}\|_2 = \frac{\sigma_{\max}(\mathbf{A})}{\sigma_{\min}(\mathbf{A})}
> $$

> [!tip] 条件数的解释
> 
> | 条件 | 含义 |
> |:---|:---|
> | $\kappa \approx 1$ | 良态的 |
> | $\kappa \gg 1$ | 病态的 |
> | $\kappa = \infty$ | 奇异的 |

---

## 8.5 后向误差分析

> [!example] 后向误差分析
> 
> **问题**：求解 $\mathbf{Ax} = \mathbf{b}$，得到 $\tilde{\mathbf{x}}$
> 
> **问题**：$\tilde{\mathbf{x}}$ 是哪个扰动问题的精确解？
> 
> **答案**：$(\mathbf{A} + \Delta\mathbf{A})\tilde{\mathbf{x}} = \mathbf{b}$，其中 $\Delta\mathbf{A} = -\mathbf{r}\tilde{\mathbf{x}}^\top / \|\tilde{\mathbf{x}}\|^2$，$\mathbf{r} = \mathbf{A}\tilde{\mathbf{x}} - \mathbf{b}$
> 
> **相对扰动（后向误差）**：
> $$
> \eta = \frac{\|\Delta\mathbf{A}\|_2}{\|\mathbf{A}\|_2} = \frac{\|\mathbf{r}\|_2}{\|\mathbf{A}\|_2 \cdot \|\tilde{\mathbf{x}}\|_2}
> $$

---

## 8.6 机器学习中的应用

> [!tip] 优化问题中的范数
> 
> **岭回归**（L2正则化）：
> $$
> \min_{\mathbf{x}} \|\mathbf{Ax} - \mathbf{b}\|_2^2 + \lambda\|\mathbf{x}\|_2^2
> $$
> 要求系数别太大，模型更稳健。
> 
> **LASSO**（L1正则化）：
> $$
> \min_{\mathbf{x}} \|\mathbf{Ax} - \mathbf{b}\|_2^2 + \lambda\|\mathbf{x}\|_1
> $$
> 自动把没用的变量系数压成零。
> 
> **核范数正则化**（矩阵补全）：
> $$
> \min_{\mathbf{X}} \|\mathbf{P}_\Omega(\mathbf{X}) - \mathbf{P}_\Omega(\mathbf{M})\|_F^2 + \lambda\|\mathbf{X}\|_*
> $$
> 逼迫补全后的表格尽量简单（低秩）。

---

# 九、总结

## 9.1 小测验：迹的性质

> [!question] 哪些陈述是正确的？
> 
> 1. $\text{tr}(\mathbf{AB}) = \text{tr}(\mathbf{A}) \cdot \text{tr}(\mathbf{B})$
> 2. $\text{tr}(\mathbf{AB}) = \text{tr}(\mathbf{BA})$
> 3. $\text{tr}(\mathbf{A}^\top) = -\text{tr}(\mathbf{A})$
> 4. $\text{tr}(\mathbf{I}_n) = n$
> 5. 若 $\mathbf{A}$ 可逆，则 $\text{tr}(\mathbf{A}^{-1}) = 1/\text{tr}(\mathbf{A})$

> [!success] 答案
> 
> | 陈述 | 正误 | 解释 |
> |:---|:---|:---|
> | 1 | ❌ | 反例：$\mathbf{A} = \mathbf{B} = \mathbf{I}_2$，$\text{tr}(\mathbf{AB}) = 2 \neq 4$ |
> | 2 | ✅ | 迹的循环性质 |
> | 3 | ❌ | 实际上 $\text{tr}(\mathbf{A}^\top) = \text{tr}(\mathbf{A})$ |
> | 4 | ✅ | 单位矩阵对角线上有 $n$ 个 1 |
> | 5 | ❌ | 反例：$\mathbf{A} = 2\mathbf{I}_2$，$\text{tr}(\mathbf{A}^{-1}) = 1 \neq 1/4$ |

---

## 9.2 小测验：计算范数

> [!question] 给定 $\mathbf{A} = \begin{pmatrix} 1 & 2 \\ 3 & 0 \end{pmatrix}$，计算各范数

> [!success] 解答
> 
> 1. $\|\mathbf{A}\|_1 = \max\{|1|+|3|, |2|+|0|\} = \max\{4, 2\} = 4$
> 2. $\|\mathbf{A}\|_\infty = \max\{|1|+|2|, |3|+|0|\} = \max\{3, 3\} = 3$
> 3. $\|\mathbf{A}\|_F = \sqrt{1^2 + 2^2 + 3^2 + 0^2} = \sqrt{14} \approx 3.74$

---

## 9.3 计算复杂度

> [!summary] 不同范数的计算复杂度
> 
> | 范数 | 复杂度 | 算法 |
> |:---|:---|:---|
> | $\|\mathbf{A}\|_1$ | $O(mn)$ | 计算列和 |
> | $\|\mathbf{A}\|_\infty$ | $O(mn)$ | 计算行和 |
> | $\|\mathbf{A}\|_F$ | $O(mn)$ | 所有元素的平方和 |
> | $\|\mathbf{A}\|_2$ | $O(\min(m^2n, mn^2))$ | 计算部分 SVD |
> | $\|\mathbf{A}\|_*$ | $O(\min(m^2n, mn^2))$ | SVD + 奇异值求和 |

---

## 9.4 核心概念总结

> [!summary] 迹的核心概念
> 
> - **定义**：$\text{tr}(\mathbf{A}) = \sum_{i=1}^{n} a_{ii}$
> - **循环性质**：$\text{tr}(\mathbf{AB}) = \text{tr}(\mathbf{BA})$
> - **特征值联系**：$\text{tr}(\mathbf{A}) = \sum_{i=1}^{n} \lambda_i$
> - **Frobenius 范数联系**：$\|\mathbf{A}\|_F = \sqrt{\text{tr}(\mathbf{A}^\top \mathbf{A})}$

> [!summary] 范数的层次结构
> 
> 1. **逐元素范数**：$\|\mathbf{A}\|_F$，$\|\mathbf{A}\|_S$，$\|\mathbf{A}\|_M$ — 易于计算，失去变换解释
> 2. **算子范数**：$\|\mathbf{A}\|_1$，$\|\mathbf{A}\|_2$，$\|\mathbf{A}\|_\infty$ — 度量拉伸程度，次可乘，相容
> 3. **Schatten范数**：$\|\mathbf{A}\|_{S,p}$ — 基于奇异值/特征值，酉不变

---

## ✅ 自测清单

- [ ] 能定义矩阵的迹并计算
- [ ] 知道迹的循环性质
- [ ] 理解迹与特征值的关系
- [ ] 能定义 Frobenius 范数
- [ ] 知道 Frobenius 范数的问题
- [ ] 能定义算子范数
- [ ] 能计算列和范数、行和范数、谱范数
- [ ] 知道正交矩阵的重要性质
- [ ] 理解谱范数与谱分解的关系
- [ ] 知道 Schatten 范数的三种特例
- [ ] 理解范数等价性、次可乘性、相容性
- [ ] 能用范数计算条件数
